# T3.2 round 2 review

Verdict: APPROVE

I reviewed the current T3.2 assembler against the specification and both round-1 records.
I own only this review file. The Place and Overlay shapes exist. I can validate this task independently.
I did not treat prior reports as evidence. I re-measured every artifact.

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 0 |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| None | No findings at any severity. | Independent artifact measurements below. | Existing regressions pin both prior repairs. |

## Prior-round residue

**Round 1, F1: zero residue.** The 1.5 s and 1.0 s collision keeps junction energy near the unfaded body.
The 10 ms window before 1.000 s measures -21.0 dB. The window after it measures -22.0 dB.
The centered 10 ms window measures -21.4 dB against a body of -21.1 dB.
`silencedetect=noise=-40dB:d=0.005` reports silence from 2.000 s to 3.000 s only.
It does not cover 1.000 s. Neighbors around t=1.000000 follow a 440 Hz sine through zero.
The stereo frame at t=1.000000 is 0 because that phase is a zero crossing, not a trough.
Restoring abutting fades would drop those 10 ms windows toward -38 dB and reopen the trough.

**Round 1, F2: zero residue.** The Truncate comment names the next take start as the cut.
Truncate artifacts still keep silence from voice-end to that start.
The 0.85 s to 0.95 s window measures -91.0 dB. Voice ended near 0.800 s.

No prior finding remains open. I found no additional defect in the full task or tests.

## Independent measurements

I own only this review file. The required API shapes exist. I can validate this task independently.

I copied the unchanged production assembler and media files into a temporary module outside the repository.
A small driver called `BuildBed`, `PrepareTake`, `Place`, and `Overlay`.
The driver supplied artifacts for external measurements. Its success messages supplied no evidence.

Every measurement selected `/opt/homebrew/bin/ffmpeg` and `/opt/homebrew/bin/ffprobe`, both version 9.0.1.
`PATH=/opt/homebrew/bin:/usr/bin:/bin`. Go reports version 1.27.1.

### Fixture placement

`BuildBed` on `testdata/clip.mp4` yields 44100 Hz, two channels, float PCM, and 75.008005 s.
The speech layer and the overlay mix share that format and duration.

The first 80 ms of each prepared take matches the speech layer at the `segments.json` start.

| Segment | Start (s) | Prepared 80 ms SHA-1 | Speech 80 ms SHA-1 | Mean / max (dB) |
|---|---|---|---|---|
| 1 | 5.908 | `c56ddbf2a60a7ea9be18187791739fbe443a28a9` | identical | -46.5 / -28.6 |
| 2 | 7.728 | `0139720ab241230551631f6cb90052bfe359c8b1` | identical | -62.6 / -54.7 |
| 3 | 13.208 | `39b31c86bb67cb2c076d807c28e97342126b911f` | identical | -63.0 / -54.9 |
| 4 | 18.818 | `f0970cd60df677fcc647a2f4b1422efdc6230864` | identical | -65.8 / -58.7 |
| 5 | 24.788 | `2f91c845c210bac59d1cebeffed1f9c76fe37802` | identical | -63.8 / -55.9 |
| 6 | 30.348 | `736b7dcfbd4a1a5adb651829d4f4eaa46935f1df` | identical | -53.5 / -45.8 |
| 7 | 38.271 | `4ec8ee03ddb261f8e52531dc85787ff52ceeb78c` | identical | -55.5 / -49.2 |
| 8 | 47.381 | `5ca81495b2991aa825e51c7d7fabb6a4cc7d7b65` | identical | -63.2 / -57.0 |

The 80 ms before each later take measures -91.0 dB when the previous take does not occupy it.
Decoded samples in those windows are digital zero.

Prepared durations place segments 1, 2, 5, 6, 7, and 8 inside their slots.
Segments 3 and 4 overrun their slots and still end before the next start.

### Speech-layer gaps and overlay

Seconds 0 through 5.5 of the speech layer measure -91.0 dB mean and maximum.
Decoded RMS there is digital zero.
The bed and mix SHA-1 for that window both equal `5651f203949a1acd7393c3d47964299cdee2e72e`.
Both measure -13.4 dB mean and 0.0 dB maximum.

Seconds 54.5 through 75 of the speech layer measure -91.0 dB.
The bed and mix SHA-1 for that window both equal `fe2c208081728b789af0578de1be50434216dbfb`.
Both measure -15.4 dB mean and -0.1 dB maximum.

### Gap policy

A 1.2 s tone in a 1.0 s slot with the next take at 1.5 s occupies the trailing gap.
Windows 0.2 s to 0.6 s, 1.05 s to 1.15 s, and 1.60 s to 1.80 s each measure -21.1 dB mean.
The remaining gap from 1.25 s to 1.40 s measures -91.0 dB.
`silencedetect` at -90 dB places that remaining silence from 1.200 s to 1.500023 s.

Fixture segment 3 continues past its 18.528 s slot end.
The 18.528 s to 18.546 s window measures -57.4 dB mean and -54.2 dB maximum.
Decoded samples there have peak 0.00195 and 1550 nonzero values in 20 ms.
The unused gap from 18.56 s to 18.80 s measures -91.0 dB.

Fixture segment 4 is 2.3 ms longer than its slot after resampling.
Samples after 24.478 s on the speech layer are zero. That is the silent file tail.

### Truncate policy (F2)

A 0.8 s tone plus 0.4 s silence in an 0.8 s slot meets the next take at 1.0 s.
The kept tone from 0.2 s to 0.6 s measures -21.1 dB mean.
The kept silence from 0.85 s to 0.95 s measures -91.0 dB.
`silencedetect` at -90 dB reports silence from 0.800612 s to 1.000023 s.
The next take from 1.10 s to 1.30 s measures -21.1 dB mean.
The colliding tail silence does not appear after 1.0 s.

`internal/assemble/place.go:28` now says the cut sits at the next take start.

### Crossfade policy (F1)

The earlier body from 0.2 s to 0.8 s matches the prepared take at -21.1 dB.
Those decoded windows share SHA-1 `7329bb4f78a81d5d3dfe1d8c1e252d471ce6521e`.
The later take body from 1.2 s to 1.6 s also measures -21.1 dB.
The later take therefore starts after the cut and does not overwrite the earlier body.

The 10 ms window from 0.990 s to 1.000 s measures -21.0 dB mean and -18.1 dB maximum.
The 10 ms window from 1.000 s to 1.010 s measures -22.0 dB mean and -18.2 dB maximum.
The centered 10 ms window from 0.995 s to 1.005 s measures -21.4 dB mean and -18.1 dB maximum.
Decoded RMS in that centered window is 0.085262 against a body of 0.088369.

`silencedetect=noise=-40dB:d=0.005` reports silence only from 2.000 s to 3.000 s.
It does not cover 1.000 s.

A decoded stereo frame at t=1.000000 is exactly 0.
Frame 44099 is -0.007837. Frame 44101 is +0.007832.
Those neighbors are a 440 Hz zero crossing, not a digital-zero trough.
Of 880 samples in the centered window, only that stereo pair is zero.

### Failed render

`Place` rejected a missing take and left the existing output bytes `previous render`.
SHA-1 `69f7e642839fee6b1c2a738ca275028f559d5177` is unchanged.
No `.assemble-*` file remained under the work directory after success or failure.

### Owns and comments

The task still owns `internal/assemble/place.go` and `internal/assemble/place_test.go`.
The phase handoff sits in the task log and does not change assembler behaviour.
Comment prose has no semicolons, no em dashes, and no sentence over 30 words.
The Truncate comment now matches the keep rule.

## Full-task sweep

All eight fixture takes land at their rounded start frames with matching 80 ms SHA-1 hashes.
Overlay leaves silent regions identical to the bed.
Gap occupies trailing silence and leaves the unused remainder quiet.
Truncate keeps post-voice silence until the next start, then yields to the later take.
Voiced collision overlaps through the fade window and keeps junction energy near the body.
Failed renders stay atomic. I found no new defect.

Temporary review artifacts were removed after measurement. No production code changed during review.
