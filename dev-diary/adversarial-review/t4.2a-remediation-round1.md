# T4.2a remediation, round 1

| Finding | Remediation | Pin | Mutation prevented |
| --- | --- | --- | --- |
| 1 | Sent a 128-sample `RecordTake` through the HTTP capture. Asserted one take row, three charge rows, and the exact numeric peak array. Sent a 129-sample attempt and asserted an error with no new request. | `go test ./internal/ledger -count=1 -run '^TestRecordTakeFixtureCapture$'` captures the maximum JSONEachRow payload and observes no transport for 129 samples. | CI fails if maximum vectors stop reaching ClickHouse as numeric arrays, or if over-range vectors reach the durable queue. |
