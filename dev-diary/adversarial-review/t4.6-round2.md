# T4.6 adversarial review, round 2

Verdict: APPROVE

Counts: C 0, H 0, M 0, L 0.

## Scope and evidence

I reviewed the T4.6 implementation and round-one remediation against the task
contract, the T4.2a peak persistence seam, the T3.5 uint8 output, and the
`api.Take.Peaks []int` wire contract.

The owned regression tests now pin both accepted boundaries, both rejected
boundaries, stale-value clearing, defensive copying, and numeric JSON delivery.
The take payload uses a nonexistent audio path. The tests therefore also pin
that mapping does not read audio files.

Independent checks passed:

- `go test ./internal/ledger -run '^TestApplyTakePeaks' -count=1`
- `go vet ./internal/ledger`
- `go test ./...`

The mapper rejects 63 and 129 samples before changing an existing payload. It
accepts 64 and 128 samples, copies every value, and clears an existing vector
for empty input. Marshaling produces JSON number arrays. No source or test
residue from the round-one finding remains.

## Findings

| # | Sev | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- | --- |
| — | — | — | No findings. | All T4.6 contract and remediation checks pass. | No mutation remains unprotected. |

## Residue

Zero residue remains from round one. The owned regression path now permanently
protects the required peak validation and API mapping behavior.
