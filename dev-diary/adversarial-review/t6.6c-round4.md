# T6.6c adversarial review, round 4

**Task:** T6.6c Record agent charges in the ledger. **Round:** 4, against HEAD `af0fc09` plus
the uncommitted round 2 and round 3 remediations in the working tree.

## Scope and role

I am the REVIEW role. I fix nothing. I edited no code, test, phase document, status line or
exit box. My only write is this record. I reviewed the working tree, not HEAD alone. That
covers `sql/schema.sql`, the ledger charge writer and whole-pass reads, the agent route and its
recorder seam, the running total, the four keyed take lists, the Details tab keys and the live
test. I ignored Phase 5 and Phase 7 except where the agent path touches them. I read every
prior record and treated each remediation table as a claim under test.

## Method

I worked in a full copy at `/tmp/t66c-r4`. I loaded a fresh ClickHouse 26.8.2.7 database from
`sql/schema.sql` on port 18971. I built the real `ajilamu` binary and the production frontend. I
built the pre-fix HEAD bundle in a detached worktree at `/tmp/t66c-r4-head`. I drove headless
Chrome 152.0.7977.82 over raw Chrome DevTools Protocol on port 19241. I read
`Runtime.exceptionThrown` and the DOM. Charge rows came from SQL through the `charges` view,
which applies FINAL. I ran the real agent route, the real recorder adapter and the real ledger
client against that database. I treat no program report as evidence.

## Verdict

REMEDIATE. Findings: 0 C, 0 H, 0 M, 1 L.

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 1 |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| `internal/ledger/workspace.go` lines 174 to 176, the `WholePassCharges` doc comment | The comment says the returned charges are ordered by commit, segment, kind, then unit. The T6.6c diff dropped `segment_index` from the statement, because the UNION ALL output has no such column. The client comparator sorts by commit, kind and unit. So the comment names a sort key the shipped code does not use. | I dumped the shipped statement and read its tail. It reads `ORDER BY commit_id ASC, kind ASC, unit ASC`. `cmpWholePassChargeRow` compares commit, kind and unit. The pre-T6.6c statement read `ORDER BY commit_id ASC, segment_index ASC, kind ASC, unit ASC`, so the comment matched before this task. | An agent that trusts the comment expects segment order and does not find it. Updating the sentence names the real order. |

## Done conditions, measured

| Condition | Independent result |
|---|---|
| A fresh database from `sql/schema.sql` accepts kind `agent` | PASS. The file loaded into an empty database and the preflight returned 0. A second load returned 0. `system.columns` read `Enum8('segment' = 1, 'translate' = 2, 'synthesize' = 3, 'agent' = 4)`. Agent rows inserted with no error. |
| One `POST /api/dubs/{id}/agent` writes its measured charges, read back by SQL | PASS. Two POSTs to `d-r4-route` wrote four rows through the real route and the real adapter. Each row carried kind `agent`, an empty `take_id`, an empty `commit_id`, `segment_index` -1, units 1200 at price 150 and units 340 at price 600. |
| The running total grows by the turn's nanodollars | PASS. `d-r4-route` read 0 before the turns and 768000 after, a rise of 768000 for two turns. Each response reported 384000. |
| The `covers` sentence names the agent calls | PASS. The `d-r4-route` payload read `0 segment calls, 0 translation calls, 0 render calls, and 2 agent calls.` |
| Two identical turns record two charges rather than one | PASS. Two identical POSTs produced four rows, two distinct `turn_id` values and four distinct `event_key` values. |
| `internal/agent` imports no writer | PASS. `go list -deps ./internal/agent` names only `internal/agent`, `internal/config` and `internal/cost` among repository packages. `internal/api` imports no ledger. |
| The `mcp_readonly` grant still refuses an INSERT | PASS. `SHOW GRANTS FOR mcp_readonly` read `GRANT SELECT ON t66c_r4.*`. A SELECT answered 20. An INSERT of kind `agent` failed with code 497 `ACCESS_DENIED`. |
| A nil recorder keeps the route answering and records nothing | PASS. A server with no `AgentCharges` answered HTTP 200 with a total of 384000. `SELECT count() FROM charges_raw WHERE dub_id='d-r4-nil'` returned 0. |
| The agent package stays read-only | PASS. No file under `internal/agent` changed against HEAD. `deploy/mcp-clickhouse/compose.yaml` keeps `CLICKHOUSE_ALLOW_WRITE_ACCESS: "false"` and the `mcp_readonly` user. |

**Nine done conditions pass. The one L sits beyond them, in a doc comment.** The task named
storage, the writer, the total, the covers sentence and the boundary. Every named behaviour
holds.

## Residue against prior rounds

| Prior finding | Residue |
|---|---|
| Round 3 H, the four duplicate take keys | Zero. On `d-r4-dup3`, whose line holds three takes with one `audio_path`, the pre-fix HEAD bundle threw `each_key_duplicate`, reported `rail:false` and found no Details tab. The fixed bundle threw no exception and rendered the rail, three take chips with one file, two ghosts with one file, three speaker takes, and the Details tab with two take charge rows and three project rows. Two takes with one file (`d-r4-dup2`) and one take (`d-r4-one`) also rendered. |
| Round 3 H, the defeat attempts | Zero. The real rerender path appended a take with an existing file after first paint. The page rendered four takes with one file and no exception. A harness that mounts the real `DetailsTab`, `Track` and `Speaker` appended, reordered and shrank the list in place. No exception, and every row kept its own take's attempt and charges. |
| Round 3 L, the stale comment | Zero. The comment above `selectWholePassCharges` names kind, units, unit price, total and position. The shipped project key at `DetailsTab.svelte:109` uses `charge.kind`, `charge.units`, `charge.unit_price_nanodollars`, `charge.total_nanodollars` and `chargeIndex`. The five fields match. |
| Round 2 H, the alias shadow in `selectWholePassCharges` | Zero. On `d-r4-mix` the fixed statement returned one agent row of units 2 and total 768000, equal to the agent rows alone. The pre-fix statement returned units 3 and total 1725100. On a segment-only dub the fixed statement returned no agent row and the pre-fix statement returned a spurious one. The live test failed against the pre-fix statement with units 3 and total 1725100, and passed against the fix with units 2 and total 768000. |
| Round 2 C, the Details tab duplicate keys | Zero. On `d-r4-r2c`, which has one take file, two translate rows and two identical segment rows, the fixed bundle rendered both translate rows and both segment rows with no exception. The HEAD bundle threw `each_key_duplicate` and rendered neither list. |
| Round 2 L, the tests | Zero. `TestWorkspaceQueriesBindAndShape` requires the substring `charges.kind = 'agent'`. Copied into the HEAD worktree it failed with exit 1 against the pre-fix statement. The live test executes the statement against ClickHouse. |
| Round 1 C1, the project charge key and the folded row | Zero. The fold units and total match the itemized agent rows. The project key is unique by construction and rendered. |
| Round 1 H1, journal before flush | Zero. A real recorder pointed at a dead port failed on the flush and left two files and 1218 bytes in the durable queue. |
| Round 1 H2, the empty commit sentinel | Zero. A route POST to a dub with no commit answered HTTP 200. Both rows carried an empty `commit_id`. |
| Round 1 M1, the whole-pass comment | Zero. The comment states that the writer attributes segmentation to the first rendered take and that agent calls own no take. |
| Round 1 M2, the running total call identity | Zero. For `d-r4-mix` SQL counted 1 segment call, 1 translation call, 1 render call and 4 agent calls. The translate call wrote two unit rows and counted once. |
| Round 1 L1, the attempt rule | Zero. `sql/schema.sql` line 41 and line 764 both state that a genuine repeated take call must raise `attempt`. Two identical synthesize rows at one attempt collapsed to one row. Raising attempt produced two. |

## Additional checks

**The owns expansion holds.** The working tree changed `internal/ledger/workspace.go`,
`internal/ledger/workspace_test.go`, `internal/ledger/workspace_live_test.go`,
`web/src/lib/tabs/DetailsTab.svelte`, `web/src/routes/d/[id]/+page.svelte`,
`web/src/lib/edit/Speaker.svelte` and `web/src/lib/Track.svelte`. Every one sits on the T6.6c
owns line. The phase handoff log carries two entries, `T6.6c owns expansion 2026-09-09` and
`T6.6c owns expansion, round 3 2026-09-09`. They name both new paths and all four web paths.

**The fold handles the edges.** A dub with agent rows only folded to units 2 and total 768000. A
dub with a segment row only returned no agent row. One turn with two rows sharing one call index
folded to units 1, and the running total counted one call. One turn with two call indexes folded
to units 1, and the running total counted two calls.

**The live test is a real pin.** It sits behind `//go:build live`, matching the three other live
files. It skips with no environment. It failed on a partial environment before any connection.
It reads no `.env` file.

**The default suite needs no database.** With the container removed and every ClickHouse
variable unset, `go build ./...`, `go vet ./...` and `go test -count=1 ./internal/... ./cmd/...`
all passed across 14 packages. `npm --prefix web run check` read 183 files with 0 errors and 0
warnings.

**Svelte 5 only.** No `export let`, no `$:` and no store entered the diff. The four changed key
expressions use the rune template already in their files.

**The agent wire shape is intentional.** `AgentCostCharge` in `web/src/lib/types.ts` mirrors
`internal/cost.Charge` without renaming fields, and the `AgentResponse` comment now says so.

**No regression test ships for the round 3 H fix.** The repository holds no frontend test
harness, so a keyed each block has no place to run under `go test` or `npm run check`. The CDP
transcripts are the pin, and the pre-fix bundle reproduces the crash from a clean HEAD build.

## Evidence paths

- Copy `/tmp/t66c-r4`, deleted after the review.
- ClickHouse container `t66c-r4-ch` and then `t66c-r4-ch2` on `127.0.0.1:18971` for HTTP and
  `127.0.0.1:18972` for native, database `t66c_r4`, removed.
- Real binary `/tmp/t66c-r4-ajilamu` on port 18141, stopped. Pre-fix HEAD bundle served from
  `/tmp/t66c-r4-head/web/build` on port 18142, stopped.
- Headless Chrome over CDP on port 19241, stopped.
- Shipped statements dumped to `/tmp/t66c-r4-statements.txt`. Pre-fix statements dumped to
  `/tmp/t66c-r4-head-statements.txt`.
- Throwaway harness `cmd/ajilamu/zz_t66c_r4_harness_test.go` in package `main` mounted the real
  route, the real recorder adapter and the real ledger client. A throwaway
  `internal/ledger/zz_t66c_r4_dump_test.go` printed the statements. Both carry the `r4harness`
  build tag and both were deleted with the copy.
- In-place reconciliation harness `web/src/routes/harness/+page.svelte`, deleted with the copy.
- Route output carried `R4_BEFORE`, `R4_POST`, `R4_AFTER`, `R4_ROWS`, `R4_NOCOMMIT`,
  `R4_NIL` and `R4_WIRE`. The dead-port run printed `R4_DEAD_ERR` and `R4_DEAD_QUEUE`.
- Browser transcripts carried the DOM lists and the CDP `Runtime.exceptionThrown` text.

## Cleanup statement

I deleted the `/tmp/t66c-r4` copy with every throwaway probe in it, the `/tmp/t66c-r4-head`
worktree, the temporary binary, the seed and dump files, and the Chrome profile. I stopped both
app servers and the browser. I removed both ClickHouse containers. No container, server or
browser survives. I wrote only `dev-diary/adversarial-review/t6.6c-round4.md` in the main
checkout.
