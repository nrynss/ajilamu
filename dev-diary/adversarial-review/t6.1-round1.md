# T6.1 Round 1 Review

## Verdict

REMEDIATE

| Severity | Count |
|---|---:|
| C | 1 |
| H | 1 |
| M | 0 |
| L | 0 |

## Independent evidence

| Check | Observation |
|---|---|
| Delivered workspace | A fresh SSR request to `/d/fixture` rendered one Timeline header, zero `boundary-editor` roots, and zero `Move start of line` or `Move end of line` controls. A whole-tree import and component-use scan found no `import Boundary` or `<Boundary>` outside the unreferenced component. |
| Fixture geometry | The frozen fixture has line 2 at 7,728ms through 13,208ms. Line 1 ends at 7,728ms. The existing timeline uses 24px per second, so its geometry remains compatible with the boundary component. |
| Minimum-duration boundary | Independently applying the shipped `constrain` then `snap` calculation to line 2's end produces 7,828ms after the 100ms clamp. The 120ms snap then selects line 1's 7,728ms end. The resulting slot measures 0ms. |
| Build and hygiene | A fresh `npm run check` returned zero diagnostics. A fresh `npm run build` completed. `Boundary.svelte` uses Svelte 5 runes and contains no legacy prop, reactive declaration, or store syntax. |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| `web/src/routes/d/[id]/+page.svelte`, integration seam for `web/src/lib/edit/Boundary.svelte` | The shipped workspace never imports or renders `Boundary`. A creator can see the T5.3 timeline but cannot drag, nudge, snap, view live boundary fit changes, or confirm a collision. This fails T6.1's done condition in the actual demo. The current `owns` line has no integration path, so remediation needs an explicit ownership correction or sibling task before changing the route. | Fresh SSR of `/d/fixture` contains one Timeline header and zero boundary-editor roots or start/end controls. An exact source scan finds no Boundary import or component tag. | Reverting an integration would again leave every T6.1 interaction unreachable from the workspace. |
| `web/src/lib/edit/Boundary.svelte:137-149` | `candidateFor` clamps before snapping, but it never clamps again. A nearby adjacent boundary can therefore undo the 100ms minimum. On the real fixture, moving line 2's end left reaches a constrained 7,828ms value and snaps to line 1's 7,728ms end. It commits a zero-duration slot without a collision because adjacent endpoints do not overlap. | Fixture calculation: line 2 is 7,728ms to 13,208ms. The shipped 100ms floor yields 7,828ms. Its 120ms snap target is line 1's 7,728ms end, producing `end_ms - start_ms = 0`. | Reverting a post-snap constraint allows an ordinary adjacent-boundary snap to create an invalid zero-length source slot and corrupt its fit measurement. |

## Residue

This is T6.1 round 1. No prior T6.1 findings exist.

## Scope

This review examined the T6.1 component and the workspace route only to verify that the promised editor behavior reaches creators. It makes no route edit.
