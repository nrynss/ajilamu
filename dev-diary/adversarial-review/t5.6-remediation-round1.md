# T5.6 Remediation Round 1

| Finding | Fix | Pin | Mutation |
|---|---|---|---|
| F1 | Expanded the shortcut guard to exclude native links, image-map links, disclosure controls, media controls, ARIA buttons, and ARIA links. | I compiled the handler independently and sent Space through each protected target. None prevented its default or changed playback. Space on a non-control still prevented its default and toggled playback. `npm run check` reported zero diagnostics. `npm run build` completed successfully. | Reverting the expanded guard lets global workspace shortcuts take over focused links or custom controls. |
