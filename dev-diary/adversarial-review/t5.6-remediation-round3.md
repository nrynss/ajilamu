# T5.6 Remediation Round 3

| Finding | Fix | Pin | Mutation |
|---|---|---|---|
| F3 | Replaced value-specific contenteditable selectors with an effective editability check. The guard finds the nearest `contenteditable` ancestor and uses `HTMLElement.isContentEditable`. This covers `true`, an empty attribute, `plaintext-only`, and inherited editable descendants. | An independently compiled handler test sent Space from a simulated plaintext-only editor. The handler returned false, did not prevent default, and did not toggle playback. The same event from a non-control prevented default and toggled playback. `npm run check` and `npm run build` completed with zero diagnostics. | Reverting the effective editability check lets workspace shortcuts consume typing or caret keys inside a plaintext editor. |
