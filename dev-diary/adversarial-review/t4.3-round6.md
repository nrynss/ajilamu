# T4.3 adversarial review, round 6

Verdict: REMEDIATE

Counts: C0, H1, M0, L0.

Prior-round residue: Round 1 remains closed. The capture regression persists roots,
chains, forks, omitted client timestamps, invalid-shape rejection, and byte-identical replay.
Round 2 remains closed. A child requires an existing, lower-version parent. Round 3 remains
closed. The retry test holds the failure until it observes the durable journal entry. Round 4
remains closed for an identity already visible in ClickHouse. Round 5 remains closed for two
concurrent callers whose first insert succeeds. The new finding exercises a distinct durable
queue state. No earlier fixed behavior has residue.

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| H | `internal/ledger/commits.go:81-89`, `internal/ledger/client.go:154-163` | The identity gate releases when the first `EnqueueJSON` returns `ErrPending`. That call has journaled its payload. A failed delivery leaves no ClickHouse row for the next identity lookup. A conflicting second call then passes identity validation and journals a second payload. `ReplacingMergeTree` can expose either row after merge. | `TestAppendCommitRejectsConflictingPendingIdentity` is missing. It must force a first delivery to stay pending, attempt conflicting data with the same identity, then recover. It must measure one durable payload and one eventual insert. `go test -race -count=20 ./internal/ledger`, `go vet ./internal/ledger`, formatting, and diff checks passed. They do not observe this queue state. | Failing to extend the gate to local pending identity state lets an outage produce two durable rows for one `(dub_id, commit_id)`. Recovery may change the visible parent, version, or message of an immutable history node. |

Independent evidence:

- The round-5 test still proves the keyed gate produces one insert, one success, and one
  `already exists` error when the accepted row becomes visible before the second lookup.
- The durable queue has the opposite required state after a 503. It writes the JSON file before
  sending, retains it on failure, and returns `ErrPending`. The remote `FINAL` lookup reads no
  local journal entries.
- `go test -race -count=20 ./internal/ledger` passed. The race detector reports memory races,
  not the valid serial interleaving between an earlier failed call and a later conflicting call.
- The existing chain and fork checks still preserve root, child, and historical-branch parent
  pointers. The parent validation still rejects absent and later parents before an insert.
- `go vet ./internal/ledger`, `gofmt -d internal/ledger/commits.go
  internal/ledger/commits_test.go`, and `git diff --check -- internal/ledger/commits.go
  internal/ledger/commits_test.go dev-diary/PHASE-4-ledger.md` passed.
