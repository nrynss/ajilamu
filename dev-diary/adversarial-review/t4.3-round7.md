# T4.3 adversarial review, round 7

Verdict: REMEDIATE

Counts: C0, H0, M1, L0.

Prior-round residue: Round 1 remains closed. `commits_test.go` keeps the
durable transport regression for roots, chains, forks, invalid shapes,
server-owned timestamps, and exact replay. Round 2 remains closed. A child
requires a visible lower-version parent. Round 3 remains closed. The replay
test retains a failed journal entry before recovery. Round 4 remains closed.
A visible duplicate identity rejects before its insert. Round 5 remains
closed. The keyed gate permits one concurrent identity append. Round 6 remains
closed. The pending-identity test measures a first 503, one durable original
payload, rejection of the conflicting append, no second journal, then exactly
one recovered original delivery. The new handoff discrepancy is not residue
from those behavior fixes.

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| M | `dev-diary/PHASE-4-ledger.md:223-224` | The T4.3 handoff says the task owns no test path and that only an isolated harness observed the DAG. The task now owns `internal/ledger/commits_test.go`, which contains committed HTTP artifact tests for those rules and the later identity repairs. The handoff is false and directs the next agent away from its permanent regression proof. | `PHASE-4-ledger.md:80` names `internal/ledger/commits_test.go` in T4.3 ownership. `rg -n '^func TestAppendCommit' internal/ledger/commits_test.go` reports six committed tests. The focused artifact suite passed ten uncached race runs. | Reverting the correction leaves a stale handoff that tells a future maintainer the required DAG and immutable-identity checks are ephemeral. They can remove or bypass the committed tests while believing no CI boundary exists. |

Independent evidence:

- `go test -count=10 -race -run 'TestAppendCommit(WritesDAGWithoutClientTimestamps|ReplaysFailedDelivery|RejectsConflictingPendingIdentity|RejectsLaterAndCyclicParentsBeforeInsert|RejectsDuplicateIDBeforeInsert|SerializesConcurrentDuplicateIdentity)$' ./internal/ledger` passed.
- `TestAppendCommitRejectsConflictingPendingIdentity` holds the first insert at 503. It observes one journal entry with the original body, rejects a changed duplicate as already queued, retains one pending entry, then accepts exactly one recovered delivery of the original body.
- The DAG artifact test captures a root, linear child, and fork. It independently asserts `commits_raw`, the parent pointers, version values, branch labels, and missing client timestamps.
- The duplicate and concurrent-duplicate tests measure zero insert transport for a visible identity, then one insert and one rejection for a conflicting race. The parent test measures zero inserts for absent and later parents.
- `go test -count=20 -race ./internal/ledger`, `go vet ./internal/ledger`, `gofmt -l internal/ledger/commits.go internal/ledger/commits_test.go`, and `git diff --check -- internal/ledger/commits.go internal/ledger/commits_test.go` passed.
