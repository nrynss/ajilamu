# T7.0a Round 1 Remediation

| # | Sev | Remediation | Independent pin |
|---|---|---|---|
| 1 | H | Added `web/build/` to `.gitignore` under Node and Svelte artifacts. The rule ignores build output emitted by the static adapter. | `git check-ignore -v web/build/index.html` returns `.gitignore:17:web/build/ web/build/index.html` with exit code 0. `git add -A -n` does not list `web/build`. |
| 2 | M | Updated the T7.0a handoff section in `dev-diary/PHASE-7-ingestion.md`. The bullet now records the `.gitignore` contract change and the ignore rule. | `dev-diary/PHASE-7-ingestion.md` line 213 records the contract change and the ignore rule. |

## Validation

- `git check-ignore -v web/build/index.html` reports the new ignore rule.
- `git add -A -n` does not list `web/build`.
- `npm --prefix web run build` emits `web/build/index.html`.
