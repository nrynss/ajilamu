# T1.3 Round 5 Review

**Verdict:** APPROVE
**Artifact:** sql/schema.sql, 889 lines, untracked at review time.
**Reviewer:** I am fresh. I did not implement T1.3. I wrote none of the round 1 through round 4 reviews. I remediated no round. I differ from every prior reviewer.

## Severity Counts

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 0 |

## Residue Against Round 4

Round 4 finding 1 has zero residue. The takes key now leads with dub and language and commit and segment and attempt and closes on event key. The charges key repeats that prefix and adds kind before event key. Both comments state the full tuples. My branch repro returned 2 matches totaling 0.003 on the five column join and 4 matches totaling 0.006 without commit on both servers.

Round 4 finding 2 has zero residue. The guard compares the exact engine argument list after stripping Cloud macro arguments. A two argument engine on takes now aborts with Code 395 on both servers and creates nothing. The unmodified file passes on self built databases on both servers.

Round 4 finding 3 has zero residue. The guard checks each declared constraint fragment with its trailing delimiter and counts CONSTRAINT tokens against the declared total. A weakened peaks check and an added reject all check each abort with Code 395 on both servers.

Round 4 finding 4 has zero residue. The guard compares default kind and default expression for every declared column in both directions. A materialized attempt column aborts with Code 395 on both servers. The unmodified file still passes on both servers.

## Findings

| # | Sev | Where | What | Pin | Mutation |
|---|---|-------|------|-----|----------|

No findings. The table stays empty.

## What I Confirmed Working

1. Cloud reported 26.2.1.641 and the local binary reported 26.8.2.7. I read both version strings myself.
2. One unmodified file created 5 tables and 7 views on each server. The preflight printed 0 on both empty databases.
3. A second full run printed 0 on both servers. A final run over the probe laden databases printed 0 on both.
4. A half applied database held 2 objects on Cloud. The full file resumed it to 12 objects and printed 0. The local resume also reached 12 user tables and printed 0.
5. The fixture load accepted 6 commits and 10 takes and 22 actions and 17 charges and 22 snapshots on both servers. Plain views read 6 and 10 and 22 and 17 and 21. The two edit pair under one commit collapsed to the corrected survivor.
6. Fixture charges grouped to 0.000067 for segmentation and 0.000067 for translation and 0.02328 for synthesis. The total read 0.023414 on both servers.
7. Segment 8 read slot 7110 and measured 4200 and delta negative 2910 on both servers. The stretched take peaks read length 64 and first value 44 and last value 92 on both.
8. A full charge resend under fresh charge ids and re cut into batches of 5 and 12 left the charges view at 17 rows and 0.023414 on both servers. A take resend under a fresh take id left the takes view at 10 on both. Raw tables grew to 34 charge rows and 20 take rows on Cloud.
9. A genuine repeat at attempt 2 raised the branch database charge view to 3 rows and 0.0045 on both servers. A retry at the same attempt collapses while a new attempt bills, as designed.
10. Two unrelated commits survived as two rows on both servers. Two takes sharing one take id survived as two rows on both. Two snapshots and two actions sharing one action id and two charges sharing one charge id did the same. The take rates view also returned both takes on both.
11. An insert into the plain charges view failed with Code 48 on both servers. Six shape probes failed with Code 469 on both. Three enum probes failed with Code 691 on both. A cost column on takes failed with Code 16 on both. A direct cost write on charges failed with Code 44 on both. Every view count and cost stayed fixed after all twelve probes on both.
12. A commit naming created at as 2099 stored that value unchanged on both servers. The parts list showed only partition 202609 for commits on Cloud, with no 209912 partition.
13. Five timeline reads returned 40 rows per server. My independent replay of the commit graph found zero field and zero version mismatches across all 80 rows. The two edit survivor carries speaker Mark Vande Hei and the corrected source text. The fork read keeps the earlier spelling and the earlier speaker.
14. All eight guard mutations aborted independently with Code 395 on both servers. Four came from round 4 and four came from round 3. Every failed run kept all 12 existing objects. Every mutated file applied cleanly to its own fresh database first, so each mutation truly exercised the guard.
15. The charges kind vocabulary matches internal cost exactly with segment and translate and synthesize. The repair vocabulary matches internal types exactly with none and atempo and rewrite and manual. Both files are frozen and I changed neither.
16. I parsed all 325 comment lines as 51 paragraphs against the four house style rules. I found no semicolon and no em dash or en dash and no sentence over 30 words. One heuristic hit names a DDL keyword used attributively in pre existing header text, which the passive allowance covers.
17. Cloud engine text shows two macro arguments before user arguments on all five tables. The guard strips them and compares exactly. Timeline state keeps ingested at while the other four tables carry no argument. Self built databases pass this comparison on both servers, so the branch holds no false positive on either.
18. The constraint counter only counts CONSTRAINT tokens on owned raw tables. Views cannot trip it. Self built databases pass on both servers, so Cloud DDL normalization of the declared constraints holds no false positive either.

## Verification

I ran every measurement myself on ClickHouse Cloud and the local ClickHouse binary. I used a separate throwaway database or local path for each independent mutation.

I applied the unmodified file through query file mode on both servers. I built each mutation from that file and changed one property for each required guard check.

I loaded fixture rows derived from the manifest totals and the expected metrics. The load held 10 fixture takes and 17 fixture charges summing to 0.023414. I queried views immediately after the load on both servers.

I sourced the gitignored environment file inside each Cloud command. The client read its user and password from the environment. No command printed them. Local runs used the scratch binary as clickhouse local under a dedicated path. Every invocation carried a timeout. Files with inline values read stdin from the null device.

I dropped all 11 throwaway Cloud databases. A final query found zero matching databases. The default takes table held 8 rows before and after the review. I never wrote to default.

I removed my scratch work area after local verification. I wrote only this verdict file.
