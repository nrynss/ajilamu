# T8.1 rerun on the deployed host

**Date:** 2026-09-09
**Agent:** T81Rerun, verification role
**Verdict:** three claims pass. One claim is partial, because the run returned 7 dialogue lines and has no segment 8. One claim fails. The run finished with a `done` event and two flagged lines.

The charges view holds 49 rows and sums to 56,884,350 nanodollars. That sum is 21,000 short of the run's reported 56,905,350. One Gemini translate charge is missing.

**Tree:** commit `ae1a3e0`, the commit the host serves. The checkout carries the same commit with a clean tree. No application file changed. The only repository write is this record.

## Run identity

| Field | Value |
|---|---|
| Dub id | `bb2c96d89be1cb62dc1ebb87979b95da` |
| Run id | `8262f498a33aefafaa1c457c99bb2e27` |
| Language | `ml`, resolved to `ml-IN` Malayalam |
| Hostname used | `34-63-219-30.sslip.io` over TLS |
| Run POST | 2026-09-09T16:03:38Z |
| Events stream close | 2026-09-09T16:07:59Z |
| Wall time | 261 seconds, run POST to events stream close |
| Terminal event | `done`, "Dubbing pipeline completed with 2 flagged lines." |
| Cost from the ledger | **56,884,350 nanodollars** |
| Run-reported cost | 56,905,350 nanodollars |
| Dialogue lines returned | 7 |
| Export | `dubbed_ducked.mp4`, 75.008267 seconds, 18,375,954 bytes |

`ajilamu.nryn.dev` also answers 200 at the origin address, checked with `curl --resolve`. The operator resolver does not resolve that name, so the run used the sslip.io name.

## How the run was driven

`deploy/run-sample.sh` sent the requests over TLS. It created the sample, started the run, followed the events stream to its terminal event, fetched the export, measured its tail, and queried the ledger. Evidence sits under `/home/nryn/t81-rerun-evidence`. The deployed systemd service served the run. I started no server on the host.

## Claim rows

| # | Claim | Verdict | Independent measurement | Evidence | Mutation that hides the defect |
|---|---|---|---|---|---|
| 1 | Segments 3 and 4 repair by time-stretch, and the repaired take lands inside its slot | **PASS** | The takes view gives segment 3 attempt 2 repair `atempo` ratio 0.9813, and segment 4 attempt 2 repair `atempo` ratio 0.9844. `ffprobe` on the two served take files measures 5.335500 seconds against a 5.34 second slot and 5.111208 seconds against a 5.12 second slot. Both land inside the slot. Attempt 1 on each segment carries repair `rewrite`, so the loop rewrote and then stretched the second attempt. | `takes.tsv`, `seg_3_try2_stretched.wav`, `seg_4_try2_stretched.wav`, `seg_3_result.json`, `seg_4_result.json`, `tail-levels.txt` | Widening the fit tolerance or the stretch dead band would report a take that misses its slot as a fit, which hides a bad landing. |
| 2 | Segment 8 reports as a miss rather than a fit, and the length bar names the shortfall | **PARTIAL, no segment 8** | Segmentation returned 7 dialogue lines, not the baseline 8. Line 7 is the returned short line. The workspace payload gives line 7 fit state `too_short` and delta -640 ms with `flagged` true. The live page at `/d/bb2c96d89be1cb62dc1ebb87979b95da` renders "Line 7 length bar. This take is 0.64 seconds short of the slot." Line 6 is `too_long` by 1360 ms. The mechanism holds on the line the run returned. | `workspace.json`, `ui-workspace.txt`, `ui-workspace.png`, `events.sse`, `takes.tsv` | Reverting the short-take check to an overrun-only test would report the 640 ms short take as a fit, and the bar would say the take fits the slot. |
| 3 | The exported tail carries audio, not silence | **PASS** | `volumedetect` over 54.5 to 75.0 seconds gives the source mean -15.4 dB and max -0.1 dB, and the export mean -16.9 dB and max -0.5 dB. The last 5 seconds give the source mean -15.1 dB and max -0.1 dB, and the export mean -17.1 dB and max -0.6 dB. Digital silence measures -91 dB. The source matches the observations baseline exactly. | `tail-levels.txt`, `source.mp4`, `dubbed_ducked.mp4`, `volumedetect-tail.txt` | Assembling the export from the silent clean buffer would measure -91 dB and hide the missing audio. |
| 4 | The ducked export plays speech at full level while the background dips | **PASS** | Least squares on the decoded export against `speech.wav` and `bed.wav` gives `alpha_speech` 0.9968 and `g_bed` 0.5078 over the segment 1 speech window 24.2 to 30.2 seconds. The speech-free gap 53.0 to 58.0 seconds gives `alpha_speech` 0.0000 and `g_bed` 0.9996. Speech sits at unity. The bed dips to 50.8 percent under speech and returns to 99.96 percent in the gap. | `fit_levels.py`, `fit-levels.txt`, `bed.wav`, `speech.wav`, `dubbed_ducked.mp4` | Passing `normalize=1` to `amix` or dropping the sidechain would halve the speech or leave the bed flat, which hides the duck. |
| 5 | Logged charges cover every Gemini call and count each one once | **FAIL** | The charges view holds 49 rows with 49 distinct `event_key`. It holds 1 segment call in 2 unit rows, 16 translate prompt rows but only 15 translate candidate rows, and 16 synthesize rows. The run made 17 Gemini calls (1 segment and 16 translate) and 16 speech calls. The 16 synthesize rows prove 16 attempts, and each attempt translates once. The view sums to 56,884,350 nanodollars. The run reported 56,905,350. The gap is 21,000 nanodollars, which is exactly one `candidate_tokens` row of 35 tokens at 0.0000006 USD. Two segment 1 translate calls both billed 35 candidate tokens. For segment 1 the writer stamps both calls with the chosen take attempt 2 and `call_index` 0, so the two rows share an `event_key` and ClickHouse keeps one. The covers string also reports 7 translation calls and 7 render calls against 16 and 16. | `charges.tsv`, `charges-by-kind.txt`, `events.sse`, `workspace.json`, `chq.sh`, `run-sample.out` | Reporting the run total from the ledger would make the two totals agree and hide the missing charge row. |

The charges sum does not equal the run's reported total. It is short by 21,000 nanodollars.

## Charge reconciliation

| Call kind | Calls the run made | Rows in the charges view | Unit rows | Nanodollars in the ledger |
|---|---:|---:|---|---:|
| Gemini segment | 1 | 2 | prompt 7227, candidate 716 | 1,513,650 |
| Gemini translate | 16 | 31 | prompt 16, candidate 15 | 590,700 |
| Chirp synthesize | 16 | 16 | characters 16 | 54,780,000 |
| Total | 33 | 49 | | 56,884,350 |

The segment total and the synthesize total match the run's own accounting. The translate total does not. The run added 611,700 nanodollars of translate cost. The ledger holds 590,700.

The identity that decides a charge row is `sipHash128` over dub, language, commit, segment, attempt, turn, `call_index`, kind, provider, unit, units and price. Two segment 1 translate calls billed the same 35 candidate tokens at the same price, and both rows carry attempt 2 and `call_index` 0. The hash is therefore identical, and the `ReplacingMergeTree` keeps one row.

## Cost

The ledger cost of this run is **56,884,350 nanodollars**. The run log reported 56,905,350. The ledger figure comes from `SELECT sum(toInt64(round(cost_usd * 1000000000))) FROM charges WHERE dub_id = ...` and it matches the workspace `total.total_nanodollars`.

## Defects outside T8.1

**The index and the chrome header report a finished dub as unstarted and free.** `GET /api/dubs` gives this dub readiness `pending` and total 0. `GET /api/dubs/bb2c96d89be1cb62dc1ebb87979b95da` gives readiness `review` and total 56,884,350. The live page header reads "Queued to start" and "$0.00 spent". The index provider appends `api.ListUploadSummaries(uploadDir)`, which reads `project.json` and never reads the ledger. The four earlier runs show the same zero.

**The flagged-line copy understates the attempt count.** Lines 6 and 7 read "after 1 try" and the Lines table shows `tries` 1. The run rendered three takes for each line. `seg_6_result.json` lists three attempts, and the charges view holds three synthesize rows for segment 6 and three for segment 7.

**Charges from discarded attempts carry the chosen take attempt number.** Every segment 1 charge row carries attempt 2, including the call that produced attempt 1. `TakeAttempt.chargeRows` writes `uint8(a.Take.Attempt)` into every row.

**The run writes only the ducked export.** The work directory holds `dubbed_ducked.mp4` and no replaced or clean export. The observations baseline clean-export row has no product counterpart. This is the open M finding in `t8.1-run.md`.

**The Cloud TTS `InvalidArgument` on a long sentence did not recur.** The run window carries no error line, no `run failed` line and no `InvalidArgument` line in the journal. All 16 speech calls returned.

## Evidence paths

All under `/home/nryn/t81-rerun-evidence` unless stated.

| Path | Contents |
|---|---|
| `run-sample.out` | the driver output, wall time 261 seconds, terminal `done`, reported total |
| `sample.json`, `run.json` | the sample id and the run id |
| `events.sse` | the full event stream, 66 frames and the terminal `done` |
| `workspace.json` | the workspace payload, 7 segments, fit states, total |
| `charges.tsv` | all 49 charge rows with their identity fields |
| `charges-by-kind.txt` | rows, distinct keys and nanodollars per kind |
| `takes.tsv` | the 7 take rows with slot, measured, delta and repair |
| `seg_*_result.json` | per-segment attempts, chosen take and repair |
| `seg_3_try2_stretched.wav`, `seg_4_try2_stretched.wav` | the two stretched takes fetched over TLS |
| `bed.wav`, `speech.wav` | the assembly layers fetched over TLS |
| `source.mp4` | the sample source, `sha256 da7e0212` |
| `dubbed_ducked.mp4` | the export fetched over TLS |
| `tail-levels.txt` | the four `volumedetect` measurements |
| `fit_levels.py`, `fit-levels.txt` | the ducking least-squares fit |
| `ui-workspace.txt`, `ui-workspace.png` | the live page text and screenshot |
| `chq.sh` | the ClickHouse query helper, no credential in the file |
| `/home/nryn/t81-rerun-evidence/.chcfg` | the curl config for the ledger queries, mode 0600 |

## Cleanup

I started no server on the host and stopped none. The deployed systemd units served every request. The ledger queue on the host is empty. No browser tab stays open. The host still serves both names.
