# T4.3 adversarial review, round 4

Verdict: REMEDIATE

Counts: C0, H1, M0, L0.

Prior-round residue: Round 1 is closed. `commits_test.go` now keeps the durable
HTTP capture and journal replay evidence in CI. Round 2 is closed. A child lookup
requires a present parent with a lower version. Round 3 is closed. The replay
server retains 503 responses until the test observes one pending `.json` journal
entry, then the reconciler replays it. I ran `go test -count=20 -race
./internal/ledger` and the three commit tests ten more uncached times. All passed.
The capture and journal assertions cannot pass before the failed delivery exists.
No residue remains from durable regression coverage, DAG parent validation, or the
deterministic replay test.

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| H | `internal/ledger/commits.go:31-42`, `internal/ledger/commits_test.go` | `AppendCommit` never checks whether its `(dub_id, commit_id)` already exists. A second call with that identity and changed parent, version, or fields reaches `commits_raw`. `ReplacingMergeTree` then collapses the natural identity at read time, so the visible commit can change without an update statement. This violates immutable commits and can replace an established DAG edge. | `TestAppendCommitRejectsDuplicateIDBeforeInsert` is missing. The current HTTP capture accepts each insert query and has no duplicate-id assertion. The source sends root commits directly to `EnqueueJSON`, so no lookup runs on that path. | Reverting a duplicate-id guard lets a retry with altered data rewrite a history node's visible parent or version. `timeline_at_commit` can then reconstruct a different ancestry for an existing commit id. |

Independent evidence:

- The race run executed the entire ledger package twenty times without a race or
  failure. The focused commit tests executed ten further uncached runs.
- `TestAppendCommitReplaysFailedDelivery` observes one pending queue entry and one
  `.json` journal artifact while every delivery returns 503. Only after recovery
  does it accept the replay and observe zero pending entries. The first and replayed
  request bodies are compared as bytes.
- The DAG capture observes a root, linear child, and historical fork. It checks the
  `commits_raw` JSONEachRow target, parent pointers, branch labels, versions, and
  absence of client timestamps. The parent-validation capture rejects missing and
  later parents before an insert.
- `AppendCommit` contains no update or delete query. That is insufficient for
  immutability because the raw table's natural key makes a changed duplicate commit
  id a replacement at read time.
- `go vet ./internal/ledger`, formatting checks for the two T4.3 files, and
  `git diff --check` for T4.3 paths passed.
