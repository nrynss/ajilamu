# T4.2 adversarial review, round 2

Verdict: REMEDIATE

Counts: C 0, H 0, M 0, L 1.

## Scope and evidence

I reviewed `internal/ledger/takes.go` and the new `takes_test.go` against T4.2, T1.1,
T1.2, T1.3, and the T4.1 queue contract. I also reviewed the round-one finding and its
remediation record.

`go test ./internal/ledger -run '^TestRecordTakeFixtureCapture$' -count=1 -v` passed.
Its fresh local HTTP capture sent ten take rows and thirty charge rows through the T4.1
transport. The capture checks three distinct charge rows per attempt, segment 8 at
`slot_ms=7110`, `measured_ms=4200`, and `delta_ms=-2910`, pre-transport rejection of an
inconsistent delta, and unchanged retry identity.

`go test ./internal/ledger -count=1`, `go test ./...`, `go vet ./...`, `gofmt -d` on the
two T4.2 Go files, and `git diff --check` on T4.2 paths passed. The source inserts only
into `takes_raw` and `charges_raw`. It emits separate prompt token, candidate token, and
character charge rows. It writes no `cost_usd` field.

## Findings

| # | Sev | Where | What | Pin | Mutation |
|---|---|---|---|---|---|
| 1 | L | `dev-diary/PHASE-4-ledger.md:172` | The T4.2 handoff still says T4.2 owns no test path and calls for a test-path extension. The task now owns `internal/ledger/takes_test.go`, and the capture test exists. This contradicts the task contract and misdirects the next reviewer. | `rg -n 'T4\\.2 owns no test path' dev-diary/PHASE-4-ledger.md` must return no T4.2 result. The corrected handoff must name the committed capture test. | Restore the obsolete statement. A future agent may treat the maintained regression pin as absent and repeat the already resolved scope change. |

## Residue

Round one's missing regression coverage finding has zero residue in the implementation and
test artifact. The task now owns `internal/ledger/takes_test.go`, and the test independently
captures the requested mapping contract. Finding 1 above is separate documentation residue.
