# T7.0 Round 2 Remediation

| Finding | Remediation | Independent pin |
|---|---|---|
| M4 | `.gitignore` now includes `/data/`. Default uploads and ledger queue files remain outside Git's staging surface. | A live default-data upload created `data/uploads/0213ea08ae5f4f35f5dc00657e0603a1/source.mp4`. `git check-ignore -v data` reported `.gitignore:9:/data/ data`. The server stopped cleanly, and the generated data was removed. |
| L5 | `internal/api/server.go` mounts a `/api/` not-found handler before the SPA fallback. Unmatched API paths and methods no longer receive HTML. `internal/api/server_test.go` pins both rejected and valid routes. | A live server used port 54938. The five rejected requests returned 404. `POST /api/dubs/new` returned 201. `GET /api/dubs` and `GET /api/healthz` returned 200. The static range test returned 206 for video. |
