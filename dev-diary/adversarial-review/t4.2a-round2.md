# T4.2a adversarial re-review, round 2

Verdict: APPROVE

Counts: C 0, H 0, M 0, L 0.

## Scope and evidence

I reviewed T4.2a against its task contract, the frozen `takes_raw.peaks Array(UInt8)` schema,
T4.2's take and charge contract, round one's finding, and its remediation record.

`TestRecordTakeFixtureCapture` independently parses each JSONEachRow HTTP request in its local
capture handler. It observed the exact 64-value unsigned numeric array on segment one and an
empty numeric array on segment two. The same capture observed an exact 128-value numeric array
and one take row with three charge rows.

The handler count remains unchanged after a 63-value vector and after a 129-value vector. Both
calls fail before transport. The test still independently observes ten take rows, thirty
itemized charge rows, segment eight's `-2910` millisecond delta, and stable retry identities.

I ran `go test ./internal/ledger -count=1 -run '^TestRecordTakeFixtureCapture$' -v` and
`go test ./internal/ledger -count=1`. Both passed. `gofmt -d` on the owned Go files and
`git diff --check` on those paths returned no output.

## Findings

No findings.

| # | Sev | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- | --- |

## Residue

Round one's sole M finding had no residue. The remediation permanently captures the 128-value
numeric payload and rejects 129 values without transport. No prior-round findings remain.
