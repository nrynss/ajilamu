# T4.7 remediation, round 1

I remediated finding 1 from `t4.7-round1.md`. I did not change its verdict.

| # | Remediation | Pin | Mutation prevented |
|---|---|---|---|
| 1 | Added `TestDurationPriorCapture` in `priors_test.go`. A local server captures bound parameters and returns controlled aggregates. The test pins `take_rates`, both counts, cold fallback, increasing creator influence, and rejected bad responses. | `go test ./internal/ledger -run '^TestDurationPriorCapture$'` | Interpolation, a dropped filter or count, reversed weighting, or accepted invalid output now fails in CI. |

Verification completed:

- `gofmt -w internal/ledger/priors_test.go`
- `go test ./internal/ledger -run '^TestDurationPriorCapture$'`
- `go vet ./internal/ledger`
