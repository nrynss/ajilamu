# T6.5b remediation, round 2

I did not change the round 2 verdict. I fixed finding M and nothing else.

I edited `cmd/ajilamu/main_test.go` and this record. I did not edit
`cmd/ajilamu/main.go`, because its behaviour is already correct. I did not commit.

| Finding | Fix | Pin | Residue |
|---|---|---|---|
| **M.** Only the fit half of the H1 fix carries a pin. `RenderLine` sets `cfg.AuthoritativeText`, but no shipped test names `RenderLine`. Removing the two lines from `main.go` leaves the suite green and reopens H1. | Added `TestRenderLineCorrectedTargetMissNeverTranslates` in `cmd/ajilamu/main_test.go`. It drives the real `pipelineRunner.RenderLine` with a counting translator and a synthesizer that misses every attempt. It requires zero translation calls, three synthesis calls, the creator's text, and a flagged line. | `TestRenderLineCorrectedTargetMissNeverTranslates`. It records zero translation calls and the creator's text. Removing the two `AuthoritativeText` lines in a `/tmp` copy makes it record two translation calls. | None. The adapter decision now carries its own pin. |

## What I measured

### 1. The new test passes on the shipped code

```text
$ go test -count=1 -run TestRenderLineCorrectedTargetMissNeverTranslates -v ./cmd/ajilamu
=== RUN   TestRenderLineCorrectedTargetMissNeverTranslates
    main_test.go:949: translation calls = 0, synthesis calls = 3, spoken text = "corrected target line", flagged = true
--- PASS: TestRenderLineCorrectedTargetMissNeverTranslates (0.24s)
PASS
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	0.240s
```

### 2. The mutation fails it

I copied the tree to `/tmp/t65b-r2-remediation`. I removed the `if req.Text != ""`
block that sets `cfg.AuthoritativeText` in `RenderLine`.

```text
$ go test -count=1 -run TestRenderLineCorrectedTargetMissNeverTranslates -v ./cmd/ajilamu
=== RUN   TestRenderLineCorrectedTargetMissNeverTranslates
    main_test.go:949: translation calls = 2, synthesis calls = 3, spoken text = "corrected target line", flagged = true
    main_test.go:953: translation calls = 2, want 0 on the corrected target path
--- FAIL: TestRenderLineCorrectedTargetMissNeverTranslates (0.24s)
FAIL
FAIL	github.com/nrynss/ajilamu/cmd/ajilamu	0.241s
FAIL
```

The mutated copy fails this test alone. The other shipped tests stay green.

```text
$ go test -count=1 ./cmd/... ./internal/api ./internal/fit
--- FAIL: TestRenderLineCorrectedTargetMissNeverTranslates (0.23s)
    main_test.go:949: translation calls = 2, synthesis calls = 3, spoken text = "corrected target line", flagged = true
    main_test.go:953: translation calls = 2, want 0 on the corrected target path
FAIL
FAIL	github.com/nrynss/ajilamu/cmd/ajilamu	4.323s
ok  	github.com/nrynss/ajilamu/internal/api	2.060s
ok  	github.com/nrynss/ajilamu/internal/fit	18.274s
FAIL
```

### 3. The shipped suite passes

```text
$ go test -count=1 ./cmd/... ./internal/api ./internal/fit
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	4.140s
ok  	github.com/nrynss/ajilamu/internal/api	1.972s
ok  	github.com/nrynss/ajilamu/internal/fit	18.290s
```

I did not edit the round 2 review file. I did not touch the verdict.
