# T6.1 Remediation Round 2

This record remediates the C finding in `t6.1-round2.md`. It does not change that review's verdict.

| Finding | Remediation | Independent pin | Mutation protection |
|---|---|---|---|
| C1 | The fixture-loading effect now assigns the parsed fixture to local `fixtureDub` and derives the panel state from that value. It no longer reads reactive `dub` after assigning it. Boundary keyboard nudges bypass pointer snap targets, so a 50ms arrow step cannot snap back to its starting edge. The direct listener delivers the key event to the handle. | Fresh headless Chromium loaded `/d/fixture` with no console errors. It selected Line 2, moved its start from 7,728ms to 7,778ms with ArrowRight, then moved it to 8,778ms with a pointer drag. The LengthBar label changed from 5.480 to 5.430 to 4.430 seconds. Its canvas hash changed after both moves. `npm run check` reported zero diagnostics. `npm run build` completed. | Reverting the local fixture value restores the recursive hydration effect and leaves controls inert. Reverting the keyboard path makes a 50ms nudge snap back to its adjacent boundary, so a creator cannot make the promised keyboard adjustment. |
