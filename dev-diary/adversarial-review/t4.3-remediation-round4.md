# T4.3 remediation, round 4

| # | Severity | What I changed | Measurement proving it closed |
| --- | --- | --- | --- |
| 1 | H | Added a natural-identity lookup before every commit enqueue. An existing `(dub_id, commit_id)` now returns an identity error and never enters the durable queue or insert transport. Added an HTTP capture regression for a changed duplicate. | `go test -race ./internal/ledger` passed. `TestAppendCommitRejectsDuplicateIDBeforeInsert` measured one lookup with `dub` and `root`, zero INSERT requests, and an `already exists` error. |
