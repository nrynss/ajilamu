# T2.6 Remediation Round 1

**Verdict:** unchanged. The round 1 reviewer wrote REMEDIATE and I do not touch that.
**Artifact:** `internal/fit/rewrite.go` and `internal/fit/rewrite_test.go`.
**Remediator:** I did not implement T2.6 and I did not review it.

I closed all six findings from the round 1 review.
Every measurement below comes from compiler checks, test executions, or mutation runs.
I quote no number that any prior log printed about its own work.
I ran every mutation in a throwaway scratch copy, never in this checkout.

I edited four paths:
`internal/fit/rewrite.go`,
`internal/fit/rewrite_test.go`,
`dev-diary/PHASE-2-fit-loop.md` (lines 662 to 673),
and this remediation document `dev-diary/adversarial-review/t2.6-remediation-round1.md`.

## Findings

| # | Sev | What I changed | Measurement that closes it |
|---|-----|----------------|-----------------------------|
| 1 | M | Added `TestRepairLineRefusesStretchedInitialTake` and `TestRepairLineRefusesStretchedInitialTakeThenSynthesizes`. The tests provide an already stretched initial take whose duration falls into the atempo repair window. They verify that `RepairLine` refuses to stretch it again. Attempt 1 marks `RepairRewrite` with `Source take is already stretched.` and attempt 2 synthesizes fresh audio. | I mutated line 358 in a throwaway module copy to `if false && isStretchedTake(audioPath)`. Running `go test -count=1 -run TestRepairLineRefusesStretchedInitialTake ./internal/fit/` failed with exit code 1. `TestRepairLineRefusesStretchedInitialTake` reported `stretch take attempt 1: output take must differ from its source`. `TestRepairLineRefusesStretchedInitialTakeThenSynthesizes` reported `got 1 attempts, want 2`. The unmutated suite passes in 0.23 s. |
| 2 | M | Updated `NewRepairer` and `RepairLine` to validate `cfg.Limits`. Unconfigured zero `StretchLimits{}` defaults cleanly to `DefaultStretchLimits()`. Any non-zero invalid limits return an error wrapping `ErrLimits` instead of silently substituting defaults. Added `TestStretchLimitsValidation` testing both functions across five invalid configurations and verifying zero default fallback. | Calling `NewRepairer` or `RepairLine` with `StretchLimits{Short: 0.25, Long: 0.25}` now returns an error where `errors.Is(err, ErrLimits)` evaluates to true. Mutating either function back to silent default substitution fails `TestStretchLimitsValidation` with exit code 1. |
| 3 | L | Removed redundant unreachable check `math.Abs(plan.Ratio-1.0) > MaxStretchLimit+1e-9` in `RepairLine`. `PlanStretchWithLimits` already handles this bound because relative miss cannot exceed `limit`. Removed unused `math` import. Updated `TestCapTotalStretchLimit` to verify that `NewRepairer` rejects limits exceeding `MaxStretchLimit` and that relative overrun beyond `MaxStretchLimit` routes to `RepairRewrite`. | `go vet ./internal/fit/` exits cleanly with code 0 after removing unused `math` imports. `TestCapTotalStretchLimit` asserts `errors.Is(err, ErrLimits)` on out of bound limits and verifies `PlanStretchWithLimits` returns `RepairRewrite`. |
| 4 | L | Revised `dev-diary/PHASE-2-fit-loop.md` lines 671 to 673. Removed inaccurate claim about cumulative stretch ratio tracking. Stated clearly that each retry synthesizes fresh audio and stretches only fresh takes, refusing re-stretching of already stretched takes. | Grepping for `cumulative` in `dev-diary/PHASE-2-fit-loop.md` matches only sentinel `ErrTotalStretchExceeded`. The handoff entry accurately describes fresh audio synthesis per attempt. |
| 5 | L | Rewrote passive voice sentences on lines 662, 665, and 666 of `dev-diary/PHASE-2-fit-loop.md`. Changed three sentences to active voice: "The loop re-evaluates direction after every attempt from measured duration minus slot." "If three attempts fail to fit, the repairer flags the line." "It sets `Flagged` to true." | A style audit script confirms zero passive voice candidates in lines 645 to 685 of `dev-diary/PHASE-2-fit-loop.md`. |
| 6 | L | Updated `SignedDeltas()` and `SignedDeltaMs()` in `internal/fit/rewrite.go` to derive `a.Fit.Measured - a.Fit.Slot` directly rather than reading `a.Fit.Delta`. Added `TestSignedDeltasDerived` asserting correct signed deltas when `a.Fit.Delta` holds zero. | In `TestSignedDeltasDerived`, input attempts carry `Delta: 0` with measured misfits of +500ms and -300ms. `SignedDeltas()` returns `[500ms, -300ms]` and `SignedDeltaMs()` returns `[500, -300]`. Mutating the code back to reading `a.Fit.Delta` fails the test with exit code 1. |

## Independent Measurements and Pins

A pin is a measurement, never a log line.

**1. Stretched take guard pin.**
`TestRepairLineRefusesStretchedInitialTake` creates a WAV file named `seg_1_try1_stretched.wav`.
Duration measures 2100 ms against a 2000 ms slot (+5.0% overrun).
This falls into the `atempo` window under `DefaultStretchLimits()`.
`RepairLine` refuses to stretch it and records `types.RepairRewrite` with `Source take is already stretched.`
`TestRepairLineRefusesStretchedInitialTakeThenSynthesizes` verifies that attempt 2 synthesizes fresh audio and completes the repair.
Mutating the guard at line 358 causes both tests to fail.

**2. Limits validation pin.**
`NewRepairer` and `RepairLine` now reject invalid non-zero `StretchLimits`.
Both functions return errors wrapping `ErrLimits`.
`TestStretchLimitsValidation` checks five distinct invalid limit pairs:
`{0.25, 0.25}`, `{-0.05, 0.08}`, `{0.05, -0.08}`, `{0.05, 0.20}`, and `{0.0, 0.08}`.
All five return errors wrapping `ErrLimits`.
Zero value `StretchLimits{}` defaults cleanly to `{0.05, 0.08}`.

**3. Derived signed delta pin.**
`SignedDeltas()` and `SignedDeltaMs()` derive signed misses directly from `Measured - Slot`.
`TestSignedDeltasDerived` verifies that attempts with omitted or zero `Delta` fields produce correct signed deltas.
Reverting lines 96 and 105 to read `a.Fit.Delta` returns zero deltas and fails the test suite.

## Mutation Testing

I ran four targeted mutations in a throwaway copy under `/tmp/`.
All four mutations failed the test suite with exit code 1.

| Mutation | Caught | Failing Test | Output |
|---|---|---|---|
| Line 358 disabled (`if false && isStretchedTake(...)`) | yes | `TestRepairLineRefusesStretchedInitialTake` | `stretch take attempt 1: output take must differ from its source` |
| Line 358 disabled with retry | yes | `TestRepairLineRefusesStretchedInitialTakeThenSynthesizes` | `got 1 attempts, want 2` |
| `NewRepairer` silent limit fallback reverted | yes | `TestStretchLimitsValidation` | `NewRepairer with limits {Short:0.25 Long:0.25} err = <nil>, want errors.Is ErrLimits` |
| `RepairLine` silent limit fallback reverted | yes | `TestStretchLimitsValidation` | `RepairLine with limits {Short:0.25 Long:0.25} err = translator is required, want errors.Is ErrLimits` |
| `SignedDeltas` reading `a.Fit.Delta` reverted | yes | `TestSignedDeltasDerived` | `deltas[0] = 0s, want 500ms` |

## Verification Suites

All five verification suites passed cleanly.

- `go test -count=1 ./internal/fit/`: passed in 10.71 s.
- `go test -count=1 -race ./internal/fit/`: passed in 11.80 s.
- `go vet ./internal/fit/`: clean, exits 0.
- `gofmt -l internal/fit/`: clean, prints nothing.
- `go test -count=1 ./...`: passed across all packages.

## House Style Audit

I checked all modified files against the four house rules.

1. No semicolons in comments, prose, or markdown text.
2. No em dashes in comments, prose, or markdown text.
3. Every sentence runs 30 words or fewer.
4. Active voice throughout.

An automated style checker evaluated `internal/fit/rewrite.go`, `internal/fit/rewrite_test.go`, lines 645 to 685 of `dev-diary/PHASE-2-fit-loop.md`, and this document.
The checker reported zero violations.

## Residue

I closed all six findings. I know of no finding from round 1 left open.
