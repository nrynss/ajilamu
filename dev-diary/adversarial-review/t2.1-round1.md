# T2.1 Round 1 Review

**Verdict:** REMEDIATE
**Commit:** New T2.1 segmentation client. Untracked files internal/gemini/segment.go, segment_test.go, segment_live_test.go.

## Severity Counts

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 2 | 2 |

## Findings

| # | Sev | Where | What | Pin | Mutation |
|---|-----|-------|------|-----|----------|
| 1 | M | `internal/gemini/segment_test.go:412-413` | The JWT lifetime check has an empty body. It asserts nothing. A signed lifetime regression passes the suite today. | Staged `Exp: now.Add(2 * time.Hour)` in the scratch copy `scratch/t21rev`. `go test -run TestServiceAccountTokenSource` passed. Filling the body with `t.Errorf` made the same mutation fail with "jwt lifetime = 7200". | Reverting the fix restores the vacuous check. A JWT lifetime regression would pass CI unnoticed again. |
| 2 | M | `internal/gemini/segment_test.go:319-351` | Every test exercises only the default location and model values. Hardcoding either default into `endpointURL` passes the whole suite. The task requires both values from configuration. | Hardcoded `us-central1` and dropped `cfg.GoogleCloudLocation` in `scratch/t21rev`. `go test ./internal/gemini/` passed. Repeated the mutation for the model. The suite passed again. | Reverting the fix leaves the suite blind to a default hardcode. A hardcode would break every non-default deployment and still pass CI. |
| 3 | L | `internal/gemini/segment.go:32-53` | The prompt is not byte verbatim against the Python reference. The Python string value carries a leading and a trailing newline. The Go const carries neither. | Extracted both strings. The Python value is 658 bytes. The Go const is 656 bytes. `cmp` reports the missing head byte and the missing tail byte. | Reverting the fix keeps the two byte drift. Charge units drift by 2 chars from the Python cost basis on every call. |
| 4 | L | `internal/gemini/segment_live_test.go:54` | The live probe assigns `config.DefaultGeminiModel` directly. It honors `GOOGLE_CLOUD_LOCATION` from the environment. `GEMINI_MODEL` never reaches the live probe. | `grep -n GEMINI_MODEL internal/gemini/segment_live_test.go` returns nothing. Line 54 assigns the constant. | Reverting the fix keeps the probe deaf to `GEMINI_MODEL`. Live evidence would silently come from a model other than the operator configured. |

## False Positives and Clean Probes

| Probe | Judgement | Pin |
|-------|-----------|-----|
| 200 reply with finishReason STOP and empty text | Correct failure, no finding. The reply is rejected with zero charges. | `go test -run TestProbeStopEmptyText` in `scratch/t21rev` printed "generateContent reply carries an empty parts text". |
| Slow or partial JSON body | Clear error, no finding. The envelope parse error names the stage. | `go test -run TestProbeTruncatedBody` printed "parse generateContent envelope: unexpected end of JSON input". Zero charges. |
| Charge on a failed parse | Contract holds. Charges land only after a full parse. | Mutation A moved `rec.Add` before `decodeSegments` in `scratch/t21rev`. Two parse-stage tests failed with "recorded 1 charges". HTTP and envelope stage tests still passed by design. |
| Offline and env isolation of default tests | Clean. Default runs touch no network and read no environment or `.env`. | `HTTP_PROXY=http://127.0.0.1:9 HTTPS_PROXY=http://127.0.0.1:9 GEMINI_MODEL=poison GOOGLE_APPLICATION_CREDENTIALS=/nonexistent go test ./internal/gemini/` passed. `.env` appears only in the tag gated live file. |
| JWT exp and iat sanity, cache thread safety | Production code is sound. The 3600 second lifetime measures correctly. The cache serves one exchange under concurrency. | Probe measured iat 1788781739, exp 1788785339. Eight goroutines produced one token server hit. `go test -race ./internal/gemini/` passed. Finding 1 covers the unpinned check. |
| Reuse seam for T2.2 | Sound. `endpointURL`, `postGenerate`, and the request structs stay unexported in package gemini. T2.2 compiles against them. | `go build ./internal/gemini/` and `go doc -all ./internal/gemini` list only the intended exports. |
| Charge units semantics | Correct. Units count prompt characters only. Media bytes are excluded. | Mutation B set `Units: len(segmentPrompt) + len(in.Data)` in `scratch/t21rev`. TestSegmentSuccess failed on 672 versus 656 units. |
| StaticTokenSource field rename to AccessToken | Accepted, not a contract violation. Go forbids a field and a method with the same name on one type. The frozen contract list does not name this struct. The TokenSource interface keeps its Token method. | `go vet` on a minimal probe package printed "field and method with the same name Token". |
| Ownership boundaries | Clean. New files sit only under internal/gemini. The board file carries the two orchestrator claim lines. | `git status --porcelain` and `git diff --stat -- dev-diary/PHASE-2-fit-loop.md` (2 insertions, both status lines). `git diff --stat dev-diary/PHASE-1-contracts.md` stays empty. |
