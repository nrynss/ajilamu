# T7.4 Round 1 Adversarial Review

## Verdict

**REMEDIATE**

| C | H | M | L |
|---|---|---|---|
| 0 | 1 | 2 | 1 |

## Scope and Evidence

I reviewed the T7.4 task contract, its handoff wire contract, `ui-ux.md`, the current
implementation, and the T7.0 mount boundary. I did not modify implementation files.

`npm --prefix web run check` completed with zero Svelte diagnostics. `npm --prefix web run
build` completed. `go test ./internal/api ./internal/config ./cmd/ajilamu` passed. The full Go
suite remains red in pre-existing `internal/media` fixture tests. Those failures do not touch
this seam.

I started the compiled server without credentials and measured `GET /api/dubs` with `xh`. It
returned 200, `Cache-Control: no-store`, and one correctly shaped JSON summary. I posted two
redacted form values and an extra URL-encoded field to `/api/config`. The handler reached its
configured callback and returned its fixed 500 response. Neither response exposed either test
value.

## Findings

| # | Sev | Where | What | Pin | Mutation |
|---|-----|-------|------|-----|----------|
| 1 | H | `web/src/routes/+page.svelte` lines 1 to 9 and 25 to 42, against the T7.4 done condition | The project index still renders one hard-coded `fixture` row. It never requests `GET /api/dubs`, so active projects cannot appear and the handler's RFC 3339 ordering cannot reach a creator. This crosses the completed T5.1 ownership seam, which leaves no current task owning the required integration. | A live server returned a JSON project whose id was `d3bca364-9c8a-4107-8df9-c4faf909b008`. The browser's root route rendered only literal id `fixture`. A source search found neither a fetch call nor `/api/dubs` in that page. | Revert the integration once added and every later project summary, including a newer upload, remains absent from `/`. The route continues to display a fixture row, not the active projects required for the demo. |
| 2 | M | `web/src/routes/config/+page.svelte` lines 10 to 31 and `internal/api/index.go` lines 108 to 114 | A successful save produces `204 No Content`, but the settings page uses a native form with no submit handler, success state, or field reset. HTTP 204 preserves the current document, so the password controls retain submitted text for client-side inspection after a save. The page also gives the creator no confirmation that persistence succeeded. | Browser inspection found two writable password controls and no success region. The source has no script, submit listener, bound value, or reset. The server explicitly responds with 204 after `save(update)` succeeds. | Revert the client submission handling and a saved credential stays in the live page DOM until the user clears or navigates away. Anyone with access to that browser session can read it, despite the write-only requirement. |
| 3 | M | `internal/api/index.go` lines 92 to 102, `ConfigHandler` | The handler accepts arbitrary additional URL-encoded fields. The contract says it accepts only `voice_key` and `translation_key`, but it validates each required name and silently ignores every other field. This weakens the settings endpoint's strict wire boundary before T7.4a attaches durable storage. | I posted URL-encoded `voice_key`, `translation_key`, and `unexpected=accepted` to the live endpoint. It returned the callback's 500 instead of the handler's fixed 400, proving the extra field passed validation and reached `save`. | Revert an unknown-field check and new client fields or an accidental secret-bearing field can reach the persistence callback without an API contract review. The endpoint then silently drops the field rather than rejecting the bad request. |
| 4 | L | `internal/api/index.go` and `internal/api` test inventory | T7.4 has no focused handler tests. The current server tests exercise static serving, deferred ledger credentials, and shutdown only. They do not pin sorting, input copying, content-type enforcement, request limits, duplicate fields, callback failures, or secret-free responses. | `rg -n 'IndexHandler|ConfigHandler|voice_key|translation_key' internal/api/*_test.go` returned no matches. The focused package test passed, so it cannot distinguish this implementation from one that reverses ordering or returns a posted value. | Revert any guard in either handler and the current suite remains green. A future change can expose a credential or change index order with no task-level failure. |

## Residue

Round one is not approvable. Findings 1 through 4 remain open. Finding 1 needs a task split or
an explicit ownership transfer for `web/src/routes/+page.svelte`, because T7.4 does not own that
file. The remaining findings fit the T7.4 seam.
