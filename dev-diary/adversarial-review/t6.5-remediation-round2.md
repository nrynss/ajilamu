# T6.5 remediation, round 2

I did not change the round 2 verdict. I fixed finding M and nothing else.

I edited `web/src/lib/edit/Speaker.svelte`, `web/src/routes/d/[id]/+page.svelte`, and this
record. I did not commit.

| Finding | Fix | Pin | Residue |
|---|---|---|---|
| **M.** `confirm()` awaits `onchange` and then writes its note from the reply. It never checks that the selected line is still the one it started on. The page's `handleSpeakerChange` writes `lineNote` from the reply with the same gap. A creator who confirms a speaker change and selects another line before the 201 lands sees the old line's sentence on the new line. | `confirm()` captures `const targetSegment = segment` before the await. It returns when `segment.id !== targetSegment.id`, before any note write. `handleSpeakerChange` writes `lineNote` only when `selectedSegmentId === change.segment.id`. Both follow the `Text.svelte` line 132 segment id guard. | Headless Chromium against the built bundle with a 1500 ms rerender reply. I confirmed the line 3 speaker change and selected line 1 within 12 ms. After 2200 ms the line 1 Speaker panel and take strip both held no note. The reply landed and applied the line 3 take. | None. |

## What I measured

I copied `web/` to `/tmp/rem65r2/web` and symlinked `node_modules`, `testdata`, and
`static/clip.mp4`. I built with `npm --prefix web run build`. A throwaway Node server
served the built bundle, the frozen `testdata/wire/dub.json` under `t65-rem-dub`, and a
rerender reply delayed 1500 ms. I drove the page with headless Chromium. The harness never
edited the working tree.

### 1. The fixed build drops the stale reply

The line 3 speaker change confirmed, then the run selected line 1 at 12 ms. The reply
landed at 1500 ms. The read happened at 2200 ms.

```json
{
  "selectedLine": "1",
  "speakerEyebrow": "Line 1",
  "speakerHeader": "Suni Williams",
  "speakerNote": null,
  "speakerError": null,
  "stripNote": null,
  "chips": ["seg_1_try1.wav"],
  "speakerTakes": ["seg_1_try1.wav"]
}
```

Both panels held no note on line 1. Neither carried the old line 3 sentence.

The reply was real and it landed. The server log recorded the request and the delay.

```text
2026-09-08T23:53:25.872Z POST /api/dubs/t65-rem-dub/lines/3/rerender
2026-09-08T23:53:25.872Z RERENDER seg=3 speaker=Mark Vande Hei replyIn=1500ms
```

The reply also applied. A later selection of line 3 read the new header and the new take.

```json
{
  "speakerHeader": "Mark Vande Hei",
  "chips": ["seg_3_try1.wav", "seg_3_stretched.wav", "seg_3_try3.wav"],
  "speakerNote": null
}
```

The guard keeps the success path. A second confirm while the run stayed on line 3 wrote
the note in both panels.

```json
{
  "speakerNote": "Line 3 re-rendered as take seg_3_try3.wav.",
  "stripNote": "Line 3 re-rendered as take seg_3_try3.wav.",
  "speakerHeader": "Suni Williams"
}
```

### 2. The mutation fails the pin

I copied the tree to `/tmp/rem65r2/mut-web`. I removed the `segment.id !== targetSegment.id`
block and the `selectedSegmentId === change.segment.id` block. I rebuilt and drove the same
sequence. The old line 3 sentence returned on line 1.

```json
{
  "selectedLine": "1",
  "speakerEyebrow": "Line 1",
  "speakerHeader": "Suni Williams",
  "speakerNote": "Line 3 re-rendered as take seg_3_try3.wav.",
  "speakerError": null,
  "stripNote": "Line 3 re-rendered as take seg_3_try3.wav.",
  "chips": ["seg_1_try1.wav"]
}
```

The line 1 panel and strip both report a line 3 take while the header reads Suni Williams
and the chip reads `seg_1_try1.wav`. The pin reddens under the mutation.

### 3. The type check

```text
$ npm --prefix web run check
1788911643248 START "/home/nryn/work/ajilamu/web"
1788911643250 COMPLETED 183 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
```

I did not edit the round 2 review file. I did not touch the verdict.
