# T6.7a review, round 2

**Verdict: APPROVE. Zero findings at every severity.**

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 0 |

Round 1's blocker B1 is closed. A fresh durable ledger measurement now exists.

## Scope

I reviewed the working tree against committed base `393b3b0`. The workspace page carries 61
additions and 20 deletions. `web/src/lib/fixture.ts` has no diff against that base.
`dev-diary/PHASE-6-editing.md` carries only the `status: claimed:gpt-5.6-sol` line, which the
protocol requires before work starts.

I read AGENTS.md in full, T6.7a and T6.7 in PHASE-6, the diary README, `t6.7a-round1.md`, and
T6.7 rounds 1 through 3. I recalled memory before I touched code.

I own only this review file. I changed no production code, no test, no phase document and no
status. I created no commit. I spawned no agent. I ran no docker command.

I differ from the implementer, from the round-1 reviewer, and from the dead round-2 session. I
treated none of their reports or `/tmp` leftovers as evidence. Every number below comes from my
own tools, run against artifacts I built.

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| None | No defect reproduced in the reviewed diff. | Every check in this file passed. See the measurement sections. | Reverting the page to `393b3b0` in a temporary build fails the fixture protection check. Measured below. |

## Environment I measured against

I built both artifacts from the working tree. `go build -o /tmp/t67a-r2-claude/ajilamu
./cmd/ajilamu` produced the server. `npm run build` in `web` produced the static frontend.
Go reports 1.27.1 and Node reports v26.8.1 from `/opt/homebrew/opt/node/bin`.

The server ran on port 18855 with `AJILAMU_DATA_DIR` under `/tmp`. I never used the leftover
process on 18741 or 18764. Chrome 153.0.8010.37 ran headless on debugging port 18991 with a
fresh profile. I drove it over the DevTools Protocol with trusted input events.

The ClickHouse server at `127.0.0.1:18129` reports version 26.8.2.7. I reused the isolated
database `t67a_r2_393b3b0_gpt5` and verified it independently. I extracted the preflight guard
from the committed `sql/schema.sql` and ran it against that database. It returned 0, so no
column, type, engine, sorting key, partition key, materialized expression or constraint differs
from the committed schema. All five append tables and all seven views exist.

I left the ClickHouse server running and untouched.

## Measurement 1: the durable ledger pin, which closes B1

I seeded a fresh non-fixture dub over the ClickHouse HTTP interface, with no Ajilamu process
involved. The seed script is `/tmp/t67a-r2-claude/seed.py`. The dub id is
`ffae153e668f459ea319438c75faa912` and its root commit is `a9c139a33ffccbfd26f110c75136e2fb`.
It holds three lines in Malayalam. I did not reuse the dead reviewer's dub.

I measured the ledger with SQL before and after the drag. The script is
`/tmp/t67a-r2-claude/counts.sh` and it queries ClickHouse directly. The raw output sits in
`before-counts.txt`, `after-counts.txt` and `final-counts.txt` beside it.

| Reading | commits | actions | timeline snapshots | takes |
|---|---:|---:|---:|---:|
| Before the drag | 1 | 0 | 3 | 3 |
| After the drag | 2 | 1 | 4 | 3 |
| Delta | +1 | +1 | +1 | 0 |

I then drove one real boundary drag through the real page with trusted CDP mouse events. The
probe is `/tmp/t67a-r2-claude/real-probe.mjs` and its record is `real-results.json`. Nothing was
mocked and no request was intercepted. The drag moved line 1's end handle from 3000ms to 1500ms.

The page emitted exactly one request that was not a GET:

```json
POST /api/dubs/ffae153e668f459ea319438c75faa912/edits
{"kind":"boundary","language":"ml","segment_id":1,"start_ms":0,"end_ms":1500}
```

The server answered 201 with commit `e3d687ef59b9e66683fe5d5ffae48a2c`, action
`boundary_nudged`, author `manual_ui`, and the sentence
`Saved the boundary. Line 1 now runs from 0.000s to 1.500s.`

The independent SQL read confirms every part of that answer.

- The new commit carries `version_seq` 2 and its parent is the root commit.
- The one new action reads `boundary_nudged`, `manual_ui`, an empty prompt,
  `before_value {"start_ms":0,"end_ms":3000,"speaker":"Mark"}` and
  `after_value {"start_ms":0,"end_ms":1500,"speaker":"Mark"}`.
- The one new timeline snapshot names segment 1 at 0 to 1500 under the new commit. It copies
  the speaker, the source text, the target text and the take id forward unchanged.
- No take row and no charge row moved.

A reload of the page read `0:00.000 to 0:01.500` on the boundary panel, so the stored boundary
survived the page. The screenshots are `real-after-drag.png` and `real-after-reload.png`.

## Measurement 2: the three fixture aliases

The probe is `/tmp/t67a-r2-claude/fixture-probe.mjs` and its record is `fixture-results.json`.
It listened to every request the page issued, without filtering by endpoint. It clicked every
rendered editor control, typed into each one, and pressed Enter. It dragged both boundary
handles in both directions with trusted pointer events. It pressed the arrow keys and used the
slash shortcut. It then forced a command submit through the DOM, which bypasses the disabled
input.

| Alias | Fieldsets disabled | Native controls disabled | Visible sentences | Non-GET requests | Page errors |
|---|---:|---:|---:|---:|---:|
| `fixture` | 4 of 4 | 10 of 10 | 4 | 0 | 0 |
| `d3bca364-9c8a-4107-8df9-c4faf909b008` | 4 of 4 | 10 of 10 | 4 | 0 | 0 |
| `6d9c2f1a-3b4e-4a8d-9c1e-7f2b5a3d8c40` | 4 of 4 | 10 of 10 | 4 | 0 | 0 |

The four fieldsets carry the labels Editor command, Line boundary, Line speaker and Line text.
Each one sets `aria-describedby`. Every one of those ids resolves to a rendered paragraph
holding the read-only sentence. The ten controls are the command input, the Preview button,
both boundary handles, both speaker toggles, both textareas and both text actions. Every one
matches `:disabled` and computes `pointer-events: none`.

The forced command submit reached `previewCommand`, which returned the read-only sentence
rather than a request. The command bar rendered that sentence as its error. That is the route
guard working behind the disabled control, and it emitted nothing.

A second probe, `fixture-probe2.mjs`, repeated the attack and then walked all eight fixture
lines through the rail. It compared every boundary readout against the frozen
`testdata/wire/dub.json`. Zero of eight lines differ. The record is `fixture-results2.json`.

The visual pin is `fixture-fixture.png` and `grid-current-1400.png`. Both show the sentence
above each of the four controls.

## Measurement 3: nothing from a fixture reached the ledger

Every fixture probe ran against the same server and the same database as the real drag. The
ledger reconciles exactly.

| Reading | commits | actions | timeline snapshots |
|---|---:|---:|---:|
| Seed | 1 | 0 | 3 |
| Two deliberate real-project drags | +2 | +2 | +2 |
| Final SQL reading | 3 | 2 | 5 |

Nothing unaccounted for landed. I also queried each of the five append tables for the three
fixture ids directly. `commits`, `actions`, `timeline_state`, `takes` and `charges` each return
0 rows for `fixture`, `d3bca364-9c8a-4107-8df9-c4faf909b008` and
`6d9c2f1a-3b4e-4a8d-9c1e-7f2b5a3d8c40`.

## Measurement 4: reactivity across a client-side route change

Svelte 5 fails silently when a value stops re-deriving, so I measured both directions. The
probes are `nav-probe.mjs` and `nav-back.mjs`.

I loaded `/d/fixture`, then reached the real project through SvelteKit's client router without
a document load. `performance.getEntriesByType("navigation").length` stayed at 1 across the
change, which proves the route change stayed in one document. The four fieldsets became
enabled, the sentences disappeared, and a trusted drag posted one edit that the ledger recorded.

I then went the other way. From the real project I reached `/d/fixture` in the same document.
The four fieldsets became disabled again, all ten controls became disabled, the four sentences
returned, and a trusted drag emitted zero requests.

`readOnly` re-derives correctly in both directions.

## Measurement 5: the real project keeps its T6.7 write path and its layout

The probe is `/tmp/t67a-r2-claude/real-layout.mjs`. I rendered the seeded real project against
the reviewed build and against a temporary build of base `393b3b0`, both at 1440 by 1100.

Both builds resolve the workspace grid rows to `45.8906px 1008.11px`. Both resolve the editor
panel columns to `1081px 300px`. All three editor panels sit at identical positions and sizes,
251px, 251px and 343px tall, at identical tops and lefts. The editor scroll height is 1007 in
both. The fieldset wrappers are layout neutral where the sentences do not render.

The History rail on the real project lists `Saved 2 e3d687ef`, `A line boundary was adjusted.`
and `you`. That is T6.7's history invariant, re-measured on this build.

I also re-measured T6.7's rule that a silent overlap must still fail. Dragging line 1's end to
5000ms, which overlaps line 2, raised the `Keep the overlap?` alert and emitted zero requests.
Declining returned the handle to 2200ms. The ledger stayed at 3 commits, 2 actions and 5
snapshots. The probe is `overlap-probe.mjs`.

## Mutation

The pin fails when the fix goes away. I extracted base `393b3b0` with `git archive` into
`/tmp/t67a-r2-claude/base`, built its frontend, and served it from a second Ajilamu process on
port 18856. The repository was never modified.

The same fixture protection check, `mutation-probe.mjs`, run against the reverted page:

| Reading | Reviewed build | Reverted base |
|---|---|---|
| Protected fieldsets | 4 | 0 |
| Read-only sentences | 4 | 0 |
| Disabled native controls | 10 | 0 |
| POSTs from one real drag | 0 | 1 |
| Response | none | `POST /api/dubs/fixture/edits` answered 404 |
| Page sentence | none | `This project has no timeline to edit.` |
| Check passed | true | false |

The reverted page reproduces exactly the defect T6.7a removes. A creator sees an error for a
state the product designed.

## Checks

`go test -count=1 ./internal/api ./cmd/...` reports `ok` for both packages, at 6.192s and
5.245s.

`PATH=/opt/homebrew/opt/node/bin:$PATH npm --prefix web run check` reports 0 errors and 0
warnings under Node 26.8.1. I ran it in the repository, so no foreign `@types` tree reached it.
Round 1's isolated-copy diagnostic does not recur.

`rg "export let"` and `rg '^\s*\$:'` over `web/src` return nothing. No file imports
`svelte/store`. `git diff --check 393b3b0` passes for both owned paths.

The added sentence reads `This offline fixture cannot save an edit.` It carries no semicolon and
no em dash, it runs seven words, and it uses active voice. The page already said `offline
project fixture` at the base commit, so the vocabulary stays consistent.

## Residue

**B1 from round 1 is closed with zero residue.** Round 1 could not measure a durable ledger
write. I measured one. SQL before the drag, a trusted pointer drag through the real page and
the real route, SQL after the drag, and a reload. Exactly one commit, one action and one
timeline snapshot landed, with the changed bounds in the snapshot. Round 1 recorded no code
finding, so no code residue exists to close.

**T6.7's durable-write invariants carry zero residue on this build.** I re-measured four of
them independently. One drag writes one commit, one action and one timeline snapshot. The
History tab shows the change under `you`. The edit survives a reload. A silent overlap never
writes. The fifth, that a failed write never reports success, stays covered by
`internal/api/mutations_test.go`, which passes here, and the reviewed diff does not touch that
path.

**P6 close's fixture-edit note carries zero residue.** All three aliases now block every edit
and show the sentence.

**T6.1's route composition failures did not recur.** Both boundary sliders rendered on every
alias, Chrome reported zero page errors, and the non-fixture pointer handler executed.

## Out of scope, recorded for the board

Neither item below is a T6.7a finding. The diff changes neither, and T6.7a's done condition
covers neither. I record both because no task owns them, which AGENTS.md names as the loop's
blind spot.

**The fixture page still starts a run.** Clicking `Start the dubbing run into ML` on
`/d/fixture` sends `POST /api/dubs/fixture/run?language=ml`. My server answered 503 and the page
showed `Runs are unavailable, so this project cannot start.` The record is
`extra-results3.json`. This is the same shape of defect T6.7a removes, but T6.7a's paragraph
enumerates boundary drags, speaker changes, text edits and command confirmations, and its done
condition says `no edit request`. A run start is none of those. It deserves its own task.

**The four sentences move a pre-existing layout threshold.** They add about 96 pixels to the
fixture editor, from 1007 to 1103. The workspace grid gives row one `minmax(0, 1fr)`, so a tall
editor squeezes the picture area away. Base `393b3b0` already collapses that row at a 900 pixel
viewport. The reviewed build collapses it at 1100 as well. At 1400 both builds render normally,
at 250 pixels against the base's 346. The real project is unaffected, because the sentences do
not render there. The measurements are `grid-base-*.json` and `grid-current-*.json`.

A third item is an observation only. A disabled editor control keeps opacity 1 and a pointer
cursor, so it does not look disabled. No specification requires a disabled appearance, and
`ui-ux.md` says nothing about one. The sentence sits directly above each control, so the
creator is told. I record the measurement rather than invent a requirement.

## Evidence

Everything below lives under `/tmp/t67a-r2-claude/`.

| Path | What it holds |
|---|---|
| `seed.py`, `dub.env` | The fresh non-fixture dub seeded straight into ClickHouse |
| `counts.sh` | The independent SQL reading, commits, actions, snapshots and takes |
| `before-counts.txt`, `after-counts.txt`, `final-counts.txt` | Raw SQL output at the three readings |
| `preflight.sql` | The committed schema guard, run against the review database |
| `cdp.mjs` | The DevTools Protocol client the probes share |
| `fixture-probe.mjs`, `fixture-results.json` | The three-alias fixture measurement |
| `fixture-probe2.mjs`, `fixture-results2.json` | The eight-line boundary walk against the frozen payload |
| `real-probe.mjs`, `real-results.json` | The real drag, its one POST, and the reload |
| `nav-probe.mjs`, `nav-back.mjs` | Client-side route changes in both directions |
| `overlap-probe.mjs` | The overlap confirmation that writes nothing |
| `grid-probe.mjs`, `real-layout.mjs`, `grid-*.json`, `layout-*.json` | Layout geometry, reviewed build against base |
| `mutation-probe.mjs`, `mutation-results.json` | The reverted-page mutation |
| `base/` | The `393b3b0` tree and its build, served on port 18856 |
| `fixture-*.png`, `real-after-drag.png`, `real-after-reload.png`, `real-history.png`, `grid-*.png` | Screenshots |
