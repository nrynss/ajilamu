# T5.4 Round 1 Review

## Verdict

REMEDIATE

| Severity | Count |
|---|---:|
| C | 0 |
| H | 1 |
| M | 0 |
| L | 0 |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| `web/src/lib/LengthBar.svelte:39-42`, `web/src/lib/LengthBar.svelte:183-185` | **H, F1.** An overlong active take changes its bar's time scale. `scaleCeilingMs` includes each take's measured duration, despite the supplied 7.11-second shared reference. Thus two 7.11-second slots do not retain equal trough widths. The overrun becomes part of the scale rather than extending past an unchanged slot. | `lengthbar-shared-scale`. I mounted the built component in a live Vite page. Each canvas measured 698 CSS pixels, leaving a 682-pixel trough span after its 8-pixel insets. The fixture segment 8 slot, 7,110 ms, filled that 682-pixel span. A separate 7,110 ms slot with an 8,000 ms take and the same `referenceSlotMs: 7110` ended its trough at 607 pixels, then drew the 76-pixel overrun. The expected trough remains 682 pixels, with the 85-pixel overrun drawn after it. The audit screenshot observed the rendered pixels. | Removing the measured-duration ceiling must fail a regression that renders a reference-length slot beside an overlong reference-length slot. Otherwise every line with a long take rescales independently and destroys the true-time comparison the instrument exists to provide. |

## Independent evidence

I own only this review file. The required `Take`, `Fit`, and Svelte 5 shapes exist. I can validate this component independently. No prior T5.4 review round exists.

`npm run check` completed with zero errors and warnings. `npm run build` completed successfully.

I mounted the built Svelte component in a local Vite audit page. The page supplied a 7,110 ms shared reference, the T1.5 segment 8 measurement, and representative repair states. These observations came from rendered canvas geometry and screenshots, not the component's text labels.

| Check | Independent observation |
|---|---|
| Segment 8 underrun | The 7,110 ms trough rendered a 4,200 ms solid take followed by a 2,910 ms `--short` region. The accessible sentence also identified line 8 as 2.91 seconds short. |
| Atempo and ghosts | The 5,338 ms atempo take rendered diagonal hatching and the visible label `107.5% speed`. Its earlier 5,920 ms overrun remained as a faint ghost beyond the active take. |
| Rewrite | The rewrite case rendered solid `--fit` and `--short` fills with the `rewritten` label. It did not inherit atempo hatching. |
| Resize repaint | Clicking the audit width control changed every canvas from a 698-pixel backing width to 348 pixels. The rendered bars redrew at the new width. |
| Theme repaint | Clicking the audit theme control changed root `data-theme` to `dark`. Computed `--fit`, `--over`, and `--short` became `#5c7259`, `#8c4f47`, and `#7a6742`. The screenshot showed all canvas fills repainting in the dark palette. |
| Listener cleanup | Source inspection confirms the mounted component disconnects both observers, removes its window resize listener, and cancels the queued animation frame during teardown. |
| Svelte 5 hygiene | The component uses `$props`, `$derived`, and `$effect`. It contains no legacy `export let`, `$:`, or store-driven reactivity. |

All passing observations remain subject to F1. The shared-scale fault prevents approval because true comparison between bars is a central T5.4 requirement.
