# T4.3 adversarial review, round 1

Verdict: REMEDIATE

Counts: C0, H0, M1, L0.

Prior-round residue: None. This is the first review round.

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| M | `internal/ledger/commits.go`, task ownership at `dev-diary/PHASE-4-ledger.md:62` | The writer has no committed regression test. T4.3 owns only source. No test guards roots, forks, timestamps, or retries. This is real. Extend ownership to a ledger test. | An isolated HTTP capture observed the required root, linear child, and fork. It measured a durable replay after one failed delivery. `rg --files internal/ledger | rg '_test.go'` returned no files. | Reverting a validation, mapping, timestamp, or replay rule leaves no committed check that fails. A later change can silently break the DAG. |

Independent evidence:

- A local HTTP capture received three JSONEachRow rows. The root had an empty parent and version one. The linear child named `root` at version two. The fork also named `root`, used branch `alt`, and used version two.
- Every captured request named `INSERT INTO commits_raw (...) FORMAT JSONEachRow`. Each payload included the schema fields and omitted `created_at` and `ingested_at`, so ClickHouse supplies both timestamps.
- Invalid self-parenting, a root at version two, and a child at version one each returned a validation error. The capture request count stayed at three, so invalid rows never entered the queue or reached the transport.
- A forced HTTP 503 left one durable queue entry. A later flush replayed byte-identical JSON to the same `commits_raw` query and reduced pending entries to zero.
- `commits_raw` deduplicates that replay by its `(dub_id, commit_id)` natural key under `ReplacingMergeTree`.
- `go test ./...`, `go vet ./...`, `gofmt -d internal/ledger/commits.go`, and `git diff --check -- internal/ledger/commits.go` passed. The temporary capture harness was removed after measurement.
- The implementation issues no update or delete query. `AppendCommit` validates before calling T4.1's durable queue, so the code remains append-only.
