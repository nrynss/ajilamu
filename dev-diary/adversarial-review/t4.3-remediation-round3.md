# T4.3 remediation, round 3

| # | Severity | What I changed | Measurement proving it closed |
| --- | --- | --- | --- |
| 1 | H | Held the HTTP capture server at 503 until the test observed one pending durable journal entry. The test then released recovery and waited for the automatic reconciler to replay it. It compares the first failed body with the recovered replay body. | Three uncached `go test -race -count=1 ./internal/ledger` runs passed. The test observes one pending queue entry and one `.json` journal file before recovery. It observes pending zero after the automatic replay. |
