# T7.5 adversarial review, round 2

Task: Workspace payload route. `internal/api/workspace.go` assembles one wire `Dub` from five
ledger reads plus the history reader. `WorkspaceHandlerFrom` mounts at `GET /api/dubs/{id}`.
`cmd/ajilamu/main.go` adapts the ledger client through `workspaceReader`. The workspace page
fetches the route for a non-fixture id.

Reviewed against HEAD `75af966` plus the uncommitted T7.5 working tree. Go 1.27.1, Node
v26.8.1, Chrome 152.0.7977.82. I did not implement T7.5. I did not write the round one review and
I did not remediate. I fixed nothing. My only write is this file. Every scratch tree and every
server lives under `/tmp/t75r2`. I stopped every process I started.

## Verdict

**REMEDIATE**

| Severity | Count |
|---|---:|
| C | 0 |
| H | 0 |
| M | 2 |
| L | 0 |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| M1 · `dev-diary/PHASE-7-ingestion.md` lines 912 to 918 | The T7.5 handoff entry misdescribes the shipped route. Line 913 says every take fitting reads ready. It omits the segment-coverage rule the remediation added. Lines 916 to 918 say the route leaves `Take.StretchFactorMilli` at zero, so a real atempo take carries no ratio. The reader now parses `repair_detail` and fills the field. The T7.5a handoff at lines 856 to 857 carries the same stale stretch claim. The next agent codes against this entry. | The live route returned `"stretch_factor_milli":1050` for the atempo take. The handoff says the field stays zero. `selectWorkspaceTakes` now selects `repair_detail` at `internal/ledger/workspace.go` line 28. | Reverting the doc fix leaves the handoff contradicting the code. Reverting the L3 fix restores the old claim and drops the ratio. |
| M2 · `internal/api/workspace.go` lines 189 to 222 and `internal/api/server.go` line 180 | `workspaceReadiness` returns pending, review, or ready. The wire declares a fourth value, `running`. Round one's M1 mutation column prescribed passing the run registry in. `runRegistry.active(dubID)` already exists at `internal/api/run.go` line 352. `NewServer` owns `server.go`, so the task can wire it without editing `run.go`. The remediation residue note says the task does not own the registry. That reason does not hold. A poller still cannot tell an active run from an idle one. | I drove a reader matrix through `WorkspaceHandlerFrom`. The route produced `[pending ready review]` and no fourth value. `internal/api/wire.go` lines 14 to 21 declare four values. The live route answered `ready`. | Reverting the fix leaves the route without a `ReadinessRunning` branch. |

## Re-measurement of round one

### M1, readiness ignored segment coverage

The fix holds. `workspaceReadiness` now takes the `segments` slice and compares rendered lines to
`len(segments)*len(tracks)`. A stand-in reader with two segments and one fitting line answered
`"readiness":"review"`. The same reader with two lines answered `"readiness":"ready"`.

Pin and mutation. I copied the module to `/tmp/t75r2` and ran the shipped pin. It passed. I
forced the coverage case to `false` in the copy. The pin failed with
`readiness = "ready", want "review"`. The fix is load bearing.

Residue. The `running` value stays unimplemented. I filed it as M2.

### L1, empty timestamps for a stored project with no ledger

The fix holds. `ProjectLookup` now returns the record `created_at`. `assembleWorkspace` falls
back to it when the ledger metadata is empty. The shipped pin posted a real upload through
`api.NewUploadHandler` and served the route. The record file carried
`"created_at":"2026-09-08T20:43:24Z"`. The response carried the same value in `created_at` and
`updated_at`.

Pin and mutation. The pin passed in the copy. I forced both fallback branches to `false`. The pin
failed with `timestamps = "" / ""`. The fix is load bearing.

Residue. None.

### L2, empty source_language

The deferral is legitimate. `Dub.SourceLanguage` has no producer. The upload record stores only
the target language. T7.5b sits on the board at `PHASE-7-ingestion.md` line 491. It owns the
source language at upload and names `internal/api/workspace.go` in its owns line. It requires
T7.5 and T7.7. T7.5's done-when does not name the source language.

Precedent. The T7.2c round one review found a pre-existing page effect loop. It approved its own
task with zero findings. It recorded the defect as T7.2d. T7.5b is already on the board, so this
deferral is stronger than that precedent. The field stays empty until T7.5b lands.

Residue. None for T7.5.

### L3, stretch_factor_milli zero

The fix holds. The takes statement selects `repair_detail`. The reader parses
`atempo stretch applied at ratio 1.0500` into thousandths. The atempo take marshalled
`"stretch_factor_milli":1050`. The none take omitted the field.

Pin and mutation. The shipped pin passed in the copy. I forced `atempoStretchMilli` to return
zero. The pin failed with `atempo stretch factor = 0, want 1050`.

Outside tool. I ran the modified statement on ClickHouse 26.8.2.7 through `clickhouse local`. The
view accepted the extra column. The row returned `"repair":"atempo"` and
`"repair_detail":"atempo stretch applied at ratio 1.0500"`. The writer emits that prefix at
`internal/fit/rewrite.go` line 390. The live route returned 1050.

Residue. An atempo take with no parseable ratio still omits the field. The writer always emits
the prefix, so no current path reaches that state.

## Route and page behaviour

A stand-in ClickHouse answered the eight statements. The real binary served
`/api/dubs/aabbccdd00112233`. The route answered 200 with the keys
`charges commits created_at id languages readiness segments source_language title total
updated_at`. The payload carried the real title `Real project.mp4`, two segments, one `ml` track,
the atempo ratio 1050, the whole-pass charge, the total, and two commits.

Headless Chrome loaded `/d/aabbccdd00112233` on that server. The DOM carried `Real project.mp4`
twice. It carried no fixture marker and no pending sentence.

A second binary ran under `env -i` with no ClickHouse credentials. The workspace route answered
503 with `The project ledger is unavailable, so this workspace cannot load.` Headless Chrome
loaded `/d/fixture` on that server. The DOM carried `NASA 75-second clip` twice and `In review`
once. It carried no real title.

## Command output

The working tree carries a concurrent T2.8 edit. `tts.NewSynthesizer` changed shape and
`cmd/ajilamu/main.go` line 385 has not caught up. The real tree therefore fails to build.

```text
$ go test -count=1 ./internal/api ./internal/ledger ./cmd/...
cmd/ajilamu/main.go:385:60: not enough arguments in call to tts.NewSynthesizer
        have (*config.Config, *chargeRouter, cost.RateCard, nil)
        want (*config.Config, string, tts.ChargeRecorder, cost.RateCard, tts.TTSClient)
FAIL	github.com/nrynss/ajilamu/internal/api	0.612s
ok  	github.com/nrynss/ajilamu/internal/ledger	1.011s
FAIL	github.com/nrynss/ajilamu/cmd/ajilamu [build failed]
```

The T7.5 snapshot at `/tmp/t75r2` predates that sibling edit. It carries the T7.5 diff and it
compiles.

```text
$ go test -count=1 ./internal/api ./internal/ledger ./cmd/...
ok  	github.com/nrynss/ajilamu/internal/api	1.433s
ok  	github.com/nrynss/ajilamu/internal/ledger	1.019s
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	0.969s
```

```text
$ npm --prefix web run check
1788900223061 START "/home/nryn/work/ajilamu/web"
1788900223062 COMPLETED 181 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
```

## Residue against round one

| Round one finding | Residue |
|---|---|
| M1, readiness ignored segment coverage | The coverage fix holds. The `running` value stays unimplemented and is filed as M2. |
| L1, empty timestamps | None. |
| L2, empty source_language | None for T7.5. T7.5b owns the value at upload. |
| L3, stretch_factor_milli zero | None for a take the writer produced. |

## Scope and evidence

I changed no implementation file. My only write is this review file. I read
`internal/api/workspace.go` and its test in full. I read the T7.5 hunks of `internal/api/server.go`
and `cmd/ajilamu/main.go`. I read `internal/ledger/workspace.go` and its test. I read the T7.5
and T7.5a handoff entries. I read `internal/api/run.go` for the registry API and
`internal/fit/rewrite.go` line 390 for the atempo detail format.

Observers. The Go pins ran in `/tmp/t75r2`. The live route and the browser runs used the binary
built from that copy. The stand-in ClickHouse is `/tmp/t75r2/standin.py` on port 18123. The
browser is `google-chrome-stable` 152.0.7977.82. The ClickHouse statement ran through
`/tmp/clickhouse-common-static-26.8.2.7/usr/bin/clickhouse local`.

## What I did not file

- **`review` covers an unflagged partial project.** The wire says `review` means the last run
  ended with flagged lines. The route also returns it when a segment has no rendered line. No
  consumer renders this route's readiness today. The wire offers no better value without the run
  registry. M2 covers the related gap.
- **The index chrome disagrees with the payload.** `ListUploadSummaries` hardcodes
  `ReadinessPending`, so the layout shows `Queued to start` for a real project whose route says
  `ready`. T7.5 owns neither path. Round one noted the same disagreement.
- **`atempoStretchMilli` trusts one writer string.** A future atempo writer with a different
  detail would drop the ratio. The current writer emits the prefix at `internal/fit/rewrite.go`
  line 390.
