# T6.2 Round 2 Remediation

## Findings

| # | Sev | Finding | Fix | Pin | Residue |
|---|---|---|---|---|---|
| 1 | M | The seam note and the T6.2 handoff entry described the pre-fix rule. They said the estimate sums the selected line's last take. They reported line 3 as `$0.00` because that take carries no charge. | I rewrote both passages. The estimate now sums the newest take whose `charges` array is non-empty. The handoff entry reports line 3 as `$0.0023182` from `seg_3_try1.wav`. It reports line 4 as `$0.0036392` from `seg_4_try1.wav`. It records that lines 1 and 7 did not move. It states the fallback and quotes the hint. | Reading `dev-diary/PHASE-6-editing.md` returns the delivered rule. The old phrases "last take" and "Line 3 shows `$0.00`" return nothing. `Speaker.svelte` derives `estimateTake` as the newest take with a non-empty `charges` array, and it falls back to the last take. The hint sentence matches the component. | None against finding 1. |

## Scope

I edited one file, `dev-diary/PHASE-6-editing.md`. I changed the seam note and the T6.2 handoff entry only. I changed no code file.

I made one edit with two hunks. A reconstruction of the pre-edit file matched the pre-edit read at lines 276 to 325. The reconstruction differs from the current file only in those two passages.

## Before and after

Seam note, before:

> T6.2 shows the re-render fee from the measured charges on the selected line's last
> take. That number is already on the payload. No new endpoint carries it.

Seam note, after:

> T6.2 shows the re-render fee from the measured charges on the selected line's newest
> billed take. A line whose takes carry no charges reads zero. That number is already
> on the payload. No new endpoint carries it.

T6.2 handoff entry, before:

> The estimate sums `Take.Charges` on the selected line's last take. The panel labels
> it as estimated and names that take. Line 1 shows `$0.0008421` from `seg_1_try1.wav`.
> Line 7 shows `$0.004751` from `seg_7_try1.wav`. Line 3 shows `$0.00`, because its
> last take is the atempo repair and carries no charge.

T6.2 handoff entry, after:

> The estimate sums `Take.Charges` on the selected line's newest take whose
> `charges` array is non-empty. The panel labels it as estimated and names that
> take. Line 1 shows `$0.0008421` from `seg_1_try1.wav`. Line 7 shows `$0.004751`
> from `seg_7_try1.wav`. Neither line moved. Line 3 shows `$0.0023182` from
> `seg_3_try1.wav`. Line 4 shows `$0.0036392` from `seg_4_try1.wav`. A line whose
> takes carry no charges reads zero and names that take. The hint reads: "A new
> speaker needs a fresh voice render. The estimate uses the newest billed take, or
> reads zero when none is billed."

## Numbers

The round 2 review measured the live panel on port 43512. Line 3 read `$0.0023182` with basis `seg_3_try1.wav`. Line 4 read `$0.0036392` with basis `seg_4_try1.wav`. Lines 1 and 7 held at 842100 and 4751000 nanodollars. The round 1 remediation record measured the same values. I did not re-measure the panel.

## Residue

Zero residue against finding 1. I did not change the verdict. I did not fix anything nobody found. I did not mark the task done.
