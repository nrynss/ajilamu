# T4.3 adversarial review, round 3

Verdict: REMEDIATE

Counts: C0, H1, M0, L0.

Prior-round residue: Round 1 finding 1 is closed in source. `commits_test.go` now
captures the durable transport contract. Round 2 finding 1 is closed in source. The
parent lookup rejects absent and later parents before it enqueues an insert. Its
strictly decreasing parent version rule prevents cycles. The failed replay test below
leaves the required regression proof unreliable, so no APPROVE is possible.

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| H | `internal/ledger/commits_test.go:171-179`, with `internal/ledger/client.go:105-118` | `TestAppendCommitReplaysFailedDelivery` assumes the failed entry remains pending until its foreground `Flush`. The client starts a reconciler that immediately calls `Flush` before it waits for its first tick. It can replay and remove the entry first. The test therefore races and fails nondeterministically. | A clean `go test -race -count=1 ./internal/ledger` failed at `commits_test.go:177`: `Pending after failed delivery = 0, <nil>, want 1, nil`. The HTTP server accepted the reconciler's replay. | Reverting a delivery or queue change can make the suite alternately pass and fail. CI then cannot provide the durable replay proof that round 1 required. |

Independent evidence:

- `go test -race ./internal/ledger` passed from the cache. The uncached command above
  failed. This difference confirms a scheduling-sensitive test, not a stable pin.
- The actual HTTP capture code sends roots directly to `commits_raw`. Children first
  issue a parameterized `SELECT` with `param_dub_id` and `param_commit_id`. URL encoding
  supplies escaping, and `NewRequestWithContext` carries cancellation into both requests.
- The lookup returns no row for a missing parent. It rejects a returned parent whose
  `version_seq` is not lower than the child. Those paths call `EnqueueJSON` only after
  validation, so they produce zero insert transports.
- The root, linear child, and fork capture asserts the immutable raw-table query, parent
  pointers, versions one and two, branch labels, and omission of both client timestamps.
  The test also captures absent, self, and later parents with zero inserts.
- `commits.go` contains no update or delete query. The schema keys `commits_raw` by
  `(dub_id, commit_id)`, and the writer emits one JSONEachRow commit per append.
- `go vet ./internal/ledger`, `gofmt -d internal/ledger/commits.go
  internal/ledger/commits_test.go`, and `git diff --check -- internal/ledger/commits.go
  internal/ledger/commits_test.go dev-diary/PHASE-4-ledger.md` passed.
