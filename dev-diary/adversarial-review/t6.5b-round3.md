# T6.5b source-text re-translation path: round 3

**Verdict: APPROVE.** Counts: **C 0, H 0, M 0, L 0.**

The round 2 remediation adds one pin at the adapter. I re-measured the corrected
target miss through the real `pipelineRunner.RenderLine`. I mutated the
`AuthoritativeText` wiring in a `/tmp` copy. The new pin reddens and reopens round
one H1. The source path still translates once. atempo still applies. No new defect
appeared. I fix nothing.

## Findings

No finding met every bar. The four columns stay empty.

| Where | What | Pin | Mutation |
|---|---|---|---|
| None | No defect met the provable-impact, actionable and patch-introduced bars. | n/a | n/a |

## What I measured

I copied the tree to `/tmp/t65b-r3`. I added throwaway tests in package main there.
They drive the real `pipelineRunner.RenderLine` with a counting translator and a TTS
client whose audio length I control. The working tree was never edited for probes.

### 1. The corrected-target miss through the real RenderLine

A fixed 10640 ms WAV against segment 3's 5320 ms slot:

```text
PROBE1 translation=0 synthesis=3 spoken="corrected target line" flagged=true repair=rewrite detail="overrun of +5320ms exceeds stretch budget of 8.0 percent"
```

A client whose length depends on the spoken text. The creator's line returns
10640 ms and misses. Any other text returns 5320 ms and fits:

```text
PROBE2 translation=0 synthesis=3 spoken="corrected target line" flagged=true repair=rewrite
```

The loop never gives the translated rewrite a chance to fit. It flags the line and
keeps the creator's text.

The shipped pin prints the same counts:

```text
$ go test -count=1 -run TestRenderLineCorrectedTargetMissNeverTranslates -v ./cmd/ajilamu
=== RUN   TestRenderLineCorrectedTargetMissNeverTranslates
    main_test.go:949: translation calls = 0, synthesis calls = 3, spoken text = "corrected target line", flagged = true
--- PASS: TestRenderLineCorrectedTargetMissNeverTranslates (0.23s)
PASS
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	0.236s
```

### 2. The wiring mutation

I removed the `if req.Text != ""` block that sets `cfg.AuthoritativeText` from
`main.go` in the `/tmp` copy. The shipped pin reddens:

```text
$ go test -count=1 -run 'TestRenderLineCorrectedTargetMissNeverTranslates|TestR3' -v ./cmd/ajilamu
=== RUN   TestRenderLineCorrectedTargetMissNeverTranslates
    main_test.go:949: translation calls = 2, synthesis calls = 3, spoken text = "corrected target line", flagged = true
    main_test.go:953: translation calls = 2, want 0 on the corrected target path
--- FAIL: TestRenderLineCorrectedTargetMissNeverTranslates (0.24s)
```

My rewrite-fit probe fails under the same mutation and speaks the rewrite:

```text
PROBE2 translation=1 synthesis=2 spoken="source line" flagged=false repair=none
```

The full mutated suite fails only that test:

```text
$ go test -count=1 ./cmd/... ./internal/api ./internal/fit
--- FAIL: TestRenderLineCorrectedTargetMissNeverTranslates (0.24s)
    main_test.go:949: translation calls = 2, synthesis calls = 3, spoken text = "corrected target line", flagged = true
    main_test.go:953: translation calls = 2, want 0 on the corrected target path
FAIL
FAIL	github.com/nrynss/ajilamu/cmd/ajilamu	5.732s
ok  	github.com/nrynss/ajilamu/internal/api	2.515s
ok  	github.com/nrynss/ajilamu/internal/fit	18.425s
```

### 3. The source path and atempo

```text
PROBE3 translation=1 synthesis=1 spoken="corrected source line" flagged=false repair=none targetName="Malayalam"
PROBE4 translation=0 synthesis=1 spoken="corrected target line" flagged=false repair=atempo detail="atempo stretch applied at ratio 1.0526"
```

The source path translates once. The translator request names Malayalam. The
authoritative path makes no translation call and still applies atempo to a 280 ms
overrun inside the 8 percent budget.

### 4. The commands

```text
$ go test -count=1 ./cmd/... ./internal/api ./internal/fit
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	4.927s
ok  	github.com/nrynss/ajilamu/internal/api	2.437s
ok  	github.com/nrynss/ajilamu/internal/fit	18.580s

$ npm --prefix web run check
1788909800092 START "/home/nryn/work/ajilamu/web"
1788909800093 COMPLETED 183 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
```

Both commands reproduce the handoff lines.

## Handoff check

The T6.5a and T6.5b entries match the shipped code. The T6.5b entry says `text`
stays authoritative on every attempt and never translates. Probes 1, 2 and 4
confirm it. The entry says the source path translates on attempt one. Probe 3
confirms it. The count paragraph names three tests. The remediation added a fourth
pin at the adapter, `TestRenderLineCorrectedTargetMissNeverTranslates`. The sentence
stays true, so it is incomplete rather than false. I file nothing there, matching
round two's treatment of the stale T6.3 sentence.

The page half is unchanged since round one drove it in headless Chromium. The round
two remediation touched no web file, so that measurement still stands. I did not
re-run the browser harness this round.

## Residue

Round one H1 carries zero behaviour residue. A corrected target line that misses
every attempt calls no translator, keeps the creator's text, and flags the line.
Probes 1 and 2 measure that. The translated rewrite that would fit the slot is never
chosen.

Round two M1 carries zero pin residue. The adapter wiring now has a shipped pin.
Removing the two `AuthoritativeText` lines from `main.go` reddens
`TestRenderLineCorrectedTargetMissNeverTranslates` alone. The full mutated suite
shows no other failure.

## Done condition

The done condition reads: a corrected source line re-translates and re-renders as a
new take, the translation model runs on that path and zero times on the target-text
path, and the page applies the response.

The source half holds. One translation call and a new take. The target half holds on
a fitting take, on a miss, and under atempo. The page half holds from rounds one and
two. The pin half now holds at the adapter. Verdict APPROVE with zero findings.
