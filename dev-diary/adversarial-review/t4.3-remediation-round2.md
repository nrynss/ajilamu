# T4.3 remediation, round 2

| # | Severity | What I changed | Measurement proving it closed |
| --- | --- | --- | --- |
| 1 | H | Added a ClickHouse parent lookup before each child insert. It requires an existing parent with a lower version sequence, so every parent edge descends and a cycle cannot form. Added HTTP-capture coverage for missing and later cyclic-parent references. | `go test -race ./internal/ledger` captures zero `INSERT` requests for an orphan and for a child pointing to a later commit. Valid root, linear child, and fork inserts still reach `commits_raw`. |
