# T4.6 adversarial review, round 1

Verdict: REMEDIATE

Counts: C 0, H 0, M 1, L 0.

## Scope and evidence

I reviewed T4.6 against its task contract, T4.2a's approved peak persistence boundary,
T3.5's uint8 output, the frozen takes_raw.peaks Array(UInt8) schema, and the
api.Take.Peaks []int wire contract.

An isolated review worktree contained the submitted peaks.go and an independent test.
It passed a 64-value vector and a 128-value vector. The test checked every JSON number
after marshaling api.Take.

The test rejected 63 and 129 values. It changed the source after mapping and observed that
the API values did not change. An empty vector also cleared stale API values.

The mapper produces []int, so JSON contains numbers rather than a byte base64 string.
rg found no filesystem, media, or assembler dependency in peaks.go. The ledger test
suite and vet passed on the review worktree.

## Findings

| # | Sev | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- | --- |
| 1 | M | dev-diary/PHASE-4-ledger.md:128 and internal/ledger/peaks.go:20-46 | T4.6 owns no regression-test path. Its only checks compile the mapper. CI cannot preserve empty clearing, 64 and 128 acceptance, 63 and 129 rejection, defensive copying, or numeric API JSON delivery. | Add an owned internal/ledger/peaks_test.go path and a unit test that calls ApplyTakePeaks. Require those boundaries, stale-value clearing, input mutation isolation, and marshaled api.Take.peaks numeric values. | Remove a length guard, assign an aliased slice, retain stale peaks, or change the API mapping. The ledger suite remains green while the timeline receives invalid, absent, or stale waveform data. |

## Residue

No prior T4.6 review exists. The submitted mapper meets the observed runtime contract, including
the no-audio-filesystem boundary. The missing permanent regression pin prevents APPROVE.
