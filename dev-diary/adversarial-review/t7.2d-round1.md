# T7.2d adversarial review, round 1

Task: Workspace page effect loop. Stop the effect reading `dub` after it writes `dub`.
Reviewed against HEAD `b598aab` plus the uncommitted working-tree diff.
Go toolchain: go1.27.1. Node v26.8.1. Google Chrome headless over the DevTools Protocol.

## Verdict

**APPROVE**

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 0 |

Zero findings at every severity.

## Scope and Evidence

I changed no implementation file. My only write is this review file. My harness lives under
`/tmp/t72d/`. I built the frontend from the working tree, built the Go binary, and drove headless
Chrome over the Chrome DevTools Protocol. Every pin below is a browser measurement on the real
built frontend served by the real Go binary. I did not mount a component in isolation.

### What I read

- The T7.2d block in `dev-diary/PHASE-7-ingestion.md`.
- The T7.2c review note `dev-diary/adversarial-review/t7.2c-round1.md`, which found the defect.
- The full working-tree diff from `git diff`.
- The whole file `web/src/routes/d/[id]/+page.svelte`, including the full `$effect` at lines
  190 to 243.
- `web/src/lib/Rail.svelte` for the tab ids and the panel id.
- `web/src/lib/tabs/HistoryTab.svelte` for the DAG markup and the history fetch.
- `web/src/lib/fixture.ts` for `isFixtureID`, `loadFixtureDub`, and the fixture id list.
- `internal/api/static.go`, `internal/api/index.go`, `internal/api/upload.go`, and
  `internal/config/config.go` for static serving, the dubs index, and the credential gate.
- `web/node_modules/svelte/src/internal/client/errors.js` and `batch.js` for the thrown text.

### Validation commands

All ran in the repository unless the path says otherwise. Every command passed.

```
git status --porcelain=v1
git diff
git diff --stat
git rev-parse HEAD
go build -o /tmp/t72d/ajilamu ./cmd/ajilamu
npm --prefix web run build
npm --prefix web run check
python3 tools/audit_docs.py
curl -s http://127.0.0.1:8099/api/dubs
grep -n "export let" web/src/routes/d/[id]/+page.svelte
grep -nE '^\s*\$:' web/src/routes/d/[id]/+page.svelte
grep -nE 'svelte/store|\$app/stores|writable|readable' web/src/routes/d/[id]/+page.svelte
grep -rlo effect_update_depth_exceeded web/build/_app/immutable/
npm run build            (in /tmp/t72d/scratch/web, the reverted copy)
npm run build            (in /tmp/t72d/mut/web, the mutation copy)
python3 /tmp/t72d/mutate.py M2_read_workspaceState_after_write
python3 /tmp/t72d/mutate.py M3_read_dub_after_assignment
python3 /tmp/t72d/mutate.py M4_predicate_flip
node /tmp/t72d/probe.mjs http://127.0.0.1:8099/d/fixture fixture
node /tmp/t72d/probe.mjs http://127.0.0.1:8099/d/fixture both
node /tmp/t72d/probe.mjs http://127.0.0.1:8099/d/6d9c2f1a-3b4e-4a8d-9c1e-7f2b5a3d8c40 fixture
node /tmp/t72d/probe.mjs http://127.0.0.1:8099/d/pendingproj nonfixture
node /tmp/t72d/probe.mjs http://127.0.0.1:8099/d/not-a-project nonfixture
BLOCK_DUBS=1 node /tmp/t72d/probe.mjs http://127.0.0.1:8099/d/not-a-project nonfixture
node /tmp/t72d/probe.mjs http://127.0.0.1:8099/d/fixture?panel=loading loading
node /tmp/t72d/probe.mjs http://127.0.0.1:8097/d/fixture fixture
node /tmp/t72d/probe.mjs http://127.0.0.1:8097/d/fixture both
node /tmp/t72d/probe.mjs http://127.0.0.1:8097/d/6d9c2f1a-3b4e-4a8d-9c1e-7f2b5a3d8c40 fixture
node /tmp/t72d/probe.mjs http://127.0.0.1:8097/d/pendingproj nonfixture
node /tmp/t72d/probe.mjs http://127.0.0.1:8097/d/not-a-project nonfixture
node /tmp/t72d/probe.mjs http://127.0.0.1:8097/d/fixture?panel=loading loading
node /tmp/t72d/probe.mjs http://127.0.0.1:8096/d/fixture fixture
```

Three servers ran the same binary with a different frontend directory and port.

```
PORT=8099 AJILAMU_FRONTEND_DIR=/home/nryn/work/ajilamu/web/build AJILAMU_DATA_DIR=/tmp/t72d/data-fixed CLICKHOUSE_HOST= CLICKHOUSE_USER= CLICKHOUSE_PASSWORD= /tmp/t72d/ajilamu
PORT=8097 AJILAMU_FRONTEND_DIR=/tmp/t72d/scratch/web/build AJILAMU_DATA_DIR=/tmp/t72d/data-reverted CLICKHOUSE_HOST= CLICKHOUSE_USER= CLICKHOUSE_PASSWORD= /tmp/t72d/ajilamu
PORT=8096 AJILAMU_FRONTEND_DIR=/tmp/t72d/mut/web/build AJILAMU_DATA_DIR=/tmp/t72d/data-mut CLICKHOUSE_HOST= CLICKHOUSE_USER= CLICKHOUSE_PASSWORD= /tmp/t72d/ajilamu
```

Port 8099 served the fixed build. Port 8097 served the reverted build, which restores HEAD's
exact two lines. Port 8096 served the mutation builds. Every server ran with empty ClickHouse
credentials, so the ledger route answers 503 and the History tab renders fixture commits. The
harness creates a fresh browser context for each probe run.

### The pin, fixed against reverted

The built bundle sets `DEV` false, so Svelte throws the URL form
`Error: https://svelte.dev/e/effect_update_depth_exceeded`. A search of `web/build/_app/immutable/`
finds `effect_update_depth_exceeded` and finds no `Maximum update depth exceeded`.

Fixed build, `/d/fixture`, console:

```
log error  Failed to load resource: the server responded with a status of 404 (Not Found)  url=http://127.0.0.1:8099/favicon.ico
log error  Failed to load resource: the server responded with a status of 503 (Service Unavailable)  url=http://127.0.0.1:8099/api/dubs/6d9c2f1a-3b4e-4a8d-9c1e-7f2b5a3d8c40/history
```

Fixed build, `/d/fixture`, measured tab state:

| Step | `aria-selected` true on | Panel id | DAG nodes | DAG edges |
|---|---|---|---|---|
| on load | `rail-lines-tab` | `rail-lines` | 0 | 0 |
| click Details | `rail-details-tab` | `rail-details` | 0 | 0 |
| click History | `rail-history-tab` | `rail-history` | 7 | 6 |

The page rendered `NASA 75-second clip`, `section.workspace` was present, and the panel state was
`populated`. Two fresh contexts reproduced the same two log entries and no exception.

Reverted build, `/d/fixture`, console:

```
log error  Failed to load resource: the server responded with a status of 404 (Not Found)  url=http://127.0.0.1:8097/favicon.ico
exception  Error: https://svelte.dev/e/effect_update_depth_exceeded  url=http://127.0.0.1:8097/_app/immutable/chunks/C4uO31jO.js:1
```

Reverted build, `/d/fixture`, measured tab state:

| Step | `aria-selected` true on | Panel id | DAG nodes | DAG edges |
|---|---|---|---|---|
| on load | `rail-lines-tab` | `rail-lines` | 0 | 0 |
| click Details | `rail-lines-tab` | `rail-lines` | 0 | 0 |
| click History | `rail-lines-tab` | `rail-lines` | 0 | 0 |

The reverted build never fetched the history route, so the 503 is absent. The workspace rendered,
and the rail refused both tab switches. This is the pin. The patch removes the only difference.

### No behaviour change

Both builds ran the same six cases. The fixture branch is the only row that differs.

| Case | URL | Fixed build | Reverted build |
|---|---|---|---|
| fixture alias | `/d/fixture` | `populated`, History 7 nodes and 6 edges | `populated`, loop error, tabs stuck |
| fixture UUID | `/d/6d9c2f1a-3b4e-4a8d-9c1e-7f2b5a3d8c40` | `populated`, History 7 nodes and 6 edges | `populated`, loop error, tabs stuck |
| pending non fixture | `/d/pendingproj` | `empty`, pending sentence | `empty`, pending sentence |
| missing non fixture | `/d/not-a-project` | `error`, missing sentence | `error`, missing sentence |
| lookup failed | `/d/not-a-project`, `/api/dubs` blocked | `error`, lookup sentence | `error`, lookup sentence |
| loading panel | `/d/fixture?panel=loading` | `loading`, loading sentence | `loading`, loading sentence |

`/d/pendingproj` came from a seeded upload record under the data directory. `curl` on
`/api/dubs` listed it, so the effect found a match and chose the pending branch. The blocked case
used `Network.setBlockedURLs` on `/api/dubs`, so the fetch rejected and the effect chose the
lookup-failed branch. The fixed and reverted builds agreed on all three non fixture outcomes.

### The effect carries no other read after write

I read lines 190 to 243. The fixture branch reads `page.params.id`, `page.url.searchParams`, and
the local `loaded`. It writes `dub` and `workspaceState`. It never reads `dub` or `workspaceState`.
The non fixture branch writes `dub` and `workspaceState` inside a fetch callback. It reads only
the local `active`, `result`, and `match`. No line reads a state value after writing it.

### Mutation matrix

Scratch copy at `/tmp/t72d/mut`, applied by `/tmp/t72d/mutate.py`, rebuilt and re-probed each time.

| Mutation | Expected | Measured |
|---|---|---|
| M1 restore HEAD's read after write | loop error | exception `effect_update_depth_exceeded`, both tab clicks stuck on `rail-lines` |
| M2 read `workspaceState` after writing it | loop error | exception `effect_update_depth_exceeded`, both tab clicks stuck on `rail-lines` |
| M3 read `dub` after assignment through a local | loop error | exception `effect_update_depth_exceeded`, both tab clicks stuck on `rail-lines` |
| M4 flip the populated predicate to `> 999` | empty state | panel state `empty`, no loop, pending sentence |

M4 proves the populated and empty selection still reads the loaded object. The fixed build passes
the same predicate on the frozen fixture and picks `populated`.

### Runes hygiene and regression

The owned file contains no `export let`, no `$:` label, and no store import. It uses 20 rune
expressions. `npm --prefix web run check` reported 177 files, 0 errors, and 0 warnings.
`npm --prefix web run build` succeeded. `python3 tools/audit_docs.py` reported no drift.
`git status --porcelain=v1` lists only the owned page and the diary status line.

## Findings

Zero findings. This patch carries no C, H, M, or L defect.

| # | Sev | Where | What | Pin | Mutation |
|---|---|---|---|---|---|
| none | none | none | none | none | none |

## Residue against prior rounds

T7.2d has no prior rounds. There is no residue to carry.

## What I did not file

- **The favicon 404.** Every fresh browser context logs a 404 for `/favicon.ico`. The static
  handler returns 404 for any missing path with a file extension. The patch does not touch
  `internal/api/static.go`, `web/build/index.html`, or `web/static/`. The defect predates this
  diff, so I did not file it.
- **The 503 log line for the history route.** With empty ClickHouse credentials the History tab
  fetch answers 503. That is the documented T7.2c fixture fallback, and the tab still renders the
  seven fixture commits. The patch does not touch `HistoryTab.svelte`, so I did not file it.
- **The implementer's zero message claim.** I could not reproduce zero console messages in a
  fresh context. I measured a favicon 404 and a history 503. Neither is
  `effect_update_depth_exceeded`, and neither comes from this diff. The done condition holds.
- **The diary status line.** `dev-diary/PHASE-7-ingestion.md` flips T7.2d from `not-started` to
  `claimed:orchestrator`. The task brief names that line as the orchestrator's claim, so I did not
  file it.
- **The duplicated load logic.** The effect repeats the initial state selection at lines 32 to 57.
  The patch does not create that duplication. I did not file it.
