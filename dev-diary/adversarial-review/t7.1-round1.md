# T7.1 Round 1 Review

## Verdict

REMEDIATE

| Severity | Count |
|---|---:|
| C | 1 |
| H | 2 |
| M | 1 |
| L | 1 |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| C1 · `internal/api/server.go` lines 40 to 74 and `cmd/ajilamu/main.go` lines 42 to 62 | The running server never mounts `POST /api/dubs/new`. Its options also carry no upload handler or persistent upload root. The completed `NewUploadHandler` therefore stays unreachable. This is a T7.0-owned integration change that blocks T7.1. | I started the current binary with no environment and sent a multipart video request to `/api/dubs/new`. It returned `503 Frontend build is unavailable`, not the upload contract. Source inspection finds zero matches for `NewUploadHandler` or `/api/dubs/new` outside the create-page fetch. When a static root exists, the fallback would instead return HTML `200`, and the page would fail while parsing it as JSON. | Revert the mount and every creator reaches the frontend fallback instead of persistent storage. No upload can create a project. |
| H1 · `web/src/routes/new/+page.svelte` lines 3 to 49 | The projected fee invents runtime, language-expansion, and response-size constants inside the screen. It has no contract with P2's completed billing model. The result can materially misstate the price before the creator starts work. | `ffprobe` reports the committed clip runs 75.008267 seconds. The page's formula yields 32,484,000 nanodollars, which displays as `$0.03`. P2's completed fixture ledger records 23,414,000 nanodollars, or `$0.02`, for that clip. The difference is 9,070,000 nanodollars, or 38.7 percent. No source defines the page's 13 characters per second, 1.1 expansion, or fifth-sized segmentation response. | Revert a shared estimate contract and a rate or pipeline change silently leaves the start screen quoting a stale figure. Creators decide whether to run against an ungrounded fee. |
| H2 · `internal/api/upload.go` | The upload handler has no repository test. Its success, error, size-limit, cleanup, path, and response behaviors have no durable pin. | `go test -coverprofile=/tmp/t71-api-coverage.out ./internal/api` reports 0.0 percent for every function in `upload.go`. I copied only the handler to a throwaway package and independently verified success, 413 cleanup, unknown-field cleanup, relative response paths, and 0600 files. Those checks disappear when this review ends. | Revert any streaming, cleanup, or path guard and the repository suite stays green. A broken upload reaches the critical demonstration path without a failing test. |
| M1 · `web/src/routes/new/+page.svelte` line 240 | Error text uses a fixed light-theme red on the dark accent panel. It fails WCAG 2.1 AA normal-text contrast when an upload or metadata error appears. | The dark screen resolves the error foreground to `#B42318` and its `--accent-q` background to `#33261A`. Independent sRGB luminance calculation gives 2.23:1. The error is 11.5px normal text, which requires 4.5:1 under WCAG 1.4.3. | Revert a contrast-safe error treatment and dark-theme creators cannot reliably read why their upload did not start. |
| L1 · `web/src/routes/new/+page.svelte` line 179 | The visible estimate explanation says “per-token rates”. The product vocabulary explicitly forbids that implementation term. | The rendered accessibility tree says, “The estimate uses the video runtime and current per-character and per-token rates.” `ui-ux.md` section 8 lists “token or model call” under Never Use. | Revert the copy fix and the creation screen leaks an internal billing term to creators. |

## Independent Evidence

| Check | Observation |
|---|---|
| Multipart streaming | The handler uses `Request.MultipartReader`, `http.MaxBytesReader`, and one 128 KiB `io.CopyBuffer`. It never calls `ParseMultipartForm` or reads a file part into memory. |
| Filesystem safety | My throwaway HTTP probe submitted names containing parent traversals. It received only project-relative paths. The stored video contents matched the input and its permission bits measured 0600. |
| Atomic failure | The same probe sent an unknown file part and an over-limit request. Both left the persistent root with zero entries. The limit response was 413. |
| Optional music | The success probe sent both `video` and `music`. The 201 JSON contained the project id, Malayalam target language, and separate video and music records. |
| Request errors | The handler returns 405 with `Allow: POST`, 415 for non-multipart input, 400 for missing required fields, and 413 for a `MaxBytesError`. |
| UI semantics | The running create screen exposes labelled video and music file controls, a labelled language menu, a disabled start button before a usable video exists, and an `aria-live` fee area. Submit errors use `role=alert`. |
| UI build | `npm run check` completed with zero errors and warnings. `npm run build` completed. These are health checks, not pins for the findings. |

## Scope and Residue

This is round 1, so no prior review residue exists.

I created only this review file in the repository.
I changed no implementation file, fixture, task status, or commit.
The temporary independent probe lived outside the repository and was removed after verification.

## Required Ownership Changes

T7.0 must add the upload handler and persistent application-data root to its server wiring.
T7.1 must add durable upload tests and consume a documented estimate contract.
The remediation must re-review all five findings.
