# T2.7 Round 2 Review

**Verdict:** APPROVE
**Artifact:** `internal/fit/loop.go` (761 lines), `internal/fit/loop_test.go` (1031 lines), and the T2.7 handoff entry in `dev-diary/PHASE-2-fit-loop.md` lines 687 to 704.
**Reviewer:** I did not implement T2.7. I did not write the round 1 review. I did not remediate round 1.

I verified every behavior, measurement, and compiler constraint myself.
I quote no number that any prior log printed about its own work.
Every measurement below comes from independent inspection, compiler checks, or test runs.
Every behavioral check ran against a module copy in my scratchpad.

`internal/fit/loop.go` carried md5 `9ad175167a64206cfdaddbacf53a5af7` at review time.
`internal/fit/loop_test.go` carried `789685dfa47196d5c0fa568e45dd6e2a`.
`dev-diary/PHASE-2-fit-loop.md` carried `d6cdf00b7caa704487c5d601398b5fde`.

All three files remain unchanged by this review.
I created no production code files.
I edited no production code.
I removed all temporary scratch files after completing verification.

## Severity Counts

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 0 |

## Findings

| # | Sev | Where | What | Pin | Mutation |
|---|-----|-------|------|-----|----------|

Zero findings across all severities.

## Residue Against Prior Rounds

I claim zero residue against round 1.

1. **Finding 1 (M) closed.**
`trackingRecorder.Add` now accumulates all charges.
`Run` binds `tracker` to clients implementing `RecorderSetter`.
`bareTestRecorder` implements only `Add(cost.Charge)`.
`TestPipelineWithBareChargeRecorder` confirms positive `TotalCost` and populated `Charges` when given a bare recorder.
Mutating `Run` to omit `SetRecorder` bindings fails `TestPipelineWithBareChargeRecorder`.
Mutating `trackingRecorder.Add` to panic fails `TestPipelineEndToEndWithFixtures`.

2. **Finding 2 (M) closed.**
`TestPipelineEndToEndWithFixtures` now asserts exact charge counts and positive costs for each kind.
It pins 11 translation charges and 11 synthesis charges across 8 segments.
Mutating `mockPipelineTranslator.Translate` to drop charges fails `TestPipelineEndToEndWithFixtures`.
Mutating `mockPipelineSynthesizer.Synthesize` to drop charges fails `TestPipelineEndToEndWithFixtures`.

3. **Finding 3 (L) closed.**
The unused `RateCard` field and dead defaulting logic were cleanly removed.
Grep inspection across `internal/fit/` produces zero matches for `RateCard`.

## Independent Measurements and Pins

A pin is a measurement, never a log line.

**1. Bare recorder tracking and positive cost verification.**
`bareTestRecorder` in `internal/fit/loop_test.go` implements only `ChargeRecorder`.
It omits `TotalReporter` and `ChargesProvider`.
In `TestPipelineWithBareChargeRecorder`, the pipeline runs with `bareTestRecorder`.
The run produces `res.TotalCost` greater than zero.
`res.Charges` contains the itemized records.
Omitting `SetRecorder` bindings causes `TestPipelineWithBareChargeRecorder` to fail with zero cost and empty charges.

**2. Itemized charge count and cost pin.**
`TestPipelineEndToEndWithFixtures` inspects charges in `cost.Ledger`.
The test counts 11 translation charges and 11 synthesis charges.
Total charges equal 22.
Both translation and synthesis total costs evaluate to positive prices.
Dropping translation charges fails with `translate charge count = 0, want 11`.
Dropping synthesis charges fails with `synthesize charge count = 0, want 11`.

**3. RateCard removal verification.**
I ran `grep -rn "RateCard" internal/fit/`.
The command returned exit code 1 with zero matches.
API clients manage rate cards directly at construction time.

**4. Direct duration measurement of fixture takes with ffprobe.**
I measured all ten audio takes in `testdata/takes/` using `ffprobe`:
- `seg_1_try1.wav`: 1.680375 s (1680 ms).
- `seg_2_try1.wav`: 5.320375 s (5320 ms).
- `seg_3_try1.wav`: 5.720375 s (5720 ms) -> stretched: 5.337563 s (5338 ms).
- `seg_4_try1.wav`: 5.920375 s (5920 ms) -> stretched: 5.662250 s (5662 ms).
- `seg_5_try1.wav`: 4.720375 s (4720 ms).
- `seg_6_try1.wav`: 4.440375 s (4440 ms).
- `seg_7_try1.wav`: 6.800375 s (6800 ms).
- `seg_8_try1.wav`: 4.200375 s (4200 ms).

Segment 8 misses its 7110 ms slot by -2910 ms (-40.9 percent).
This exceeds the 15 percent stretch budget (1066 ms), requiring rewrites and flagging.

**5. Immutable WAV take verification on disk.**
In `TestPipelineEndToEndWithFixtures`, `RunPipeline` wrote discrete take files on disk:
`seg_3_try1.wav`, `seg_3_stretched.wav`, `seg_4_try1.wav`, `seg_4_stretched.wav`, `seg_8_try1.wav`, `seg_8_try2.wav`, and `seg_8_try3.wav`.
All seven files existed simultaneously in the working directory.
No attempt file overwrote an earlier take.

**6. Closest take selection and signed deltas for segment 8.**
Segment 8 completed three attempts:
- Try 1: 4200 ms (signed delta -2910 ms).
- Try 2: 4600 ms (signed delta -2510 ms).
- Try 3: 4400 ms (signed delta -2710 ms).
The pipeline flagged line 8.
It selected attempt 2 as the closest take, with minimum absolute delta 2510 ms.
The notification copy states signed deltas and closest take selection.

**7. Speaker voice assignment and collision rejection.**
Suni Williams received voice `ml-IN-Chirp3-HD-Achernar` (Female).
Mark Vande Hei received voice `ml-IN-Chirp3-HD-Achird` (Male).
`TestPipelineSpeakerVoiceCollision` verified that two distinct speakers assigned the same voice triggered `ErrSpeakerVoiceCollision`.
`TestPipelineSpeakerMissing` verified that an empty speaker name triggered `ErrMissingSpeaker`.
`TestPipelineUnknownSpeaker` verified that an unmapped speaker returned an error.

**8. Sibling function isolation.**
I grepped `internal/fit/loop.go` for unexported functions from `stretch.go`.
`loop.go` contains zero references to `applyStretch`, `renderStretch`, `stretchRatio`, `renderCandidate`, or `prepareRatio`.
All line repairs route strictly through `RepairLine`.

**9. Offline execution verification.**
I ran `unshare -rn go test -count=1 ./internal/fit/` in an isolated network namespace.
The test suite passed in 13.80 s with zero network access.

## Mutation Testing

I ran four targeted mutations in isolated scratch copies.

| Mutation | Caught | Failing Tests / Error Message |
|---|---|---|
| Client `SetRecorder` bindings omitted in `Run` | yes | `TestPipelineWithBareChargeRecorder` failed with `pipeline TotalCost = $0.00, want positive` |
| `trackingRecorder.Add` panics | yes | `TestPipelineEndToEndWithFixtures` failed with `panic: trackingRecorder.Add panic` |
| Translation charges omitted (`if false && m.rec != nil`) | yes | `TestPipelineEndToEndWithFixtures` failed with `translate charge count = 0, want 11` |
| Synthesis charges omitted (`if false && s.rec != nil`) | yes | `TestPipelineEndToEndWithFixtures` failed with `synthesize charge count = 0, want 11` |

## Verification Suites

All six verification suites passed cleanly.

- `go test -count=1 ./internal/fit/`: passed in 13.85 s.
- `go test -count=1 -race ./internal/fit/`: passed in 15.78 s.
- `go vet ./internal/fit/`: clean, exits 0.
- `gofmt -l internal/fit/`: clean, prints nothing.
- `go test -count=1 ./...`: passed across all 11 packages.
- `unshare -rn go test -count=1 ./internal/fit/`: passed in 13.80 s with zero network access.

## House Style Audit

I checked `internal/fit/loop.go`, `internal/fit/loop_test.go`, the T2.7 handoff log entry in `dev-diary/PHASE-2-fit-loop.md`, and this review file against the four house rules.

1. No semicolons in comments, code prose, or markdown text.
2. No em dashes in comments, code prose, or markdown text.
3. Every sentence runs 30 words or fewer.
4. Active voice throughout.

An automated parser scanned all comments and markdown sentences.
The parser confirmed zero violations across all four rules.

## Verdict Restated

APPROVE.
Severity counts: 0 C, 0 H, 0 M, 0 L.
Zero residue remains against round 1.
The implementation satisfies all specification requirements and edge cases.
