# T5.5 Round 2 Review

## Verdict

REMEDIATE

| Severity | Count |
|---|---:|
| C | 0 |
| H | 1 |
| M | 0 |
| L | 0 |

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| H | `web/src/lib/tabs/HistoryTab.svelte:96-99` | The history still does not render a DAG faithfully. Every parent edge uses the same x coordinate and one vertical stroke. A fork therefore appears as one continuous chain through unrelated saved nodes. The renderer has corrected its data model but still invents visible ancestry. | In a three-node fixture where saved 2 and saved 3 both name saved 1 as their parent, the SVG emits `M 7 13 V 81` and `M 7 13 V 149`. Those overlapping strokes run through saved 2 before reaching saved 3. | Reverting a branch-aware edge layout preserves the parent map but makes two sibling saves look like ancestor and descendant saves. |

## Independent evidence

I own only this review file. The `Dub`, `Line`, `Take`, `Charge`, and `Commit` shapes already exist. I can validate the rail without a running backend.

All four round-one remedies were inspected independently. The history now derives edges from `parent_commit_id`. It still fails F1 because the drawing collapses every branch into a single spine.

The learned-voice panel derives its voice, count, and signed ratio from active takes. The fixture measures 38,160 milliseconds across 41,900 milliseconds of source slots. That renders as 8.9 percent shorter from 8 recorded lines.

The tab handler moves focus with Arrow keys, Home, and End. The desktop rail declares `flex: 0 0 352px` and `min-width: 352px`. Both duration columns and the active-take duration retain their take-playback callbacks.

`npm run check` completed with zero diagnostics. `npm run build` completed successfully. These establish build health only. The synthetic fork edge measurement is the failing pin.

Round-one F2, F3, and F4 have zero residue. Round-one F1 remains open through its visual rendering requirement.
