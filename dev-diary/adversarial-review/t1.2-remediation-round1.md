# T1.2 Remediation Round 1

| # | Finding | Fix Applied |
|---|---------|-------------|
| 1 | `Price.String()` formats to exactly two decimal places. This rounds micro-costs down to `$0.00`. | Updated `Price.String()` to use up to nine decimal places. The method trims trailing zeros while keeping at least two decimal places. Added `TestPriceString` to test micro-cost formatting. |
