# T5.7 Round 1 Remediation

| Finding | Change | Files | Residual |
|---|---|---|---|
| F1 | Named each flagged line and moved flagged notes into a separate labelled block. | `web/src/routes/d/[id]/+page.svelte` | none |
| F2 | Removed selection-driven `scrollIntoView`, so route selection no longer scrolls the picture area. | `web/src/routes/d/[id]/+page.svelte` | none |
| F3 | Set the picture duration to 75008 milliseconds for Preview and Timeline. | `web/src/lib/fixture.ts`, `web/src/routes/d/[id]/+page.svelte` | none |
| F4 | Stated that the picture plays original audio and discrete takes play from length checks or the Lines rail. | `web/src/routes/d/[id]/+page.svelte` | T5.2 fixture carries no per-language mixed track |
| F5 | Derived a rail Dub containing only the active language while preserving segments, charges, commits, and totals. | `web/src/routes/d/[id]/+page.svelte` | none |
| F6 | Removed the fabricated processing banner because the fixture carries no progress event. | `web/src/routes/d/[id]/+page.svelte` | none |
| F7 | Added a natural command status sentence with `role="status"` after submission. | `web/src/routes/d/[id]/+page.svelte` | none |
| F8 | Left Timeline unchanged because its contract lacks selection, callbacks, and playback position inputs. | `web/src/routes/d/[id]/+page.svelte` | T5.3 Timeline needs a selection input and a playhead |
| F9 | Made the project title the route `h1`, changed the selected line heading to `h2`, and included the project title in `document.title`. | `web/src/routes/d/[id]/+page.svelte` | none |
| F10 | Kept loading unused on the synchronous happy path so first paint stays populated. Empty and error states remain reachable. | `web/src/routes/d/[id]/+page.svelte` | none |
