# T6.3 text correction: round 2

**Verdict: APPROVE.** Counts: **C 0, H 0, M 0, L 0.**

The remediation fixes both round 1 findings and breaks nothing. I rebuilt the frontend and the
stand-in harness from the working tree. I drove the page in headless Chromium and measured the
DOM, the requests and the harness log. The source panel reports the
gap before the confirm. The source confirm sends no request. The target path sends one POST,
answers 201, and the page applies the take. The handoff sentence matches the measured list.
Zero residue stands against M1 and L1.

## Findings

| # | Sev | Where | What | Pin | Mutation |
|---|---|---|---|---|---|

No finding met the criteria. The four columns carry no row this round.

## What I measured

I copied `web/` to `/tmp/t63r2/app/web`, symlinked `node_modules` and `testdata`, and ran
`npm run build` there. I rebuilt the stand-in harness at `/tmp/t63r2/harness` against the
working tree. It mounts the real `api.RerenderHandler` behind fake renderer, recorder and
history seams. It serves the fresh bundle and the frozen `testdata/wire/dub.json` under the id
`t63-real-dub`. I drove it with headless Chromium through Playwright. I wrote my own probes. I
used the storage root `/tmp/t63/storage` so the handoff path could be compared exactly. The
working tree was never edited.

### 1. The source panel reports the gap before the confirm

Line 3 was selected. The source field held the fixture source sentence. It was replaced with
`An amazing research lab orbiting Earth, corrected by the creator.` The `Re-translate line`
field action opened the panel. The panel read:

```text
Re-translating line 3 from the corrected source needs a route that runs the translation
model first. The landed re-render route accepts a corrected target line only, so no handler
takes a corrected source line yet. T6.5b owns that path. This confirm reports the gap only,
so it sends nothing and runs no model. Cancel Re-translate line
```

The confirm label read `Re-translate line`. The confirm carried no `disabled` attribute, so
`confirmDisabled` was `false`. The source field hint read `A corrected source line needs
re-translation. T6.5b lands that path.` The panel names the missing route and the owner before
the creator confirms. The old promise of `calls the translation model, then a fresh voice
render` is gone.

### 2. The source confirm sends nothing

The probe counted every request whose URL contains `/rerender`. It clicked the source confirm
and waited 600 ms. The count moved by zero. The whole browser run carried exactly one
`/rerender` request, and that was the later target call. The note after the confirm read:

```text
Line 3 cannot re-translate yet. The re-render route accepts a corrected target line, not a
corrected source line. Nothing was sent and no model ran. T6.5b owns that path.
```

The harness log agrees. It recorded one `POST /rerender` for the whole run, and that POST
carried the corrected target text. The source confirm produced no request, no render call and
no record.

### 3. The target path still works

The target field was replaced with `ഹായ്, ഞാൻ സുനി വില്യംസ് ആണ്, തിരുത്തിയത്.` The
`Save and re-render` action opened the panel. The confirm sent exactly one request:

```text
POST /api/dubs/t63-real-dub/lines/3/rerender
{"language":"ml","text":"ഹായ്, ഞാൻ സുനി വില്യംസ് ആണ്, തിരുത്തിയത്."}
```

The route answered 201. The body carried `segment_id` 3, `language` `ml-IN`, commit
`a84271824be345f34bb0d4776ae3a2b4`, `total_nanodollars` 630000, and the sentence
`Line 3 re-rendered as take seg_3_try2.wav.` The take carried
`"file":"/tmp/t63/storage/t63-real-dub/work/ml-IN/seg_3_try2.wav"`, attempt 2, repair `none`,
`flagged` false, a fit of 5320 of 5320 ms, and one synthesize charge of 630000 nanodollars.

The panel closed. The note read `Line 3 re-rendered as take seg_3_try2.wav.` The line text
became the corrected text. The estimate basis moved to the new take. I also selected line 4
and returned to line 3. Line 3 kept the corrected text and the three takes. Console messages
were 0 and page errors were 0. The harness log recorded one render call and one record.

### 4. The handoff sentence matches the measurement

`dev-diary/PHASE-6-editing.md:513-514` says both take lists then read
`seg_3_try1.wav,seg_3_stretched.wav,/tmp/t63/storage/t63-real-dub/work/ml-IN/seg_3_try2.wav`.
The measured list matched that string exactly. The Text panel and the Speaker panel both
carried it in `data-take-files`. `data-estimate-basis` carried the same absolute path. The
Surprises paragraph also calls the take `file` an absolute server path, so the two statements
now agree. The round 1 disagreement is gone.

### 5. Svelte 5 and the check

`Text.svelte` and `+page.svelte` carry no `export let`, no `$:`, and no store. Both use
`$state`, `$derived`, `$derived.by`, `$effect` and `$props`. The check passed:

```text
$ npm --prefix web run check
COMPLETED 183 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
```

## Residue against round 1

M1 carried zero residue. The panel now reports the gap before the confirm. It states that the
confirm sends nothing and runs no model. The confirm sends no request, measured. The source
field hint names the same owner.

L1 carried zero residue. The handoff now quotes the absolute path the route returns. The
measured take list matched the handoff string exactly. The sentence and the Surprises
paragraph agree.

## Done condition

The done condition reads: both text fields edit cleanly, and editing target text triggers
synthesis without calling translation models.

Both halves hold on the measured path. Both fields edit. The target confirm sends one request.
The route synthesizes a new take and the page applies it. The source half reports the owned
gap before the confirm. T6.5b owns the missing source-text route. That gap is recorded and
unchanged.

## Known edges

Two round 1 edges still stand and neither is a T6.3 defect. A corrected target line that
misses its slot re-translates on attempt two, and `fit.RepairLine` owns that. The new take
carries an absolute server path, so `fixtureTakeSource` cannot resolve it and `Play this take`
does nothing for the new take. T6.5 owns the asset URL. Both edges are recorded in the handoff.
