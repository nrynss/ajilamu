# T7.2 Round 3 Remediation

Operator skipped a fourth review. The done condition already measured closed. These three edits are chrome, error copy, and pending wording.

| Finding | Sev | Resolution | Pin |
|---|---|---|---|
| M1 | M | Closed. Fixture chrome now reads readiness and spend from the fixture payload. The fixture records `review` and 23,414,000 nanodollars. Chrome therefore shows `In review` with the amber class, matching the index. | `fixtureChrome` returns `dub.readiness` and `dub.total.total_nanodollars`. `npm --prefix web run check` must pass. |
| M2 | M | Closed. A failed or non-ok `GET /api/dubs` is now `lookup_failed`. The workspace says the list could not load. Chrome says `Could not load status`. Only a successful list without the id still says the project is missing. | Both fetch paths branch `ok` from `not_found`. A catch lands on `lookup_failed`. |
| L1 | L | Closed. Pending copy no longer names a sample. Uploads and samples share `This project is waiting for dubbing.` | The empty-state sentence contains no `sample` word. |

## Validation

- `npm --prefix web run check` passed with zero errors and zero warnings.
