# T5.3 Remediation Round 1

| Finding | Fix | Pin | Mutation |
|---|---|---|---|
| M1 | Replaced the resize button with a focusable horizontal separator. It exposes the 156px minimum, current height, and 640px maximum. Pointer and arrow-key resizing now share one bounded height calculation. | An independent Vite SSR render emitted `role="separator"`, `aria-valuemin="156"`, `aria-valuenow="256"`, `aria-valuemax="640"`, and `tabindex="0"`. `npm run check` completed with zero diagnostics. `npm run build` completed successfully. | Reverting this change restores button semantics and removes the height range from assistive technology. |
