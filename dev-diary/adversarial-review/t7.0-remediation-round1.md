# T7.0 Round 1 Remediation

| Finding | Remediation | Independent pin |
|---|---|---|
| C1 | `.gitignore` line 8 now reads `/ajilamu`. The anchor limits the rule to the repository-root build output. Git tracks `cmd/ajilamu/main.go` again, and a fresh clone receives the entrypoint source. | `git check-ignore -v cmd/ajilamu/main.go` exits 1 and prints nothing. `git add --dry-run cmd/ajilamu/main.go` prints `add 'cmd/ajilamu/main.go'`. The root binary stays ignored, because `git check-ignore -v ajilamu` prints `.gitignore:8:/ajilamu`. |
| C2 | `cmd/ajilamu/main.go` assigns `ledgerFlusher` only inside the branch that opens a real client. Development startup therefore leaves the `api.LedgerFlusher` interface genuinely nil, and `Server.Shutdown` returns before any flush call. `internal/api/server_test.go` adds `TestEntrypointExitsCleanlyWithoutLedger`, which builds the entrypoint, starts it with no credentials on `PORT=0`, waits for the listening line, sends `os.Interrupt`, and requires exit code 0. | I built the binary and ran it under `env -i PATH="$PATH" HOME="$HOME" PORT=45871`. `kill -INT` produced exit code 0. `kill -TERM` produced exit code 0. Neither run logged `ledger client is nil`. Under `go run ./cmd/ajilamu` with the same scrubbed environment, interrupting the server child gave `go run` exit code 0. |
| C3 | `internal/api/server.go` mounts `options.Upload` at `POST /api/dubs/new` when the option arrives. `cmd/ajilamu/main.go` supplies `api.NewUploadHandler(filepath.Join(cfg.DataDir, "uploads"))`, so storage comes from T7.0's `AJILAMU_DATA_DIR` contract. `internal/api/server_test.go` adds `TestServerMountsUploadHandlerOnPersistentStorage`. | I started the entrypoint with `AJILAMU_DATA_DIR` pointed at a temporary directory, then posted multipart input with curl. The response was `http_status=201` with `content_type=application/json` and a project-relative video path. `find` showed `data/uploads/<id>/source.mp4` and `data/uploads/<id>/music.mp3` on disk at mode 0600, holding the exact bytes I sent. |

## Mutation checks

I reintroduced each defect and re-measured, then restored every file and confirmed the
checksums matched.

Restoring the unanchored `ajilamu` rule made `git check-ignore -v cmd/ajilamu/main.go` print
`.gitignore:8:ajilamu` again.

Assigning the `*ledger.Client` to the interface unconditionally made
`TestEntrypointExitsCleanlyWithoutLedger` fail with `entrypoint exit after interrupt: exit
status 1`.

Removing the mount made `TestServerMountsUploadHandlerOnPersistentStorage` fail with
`upload status = 200, want 201`, because the request fell through to the static catch-all.

## Other measurements

`go test -count=1 ./internal/api ./internal/config ./cmd/ajilamu` passes. The `cmd/ajilamu`
package carries no test files of its own.

`env -i PATH="$PATH" HOME="$HOME" ENV=production go run ./cmd/ajilamu` exits 1 and names
`CLICKHOUSE_HOST`.

`python3 tools/audit_docs.py` reports no drift. `go vet` is clean across the three packages.

## Scope

Round 1 arrived with three Critical findings and no Highs, Mediums, or Lows. Prior agents in
this worktree had already applied all three fixes before I started. I re-measured each one
independently rather than trusting that state, and I found no remaining gap. I therefore
changed no file in this round beyond adding this record.

The T0.2 block in `PHASE-0-ground.md` already describes the deferred-credential loader that
`internal/config/config.go` implements, so it needed no edit.

The only gofmt complaint in the repository is `internal/types/types_test.go`, which predates
T7.0 and sits outside this task's owned paths.
