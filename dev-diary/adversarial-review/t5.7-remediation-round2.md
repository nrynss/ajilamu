# T5.7 Round 2 Remediation

| Finding | Change | Files | Residual |
|---|---|---|---|
| F1 | Added a `panel=loading` query seam. When it is present, the route sets PanelState to loading with the offline fixture sentence and skips mounting the workspace. The default path still loads synchronously into populated on first paint. | `web/src/routes/d/[id]/+page.svelte` | none |
| F2 | Added `revealSelectedLengthRow` to adjust only `.length-list.scrollTop` so the selected `#length-line-{id}` row stays inside the list viewport. The helper runs after user and playhead selection via `tick`. It does not call `scrollIntoView`. | `web/src/routes/d/[id]/+page.svelte` | none |
