# T4.1 remediation, round 1

Verdict remains REMEDIATE.

| Severity | Where | Remediation | Pin | Mutation |
| --- | --- | --- | --- | --- |
| H | `internal/ledger/client.go:100-113` | `New` now starts a background reconciler. It flushes immediately, then retries every second without a later enqueue or explicit flush. | An isolated external harness forced an HTTP disconnect after a durable journal write. It observed one `.json` file and `Pending() == 1`. After the endpoint returned `200`, it observed `Pending() == 0`, no `.json` files, and at least two requests without caller action. Command: `go test -count=1 -run TestRecoveredJournalReconcilesWithoutCallerAction -v ./internal/ledger` in `/tmp/ajilamu-t41-remediation.50kyWb`. | Reverting the reconciler leaves retained creator edits on disk after connectivity returns. |

Validation also passed from the repository root: `go test ./...`, `go vet ./...`, `gofmt -d internal/ledger/client.go internal/ledger/queue.go`, and `git diff --check`.
