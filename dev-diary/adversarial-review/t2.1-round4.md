# T2.1 Round 4 Review

**Verdict:** APPROVE
**State:** Uncommitted ADC cutover delta, same shape round 3 approved. `internal/gemini/` stays untracked. `go.mod` carries the approved oauth2 require. `go.sum` stays untracked. The only code file changed since round 3 is `internal/gemini/segment_live_test.go`.
**History:** Round 3 returned REMEDIATE with one M and one L. The remediation record claims both fixes. This round re-measures both claims and sweeps for regressions.

## Severity Counts

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 0 |

## Round 3 Residue

| # | Round 3 Finding | Residue | Pin | Mutation |
|---|-----------------|---------|-----|----------|
| 1 (M) | Probe gated live evidence on fixture exactness. Count mismatch called Fatalf. Per-id drift past 2500 ms called Errorf. | Zero. No count gate remains. No tolerance gate remains. The probe logs the count comparison and prints the drift table without failing on either. Structural checks enforce increasing ids, per-row validation, clip bounds, and overlap freedom. The fixture decode gate and the speaker set check remain. | Grep for `2500` across the probe returns nothing. Remaining Fatalf calls cover setup, media prep, client build, request failure, endpoint build, fixture load, and clip duration. The count now appears only in a Printf comparison at line 97. Structural Errorf calls at lines 107 through 127 enforce the four downstream properties. The decode gate at lines 93 through 96 and the speaker set check at lines 138 through 147 remain. A scratch replay fails a valid 11 segment run under the old gates and passes it under the new gates. | Reintroducing a count Fatalf reds the next valid generative run and repeats the original defect. The scratch overlap case proves tightened structural checks still fire. |
| 2 (L) | Probe printed transcript and charges without model, location, or endpoint. Readers could not trace evidence to a run. | Zero. The probe prints one provenance line before the transcript. It names model, location, and endpoint. | Read of line 78 shows the format string with all three fields. The transcript follows at line 79. The live run opens with the provenance line naming gemini-2.5-flash, global, and the full Vertex endpoint. | Deleting line 78 restores anonymous evidence. The run header would lose its source. |

## Regression Sweep

| Claim | Pin |
|---|---|
| `segment.go` still carries the approved ADC surface and the verbatim prompt. | Auth grep across `internal/gemini/` returns nothing. The oauth2 import is present. The prompt literal measures 658 bytes. |
| `segment_test.go` still pins behavior at every site. | Grep finds 7 StaticTokenSource sites and no ServiceAccount text. The request path assertion stands at line 97. TestEndpointURL stands at line 315. |
| `go.mod` and `go.sum` still carry only the approved delta. | `go.mod` requires oauth2 v0.36.0 direct with compute metadata indirect. `go mod verify` passes for all modules. |
| Sibling translate files are untouched. No other code moved. | `git status` shows no tracked modification under `internal/`. Modified tracked files are `go.mod` and one board doc owned elsewhere. `go.sum` and `internal/gemini/` stay untracked, same as round 3. |
| Offline suite is green. | `go build ./...` plus `go test -count=1 ./...` passes across all seven packages. |
| Live-tagged vet is green. | `go vet -tags live ./...` exits 0. |
| Owned paths are gofmt clean. | `gofmt -l internal/gemini/` prints nothing. One flag exists in `internal/types/types_test.go`. That file sits outside T2.1 paths. This review edits no code, so the flag predates it. |

The scratch snapshot at `scratch/t21rev` predates the cutover. It still imports crypto rsa and pem. Diffs against it show the approved ADC delta, not regressions.

## Live Evidence

Ran the probe once with GEMINI_MODEL set to gemini-2.5-flash and GOOGLE_CLOUD_LOCATION set to global. Overrides bypass the prefixed .env model id. Result is PASS in 9.89 seconds. It returned 8 segments with both fixture speakers present. The ledger records one 658 unit segment charge at $0.0000001 for a total of $0.0000658. The drift table printed per-id drift without failing. Largest drift measured near 1.7 seconds.

Measured the skip path with a bogus credentials path. Result is SKIP in 0.29 seconds with the cause named. No network call runs on that path.

Spend is one Vertex generateContent call at $0.0000658. Offline runs and the skip run cost nothing.

## Mutation Re-measurement

Staged a gate replay in `scratch/t21rev4/mutation_check.py`. Case A replays a valid 11 segment run. Old gates fail it with a count fatal plus drift errors. New gates log the count and pass it. Case B replays an 8 segment run with a 20 second drift on one id. Old gates error on every drifted id. New gates pass it. Case C injects one overlap into case B. New gates catch it. The script printed PASS. Cleanup removed the scratch directory after the run.

## Notes

The repo .env still carries the prefixed model id per the remediation record. Live runs need explicit overrides until the environment changes. That state sits outside owned paths.

The AGENTS.md stack exception for golang.org/x/oauth2 v0.36.0 still lands at land time per round 3. This review duplicates no provenance.

## Verdict Restated

APPROVE with zero findings. Both round 3 findings show zero residue. The regression sweep is clean and the live probe passes.
