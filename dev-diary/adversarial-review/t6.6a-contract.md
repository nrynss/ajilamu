# T6.6a contract proposal

The route needs an offline integration test with the real agent and a fake MCP transport and model.
The fake model requires ADK types. AGENTS.md restricts ADK imports to internal/agent.

Add `internal/agent/route_test.go` to T6.6a ownership. Use external package `agent_test` to import the API server without an import cycle.
Production files keep their existing ownership. No other wave task owns the proposed test path.

The orchestrator will record acceptance in the task block before this path changes.

Accepted by the orchestrator on 2026-09-09. The task block now owns the test path.
This keeps ADK imports within internal/agent and leaves concurrent ownership disjoint.
