# T6.7a review, round 1

**Verdict: REMEDIATE. Approval blocked by missing ledger measurement.**

Code findings: 0 C, 0 H, 0 M, 0 L. One validation blocker remains.

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 0 |

## Scope

I reviewed the workspace page diff against `99a7d36`. The page diff contains 61 additions and 20 deletions.
`web/src/lib/fixture.ts` has no diff against that base.

I read AGENTS.md in full, T6.7a, the diary README, T6.1 rounds 1 through 3, and T6.7 reviews and remediations.
I also read P6 close rounds 1 through 3.
I recalled memory before code and again using the route callback names.

I own only this review file. Its required document shape exists, and the workspace checks run independently.
The durable ledger check requires an unavailable local runtime, as detailed below.
I changed no implementation, status, or commit. I spawned no agent.
API and DetailsTab reviews remain separate.

## Findings

No code defect reproduced in the reviewed diff.

| Where | What | Pin | Mutation |
|---|---|---|---|
| None | No code findings. | Fresh browser checks below passed. | Reverting the page in a temporary copy fails the fixture protection check. |

## Fresh browser measurements

The probe uses Node 26.8.1 from `/opt/homebrew/opt/node/bin` and headless Google Chrome.
I copied the current source into `/tmp/t67a-r1/repo` and started Vite on `127.0.0.1:18671`.
Chrome used debugging port 18673. I did not reuse port 18567 or previous results.

The independent probe is `/tmp/t67a-r1/browser.mjs`. Raw measurements are `/tmp/t67a-r1/results.jsonl`.

| Fixture route ID | Disabled native controls | Visible read-only notes | POST requests | Page errors |
|---|---:|---:|---:|---:|
| `fixture` | 10 | 4 | 0 | 0 |
| `d3bca364-9c8a-4107-8df9-c4faf909b008` | 10 | 4 | 0 | 0 |
| `6d9c2f1a-3b4e-4a8d-9c1e-7f2b5a3d8c40` | 10 | 4 | 0 | 0 |

Each route rendered four disabled fieldsets: Editor command, Line boundary, Line speaker, and Line text.
Their native controls matched `:disabled`, including both boundary sliders, both speaker toggles, both textareas, both text actions, command input, and Preview.

Each fieldset displayed `This offline fixture cannot save an edit.` Its `aria-describedby` points to that sentence.
I measured visibility through the DOM and inspected the fresh fixture screenshot.
Screenshots for all aliases live beside the probe.

The probe clicked every rendered editor control and attempted focus, typing, ArrowLeft, and Enter.
It also dragged both boundary handles and used the slash shortcut followed by a command and Enter.
All measured control values and boundary values remained unchanged. The request listener captured every POST, without filtering endpoints.

Conditional confirmations remained unreachable through these disabled inputs.
Their component markup remains inside the disabled fieldsets, so native disabling also covers confirmations when rendered.
The route additionally guards boundary changes, command preview and confirmation, speaker changes, edit recording, and line re-rendering before requests.

## Non-fixture control

A browser-intercepted non-fixture project enabled all four fieldsets and removed their fixture restriction.
A real pointer drag changed line 1's end from 7728ms to 6895ms and emitted exactly one POST:

```json
{"kind":"boundary","language":"ml","segment_id":1,"start_ms":5908,"end_ms":6895}
```

The path was `/api/dubs/r1-real/edits`. The page applied the intercepted successful response without a page error.
This measures the browser write path only. It does not measure a ledger commit, action, snapshot, or reload persistence.

## Validation blocker

| Where | What | Pin | Mutation |
|---|---|---|---|
| T6.7a done condition, real project boundary commit | B1: A fresh durable ledger measurement remains unavailable. This is a validation blocker, not a proven code defect. | No local `clickhouse` executable exists. Docker lists only `docs-telemetry:dev` and `debian:stable-slim`. The image pull was blocked before execution. | A swallowed POST or a recorder that writes zero commits could pass the mocked browser control. Only independent ledger queries detect that failure. |

Automatic approval review rejected `docker pull clickhouse/clickhouse-server:26.8.2.7` pending confirmation.
It cited Endor Labs policy `CmdLine: ask before package installation or container commands`.
No existing local ClickHouse runtime was available, and I did not bypass the rejection.

To close B1, provide an authorized local ClickHouse runtime and load the committed schema into an isolated test database.
Seed a non-fixture project, record its commit count with SQL, then drag one boundary through the real page and API.
Query SQL again and require exactly one additional commit, with the changed boundary in its snapshot.
Reload the page and verify that stored boundary remains visible.
T6.6b remains dependent on an approved T6.7a commit.

## Checks and mutation

A temporary page reversion to `99a7d36` ran on Vite port 18672.
The same browser pin failed immediately: zero protected editor fieldsets, expected four.
The output is `/tmp/t67a-r1/mutation.log`. No mutation touched the repository.

Initial temporary-copy checks found `progress.ts:106`, where `Timeout` was incompatible with `number`.
The baseline page produced the identical diagnostic. Both copies inherited unrelated Node types from `/tmp/node_modules/@types`.

Restricting `typeRoots` in the temporary copy to the repository's `web/node_modules/@types` produced zero errors and zero warnings.
The output is `/tmp/t67a-r1/isolated-check.log`. Repository configuration remained unchanged.

The reviewed page contains no `export let` or `$:` syntax.
`git diff --check 99a7d36` for the page and fixture helper passed.

## Residue

No prior T6.7a review exists, so T6.7a has zero prior-round finding residue.
The P6 close fixture-edit note has zero residue in the freshly measured browser cases across all three aliases.

T6.1's route composition and hydration failures did not recur. Both boundary sliders rendered, Chrome reported no page errors, and the non-fixture pointer handler executed.
This review does not reopen T6.1's unchanged minimum-duration implementation.

I cannot claim fresh zero residue for the durable-write invariants from T6.7 or P6 close rounds.
Their prior approvals provide context, not substitute measurements. B1 leaves the real-ledger done condition unverified.
Approval must wait for that measurement.
