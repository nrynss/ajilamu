# T2.7 Remediation Round 1

**Verdict:** unchanged. The round 1 reviewer wrote REMEDIATE and I do not touch that.
**Artifact:** `internal/fit/loop.go` and `internal/fit/loop_test.go`.
**Remediator:** I did not implement T2.7 and I did not review it.

I closed all three findings from the round 1 review.
Every measurement below comes from compiler checks, test executions, or mutation runs.
I quote no number that any prior log printed about its own work.
I ran every mutation in a throwaway scratch copy, never in this checkout.

I edited four paths:
`internal/fit/loop.go`,
`internal/fit/loop_test.go`,
`dev-diary/PHASE-2-fit-loop.md` (lines 687 to 704),
and this remediation document `dev-diary/adversarial-review/t2.7-remediation-round1.md`.

## Findings

| # | Sev | What I changed | Measurement that closes it |
|---|-----|----------------|-----------------------------|
| 1 | M | Added `RecorderSetter` interface and proxy decorators `TranslatorProxy`, `SynthesizerProxy`, and `SegmenterProxy` to `internal/fit/loop.go`. Added `SetRecorder` methods to `mockPipelineTranslator`, `mockPipelineSynthesizer`, and `mockSegmenter` in `internal/fit/loop_test.go`. Updated `Run` in `internal/fit/loop.go` to bind `tracker` to clients via `RecorderSetter` and wrap `Segmenter` in `loopSegmenter`. Added `TestPipelineWithBareChargeRecorder` and `TestPipelineProxyDecorators`. | `TestPipelineWithBareChargeRecorder` passes with `res.TotalCost > 0` and populated charges. Mutating `trackingRecorder.Add` to panic causes `TestPipelineEndToEndWithFixtures` to fail with exit code 1 (`panic: trackingRecorder.Add panic`). Mutating `Run` to omit `SetRecorder` bindings fails `TestPipelineWithBareChargeRecorder` with `pipeline TotalCost = $0.00, want positive`. |
| 2 | M | Updated `TestPipelineEndToEndWithFixtures` in `internal/fit/loop_test.go` to assert exact charge counts and positive costs for each kind. Specifically asserted 11 translation charges and 11 synthesis charges totaling 22 charges. Asserted positive costs for both kinds. | Mutating `mockPipelineTranslator.Translate` to omit charges fails `TestPipelineEndToEndWithFixtures` with `translate charge count = 0, want 11`. Mutating `mockPipelineSynthesizer.Synthesize` to omit charges fails `TestPipelineEndToEndWithFixtures` with `synthesize charge count = 0, want 11`. The unmutated test passes cleanly in 1.44 s. |
| 3 | L | Removed unused `RateCard cost.RateCard` field from `PipelineConfig` in `internal/fit/loop.go`. Removed dead rate card defaulting logic from `NewPipeline`. Updated handoff documentation in `dev-diary/PHASE-2-fit-loop.md`. | Running `grep -rn "RateCard" internal/fit/` produces zero matches. `go vet ./internal/fit/` exits cleanly with code 0. All 27 tests in `internal/fit/` pass with exit code 0. |

## Independent Measurements and Pins

A pin is a measurement, never a log line.

**1. Bare recorder tracking pin.**
`bareTestRecorder` implements only `Add(cost.Charge)`.
It deliberately omits `TotalReporter` and `ChargesProvider` methods.
`TestPipelineWithBareChargeRecorder` runs the pipeline with `bareTestRecorder`.
The test verifies that `res.TotalCost` evaluates to a positive price.
It verifies that `res.Charges` holds the recorded charges.
Mutating `Run` to omit `SetRecorder` bindings causes `TestPipelineWithBareChargeRecorder` to fail with `pipeline TotalCost = $0.00, want positive`.

**2. Itemized charge count and cost pin.**
`TestPipelineEndToEndWithFixtures` now itemizes all charges recorded in `cost.Ledger`.
The test counts 11 translation charges and 11 synthesis charges across 8 segments.
Segment 1 uses 2 attempts.
Segment 8 uses 3 attempts.
Segments 2 through 7 use 1 attempt each.
Total attempts equal 11.
Both `translateCost` and `synthesizeCost` evaluate to positive prices.
Mutating either translator or synthesizer to skip charge logging causes `TestPipelineEndToEndWithFixtures` to fail immediately.

**3. Dead code and rate card removal pin.**
I inspected `internal/fit/loop.go` for occurrences of `RateCard`.
Zero occurrences remain in `internal/fit/loop.go`.
Unit pricing belongs to API clients at construction time.
Removing the unused field eliminates dead defaulting logic.

## Mutation Testing

I ran four targeted mutations in throwaway scratch tests.
All four mutations failed the test suite with exit code 1.

| Mutation | Caught | Failing Test | Output |
|---|---|---|---|
| `trackingRecorder.Add` panics | yes | `TestPipelineEndToEndWithFixtures` | `panic: trackingRecorder.Add panic` |
| Translation charges omitted (`if false && m.rec != nil`) | yes | `TestPipelineEndToEndWithFixtures` | `translate charge count = 0, want 11` |
| Synthesis charges omitted (`if false && s.rec != nil`) | yes | `TestPipelineEndToEndWithFixtures` | `synthesize charge count = 0, want 11` |
| Client `SetRecorder` bindings omitted in `Run` | yes | `TestPipelineWithBareChargeRecorder` | `pipeline TotalCost = $0.00, want positive` |

## Verification Suites

All five verification suites passed cleanly.

- `go test -count=1 ./internal/fit/`: passed in 14.13 s.
- `go test -count=1 -race ./internal/fit/`: passed in 15.20 s.
- `go vet ./internal/fit/`: clean, exits 0.
- `gofmt -l internal/fit/`: clean, prints nothing.
- `go test -count=1 ./...`: passed across all 11 packages.

## House Style Audit

I checked all modified files against the four house rules.

1. No semicolons in comments, prose, or markdown text.
2. No em dashes in comments, prose, or markdown text.
3. Every sentence runs 30 words or fewer.
4. Active voice throughout.

An automated style checker evaluated `internal/fit/loop.go`, `internal/fit/loop_test.go`, lines 687 to 704 of `dev-diary/PHASE-2-fit-loop.md`, and this document.
The checker reported zero violations.

## Residue

I closed all three findings. I know of no finding from round 1 left open.
