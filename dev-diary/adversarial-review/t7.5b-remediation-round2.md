# T7.5b remediation, round 2

Task: user-defined languages at upload. I remediated the two findings in
`dev-diary/adversarial-review/t7.5b-round2.md`. I did not change the verdict. The verdict stays
REMEDIATE. The task stays not done. I changed `internal/api/run.go`, `internal/api/run_test.go`,
`cmd/ajilamu/main.go`, `cmd/ajilamu/main_test.go`, the T7.5b handoff entry, and this record.

## Rows

| Finding | Fix | Pin | Residue |
|---|---|---|---|
| M1 · `internal/api/run.go` lines 597 to 604 | `runWorkDirFor` names the language directory from `resolveRunTag(language)`, which returns the catalog tag that names the same language. It also reuses a raw code directory that holds a record for the same language, so a resume of a pre-fix pass still reads its takes. | The probe interrupted a run with `ml`, then resumed with `ml-IN`. Both passes chose `work/ml-IN` and the resume rendered `[2 3]`. The full revert mutation chose `work/ml` then `work/ml-IN` and rendered `[1 2 3]`. `TestRunStartAnswersAccepted` and `TestRunStartReusesRawCodeWorkDir` pin the two choices. | A bare code the map does not carry, such as `hi`, names the catalog tag `hi-IN` for the directory while `resolveRunLanguage` falls back to `hi`. The create screen sends no such code. A provider refresh can add a code outside the committed list, which keeps its raw name. |
| M2 · `cmd/ajilamu/main.go` lines 413 to 422 | Added `"en": {tag: "en-US", name: "English"}` to `createScreenLanguages`. The doc comment now names both sample spellings. | The probe resolved the sample pair `ml` and `en` to `Malayalam` and `English`. It fed both into `gemini.translatePrompt`. The prompt reads `Translate this English dialogue line into natural spoken Malayalam script (മലയാളം):`. Removing the entry restored `en`. | A target run for the bare code `en` would name the tag `en-US` and the directory `en`, because the catalog holds four English tags. The create screen offers no such target. |
| M3 · the handoff entry, `dev-diary/PHASE-7-ingestion.md` lines 999 to 1002 | Replaced the false claim about the prompt. The entry now names the full map and the two sample codes. | The before and after text below. | None. |

## M1, one directory per language

The probe lived at `/var/tmp/t75b-r2/probe_test.go` and reached the package through
`go test -overlay`. It started the real `POST /api/dubs/{id}/run` route and ran
`fit.RunPipeline` behind it. It recorded the lines that produced a take.

```text
FIRST route=ml workdir=.../dub-fresh/work/ml-IN rendered=[1] err=repair line 2: synthesize line 2 attempt 1: probe interrupted mid run
RESUME route=ml-IN workdir=.../dub-fresh/work/ml-IN rendered=[2 3] err=<nil>
LEGACY record=.../dub-legacy/work/ml resume route=ml-IN workdir=.../dub-legacy/work/ml rendered=[2 3]
```

The first pass rendered line one and stopped on line two. The resume read the same directory and
rendered lines two and three. It did not render or charge line one again. The legacy case wrote
the record in `work/ml`, and the resume read it there.

I overlaid `run.go` with the directory named from the raw code and the raw code reuse disabled.
The same probe restored the split:

```text
FIRST route=ml workdir=.../dub-fresh/work/ml rendered=[1] err=repair line 2: synthesize line 2 attempt 1: probe interrupted mid run
RESUME route=ml-IN workdir=.../dub-fresh/work/ml-IN rendered=[1 2 3] err=<nil>
resumed rendered = [1 2 3], want [2 3]
--- FAIL: TestProbeT75bR2FreshResume
```

The identity mutation fails `TestRunStartAnswersAccepted` on the fresh name.

```text
run_test.go:84: recorded work dir = ".../dub-accept/work/ml", want ".../dub-accept/work/ml-IN"
--- FAIL: TestRunStartAnswersAccepted (0.00s)
```

## M2, the sample prompt names English

The probe printed the resolved sample names.

```text
T75B_TARGET_TAG=ml-IN
T75B_TARGET_NAME=Malayalam
T75B_SOURCE_NAME=English
```

It fed the two names into `gemini.translatePrompt`. The measured prompt:

```text

Translate this English dialogue line into natural spoken Malayalam script (മലയാളം):
Original English: "Hello from orbit"
Speaker emotion: Warm
Constraint: The translated line will be spoken in Malayalam in a video slot that lasts approximately 1.8 seconds (1800 ms). Keep the translation natural, spoken, and fit the rhythm.

Respond with strictly the translated Malayalam text. No markdown, no quotes, no explanation.
```

The opening names English and Malayalam. The opening slot keeps `Malayalam script (മലയാളം)`.
The closing slot names `Malayalam`. No code appears. Removing the `en` entry returned
`T75B_SOURCE_NAME=en` and the prompt read:

```text
Translate this en dialogue line into natural spoken Malayalam script (മലയാളം):
Original en: "Hello from orbit"
```

## M3, the corrected handoff sentence

Before:

> A new upload stores `ml-IN`, so the translation prompt names `ml-IN` rather than Malayalam.

After:

> A new upload stores `ml-IN`, and the prompt reads `Malayalam`.

I also corrected the measured pin that carried `SourceLanguageName:"en"` to
`SourceLanguageName:"English"`, because the fix changes that value.

## Command output

```text
$ go test -count=1 ./internal/api ./cmd/...
ok  	github.com/nrynss/ajilamu/internal/api	1.360s
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	3.953s
```

`gofmt -l` on the four Go files printed nothing.

## Residue against round 2

M1 is closed for the sample code and the catalog tag. M2 is closed for the sample pair. The
handoff entry is corrected. The two open notes sit in the table. Neither changes the verdict.
The probe copy lives at `/var/tmp/t75b-r2` and wrote nothing in the repository.
