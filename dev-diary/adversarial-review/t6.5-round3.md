# T6.5 ghost take history: round 3

**Verdict: REMEDIATE.** Counts: **C 0, H 0, M 2, L 3.**

The round two remediation fixes the line switch leak. I re-measured that case with a delayed
reply and pinned it by mutation. Both panels on the new line stay empty. The lane ghosts and
the Speaker note still hold from round one. Two new defects sit in the same async speaker path.
A busy flag follows the panel across a line switch. A stale reply also clears a new pending
choice on the new line. The open H stays with T6.5c.

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| `web/src/lib/edit/Speaker.svelte:186-188`, with the state at `:45` and the effect at `:65-73` | **M (busy).** `busy` is component state, and the component survives a line switch. The new line then inherits it. While line 3 re-renders, the line 1 confirm reads `Re-rendering…` and stays disabled, and Cancel stays disabled too. The panel names line 1 and its fee, so the button contradicts the panel. | I confirmed the line 3 speaker change, selected line 1 at 41 ms, and chose a line 1 speaker at about 300 ms. The line 1 pending panel read `confirmText: "Re-rendering…"`, `confirmDisabled: true` and `cancelDisabled: true`, with the reply still in flight. | Adding `busy = false` to the segment effect flipped the pin. The line 1 confirm then read `Change speaker` and enabled. |
| `web/src/lib/edit/Speaker.svelte:120-123` | **M (pending).** The stale reply guard clears `pendingSpeaker` before it returns. That line is dead on the same line, because confirm cleared the state before the await. Across a switch it dismisses a new pending choice the creator made on the new line. The estimate panel then vanishes with no sentence. | The fixed run read `pendingOpen: 1` on line 1 during the flight. The line 3 reply landed, and the same panel read `pendingOpen: 0`. Nothing else on line 1 changed. | Removing the `pendingSpeaker = undefined` line kept the panel open. The reply landed, and the line 1 panel still read `pendingOpen: 1` and `Change speaker`. |
| `dev-diary/PHASE-6-editing.md:672-675` | **L (handoff).** The seam paragraph is stale. It says `Track.svelte` renders only `takes.at(-1)`, that the page cannot inject ghost shapes into the lane, and that moving the strip into Track needs an owns change. This patch does all three the other way. The next agent codes against the paragraph. | I read the paragraph against `Track.svelte:64-91` and `:214-223`. The lane renders every older take as a dashed ghost, and the owns line now lists `Track.svelte`. | Reverting `Track.svelte` to `takes.at(-1)` makes the paragraph true and reopens round one M1. |
| `dev-diary/PHASE-6-editing.md:164-166` | **L (heading).** The new T6.5c block has no blank line between its Done when sentence and the `---` rule. Markdown reads that sentence as a setext H2 heading and drops the rule. | `marked` rendered lines 148 to 166 and returned the Done when sentence as `<h2>`. The same render of T6.5 and T6.5b returned no `<h2>`. | Adding the blank line renders the sentence as `<p>` and restores the rule. |
| `dev-diary/PHASE-6-editing.md:148-166` | **L (board).** The new block makes P6 nine tasks. The README progress row at `dev-diary/README.md:196` still says 7 / 8, so the project audit exits 1. | `python3 tools/audit_docs.py` printed `[readme] P6 table says 7/8, phase file has 7/9` and exited 1. | I set the row to 7 / 9 in a mirror of the tree. The audit then printed `No drift. Docs and repository agree.` and exited 0. |

## What I measured

I copied the working tree to `/tmp/t65r3/repo` and symlinked `node_modules` and `testdata`. I
built with `npm --prefix web run build`. A throwaway Node server served the built bundle, the
frozen `testdata/wire/dub.json` under the non-fixture id `t65-r3-dub`, and a rerender reply
delayed 1500 ms. I drove the page with headless Chromium. The working tree was never edited.

### 1. The line switch with a delayed reply

The run confirmed the line 3 speaker change and selected line 1 at 41 ms. The reply landed at
1500 ms. The read happened at 2446 ms.

```json
{
  "selectedLine": "1",
  "speakerEyebrow": "Line 1",
  "speakerHeader": "Suni Williams",
  "speakerNote": null,
  "speakerError": null,
  "stripNote": null,
  "chips": ["seg_1_try1.wav"]
}
```

Both panels on the new line held no note. Neither carried the line 3 sentence. The reply
landed and applied. A later selection of line 3 read the new header and three chips.

```json
{
  "speakerHeader": "Mark Vande Hei",
  "chips": ["seg_3_try1.wav", "seg_3_stretched.wav",
            "/tmp/t65r3-storage/t65-r3-dub/work/ml-IN/seg_3_try3.wav"],
  "stripNote": null
}
```

The server recorded one POST with the body `{"language":"ml","speaker":"Mark Vande Hei"}`.

### 2. The mutation fails the pin

I copied the tree to `/tmp/t65r3/mut-guard`. I removed the `segment.id !== targetSegment.id`
block and the `selectedSegmentId === change.segment.id` block. I rebuilt and drove the same
sequence. The old line 3 sentence returned on line 1.

```json
{
  "selectedLine": "1",
  "speakerEyebrow": "Line 1",
  "speakerHeader": "Suni Williams",
  "speakerNote": "Line 3 re-rendered as take seg_3_try3.wav.",
  "stripNote": "Line 3 re-rendered as take seg_3_try3.wav.",
  "chips": ["seg_1_try1.wav"]
}
```

The line 1 panel and strip both report a line 3 take while the header reads Suni Williams and
the only chip reads `seg_1_try1.wav`. The pin reddens under the mutation.

### 3. The lane ghosts (round one M1)

Line 3 held three shapes after the re-render. The `Dub ML` lane ran top 1084 to bottom 1148, a
height of 64 pixels. All three shapes sat inside it.

```json
{
  "lane": { "top": 1084, "bottom": 1148, "height": 64 },
  "laneContainsShapeCount": 3,
  "shapes": [
    { "file": "seg_3_try1.wav", "state": "ghost", "top": 1094, "bottom": 1138,
      "height": 44, "borderStyle": "dashed", "opacity": "0.72", "insideLane": true },
    { "file": "seg_3_stretched.wav", "state": "ghost", "top": 1097, "bottom": 1135,
      "height": 38, "borderStyle": "dashed", "opacity": "0.72", "insideLane": true },
    { "file": "/tmp/t65r3-storage/t65-r3-dub/work/ml-IN/seg_3_try3.wav", "state": "active",
      "top": 1100, "bottom": 1132, "height": 32, "borderStyle": "solid", "opacity": "1",
      "insideLane": true }
  ]
}
```

The older ghost carries the wider outline, so the depth order reads oldest to newest.

### 4. The Speaker note after a re-render (round one M2)

The run stayed on line 3, confirmed the change and waited for the 201. Both panels named the
same take.

```json
{
  "speakerNote": "Line 3 re-rendered as take seg_3_try3.wav.",
  "stripNote": "Line 3 re-rendered as take seg_3_try3.wav.",
  "speakerError": null,
  "pendingOpen": 0
}
```

### 5. The busy flag across a line switch (finding M busy)

The same run measured the new defect. The line 1 pending panel inherited the line 3 busy flag at
about 300 ms.

```json
{
  "speakerEyebrow": "Line 1",
  "pendingText": "Re-rendering line 1 as Mark Vande Hei is estimated at $0.0008421. Estimated from the take seg_1_try1.wav. Cancel Re-rendering…",
  "confirmText": "Re-rendering…",
  "confirmDisabled": true,
  "cancelDisabled": true
}
```

I copied the tree to `/tmp/t65r3/mut-busy` and added `busy = false` to the segment effect. The
same sequence then read `confirmText: "Change speaker"`, `confirmDisabled: false` and
`cancelDisabled: false`.

### 6. The dismissed pending choice (finding M pending)

The same run chose a line 1 speaker during the flight. The panel opened with the estimate. The
line 3 reply then closed it.

```json
{
  "duringFlight": { "pendingOpen": 1, "confirmText": "Re-rendering…" },
  "afterReply": { "pendingOpen": 0, "confirmText": null, "stripNote": null }
}
```

I copied the tree to `/tmp/t65r3/mut-pending` and dropped the `pendingSpeaker = undefined`
line. The same sequence then read `pendingOpen: 1` and `confirmText: "Change speaker"` after
the reply.

### 7. The type check and the console

```text
$ npm --prefix web run check
1788911737064 START "/home/nryn/work/ajilamu/web"
1788911737065 COMPLETED 183 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
```

The success run raised 0 console messages and 0 page errors. A ghost chip click fetched
`seg_3_try1.xBIiyNfY.wav` with status 200 and type `audio/wav`. An active chip click fetched
`seg_3_stretched.ClDJvJby.wav` with the same status and type. Each click fetched one take.

A failed reply does raise a console error. I set the stand-in status to 500 and repeated the
switch. The browser logged one message.

```text
Failed to load resource: the server responded with a status of 500 (Internal Server Error)
```

That message belongs to the browser and not to the page. The page raised 0 page errors. My
server also ignored range requests on `/clip.mp4`, so Chromium aborted 30 partial loads. That
is a harness artifact.

### 8. The open H

The re-render reply carries an absolute take path. A click on the new active chip showed the
missing URL note and fetched 0 audio.

```text
The take /tmp/t65r3-storage/t65-r3-dub/work/ml-IN/seg_3_try3.wav has no browser audio URL.
```

The two fixture takes still play. T6.5c owns the route that serves a take file.

## Residue against round one

**M1 residue: none.** Three line 3 shapes sit inside the lane. They read ghost, ghost, active,
and each one reports `insideLane: true`.

**M2 residue: none.** Both panels name the new take after the 201. The Speaker note and the
strip note read the same sentence.

**H1 residue: open.** T6.5c owns the take audio route. The new active take stays unplayable
until that route lands.

## Residue against round two

**M1 residue: none.** The line switch with a delayed reply leaves both panels on the new line
empty. The mutation restores the old sentence and the pin reddens. The two new M findings sit
beside that fix, in the same component. They do not reopen the round two M.

## Done condition

The timeline shows ghost take history in the lanes. Both fixture takes play independently. The
estimate shows before every re-render call. The line switch no longer leaks the old note. The
play clause holds for the fixture and not for a real project, and T6.5c owns that half. The two
new M findings block approval.

## Notes, not findings

1. The chip carries `aria-pressed` on a play button. Round one and round two declined it, and I
   do not file it again.
2. A 500 reply that lands after a line switch leaves the optimistic speaker in place and shows
   no error on the returned line. The optimistic update predates this patch, and round two
   declined the same path, so I do not file it.
3. The run button starts a billable run and shows no estimate before the call. That gap
   predates this patch and belongs to the run task.
