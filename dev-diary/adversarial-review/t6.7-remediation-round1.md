# T6.7 remediation, round 1

**Scope:** the one H finding in `t6.7-round1.md`. I fixed it and nothing else.
**Verdict:** unchanged. The review file stays REMEDIATE.
**Method:** I measured a server built from the working tree against ClickHouse 26.8.2.7 and
`sql/schema.sql`. I removed the lock again to take a control measurement.

## Finding

| Finding | Fix | Pin | Residue |
|---|---|---|---|
| H1: `editRecorder.RecordEdit` read the dub head, then appended a child whose parent and version_seq came from that read. Two edits in flight forked the DAG. The head timeline dropped the losing branch, so an edit answered 201 and vanished. | `EditsHandler` holds a per-dub lock from `editLocks` across the head read, the derivation, the recorder call, and the response. The adapter keeps its head read and its append inside that call, so the parent and version_seq come from a head no other edit can move. `runRegistry.start` already serializes one run per dub, so the pattern is the same. | `TestEditsSerializePerDub` in `internal/api/mutations_test.go`. Twelve concurrent posts to one dub must chain, not fork. The test fails when I remove the lock from `EditsHandler`. | None against H1. The run and re-render commit path in `runRecorder.persist` reads the head the same way and takes no edit lock. That path is outside H1. |

## Measured output

### 1. Twelve concurrent boundary POSTs, one dub

The requests posted `{"kind":"boundary","language":"ml","segment_id":1,"start_ms":0,"end_ms":2950}`
at once to `t67-rem-boundary`.

- Statuses: 12 of 12 returned 201, with 12 distinct commit ids.
- Commits in `commits_raw FINAL`: 13.
- version_seq distribution: 1 through 13, one commit each.
- Head ancestry: 13 commits, the root plus all 12 edits.
- Head timeline at the head commit: line 1 `0..2950`, line 2 `3500..8000`.

### 2. Twelve concurrent commands, one dub

The requests posted `shift line 2 right by 250ms` at once to `t67-rem-command`.

- Statuses: 12 of 12 returned 201, with 12 distinct commit ids.
- Commits in `commits_raw FINAL`: 13.
- version_seq distribution: 1 through 13, one commit each.
- Head ancestry: 13 commits, the root plus all 12 edits.
- Head timeline at the head commit: line 1 `0..3000`, line 2 `6500..11000`. All twelve
  250ms shifts landed.

### 3. The pin and its mutation

The pin passes on the fix.

```
$ go test -count=1 -run TestEditsSerializePerDub -v ./internal/api
--- PASS: TestEditsSerializePerDub (1.21s)
    --- PASS: TestEditsSerializePerDub/boundary (0.61s)
    --- PASS: TestEditsSerializePerDub/command (0.61s)
ok  	github.com/nrynss/ajilamu/internal/api	1.221s
```

I removed the three lock lines from `EditsHandler`. The pin failed on both subtests.

```
--- FAIL: TestEditsSerializePerDub/boundary
    peak concurrent head reads = 12, want 1
    version 2 appears 12 times, want once
    commit 3 parent = "c1", want "97bac28e4d7369a2f2c0d541e4a63d64"
    head ancestry = 2 commits, want 13
```

The control server carried the same mutation. Twelve concurrent boundary POSTs all returned
201. `commits_raw FINAL` held 13 commits, `version_seq` 2 counted twelve times, and the head
ancestry was 2. The twelve requests kept 1 edit and lost 11. This is the H1 failure mode.

### 4. Tests

```
$ go test -count=1 ./internal/api ./cmd/...
ok  	github.com/nrynss/ajilamu/internal/api	3.183s
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	4.319s
```
