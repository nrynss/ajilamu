# T6.6a review, round 1

**Verdict: APPROVE. Findings: 0 C, 0 H, 0 M, 0 L.**

Reviewed the working diff against `99a7d36`, including untracked task files, on 2026-09-09.
The spend-cap handoff supplied context, not evidence. This verdict judges the actual files.

## Scope

I read AGENTS.md in full, the task block, diary README, accepted `t6.6a-contract.md`, and T6.6 review and remediation rounds.
I also read the P6 close round 3 and the relevant product specification.
Memory recall preceded code inspection. A second recall used the implementation's specific names.

The reviewed paths are `internal/agent/route_test.go`, `internal/api/agent.go`, `internal/api/agent_test.go`, and the task-owned server and wire files.
They also include `cmd/ajilamu/main.go`, `cmd/ajilamu/main_test.go`, `web/src/lib/types.ts`, and both new agent wire examples.
The accepted contract owns the external-package integration test. ADK imports stay under `internal/agent`.
I excluded concurrent task changes and shared documentation from the verdict. The root handles shared documentation and the final handoff.

I own every repository path I edited: this review file only.
The required shapes already exist. I can validate this task independently.
I made no fixes, commits, status edits, or agent dispatches.

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| None | No C, H, M, or L finding. | Independent measurements below passed. | Route removal and candidate-price omission each fail the independent probe. |

## Independent HTTP and accounting measurements

All probes ran in `/var/folders/q1/4dwfdvt563ng8lwybj8bry_c0000gn/T/t66a-review-04s849a1`.
The copy includes the untracked implementation and examples. Production files in the repository stayed untouched.

`internal/agent/review_probe_test.go` constructs the real agent with an in-memory MCP server and a reviewer-controlled model.
It sends HTTP requests through a listening API server. The first model call invokes `run_query` with `SELECT 3 AS line`.
The second model call supplies the answer. The observer counted one MCP read and two model calls.

| Observation | Measured result |
|---|---|
| First response | HTTP 200, answer `Line 3 costs the most.` |
| First model usage | 17 prompt tokens, 5 candidate tokens |
| Second model usage | 34 prompt tokens, 10 candidate tokens |
| Returned prices | 150 prompt and 600 candidate nanodollars per token |
| Independent sum | `(17 + 34) * 150 + (5 + 10) * 600 = 16650` |
| Returned charges | Two native charges, numeric agent kind 3, total 16650 |
| Next turn in the same dub | One charge, 51 prompt tokens and 15 candidate tokens, total 16650 |
| Writer observations across both turns | Zero edit-recorder, run-recorder, flush, or pending calls |

The next turn's one charge proves that the response does not accumulate prior-turn charges.
The route receives no ledger writer. The probe installs writer spies on the surrounding server to detect accidental dispatch.
The existing read-tool filter remains unchanged. This offline measurement does not re-certify a deployed ClickHouse grant.

Two temporary mutations demonstrated the observer's sensitivity.
Removing the route registration failed response decoding on the 404 body.
Omitting candidate pricing returned 7650 instead of 16650 and failed the independent arithmetic assertion.
I restored both files and reran the real-agent route probes successfully.

## Entrypoint measurements

I built `review-server` from the copied working tree and drove it with `startup_probe.py`.
Each process used an isolated data directory and a static workspace marker.
The configured case used synthetic ADC data and an unreachable loopback MCP endpoint. No live model call ran.

| Process configuration | Agent POST | Workspace GET | Health GET |
|---|---|---|---|
| No MCP settings | 503 | 200 | 200 |
| MCP URL only | 503 | 200 | 200 |
| MCP URL and token, missing cloud project | 503 | 200 | 200 |
| MCP URL and token, synthetic ADC and cloud project, unreachable MCP | 502 | 200 | 200 |

Every 503 carried exactly `{"error":"The editor agent is unavailable."}` followed by a newline.
The unreachable MCP response carried `The editor agent could not answer this question.`
The workspace body matched the independent static marker. I stopped all four processes.
The startup construction test also covers absent, partial, configured, and failed-builder cases.

## Wire and regression checks

Go 1.27.1 ran `go test -count=1 ./internal/agent ./internal/api ./cmd/ajilamu` successfully.
The API suite includes strict example decoding, registered-struct parity, charge-field mirroring, malformed requests, and unavailable-agent behavior.
The committed offline route test independently expects two charges totaling 768000 from 1200 prompt and 340 candidate tokens per call.

I compared the native Go charge fields against all eight TypeScript fields and both JSON examples.
Request and response names, numeric kinds, required fields, arrays, and nanodollar types agree.
The single-charge example reconciles to `1200 * 150 + 340 * 600 = 384000`.

`npm --prefix web run check` passed with zero errors and zero warnings using Node 26.8.1 from `/opt/homebrew/opt/node/bin`.
The copied entrypoint built successfully. The scoped diff whitespace check passed.

## Residue

T6.6a has no earlier verdict round. Its accepted ownership contract has zero residue.
T6.6 round 1 H1 and L1, and round 2 M1, have zero newly introduced residue in this diff.
Their approved grant-probe and parser code remains unchanged. The agent suite passes.
The prior deployment-transcript limitation remains an environmental limit, not a new route defect.

The P6 close's missing-route observation is resolved by the measured HTTP route and returned price.
Command-bar rendering belongs to T6.6b. Shared-document completion remains with the root under this dispatch.
No task-scoped finding or unresolved residue remains across C, H, M, or L.
