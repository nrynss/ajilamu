# T7.2 Round 3 Review

## Verdict

REMEDIATE

| Severity | Count |
|---|---:|
| C | 0 |
| H | 0 |
| M | 2 |
| L | 1 |

Every round 1 and round 2 finding measured closed. All three findings below are new this round.

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| M1 · `web/src/routes/+layout.svelte` lines 46 to 68 | The chrome hardcodes `Ready for review` and `status-ok` for every fixture id. It skips its own `readinessLabel` for that branch and never fetches the index. The fixture readiness is `review`, not `ready`. The same file maps `review` to `In review` and `status-warn`, and T7.4's index page maps it identically. So one project carries two different statuses and two different dot colours on two screens. The workspace body under the green dot flags a line as unfinished. Round 2 M1 removed the hardcode for pending ids and left it standing for fixture ids. | Headless Chrome over CDP on a freshly built bundle. `/d/d3bca364-9c8a-4107-8df9-c4faf909b008` rendered chrome text `Ready for review` with class `project-status status-ok`, and a body reading `LINES NEEDING YOU Line 8 is still 2.91 seconds too short after 1 try, this one needs you.` The index row for the same id rendered `In review` with class `status-warn`. `GET /api/dubs` returned `"readiness":"review"` for that id, and `testdata/manifest.json` plus `testdata/wire/dub.json` both record `"readiness": "review"`. | Revert an index-derived or payload-derived fixture status and the demo's flagship route shows a green Ready badge over a workspace that says a line still needs work. The index row and the chrome keep contradicting each other about one project. |
| M2 · `web/src/routes/d/[id]/+page.svelte` lines 230 to 246 with `web/src/routes/+layout.svelte` lines 104 to 124 | Both files treat a failed or non-ok `GET /api/dubs` as proof the project does not exist. The workspace `.catch` and the `res.ok ? … : null` branch land on the same error state as a genuinely unknown id. A creator whose index request fails sees a real project declared missing. The remediation added this branch for round 2 L1, so the regression is new this round. | Headless Chrome with `Network.setBlockedURLs` on `*/api/dubs`. `/d/48dd158ddb0323b92c9da48064990603` rendered `data-state="error"` with `We could not find this project. Check the URL or return to the project index.` and chrome `Unknown project`. With the block lifted the same id rendered `data-state="empty"` and chrome `Queued to start`. The project exists. `GET /api/dubs` lists that id, and `/tmp/t72r3/data/uploads/48dd158ddb0323b92c9da48064990603` holds its `source.mp4`. | Revert a split between a failed lookup and a missing project, and one dropped index request tells a creator their saved work is gone. Both the body sentence and the chrome badge repeat the false claim. |
| L1 · `web/src/routes/d/[id]/+page.svelte` lines 218 to 222 and lines 37 to 39 | The empty state calls every real pending project a sample. A creator who uploads their own video through T7.1 lands on `/d/{id}` and reads `This sample project is waiting for dubbing.` The route cannot tell a sample from an upload because it only asks whether the index knows the id. | Headless Chrome. I posted a 200,000 byte file named `tiny.mp4` to `POST /api/dubs/new`, which returned 201 with id `c6544e4ee827f1aca9e8b61702295bbb`. `/d/c6544e4ee827f1aca9e8b61702295bbb` rendered the sentence `This sample project is waiting for dubbing. Its video is saved, but no lines, takes, or costs exist yet.` A sample id rendered the identical sentence. | Revert neutral pending copy and every creator's own upload is described back to them as the NASA sample. The one screen that should confirm their file arrived says the opposite. |

## Independent Evidence

I ran the built binary from the checkout on port 9743 and from `/tmp/t72r3/fail/site` on ports 9751
through 9759. Nothing bound 8080. I rebuilt `web/build` from a clean `rm -rf build` before every
browser probe, so no measurement read a stale bundle.

| Check | Observation |
|---|---|
| Advertised fixture id | `/d/d3bca364-9c8a-4107-8df9-c4faf909b008` rendered `data-state="populated"`, 8 `button.line-select` controls, video source `/clip.mp4`, and document title `NASA 75-second clip · Ajilamu`. `/d/fixture` and `/d/6d9c2f1a-3b4e-4a8d-9c1e-7f2b5a3d8c40` rendered the same populated workspace. Round 2 H1 measured the first of those three as the empty pending state. |
| Fresh sample chrome | A real click on `button.sample` landed on `/d/7be64639e898b270290b9b41b63051b3`. The chrome read `Queued to start` with class `status-dim` and `$0.00 spent`. The body read `data-state="empty"` with the pending sentence. `document.body.innerText` contained neither `Ready for review` nor `$0.02`. A separate direct navigation to `/d/48dd158ddb0323b92c9da48064990603` measured the same four values. |
| Garbage id | `/d/not-a-real-project` rendered `data-state="error"` with `We could not find this project. Check the URL or return to the project index.` and chrome `Unknown project`. No sentence claimed a saved video. |
| One-click control | The built `/new` carried `button.sample` labelled exactly `Try sample video (NASA 75s clip)`. `document.querySelectorAll('a[href*="fixture"]')` returned 0. A recursive text sweep of `web/build` found no `d/fixture` reference. |
| Live 201 contract | `POST /api/dubs/sample` on port 9743 returned `201 Created` with `Content-Type: application/json`. The body carried a 32-character hex id, `"language":"ml"`, `"name":"NASA 75-second clip"`, path `<id>/source.mp4`, and `"bytes":18385109`. The path holds no parent traversal. |
| Byte identity | `shasum -a 256` returned `da7e0212fdf5654a81ec2d00ec1422bff636d9d42eb201c62d0bbe7ce083be9e` for `testdata/clip.mp4` and for three separately stored `source.mp4` files, including one from the deployed-layout probe. Each measured 18,385,109 bytes. |
| Permissions | `stat -f %Sp` reported `-rw-------` on the stored `source.mp4` and `project.json`, `drwx------` on the project directory, and `drwxr-x---` on the storage root. |
| Deployed-layout discovery | The binary ran from `/tmp/t72r3/fail/site`, a copy of `testdata/` outside the checkout, with no override set. The sample POST returned 201 and stored the identical bytes. |
| Missing clip, unset override | The binary ran from `/tmp/t72r3/fail/noclip`, whose `testdata/` lacks the clip. The POST returned 500 with `We could not find the sample video. Upload a video instead.` The log recorded `ERROR The sample video could not be opened. error="clip not found in searched locations [clip.mp4 testdata/clip.mp4 ../clip.mp4 ../testdata/clip.mp4 ../../clip.mp4 ../../testdata/clip.mp4] (AJILAMU_SAMPLE_CLIP is unset): file does not exist"`. |
| Missing clip, bad override | `AJILAMU_SAMPLE_CLIP` pointed at a nonexistent path. The POST returned 500 and logged `error="open override AJILAMU_SAMPLE_CLIP=/tmp/t72r3/fail/does-not-exist.mp4: open …: no such file or directory"`. |
| Storage directory failure | `uploads` existed as a regular file. The POST returned 500 with `We could not prepare storage for the sample. Try again.` and logged `ERROR Could not prepare sample storage directory. dir=… error="mkdir …: not a directory"`. |
| Staging directory failure | `uploads` existed with mode 0500. The POST returned 500 with `We could not prepare the sample workspace. Try again.` and logged `ERROR Could not create sample staging directory. dir=… error="mkdir …/.sample-…: permission denied"`. |
| Copy failure | I mounted a 4 MiB HFS RAM disk and pointed `AJILAMU_DATA_DIR` at it. The 17.5 MiB copy failed. The POST returned 500 with `We could not save the sample video. Try again.` and logged `ERROR Could not save sample video. path=… error="copy sample: write …: no space left on device"`. The volume held zero leftover staging directories afterwards. |
| Unforced failure paths | Three branches resisted live forcing. `uploadID`, `writeUploadRecord`, and the final `os.Rename` each carry a `slog.Error` with named fields at `sample.go` lines 56, 90, and 96. Two RAM disks sized to 18,032 KiB and 18,152 KiB both let the whole sample succeed rather than failing the 123 byte record write. I record these three as source-read only. |
| Concurrency and cleanup | Six concurrent sample POSTs returned six distinct ids and six complete project directories. The storage root held no `.sample-` entry afterwards. Server RSS measured 14,256 KiB after nine samples of 18 MB each. |
| Index honesty | `GET /api/dubs` listed 10 projects. The nine sample and upload rows read `"readiness":"pending"` with `"total_nanodollars":0`. The fixture row read `"readiness":"review"` with `23414000`. The index page rendered 10 rows, newest first, the pending ones as `Queued to start $0.00`. |
| Fee before click | The `/new` fee panel read `PROJECTED FEE Choose a video to estimate its fee. The NASA sample has a measured fee of $0.023414.` before any interaction. The caption under the button read `Measured fee: $0.023414`. |
| Busy state | With the sample fetch stalled, the button read `We are copying the NASA sample into your workspace.` at computed opacity `1`, `aria-busy="true"`, and disabled. The submit button disabled with it. Measured colours give 5.87:1 in light and 6.40:1 in dark, both above the WCAG 2.1 AA threshold for normal text. |
| Byte-identity mutation | `AJILAMU_SAMPLE_CLIP=$PWD/go.mod go test -run TestSampleHandlerCreatesListedProject ./internal/api` failed with `sample_test.go:57: stored sample differs from committed clip`. |
| Server mount mutation | A `go test -overlay` run that deleted the `POST /api/dubs/sample` mount from `server.go` failed two tests. `TestServerMountsSampleHandlerOnPersistentStorage` reported `sample status = 404, want 201`, and `TestServerRejectsUnmatchedAPIRoutesBeforeSPA` reported the same status at line 334. |
| Entrypoint mount mutation | A `go build -overlay` binary that dropped `Sample: api.NewSampleHandler(uploadDir)` from `main.go` answered `404 page not found` to the live sample POST. `TestEntrypointExitsCleanlyWithoutLedger` asserts 201 on that request at `server_test.go` line 432. |
| Frontend mutations | I copied `web/` and `testdata/` to `/tmp/t72r3/webmut`, reverted three round 2 fixes there, and built and served that bundle on port 9762. Dropping the two uuid entries from `FIXTURE_IDS` returned `/d/d3bca364-…` to `data-state="empty"`. Rehardcoding the chrome returned `Ready for review` and `$0.02 spent` to the pending sample id. Restoring the sample sentence in the unknown-id branch made `/d/not-a-real-project` claim a saved video again. All three round 2 fixes are load-bearing. |
| Suite state | `go test ./...` failed only `internal/media`, for missing `scratch/takes/` and `assets/source/` fixtures. Both prior rounds recorded that redness, which predates this task. `go vet ./internal/api ./cmd/ajilamu` reported nothing. `npm run check` found 0 errors and 0 warnings. `npm run build` succeeded from a deleted `build` directory. |
| Svelte 5 hygiene | A sweep of `+layout.svelte`, `d/[id]/+page.svelte`, `new/+page.svelte`, and `fixture.ts` found no `export let`, no `$:` label, and no store import. All four use `$state`, `$derived`, `$props`, and `$effect`. |
| Vocabulary | A word-boundary sweep of `sample.go` and the three owned Svelte files found no term from the `ui-ux.md` section 8 Never Use column. Every 500 body is a complete sentence with an action. The 405 body stays the label `method must be POST`, which the mounted mux never reaches. Round 1 accepted that contract. |
| Duplicate index fetch | The workspace route and the chrome each fetch `GET /api/dubs` on the same navigation. `AGENTS.md` forbids a store as the reactivity mechanism, so the duplication looks forced rather than careless. I record it and file no finding. |

## Residue Against Round 1

| Finding | Disposition |
|---|---|
| H1 | Cleared. The binary discovered the clip from a deployed layout outside the checkout, honoured `AJILAMU_SAMPLE_CLIP`, and logged the searched candidate list when both failed. |
| H2 | Cleared. Both halves that round 2 left open are now measured green. The advertised fixture id renders the populated workspace, and the chrome over a fresh sample reads `Queued to start` with `$0.00 spent`. The fixture branch of that chrome carries this round's M1, which is a different claim about a different project. |
| M1 | Cleared. The identity test opens `../../testdata/clip.mp4` directly. Pointing the override at `go.mod` fails it with the expected message. |
| M2 | Cleared. Both mount mutations fail, one through `NewServer` and one through the compiled entrypoint. |
| M3 | Cleared. Six live 500 responses across four failure paths all returned creator sentences with an action. No internal label reached a response body. |
| L1 | Cleared. The deferred cleanup now closes over `staging` and reads its current value. The copy-failure probe on the RAM disk left zero staging directories, and six concurrent successes left zero as well. |
| L2 | Cleared. The 201 body, the index JSON, and the index page all title the project `NASA 75-second clip`. |
| L3 | Cleared. `$0.023414` appears in the fee panel and under the button before any click. |
| L4 | Cleared. The busy label names the copy step in a full sentence at computed opacity 1, measuring 5.87:1 and 6.40:1. |

## Residue Against Round 2

| Finding | Disposition |
|---|---|
| H1 | Cleared. All three fixture ids render `data-state="populated"` with 8 line controls and video `/clip.mp4`. Removing the two uuid entries from `FIXTURE_IDS` in a scratch build returns the manifest id to the empty state, so the fix carries the behaviour. |
| M1 | Cleared for the claim filed. A fresh sample id minted by a real button click shows `Queued to start` and `$0.00 spent`, and a garbage id shows `Unknown project`. The fixture branch still hardcodes a status the fixture data contradicts, which this round files as M1. That is a new claim about the fixture project rather than residue about a pending one. |
| L1 | Cleared. `/d/not-a-real-project` renders the error state and claims no saved video. The fix conflates a failed lookup with a missing project, which this round files as M2. |
| L2 | Cleared. Every failure path after the clip opens now logs. I forced four of them live and read the remaining three in source. The clip-open log names the six searched candidates and the `AJILAMU_SAMPLE_CLIP` variable. |

## Scope and Residue

I created only this review file in the repository. I changed no implementation file, test, fixture,
task status, or commit. `git status` shows the same eight modified files and six untracked files it
showed before I started, plus this review.

Every mutation ran outside the checkout. The two Go mutations used `go test -overlay` and
`go build -overlay` against copies under `/tmp/t72r3/gomut`. The three frontend mutations ran in a
`rsync` copy of `web/` and `testdata/` under `/tmp/t72r3/webmut`, built there and served from there.

I regenerated `web/build` with `npm run build` after deleting it, so the browser probes measured
current source. That directory is gitignored build output. Every probe server, the headless Chrome
instance, both Chrome profiles, the RAM disks, and all scratch directories lived under `/tmp/t72r3`.
All servers and Chrome are stopped, both RAM disks are detached, and the scratch tree is removed.
No listener remains on ports 9743, 9744, 9751 through 9762. Nothing bound 8080.

## Required Ownership Changes

None. All three findings live in files T7.2 already owns.

M1 needs the chrome to derive the fixture status rather than assert it. `+layout.svelte` already
imports `$lib/fixture`, so it can read `readiness` and `total.total_nanodollars` from the payload.
The simpler route drops the fixture special case and lets the existing `/api/dubs` fetch answer for
the manifest id, which the server already advertises.

M2 needs a third outcome beside found and missing. The route and the chrome should keep a distinct
state for a lookup that did not answer, and say so.

L1 needs pending copy that does not name the sample. The index summary carries the project title,
so the sentence can use it instead of guessing at origin.

The remediation must re-review all three findings.
