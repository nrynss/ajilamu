# T4.1 adversarial review, round 2

Verdict: REMEDIATE

Counts: C0, H1, M0, L0.

Prior-round residue: Zero. The round-one reconnect defect is fixed. A writer process
retained one nonempty journal file after a forced dropped response. After that process
exited, a new client drained the file after the endpoint returned `200`, without an
enqueue or explicit flush.

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| H | `internal/ledger/client.go:102-115` | `New` starts an unbounded reconciler goroutine with no cancellation, join, or `Client.Close` method. Each constructed client keeps issuing `context.Background()` flushes forever. The worker cannot stop during service shutdown or when a client is discarded. | `rg -n 'func \(c \*Client\) (Close\|Shutdown)\|startReconciler\|go func\|time\.Sleep' internal/ledger` finds the launch and perpetual sleep, but no stop path. `go test -count=1 -race -run '^TestRound2DropRecoverRestartArtifacts$' -v ./internal/ledger` passed in an isolated copy. It confirms recovery and restart, but it cannot retire the second client's worker. | Reverting a cancellation and close path leaves a goroutine and recurring network work alive for every discarded client. A shutdown can continue writing after its caller believes the ledger stopped. |

Independent checks:

- The isolated artifact probe used a child writer process to force a server-side disconnect.
  It verified one nonempty `*.json` journal file after that process exited. A fresh client
  then observed zero pending entries, zero journal files, and at least two requests after
  the server recovered.
- `go test -count=1 ./internal/ledger`, `go vet ./internal/ledger`, `gofmt -d
  internal/ledger/client.go internal/ledger/queue.go`, and `git diff --check` returned no
  findings in ledger scope.
- `Queue.Flush` serializes foreground and retry flushes for one `Queue`. The missing worker
  lifecycle remains a real defect despite that serialization.
