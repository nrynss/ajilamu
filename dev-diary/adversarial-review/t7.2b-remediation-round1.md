# T7.2b Remediation, Round 1

Remediation for the round 1 review and the orchestrator refinement. I changed no implementation
file before this round, and I wrote no review. I changed four paths only, the three Go files this
task owns plus this record.

|Item|Sev|Where|Resolution|Pin|
|---|---|---|---|---|
|F1|M|`internal/api/server.go`, `probeClickHouse`|The probe decides on the status line and closes the body unread. The `io.Copy` drain is gone, and the unused `io` import is gone.|`TestLedgerReadyAnswersStalledPingBodyWithoutWaiting` in `ready_internal_test.go`. A 200 status line followed by a stalled body answers 200 ready in under 500ms against a 2 second budget.|
|F2|L|`internal/api/server.go`, `probeDialTimeout` and the `net.Dialer` in `probeClickHouse`|The dial bound now has a pin. The dialer keeps `Timeout: probeDialTimeout`, so the pin measures the shipped bound.|`TestLedgerReadyHonorsDialTimeout` in `ready_internal_test.go`. A 1ns dial against a live `/ping` stand-in answers 503 unreachable within 1 second against a 5 second budget.|
|F3|L|`internal/api/server.go`, the `CheckRedirect` field in `probeClickHouse`|The redirect refusal now has a pin. The probe keeps `http.ErrUseLastResponse`, so a 3xx answer cannot become a 2xx answer.|`TestLedgerReadyReportsUnreachableOnRedirect` in `server_test.go`. A 302 `/ping` that points at a 200 path answers 503 unreachable.|
|F4|Refinement|`internal/api/server.go`, the `client.Do` error branch in `probeClickHouse`|A post-connect failure reports waking only when it timed out. The new `probeTimedOut` helper accepts `context.DeadlineExceeded` and a `net.Error` with `Timeout` true. Every other post-connect failure reports unreachable.|`TestLedgerReadyReportsUnreachableOnSchemeMismatch` in `server_test.go`. A TLS probe against a plain HTTP listener answers 503 unreachable. `TestLedgerReadyReportsWaking` keeps the timeout row.|

## Mutation checks

Every mutation ran on a scratch copy outside the repository at `/tmp/t72b-remed-scratch`. Each
reverted fix killed exactly its pin.

|Mutation|Test that died|
|---|---|
|F1 reverted, the body drain restored|`TestLedgerReadyAnswersStalledPingBodyWithoutWaiting`, ready after 2.001s against a 2s budget|
|F2 reverted, `Timeout: probeDialTimeout` removed from the dialer|`TestLedgerReadyHonorsDialTimeout`, the route answered 200 instead of 503|
|F3 reverted, the `CheckRedirect` field removed|`TestLedgerReadyReportsUnreachableOnRedirect`, the route answered 200 instead of 503|
|F4 reverted, `connected.Load()` alone selects waking|`TestLedgerReadyReportsUnreachableOnSchemeMismatch`, status was waking instead of unreachable|
|F4 timeout branch broken, `probeTimedOut` returns false|`TestLedgerReadyReportsWaking`, status was unreachable instead of waking|

The last row shows the timeout direction of F4 stays pinned by the existing waking test.
