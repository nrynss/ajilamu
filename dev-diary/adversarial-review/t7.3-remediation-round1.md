# T7.3 remediation, round 1

I fixed the three findings from `t7.3-round1.md`. I did not change the verdict.

| Item | Sev | Where | Resolution | Pin |
|---|---|---|---|---|
| F1 | H | `cmd/ajilamu/main.go:382-428`, `assembleRun` | I stamped `TotalNanodollars: result.TotalCost` on the four assembly and export emits. Every frame in that phase now carries the cumulative run cost, so a reconnect during assembly resumes at that cost. | `TestAssembleEventsCarryRunCost` drives the real `assembleRun` and requires all four events to carry 900. Reverting the stamps leaves every event at 0. |
| F2 | H | `internal/api/run.go:585-600`, `safeDubID` | I replaced the separator check with a plain-identifier check. Letters, digits, dash, underscore, and dot pass. A glob metacharacter fails before any filesystem access, so the route answers 404 and no run starts. | `TestRunStartRejectsGlobDubID` posts the `%2A` id and requires 404 with zero runner calls. It also starts a real hex id and requires 202. Reverting the check answers 202 for `%2A`. |
| F3 | M | `cmd/ajilamu/main_test.go` | I restored `TestNewHistoryReaderReturnsNilForNilClient`, `TestCommitHistoryMapsLedgerRows`, `TestTimelineEntriesMapLedgerSegments`, and `TestBranchComparisonMapsLedgerHeads` from HEAD. Their intent is unchanged. | The four names exist and pass against the current `main.go`. Deleting them leaves `go test -run <the four names>` with no tests to run. |

## Mutation checks

I reverted each fix in a scratch copy at `/tmp/t73rem1`, outside the repository, and ran the pin.

| Reverted fix | Test that died |
|---|---|
| Removed `TotalNanodollars: result.TotalCost` from the four `assembleRun` emits | `TestAssembleEventsCarryRunCost` failed with event 0 through event 3 at cost 0, want 900 |
| Restored the original separator-only `safeDubID` | `TestRunStartRejectsGlobDubID` failed with status 202 for the `%2A` id, want 404 |
| Deleted the four restored history tests | `go test -run TestNewHistoryReaderReturnsNilForNilClient\|TestCommitHistoryMapsLedgerRows\|TestTimelineEntriesMapLedgerSegments\|TestBranchComparisonMapsLedgerHeads -v ./cmd/ajilamu` reported no tests to run |

## Notes

F1 changes what `dubRun.broadcastLocked` copies into the run cost during assembly. The reconnect
resume path itself is already pinned by `TestEventsReconnectKeepsCumulativeCost` in
`internal/api/events_test.go`. I did not change that test.

F2 keeps the existing ids in the suite working. Those ids use letters, digits, and dashes only, so
the plain-identifier check accepts them.
