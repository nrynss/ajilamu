# T6.5b remediation, round 1

I did not change the round 1 verdict. I fixed finding H and nothing else.

I edited `internal/fit/rewrite.go`, `internal/fit/rewrite_test.go`,
`internal/api/rerender.go`, `internal/api/rerender_test.go`, `cmd/ajilamu/main.go`,
and the T6.5b handoff entries in `dev-diary/PHASE-6-editing.md`. The orchestrator
granted that handoff edit. I did not commit.

| Finding | Fix | Pin | Residue |
|---|---|---|---|
| H, a corrected target line that missed its slot called the translation model on attempts two and three, so the chosen take could speak the model's rewrite | `fit.RewriteConfig` gained `AuthoritativeText`. `RepairLine` then speaks `InitialText` on every attempt, calls no translator, and still applies atempo. The loop flags a line that still misses rather than rewriting it. `RenderLine` sets the mode when `req.Text` is non-empty. The source path keeps its translation behaviour. | `TestAuthoritativeTextFlagsRatherThanTranslates` and `TestRerenderCorrectedTargetMissNeverTranslates`. Both record zero translation calls on the corrected target path. Reverting the authoritative branch makes them record two translation calls and speak `translated source line`. | None. |

## Finding H measurements

### 1. A corrected target that misses attempt one

`TestAuthoritativeTextFlagsRatherThanTranslates` drives the real fit loop with a
counting translator. The synthesizer returns 4000 ms against a 2000 ms slot, so
every attempt misses the atempo budget.

```text
=== RUN   TestAuthoritativeTextFlagsRatherThanTranslates
    rewrite_test.go:1252: translation calls = 0, synthesis calls = 3, chosen attempt = 1, spoken text = "corrected target line", flagged = true
--- PASS: TestAuthoritativeTextFlagsRatherThanTranslates (0.15s)
```

`TestRerenderCorrectedTargetMissNeverTranslates` drives the same loop through the
real route, handler and server. Its synthesizer returns 10640 ms against segment 3's
5320 ms slot.

```text
    rerender_test.go:749: translation calls = 0, synthesis calls = 3, attempt = 2, text = "corrected target line", flagged = true
```

The route seam maps every attempt onto one take file, so the ledger reads that
file's try number 2. The fit loop chose attempt 1, and every attempt spoke the
creator's line.

### 2. The source path still translates

`TestRerenderCorrectedSourceRetranslates` runs the real fit loop through the route.

```text
    rerender_test.go:640: translation calls = 1, synthesis calls = 1
```

### 3. The pin and its mutation

The pin is `TestAuthoritativeTextFlagsRatherThanTranslates`, with
`TestRerenderCorrectedTargetMissNeverTranslates` at the route boundary. I removed
the `case cfg.AuthoritativeText:` branch from the text switch in `RepairLine`.

```text
--- FAIL: TestAuthoritativeTextFlagsRatherThanTranslates (0.14s)
    rewrite_test.go:1252: translation calls = 2, synthesis calls = 3, chosen attempt = 1, spoken text = "corrected target line", flagged = true
    rewrite_test.go:1256: translation calls = 2, want 0
    rewrite_test.go:1263: synthesized text = "translated source line", want the creator's corrected target line
    rewrite_test.go:1263: synthesized text = "shorter source line", want the creator's corrected target line
    rewrite_test.go:1271: attempt 2 text = "translated source line", want the creator's corrected target line
    rewrite_test.go:1271: attempt 3 text = "shorter source line", want the creator's corrected target line
--- FAIL: TestAuthoritativeTextStillStretches (0.23s)
    rewrite_test.go:1314: translation calls = 1, want 0
    rewrite_test.go:1333: attempt 2 text = "translated source line", want the creator's corrected target line
FAIL
FAIL	github.com/nrynss/ajilamu/internal/fit	0.380s
--- FAIL: TestRerenderCorrectedTargetMissNeverTranslates (0.14s)
    rerender_test.go:749: translation calls = 2, synthesis calls = 3, attempt = 2, text = "translated source line", flagged = true
    rerender_test.go:753: translation calls = 2, want 0 on the corrected target path
    rerender_test.go:759: spoken text = "translated source line", want the creator's corrected target line
FAIL
FAIL	github.com/nrynss/ajilamu/internal/api	0.150s
```

I restored the branch before the validation run below.

### 4. Validation

```text
$ go test -count=1 ./internal/api ./internal/fit ./cmd/...
ok  	github.com/nrynss/ajilamu/internal/api	1.973s
ok  	github.com/nrynss/ajilamu/internal/fit	17.996s
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	3.892s
```

## Handoff before and after

### The T6.3 call paragraph

Before.

```text
T6.3 calls it after a target-text edit. Send `{"text": "<the edited line>"}`. The
route passes that text as `RewriteConfig.InitialText`, so attempt one synthesizes
without a translation call. A measured run made zero translation calls and one TTS
call. T6.3 must keep its confirmation panel before the call, because the call bills.
```

After.

```text
T6.3 calls it after a target-text edit. Send `{"text": "<the edited line>"}`. The
route passes that text as `RewriteConfig.InitialText` and sets
`RewriteConfig.AuthoritativeText`, so every attempt speaks the creator's line and
the loop calls no translator. The loop flags a line that still misses the slot
rather than rewriting it. A measured run made zero translation calls and three TTS
calls. T6.3 must keep its confirmation panel before the call, because the call bills.
```

### The T6.5a surprise

Before.

```text
Surprises. `fit.RepairLine` reads `InitialText` on attempt one alone. Attempts two and
three still translate when the authoritative text misses its slot. The route always
passes the stored or corrected text, so a speaker-only re-render also skips
translation.
```

After.

```text
Surprises. `fit.RepairLine` reads `InitialText` on attempt one alone unless the
caller sets `RewriteConfig.AuthoritativeText`. That mode speaks the text on every
attempt, may still apply an atempo stretch, and never translates. The route always
passes the stored or corrected text, so a speaker-only re-render also skips
translation.
```

### The T6.5b translation counts

Before.

```text
Translation counts came from the real fit loop with a counting translator. A
slot-fitting take gave one translation call on the source path and zero on the
target-text path. `TestRerenderCorrectedSourceRetranslates` and
`TestRerenderCorrectedTargetSkipsTranslation` pin both counts.
```

After.

```text
Translation counts came from the real fit loop with a counting translator. A
slot-fitting take gave one translation call on the source path and zero on the
target-text path. A corrected target that missed its slot also gave zero
translation calls, and the take spoke the creator's line.
`TestRerenderCorrectedSourceRetranslates`, `TestRerenderCorrectedTargetSkipsTranslation`,
and `TestRerenderCorrectedTargetMissNeverTranslates` pin those counts.
```

### The T6.5b surprise

Before.

```text
Surprises. `fit.RepairLine` only reads `InitialText` on attempt one. Attempts two
and three translate again when the take misses its slot. A live target correction
therefore paid for two translation calls while the route itself called the translator
zero times. `RenderLine` names the target and source language on the translator
request, because it calls the translator directly and skips the fit loop wrapper.
The live model needs `GOOGLE_CLOUD_LOCATION=global`. The region `us-central1`
answers 404 for `gemini-3.8-flash`.
```

After.

```text
Surprises. `fit.RepairLine` only read `InitialText` on attempt one, so attempts two
and three translated when the take missed its slot. A live target correction paid
for two translation calls while the route itself called the translator zero times.
Round 1 of the T6.5b review found the gap. `RewriteConfig.AuthoritativeText` now
speaks the creator's text on every attempt, and the loop flags a line that still
misses. `RenderLine` names the target and source language on the translator
request, because it calls the translator directly and skips the fit loop wrapper.
The live model needs `GOOGLE_CLOUD_LOCATION=global`. The region `us-central1`
answers 404 for `gemini-3.8-flash`.
```

### The one-line notes

The `text` sentence changed from `text stays authoritative and skips translation.`
to `text stays authoritative on every attempt and never translates.`

The validation line changed from `` `go test -count=1 ./internal/api ./cmd/...`
reports ok for both packages. `` to `` `go test -count=1 ./internal/api
./internal/fit ./cmd/...` reports ok for all three packages. ``

I did not tick any PHASE-6 exit box. I did not edit the round 1 review file.
