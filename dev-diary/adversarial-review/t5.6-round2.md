# T5.6 Round 2 Review

## Verdict

REMEDIATE

| Severity | Count |
|---|---:|
| C | 0 |
| H | 0 |
| M | 1 |
| L | 0 |

## Findings

| ID | Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|---|
| F2 | M | `web/src/lib/shortcuts.ts:17-34` | The guard now preserves links and ARIA buttons, but it still omits interactive ARIA roles such as `checkbox`, `radio`, `switch`, `tab`, `menuitem`, `option`, and `treeitem`. Space or the arrow keys from one of those controls reach the global handler and prevent its normal keyboard action. The workspace therefore cannot promise keyboard-only operation for an ARIA control. | I measured the guard's selector against each keyboard-interactive role. None matches the selector. A `KeyboardEvent` from a focused `role=checkbox` reaches the Space branch, while one from `role=radio` reaches the arrow branch. Both branches prevent the event before the control receives its standard action. | Reverting a complete interactive-role guard leaves selection or playback changing when a creator uses Space or the arrows inside an ARIA control. |

## Independent evidence

| Check | Independent observation |
|---|---|
| Prior F1 | The repaired selector protects native links, image-map links, disclosure controls, media controls, ARIA buttons, ARIA links, textboxes, comboboxes, and sliders. F1 has no residue. |
| Required global keys | The handler maps `?`, `/`, Space, `[`, `]`, all four arrows, and Escape. It rejects repeated, composing, Alt, Control, and Meta-modified events. |
| Four panel states | `PanelState` exposes loading, empty, error, and populated variants. Loading, empty, and error render complete sentences. Populated renders the supplied snippet. |
| Processing state | `ProcessingBanner` renders the active pipeline step, sentence, optional line number, and language in a polite live region. It contains no spinner. |
| Build health | I ran `npm run check` and `npm run build` in `web/`. Both completed with zero diagnostics. Build success does not exercise the missing ARIA guard roles. |

## Residue

Round one's F1 is closed. F2 remains and prevents approval.
