# T5.4 Remediation Round 1

| Finding | Fix | Pin | Mutation |
|---|---|---|---|
| F1 | Removed take `measured_ms` values from `scaleCeilingMs`. The shared reference slot and current slot now alone define pixels per millisecond. An overlong take keeps its trough width and paints its excess after the trough. | `lengthbar-shared-scale`. With a 698px canvas and 8px insets, the trough span is 682px. A 7,110ms reference slot remains 682px. An 8,000ms take measures 767.37px, so 85.37px paints beyond the unchanged trough. `npm run check` reported zero errors and warnings. `npm run build` completed. | Reintroducing measured durations into the scale makes an 8,000ms take shrink a 7,110ms trough. Equal slots then stop being comparable, and the overrun no longer extends past the slot. |
