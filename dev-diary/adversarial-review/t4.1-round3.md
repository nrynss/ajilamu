# T4.1 adversarial review, round 3

Verdict: APPROVE

Counts: C0, H0, M0, L0.

Prior-round residue: Zero. Round one required automatic recovery of a retained journal
after reconnect or restart. A dropped-response artifact probe left one durable entry, then a
fresh client drained it after the endpoint recovered. The probe used neither `Flush` nor a
new enqueue on the recovering client. Round two required a cancellable, joined, idempotent
reconciler. A separate artifact probe observed retry traffic before `Close`, called `Close`
twice, then observed no post-close requests for more than one retry interval.

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| None | `internal/ledger/client.go:111-145`, `internal/ledger/queue.go:76-118` | Zero findings across all severities. Startup reconciliation retries retained entries without caller intervention. `Close` cancels and joins the reconciler, and `sync.Once` makes it idempotent. Queue locking serializes foreground and background delivery. | `go test -count=1 -race -run '^TestRound3' -v ./internal/ledger` in an isolated copied tree passed. `TestRound3DroppedResponseRestartRecovery` forced a connection drop and measured retained then drained journal files. `TestRound3CloseStopsRetries` measured retry traffic, called `Close` twice, and measured no later requests for 1.2 seconds. | Reverting recovery, cancellation, joining, idempotence, or queue serialization would strand a durable event, leak retry work, allow post-close retry traffic, or permit competing delivery work. |

Independent checks:

- `go test -count=1 ./internal/ledger` and `go vet ./internal/ledger` passed in the isolated
  copied tree.
- `gofmt -d internal/ledger/client.go internal/ledger/queue.go` and `git diff --check --
  internal/ledger/client.go internal/ledger/queue.go` produced no findings.
- The reviewed client validates one JSONEachRow INSERT, writes the journal before delivery,
  sends same-query batches, and removes entries only after a successful response.
- The T1.3 schema requires writers to target `_raw` tables. Its natural event keys make an
  ambiguous dropped response safe to replay.
