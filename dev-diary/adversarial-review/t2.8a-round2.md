# T2.8a adversarial review, round 2

Task: Language contract in TTS and translation. `NewSynthesizer` takes the target language.
`ValidateLanguage` rejects an empty or malformed code. The translation prompt names the target
language and the source language when the request carries one. The prompt never claims English
when the source language is unknown.

Reviewed against the uncommitted working tree at commit `9258f5c`. Go toolchain go1.27.1. I did
not implement T2.8a. I am not the round one reviewer. I changed no implementation file. My only
write is this review file. A scratch copy of the module lives at `/tmp/t28a-r2`.

## Verdict

**APPROVE**

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 0 |

Round one filed two medium findings and one low finding. All three are resolved under my own
measurement. I found no new defect.

## Findings

Zero findings passed the filing bar. The four column table is empty for that reason.

| Where | What | Pin | Mutation |
|---|---|---|---|

## What I measured

### 1. Malayalam prompt, byte for byte

I copied the module to `/tmp/t28a-r2`. I added one probe test that calls `translatePrompt` with
the fixture request. I extracted the proven f-string from `tools/validate_pipeline.py` with an
AST reader. I evaluated it with the fixture values. I compared the bytes with `cmp`.

Command:

```
python3 /tmp/r2_extract.py
go test -count=1 -run TestR2PinProbe ./internal/gemini
cmp /tmp/r2_py_malayalam.txt /tmp/r2_go_malayalam.txt && echo "cmp: identical"
wc -c /tmp/r2_py_malayalam.txt /tmp/r2_go_malayalam.txt
```

Output:

```
constraint: 'The translated line will be spoken in Malayalam in a video slot that lasts approximately 7.1 seconds (7110 ms). Keep the translation natural, spoken, and fit the rhythm.'
prompt bytes: 509
ok  	github.com/nrynss/ajilamu/internal/gemini	0.004s
cmp: identical
 509 /tmp/r2_py_malayalam.txt
 509 /tmp/r2_go_malayalam.txt
```

Go and Python build the same 509 bytes. The proven ` script (മലയാളം)` suffix is present. M2 is
resolved.

### 2. Spanish source prompt

The probe logged the Spanish source prompt:

```
"\nTranslate this Spanish dialogue line into natural spoken Malayalam script (മലയാളം):\nOriginal Spanish: \"Also, an object in motion tends to stay in motion unless acted on by an outside force.\"\nSpeaker emotion: Explanatory\nConstraint: The translated line will be spoken in Malayalam in a video slot that lasts approximately 7.1 seconds (7110 ms). Keep the translation natural, spoken, and fit the rhythm.\n\nRespond with strictly the translated Malayalam text. No markdown, no quotes, no explanation.\n"
```

Spanish appears in the opening slot and in the `Original Spanish:` slot. The prompt holds 509
bytes, which matches the Malayalam length because both language names hold seven characters.

Mutation: I hardcoded `"English"` in both known-source slots in the scratch copy. Command:

```
go test -count=1 -run TestTranslatePromptNamesNonEnglishSource ./internal/gemini
```

Output:

```
--- FAIL: TestTranslatePromptNamesNonEnglishSource (0.00s)
    translate_test.go:379: prompt mismatch
         got: "...Translate this English dialogue line..."
        want: "...Translate this Spanish dialogue line..."
    translate_test.go:382: prompt misses the Spanish opening slot
    translate_test.go:385: prompt misses the Spanish original slot
FAIL
```

This mutation is round one M7. It survived the old suite. It now fails. M1 is resolved.

### 3. Non-Malayalam target

The probe logged the Spanish target prompt at 475 bytes. It carries no `script` token and no
`മലയാളം` token. It reads `Translate this English dialogue line into natural spoken Spanish:` and
`Respond with strictly the translated Spanish text.`

Mutation: I made `targetOpeningSlot` append the Malayalam hint to every target. Command:

```
go test -count=1 -run TestTranslatePromptNamesTargetLanguage ./internal/gemini
```

Output:

```
--- FAIL: TestTranslatePromptNamesTargetLanguage (0.00s)
    translate_test.go:291: prompt mismatch
         got: "...natural spoken Spanish script (മലയാളം):..."
        want: "...natural spoken Spanish:..."
FAIL
```

The Spanish target pin holds the plain name.

### 4. Vacuous assertions

The two assertions are gone. The test is now `TestNewSynthesizerRejectsBadLanguage` at
`internal/tts/chirp_test.go:327`. It asserts only a non nil error. No `client.calls == 0` and no
`len(ledger.Charges()) == 0` remain in a constructor test.

I dropped the `ValidateLanguage` call from `NewSynthesizer` in the scratch copy. The remaining
assertion failed:

```
--- FAIL: TestNewSynthesizerNilArguments (0.00s)
--- FAIL: TestNewSynthesizerRejectsBadLanguage (0.00s)
```

The remaining assertion is load bearing. L1 is resolved.

I judged every other assertion in the six changed files. Each one can fail under a plausible
mutation of the non test code. Three assertions guard test fixtures rather than code. Those are
`runes == len(malayalamLine)`, `len(unique) != 2` and `len(matches) == 0`. A fixture change
breaks them. They protect the pin below them. I did not file them.

### 5. Test run

Command:

```
go test -count=1 ./internal/tts ./internal/gemini
```

Output:

```
ok  	github.com/nrynss/ajilamu/internal/tts	0.006s
ok  	github.com/nrynss/ajilamu/internal/gemini	0.007s
```

A `-v` run reports 41 passing tests. `internal/tts` holds 20. `internal/gemini` holds 21.

### 6. Residue against round one

| Finding | Residue | Evidence |
|---|---|---|
| M1, unpinned non-English source name | Zero | `TestTranslatePromptNamesNonEnglishSource` drives `SourceLanguageName: "Spanish"` and pins the full prompt. The round one mutation now fails. |
| M2, dropped ` script (മലയാളം)` suffix | Zero for the filed scope. One open note. | Go and Python build the same 509 bytes. `targetOpeningSlot` special cases only Malayalam, so a later language with a script hint needs a new entry. The remediation records this note. |
| L1, two assertions that cannot fail | Zero | Both assertions are deleted. The remaining error assertion fails when validation is removed. |

## What I did not file

- The runner call site is still stale. `cmd/ajilamu/main.go` calls `tts.NewSynthesizer` with four
  arguments. T7.5b owns that wiring.
- The language trim in `NewSynthesizer` is still unpinned. No contract clause names trimming.
- `targetOpeningSlot` matches the display name `"Malayalam"` exactly. The catalog supplies the
  display name. No defect is provable today.

## Done condition

Clause 1 holds. A Spanish target reaches Cloud TTS as `es-ES`.

Clause 2 holds. The prompt names Spanish and never names Malayalam.

Clause 3 holds. An empty and a malformed code both fail before any call.

Clause 4 holds. The Malayalam fixture prompt is byte identical to the proven body.

Verdict: **APPROVE**.
