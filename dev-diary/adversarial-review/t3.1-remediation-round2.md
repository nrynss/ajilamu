# T3.1 round 2 remediation

I own both assembler files and this remediation record. The required shapes exist. I can validate this task independently.
This remediation addresses F1 from `t3.1-round2.md`. The review verdict remains unchanged pending independent re-review.

| Finding | Changes | Pin | Result |
|---|---|---|---|
| H, F1 residue | `BuildBed` adds `min_hard_comp=0` to timestamp resampling. The default 100 ms threshold previously collapsed shorter gaps. | `TestBedPreservesShortSourceTimestampGap` probes source packet timestamps and independently decodes every source and output sample. It checks exactly 50 ms of inserted silence and exact subsequent sample placement. | The regression fails before repair and passes afterward. The gap contains zero samples. Later audio retains its original samples through 2.050 seconds. |

## Measurements

Validation selects `/opt/homebrew/bin/ffmpeg` and `/opt/homebrew/bin/ffprobe`, version 9.0.1.

```sh
PATH=/opt/homebrew/bin:/usr/bin:/bin go test -count=1 ./internal/assemble -v
git diff --check
```

The complete assembler suite passes. The diff check passes.
The new regression generates the round-two source reproduction with 48000 Hz stereo audio and 10 ms PCM packets.
ffprobe confirms 200 packets. Packet 99 starts at 0.990000 seconds and contains 0.010000 seconds of samples.
Packet 100 resumes at 1.050000 seconds. These timestamps independently establish the 50 ms gap.

The test decodes source and bed with ffmpeg into float PCM.
The source contains 192000 interleaved samples. The three-second bed contains 288000.
The test inserts 4800 zero samples into its expected sequence at one second.
It then compares every output sample with that sequence, including the complete trailing silence.
This observes the rendered artifact without trusting the renderer's success output.

| Measurement | Before repair | After repair |
|---|---|---|
| RMS over 1.000 through 1.050 seconds | 0.062484665 | 0.000000000 |
| RMS over 2.000 through 2.050 seconds | 0.000000000 | 0.062484665 |
| Exact expected sample sequence | Fails at interleaved sample 96002, time 1.000020833 seconds | Every sample matches |

The prior one-second offset regression passes. Initial and trailing silence measure zero RMS.
Its active windows measure RMS from 0.062485 through 0.062488.
The prior large internal gap regression also passes.
Separate mono music retains equal stereo channels and a measured level difference of 0.000067 dB.
Fixture samples remain exactly equal over seconds 0 through 75. Both measured fixture intervals retain a 0.000000 dB level difference.
All ten take conversions pass their duration, level, and channel checks.
Separate 5.1 music, padding, trimming, and failure preservation checks pass.

The regression fails against the original filter and passes after the threshold change.
Reverting that change restores both misplaced gap audio and the missing final 50 ms of tone.
Test temporary directories clean up automatically. This remediation changes no verdict or task status.
