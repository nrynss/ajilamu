# T2.8b adversarial review, round 4

Task: Fit loop language wiring. This round re-reviews the remediation in
`t2.8b-remediation-round3.md`. I read the specification, the three prior reviews, the three
remediations, and the shipped working tree at commit `9258f5c`. Go toolchain go1.27.1. I did not
implement T2.8b. I did not remediate it. I am none of the prior reviewers. I changed no
implementation file. My only write is this review. Scratch copies lived under `/tmp/t2.8b-r4` and
`TMPDIR=/home/nryn/tmpgo`, because the shared `/tmp` tmpfs is full.

## Verdict

**REMEDIATE**

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 1 |

The round 3 finding is closed. The extracted factory forwards its language argument. The
committed test fails a factory that hardcodes the create screen tag. Production still passes a
nil client and opens Cloud TTS on ADC. One defect remains. The remediation changed the factory
seam and left the two seam records describing the old shape.

## What I read

- The T2.8b block and the T2.8b handoff entry in `dev-diary/PHASE-2-fit-loop.md`.
- `t2.8b-round1.md`, `t2.8b-round2.md`, `t2.8b-round3.md`, and the three remediation records.
- `dev-diary/adversarial-review/t2.8-contract-change.md`.
- `cmd/ajilamu/main.go` around `runSynthesizerFactory`, `newPipelineRunner`, and `Run`.
- `cmd/ajilamu/main_test.go` around the three runner tests.
- `internal/tts/chirp.go` around `NewSynthesizer` and `newProductionClient`.
- `internal/tts/voices.go` around `Assign`.
- The T7.5b and T7.7 blocks in `dev-diary/PHASE-7-ingestion.md`.

## How I measured

### Validation commands

```
$ go build ./...
build exit=0

$ go test -count=1 ./cmd/... ./internal/fit ./internal/api ./internal/tts ./internal/gemini
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	3.026s
ok  	github.com/nrynss/ajilamu/internal/fit	16.566s
ok  	github.com/nrynss/ajilamu/internal/api	1.385s
ok  	github.com/nrynss/ajilamu/internal/tts	0.009s
ok  	github.com/nrynss/ajilamu/internal/gemini	0.008s
test exit=0
```

### Production passes a nil client and opens ADC

`runSynthesizerFactory` sits at `cmd/ajilamu/main.go:413-417`. `newPipelineRunner` points the
`newSynthesizer` field at `runSynthesizerFactory(cfg, card, nil)` at `cmd/ajilamu/main.go:438`.
So production passes a nil client.

`tts.NewSynthesizer` calls `newProductionClient` when the client is nil
(`internal/tts/chirp.go:84-90`). That function calls `texttospeech.NewClient` on ADC with no key
and no key file (`internal/tts/chirp.go:101-107`).

I measured the path with a throwaway probe in the scratch copy. A broken credential path fails.
The real ADC file opens the client.

```
$ GOOGLE_APPLICATION_CREDENTIALS=/nonexistent/review-r4.json EXPECT_ADC_ERROR=1 \
    go test -count=1 -run 'TestProbeNilClientUsesADC' -v ./cmd/ajilamu
=== RUN   TestProbeNilClientUsesADC
    probe_nil_test.go:18: nil-client factory error = construct Cloud Text-to-Speech client: open /nonexistent/review-r4.json: no such file or directory
--- PASS: TestProbeNilClientUsesADC (0.00s)

$ go test -count=1 -run 'TestProbeNilClientUsesADC' -v ./cmd/ajilamu
=== RUN   TestProbeNilClientUsesADC
    probe_nil_test.go:18: nil-client factory error = <nil>
--- PASS: TestProbeNilClientUsesADC (0.00s)
```

### Round 3 M1 pinned by mutation

The committed test passes on the clean copy.

```
$ go test -count=1 -run 'TestRunSynthesizerFactoryForwardsLanguage' -v ./cmd/ajilamu
=== RUN   TestRunSynthesizerFactoryForwardsLanguage
=== RUN   TestRunSynthesizerFactoryForwardsLanguage/create_screen_tag
    main_test.go:839: Cloud TTS LanguageCode = "ml-IN", Name = "ml-IN-Chirp3-HD-Achernar"
=== RUN   TestRunSynthesizerFactoryForwardsLanguage/full_tag
    main_test.go:839: Cloud TTS LanguageCode = "es-ES", Name = "es-ES-Chirp3-HD-Achernar"
--- PASS: TestRunSynthesizerFactoryForwardsLanguage (0.09s)
    --- PASS: TestRunSynthesizerFactoryForwardsLanguage/create_screen_tag (0.04s)
    --- PASS: TestRunSynthesizerFactoryForwardsLanguage/full_tag (0.04s)
```

I hardcoded `ml-IN` inside the factory in the scratch copy.

```
$ sed -i 's|return tts.NewSynthesizer(cfg, language, rec, card, client)|return tts.NewSynthesizer(cfg, "ml-IN", rec, card, client)|' cmd/ajilamu/main.go
$ grep -n 'NewSynthesizer(cfg' cmd/ajilamu/main.go
415:		return tts.NewSynthesizer(cfg, "ml-IN", rec, card, client)
$ go test -count=1 -run 'TestRunSynthesizerFactoryForwardsLanguage' -v ./cmd/ajilamu
=== RUN   TestRunSynthesizerFactoryForwardsLanguage
=== RUN   TestRunSynthesizerFactoryForwardsLanguage/create_screen_tag
    main_test.go:839: Cloud TTS LanguageCode = "ml-IN", Name = "ml-IN-Chirp3-HD-Achernar"
=== RUN   TestRunSynthesizerFactoryForwardsLanguage/full_tag
    main_test.go:839: Cloud TTS LanguageCode = "ml-IN", Name = "ml-IN-Chirp3-HD-Achernar"
    main_test.go:841: Cloud TTS LanguageCode = "ml-IN", want es-ES
    main_test.go:844: Cloud TTS Name = "ml-IN-Chirp3-HD-Achernar", want es-ES-Chirp3-HD-Achernar
--- FAIL: TestRunSynthesizerFactoryForwardsLanguage (0.09s)
    --- PASS: TestRunSynthesizerFactoryForwardsLanguage/create_screen_tag (0.05s)
    --- FAIL: TestRunSynthesizerFactoryForwardsLanguage/full_tag (0.05s)
FAIL
FAIL	github.com/nrynss/ajilamu/cmd/ajilamu	0.100s
```

The `ml-IN` case stays green, because its tag is the constant. The `es-ES` case fails on the
Cloud TTS code and the name. Round 3 M1 is closed.

I also re-ran the round 2 M1 mutation. `Run` passes `"ml-IN"` at the call site.

```
$ sed -i 's|p.newSynthesizer(language.tag, charges)|p.newSynthesizer("ml-IN", charges)|' cmd/ajilamu/main.go
$ grep -n 'p.newSynthesizer' cmd/ajilamu/main.go
459:	synthesizer, err := p.newSynthesizer("ml-IN", charges)
$ go test -count=1 -run 'TestPipelineRunnerLanguageWiring|TestRunSynthesizerFactoryForwardsLanguage' -v ./cmd/ajilamu
    main_test.go:722: factory language = "ml-IN", want es-ES
    main_test.go:735: Cloud TTS LanguageCode = "ml-IN", want es-ES
--- FAIL: TestPipelineRunnerLanguageWiring (1.94s)
--- PASS: TestRunSynthesizerFactoryForwardsLanguage (0.09s)
FAIL
```

The two pins are complementary. The wiring test catches a constant at the call site. The factory
test catches a constant inside the closure.

### No other production change since round three

`cmd/ajilamu/main.go` was byte identical from round one through round three. A diff against the
round one scratch copy shows only the extraction and the new call site.

```
$ diff -u /tmp/t2.8b-review/cmd/ajilamu/main.go cmd/ajilamu/main.go
+func runSynthesizerFactory(cfg *config.Config, card cost.RateCard, client tts.TTSClient) func(language string, rec tts.ChargeRecorder) (tts.Synthesizer, error) {
+	return func(language string, rec tts.ChargeRecorder) (tts.Synthesizer, error) {
+		return tts.NewSynthesizer(cfg, language, rec, card, client)
+	}
+}
...
-		newSynthesizer: func(language string, rec tts.ChargeRecorder) (tts.Synthesizer, error) {
-			return tts.NewSynthesizer(cfg, language, rec, card, nil)
-		},
+		newSynthesizer: runSynthesizerFactory(cfg, card, nil),
```

`cmd/ajilamu/main_test.go` gains one test at line 804. The lines the round 3 review quoted
(`main_test.go:720` and `main_test.go:733`) still match. So no other production behaviour
changed.

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| L1 · `dev-diary/PHASE-2-fit-loop.md:763` | The handoff describes the pre-extraction factory and names stale lines. It says `newPipelineRunner` points the factory at `tts.NewSynthesizer(cfg, language, rec, card, nil)` at `cmd/ajilamu/main.go:429-431`. The factory now lives in `runSynthesizerFactory` at `cmd/ajilamu/main.go:413-417`. The call site sits at `cmd/ajilamu/main.go:438`. The handoff also names `main.go:440` and `main.go:452`, now 447 and 459. The contract-change record repeats the claim at `t2.8-contract-change.md:25-26`. Its line 49 counts two committed runner pins, where the tree holds three. | `sed -n '429,431p' cmd/ajilamu/main.go` prints the segmenter and translator error blocks, not the factory. `grep -n 'newSynthesizer:' cmd/ajilamu/main.go` prints line 438 with `runSynthesizerFactory(cfg, card, nil)`. `grep -n 'func runSynthesizerFactory' cmd/ajilamu/main.go` prints line 413. `grep -c 'TestRunSynthesizerFactoryForwardsLanguage' cmd/ajilamu/main_test.go` prints 1. | Restore the stale sentences. No test fails, so the false seam returns. A next agent then reads a factory shape that no longer exists. |

### L1: The seam records describe the pre-extraction factory

The remediation extracted the inline closure into `runSynthesizerFactory`
(`cmd/ajilamu/main.go:413-417`). It did not update the two records that describe the seam.

The handoff at `dev-diary/PHASE-2-fit-loop.md:763` reads:

> `newPipelineRunner` points that factory at `tts.NewSynthesizer(cfg, language, rec, card, nil)`
> (`cmd/ajilamu/main.go:429-431`). `Run` resolves the language first
> (`cmd/ajilamu/main.go:440`), then builds the synthesizer with the resolved tag
> (`cmd/ajilamu/main.go:452`).

`cmd/ajilamu/main.go:429-431` now holds the end of the segmenter error block and the translator
construction. The factory call is `runSynthesizerFactory(cfg, card, nil)` at line 438. `Run`
resolves at line 447 and builds at line 459.

The contract-change record at `t2.8-contract-change.md:25-26` repeats the claim. Its line 49
says two committed tests pin the runner wiring. The tree now holds three, because
`TestRunSynthesizerFactoryForwardsLanguage` pins the factory. The handoff test paragraph at
`dev-diary/PHASE-2-fit-loop.md:759` also names only the first two.

The contract-change file warns that its line table may move. That warning covers the table. It
does not cover the false factory shape in the prose or the stale test count.

## Residue against prior rounds

- **Round 1 M1, the handoff misdescribed the runner: closed for the claims it named.** The
  handoff now names the per-run factory and the clean build. The extraction introduced a new and
  narrower drift in the same paragraph. L1 records it.
- **Round 1 M2, no committed pin for the runner wiring: closed.** The factory mutation fails
  `TestRunSynthesizerFactoryForwardsLanguage`. The call-site mutation fails
  `TestPipelineRunnerLanguageWiring`.
- **Round 2 M1, the wiring test drove only `ml`: closed.** The table drives `ml` and `es-ES`. The
  constant-tag mutation fails the `es-ES` case.
- **Round 2 L1, the contract-change ownership record: closed.** The record names the `owns` line
  and the committed tests. L1 records the new test-count drift.
- **Round 2 L2, the loop literal count: closed.** `grep -c 'PipelineConfig{'` prints 27 and
  `grep -c 'Language: *tts.Malayalam'` prints 25.
- **Round 3 M1, the production factory had no pin: closed.** The hardcoded factory fails the
  committed test on the `es-ES` case.

The three out-of-task items in `t2.8b-round1.md` stay outside this task. T7.5b owns the api event
stream, the request change, and the source language. T2.8a owns the Cloud TTS client lifetime.
T7.7 owns the fetched catalog.

## Done condition

The three clauses of the T2.8b block hold.

- `go test -count=1 ./internal/fit` passes.
- `TestPipelineLanguageWiring` drives `es-ES` into the prompt as `Spanish` and into Cloud TTS as
  `es-ES`.
- `t2.8-contract-change.md` names the runner wiring and its owners.

The verdict is REMEDIATE. The code behaviour is correct and the round 3 pin holds. One record
defect remains.
