# T2.1 Remediation Round 3

| # | Finding | Fix Applied |
|---|---------|-------------|
| 1 | Probe gated live evidence on fixture exactness. Count mismatch called Fatalf and per-id drift past 2500 ms called Errorf, so valid generative runs failed. | Probe now logs a count comparison and enforces structural checks only. It requires ordered ids, non-overlapping ranges inside the clip, and client validation on every row. It keeps the decode gate and the speaker set check and prints a drift table for readers. |
| 2 | Probe printed transcript and charges without model, location, or endpoint, so readers could not trace evidence to a run. | Probe now prints one line naming model, location, and endpoint before the transcript. Behavior stays unchanged. |

## Verification

Vet passes with the live tag and gofmt reports no files. Live probe ran against gemini-2.5-flash in global and passed in 9.89 seconds. Runs used explicit GEMINI_MODEL and GOOGLE_CLOUD_LOCATION overrides. The as-configured .env model id still carries the prefixed form, so the probe used overrides instead. Process credentials came from the repo .env through the probe setdefault loader. It returned 8 segments with full speaker coverage and a 658 unit segment charge.
