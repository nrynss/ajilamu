# T7.5 remediation, round 2

I fixed M1 and M2 from `t7.5-round2.md`. I did not change the verdict. I did not fix anything the
review did not find. I did not mark the task done. I did not commit and I did not edit a status
line.

| Finding | Fix | Pin | Residue |
|---|---|---|---|
| M1 M, `dev-diary/PHASE-7-ingestion.md` lines 855 to 857 and 912 to 918 | The T7.5a entry now says the takes read selects `repair_detail` and the reader parses the ratio. The T7.5 entry names segment coverage in the readiness rule and names the running value. The stretch paragraph now says the reader fills `stretch_factor_milli` from the parsed prefix. | `go test -count=1 -run TestWorkspaceTakesRecoversAtempoStretch -v ./internal/ledger` serves the atempo row with `"stretch_factor_milli":1050` and the none row with no such field. The corrected sentences match that shipped reader. | None. |
| M2 M, `internal/api/workspace.go` and `internal/api/server.go` line 180 | `WorkspaceHandlerFrom` takes a `RunActive` check. `workspaceReadiness` returns `running` first when the check reports an in-flight run. `NewServer` builds the registry before the mount and passes `runs.active`. | `TestWorkspaceReadinessReportsRunningWhileARunHoldsTheDub` drives six reader states through the handler. `TestWorkspaceRouteReportsRunningThroughTheServer` starts a real run over `NewServer` and reads `ready`, then `running`, then `ready`. | None. |

## M1 before and after

The T7.5a entry at lines 855 to 857 said the stretch factor stays zero.

Before:

> The ledger stores no project title and no readiness. `ProjectMetadata` leaves both empty for
> the route to fill from the upload record. The ledger stores no atempo ratio column either, so
> `Take.StretchFactorMilli` stays zero.

After:

> The ledger stores no project title and no readiness. `ProjectMetadata` leaves both empty for
> the route to fill from the upload record. The ledger stores no atempo ratio column either. The
> takes read selects `repair_detail`, and the reader parses the `atempo stretch applied at ratio`
> prefix into thousandths, so `Take.StretchFactorMilli` carries 1050 for a ratio of 1.0500.

The T7.5 readiness sentence at lines 912 to 914 omitted segment coverage and the running value.

Before:

> The upload record stores no readiness, so the route derives it. No take at all reads pending, a
> flagged line reads review, and every take fitting reads ready. The title comes from the upload
> record. `source_language` stays empty, because no table or record stores the film language.

After:

> The upload record stores no readiness, so the route derives it. `NewServer` passes
> `runRegistry.active` into `WorkspaceHandlerFrom`, so a run in flight reads running. No take at
> all reads pending. A flagged line reads review. Every take fitting with every segment covered
> reads ready, and a segment with no rendered line reads review. The title comes from the upload
> record. `source_language` stays empty, because no table or record stores the film language.

The T7.5 stretch paragraph at lines 916 to 918 said the field stays zero.

Before:

> Zero stretch factor. The ledger has no atempo ratio column, so `Take.StretchFactorMilli` stays
> zero and `omitempty` drops it. A real atempo take therefore carries `repair` atempo with no ratio.
> `LengthBar.svelte` and `DetailsTab.svelte` already guard on the missing field and degrade.

After:

> Stretch factor. The ledger has no atempo ratio column, so the takes read selects `repair_detail`.
> The reader parses the `atempo stretch applied at ratio` prefix into thousandths, so an atempo take
> at ratio 1.0500 carries `stretch_factor_milli` 1050. A take with no parsed ratio leaves the field
> zero and `omitempty` drops it. `LengthBar.svelte` and `DetailsTab.svelte` already guard on the
> missing field and degrade.

### M1 pin

```text
$ go test -count=1 -run TestWorkspaceTakesRecoversAtempoStretch -v ./internal/ledger
=== RUN   TestWorkspaceTakesRecoversAtempoStretch
    workspace_test.go:536: atempo take: {"file":"a.wav","voice":"voice-a","attempt":1,"repair":"atempo","stretch_factor_milli":1050,"fit":{"slot_ms":1820,"measured_ms":1830,"delta_ms":10,"state":"fits"},"charges":[]}
    workspace_test.go:537: none take: {"file":"b.wav","voice":"voice-a","attempt":1,"repair":"none","fit":{"slot_ms":1000,"measured_ms":1000,"delta_ms":0,"state":"fits"},"charges":[]}
--- PASS: TestWorkspaceTakesRecoversAtempoStretch (0.00s)
PASS
ok  	github.com/nrynss/ajilamu/internal/ledger	0.005s
```

## M2 measured states

The handler matrix answered every state, including `running`.

| Reader state | Run in flight | Readiness |
|---|---|---|
| no take | yes | `running` |
| no take | no | `pending` |
| flagged line | yes | `running` |
| flagged line | no | `review` |
| every take fits | yes | `running` |
| every take fits | no | `ready` |

```text
$ go test -count=1 -run 'TestWorkspaceReadinessReportsRunningWhileARunHoldsTheDub|TestWorkspaceRouteReportsRunningThroughTheServer' -v ./internal/api
=== RUN   TestWorkspaceReadinessReportsRunningWhileARunHoldsTheDub
    workspace_test.go:456: no take with a run in flight: {"id":"dub-1","title":"Title","source_language":"","readiness":"running","segments":[],"languages":[{"language":"ml","lines":[{"segment_id":1,"text":"","flagged":false,"takes":null}]}],"charges":[],"total":{"total_nanodollars":0,"covers":""},"commits":[],"created_at":"","updated_at":""}
    workspace_test.go:456: no take with no run: {"id":"dub-1","title":"Title","source_language":"","readiness":"pending","segments":[],"languages":[{"language":"ml","lines":[{"segment_id":1,"text":"","flagged":false,"takes":null}]}],"charges":[],"total":{"total_nanodollars":0,"covers":""},"commits":[],"created_at":"","updated_at":""}
    workspace_test.go:456: flagged line with a run in flight: {"id":"dub-1","title":"Title","source_language":"","readiness":"running","segments":[],"languages":[{"language":"ml","lines":[{"segment_id":1,"text":"","flagged":true,"takes":[{"file":"a.wav","voice":"","attempt":0,"repair":"","fit":{"slot_ms":0,"measured_ms":0,"delta_ms":0,"state":""},"charges":[]}]}]}],"charges":[],"total":{"total_nanodollars":0,"covers":""},"commits":[],"created_at":"","updated_at":""}
    workspace_test.go:456: flagged line with no run: {"id":"dub-1","title":"Title","source_language":"","readiness":"review","segments":[],"languages":[{"language":"ml","lines":[{"segment_id":1,"text":"","flagged":true,"takes":[{"file":"a.wav","voice":"","attempt":0,"repair":"","fit":{"slot_ms":0,"measured_ms":0,"delta_ms":0,"state":""},"charges":[]}]}]}],"charges":[],"total":{"total_nanodollars":0,"covers":""},"commits":[],"created_at":"","updated_at":""}
    workspace_test.go:456: every take fits with a run in flight: {"id":"dub-1","title":"Title","source_language":"","readiness":"running","segments":[],"languages":[{"language":"ml","lines":[{"segment_id":1,"text":"","flagged":false,"takes":[{"file":"a.wav","voice":"","attempt":0,"repair":"","fit":{"slot_ms":0,"measured_ms":0,"delta_ms":0,"state":"fits"},"charges":[]}]}]}],"charges":[],"total":{"total_nanodollars":0,"covers":""},"commits":[],"created_at":"","updated_at":""}
    workspace_test.go:456: every take fits with no run: {"id":"dub-1","title":"Title","source_language":"","readiness":"ready","segments":[],"languages":[{"language":"ml","lines":[{"segment_id":1,"text":"","flagged":false,"takes":[{"file":"a.wav","voice":"","attempt":0,"repair":"","fit":{"slot_ms":0,"measured_ms":0,"delta_ms":0,"state":"fits"},"charges":[]}]}]}],"charges":[],"total":{"total_nanodollars":0,"covers":""},"commits":[],"created_at":"","updated_at":""}
--- PASS: TestWorkspaceReadinessReportsRunningWhileARunHoldsTheDub (0.00s)
=== RUN   TestWorkspaceRouteReportsRunningThroughTheServer
    workspace_test.go:502: readiness = "ready"
    workspace_test.go:511: readiness = "running"
    workspace_test.go:517: readiness = "ready"
--- PASS: TestWorkspaceRouteReportsRunningThroughTheServer (0.00s)
PASS
ok  	github.com/nrynss/ajilamu/internal/api	0.006s
```

The second pin drives the real `NewServer` mount. It starts a run with a blocking runner, reads
`ready` before the start, `running` while the run holds the dub, and `ready` after the run ends.

## Mutation check

I replaced `runs.active` with `nil` in `NewServer` and ran the server pin.

```text
--- FAIL: TestWorkspaceRouteReportsRunningThroughTheServer (0.00s)
    workspace_test.go:502: readiness = "ready"
    workspace_test.go:511: readiness = "ready"
    workspace_test.go:512: readiness during the run = "ready", want "running"
FAIL
```

I restored `runs.active`. The pin passed again.

## Validation

```text
$ go test -count=1 ./internal/api ./cmd/...
# github.com/nrynss/ajilamu/cmd/ajilamu [github.com/nrynss/ajilamu/cmd/ajilamu.test]
cmd/ajilamu/main.go:385:60: not enough arguments in call to tts.NewSynthesizer
	have (*config.Config, *chargeRouter, cost.RateCard, nil)
	want (*config.Config, string, tts.ChargeRecorder, cost.RateCard, tts.TTSClient)
--- FAIL: TestEntrypointExitsCleanlyWithoutLedger (0.12s)
    server_test.go:451: build entrypoint: exit status 1: # github.com/nrynss/ajilamu/cmd/ajilamu
        cmd/ajilamu/main.go:385:60: not enough arguments in call to tts.NewSynthesizer
        	have (*config.Config, *chargeRouter, cost.RateCard, nil)
        	want (*config.Config, string, tts.ChargeRecorder, cost.RateCard, tts.TTSClient)
FAIL
FAIL	github.com/nrynss/ajilamu/internal/api	0.627s
FAIL	github.com/nrynss/ajilamu/cmd/ajilamu [build failed]
```

The failure is a concurrent T2.8 edit to `internal/tts`, not this remediation. The round two
review recorded the same build failure at the same line. Only
`TestEntrypointExitsCleanlyWithoutLedger` fails in `internal/api`, because it builds the
entrypoint. Every other `internal/api` test passes.

```text
$ go test -count=1 -v ./internal/api 2>&1 | grep -E '^--- FAIL'
--- FAIL: TestEntrypointExitsCleanlyWithoutLedger (0.12s)

$ go test -count=1 ./internal/ledger
ok  	github.com/nrynss/ajilamu/internal/ledger	1.011s

$ npm --prefix web run check
1788900542099 START "/home/nryn/work/ajilamu/web"
1788900542100 COMPLETED 181 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
```

## Scope

I wrote `internal/api/workspace.go`, `internal/api/workspace_test.go`,
`internal/api/server.go`, the T7.5 and T7.5a entries in `dev-diary/PHASE-7-ingestion.md`, and this
record. I left `cmd/ajilamu/main.go` and `internal/api/run.go` unchanged, because `NewServer`
already owns the registry and passes `runs.active`. I did not touch `internal/tts`,
`internal/gemini`, or `internal/fit`. I did not commit and I did not edit a status line.
