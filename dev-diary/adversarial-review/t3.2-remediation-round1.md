# T3.2 round 1 remediation

I own both assembler placement files and this remediation record. The required shapes exist. I can validate this task independently.
This remediation addresses F1 and F2 from `t3.2-round1.md`. The review verdict remains unchanged pending independent re-review.

| Finding | Changes | Pin | Result |
|---|---|---|---|
| H, F1 | `decidePolicy` keeps a colliding take past the next start by the fade length. `clampKeep` then limits that keep to the bed. The later take still fades in over the same window, so the takes overlap. | `TestCollisionCrossfadesAndLogs` decodes the speech layer and measures RMS plus peak in a 10 ms window around 1.000 s. External `silencedetect=noise=-40dB:d=0.005` must not cover 1.000 s. | Junction RMS is 0.085262 against a body of 0.088369. Peak stays above the voiced floor. silencedetect finds no trough at 1.000 s. Restoring abutting fades drops that window below half the body RMS. |
| L, F2 | The `PolicyTruncate` comment now names the next take start as the cut. Truncate keep is still `available`. | `TestOverrunTruncatesAtSilence` still measures tone at 0.2 s to 0.6 s, silence at 0.85 s to 0.95 s, and the next take after 1.0 s. | Truncate artifacts match the review: tone RMS 0.088369, kept gap 0.000000, next RMS 0.088369. |

## Measurements

Validation selects `/opt/homebrew/bin/ffmpeg` and `/opt/homebrew/bin/ffprobe`.
Go reports version 1.27.1. The complete assembler suite passes:

```sh
PATH=/opt/homebrew/bin:/usr/bin:/bin go test -count=1 ./internal/assemble -v
```

Fixture placement still matches prepared takes within 0.000000 dB.
Overlay gaps stay silent at RMS 0.000000000.
Gap policy still occupies the trailing window at RMS 0.088369.
Truncate still cuts at the next start after voiced audio has already ended.

The crossfade regression places a 1.5 s tone against a 1.0 s tone at 1.000 s.
The earlier take now ends at 1.040 s. That is the 40 ms overlap.
Decoded body RMS is 0.088369. Junction RMS is 0.085262.
Abutting fades produced a digital-zero junction and a 7.4 ms silencedetect trough.
The overlapping keep removes that trough.
