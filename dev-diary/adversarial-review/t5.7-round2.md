# T5.7 Round 2 Review

## Verdict

REMEDIATE

| Severity | Count |
|---|---:|
| C | 0 |
| H | 0 |
| M | 1 |
| L | 1 |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| F1 · `web/src/routes/d/[id]/+page.svelte` lines 25 to 40 | Round-1 F10 stands. The panel still ships three of the four required states. The route computes its initial state synchronously and never enters loading. The remediation table claims residual "none" while its own change note says loading was kept unused. | Five loads against a valid, an empty, a shape-broken, and a malformed fixture produced `data-state` values `populated`, `empty`, `error`, `error`, and `populated`. No load produced `loading`. `ui-ux.md` section 9 asks for four states on every panel. | Wire the loading state and the panel can announce a load in progress. Revert that and the required state stays unreachable in this route. |
| F2 · `web/src/routes/d/[id]/+page.svelte` lines 84 to 98 and 406 to 411 | The round-1 F2 fix removed `scrollIntoView` entirely. Keyboard selection past row five now moves the list highlight out of the 420px scroll viewport, and nothing reveals it. The heading, the rail row, and the video seek still carry the selection. | At 1440 by 900, seven `]` presses moved the heading to `Line 8` and seeked the video to 47.381 seconds. The list kept `scrollTop` 0, and the selected row's rect sat outside the list viewport. The rail row stayed visible. Screenshot `t5.7-round2-line8.png` shows the `Line 8` heading over a list with no visible highlight. | Reveal the selected row inside the list viewport and keyboard stepping keeps the highlight in view. Revert that and every step past row five moves the highlight out of sight. |

## Independent evidence

| Check | Independent observation |
|---|---|
| Clip duration | `ffprobe` reports `duration=75.008267` for `web/static/clip.mp4`. `pictureDurationMs` now returns 75008. |
| Picture duration plumbing | The scrubber carries `max="75.008"` and the timeline carries `data-duration-ms="75008"`. The ruler aria label reads "Time ruler ending at 1:15.0" and the readout says `0:00.0 / 1:15.0`. Setting the scrubber to 70 lands the video at 70 seconds. |
| Flagged lines block | On first load the block label reads "Lines needing you" and the note reads "Line 8 is still 2.91 seconds too short after 1 try, this one needs you." Its bottom edge sits at 862.69px, above the length checks top at 876.69px and the `Line 1` heading at 903.69px. |
| Selection does not scroll the picture | `.picture-area` `scrollTop` stayed 0 through a synthetic click on line 8 and a seven-step `]` walk. 498.22px of the 589.5px frame stayed visible throughout. Round 1 measured a `scrollTop` of 671 and a visible frame of 0px. |
| Language disclosure | The strip reads "Now hearing Malayalam." with zero audio elements and `video.muted` false. The note beneath reads "The picture plays its original audio. Play discrete takes from the length checks or Lines rail." Clicking Play take fetched `seg_1_try1.wav` and played it. |
| Rail follows the active language | On a two-language scratch fixture, switching to German moved the rail Lines row 1 to `1.82s / 2.50s`. Details showed voice `de-DE-Chirp3-HD-Test` and "This take overruns the slot by 0.68 seconds." The line 1 length bar sentence matched word for word. Malayalam showed `1.82s / 1.68s`. |
| No invented processing banner | With `readiness` forced to `running`, the route rendered zero `.processing-banner` nodes and no "Working now" text. The route no longer imports `ProcessingBanner`. |
| Command bar speaks | Submitting "move wav 1 to 0:0005 to right" added a `role="status"` sentence: "This workspace cannot run editor commands yet." Body text grew from 2171 to 2217 characters. |
| Timeline still cannot participate | Concatenated `.slot`, `.take`, and `.short-tail` markup is byte-identical before and after selecting line 8. Zero selection markers exist. Clicking a timeline take left the heading unchanged. Playing to 21.346 seconds moved the heading to `Line 4` with zero timeline markers. `Timeline.svelte` accepts only `segments`, `tracks`, and `durationMs`. |
| Headings | One `h1` reads "NASA 75-second clip". `document.title` reads "NASA 75-second clip · Ajilamu". The selected-line heading is now an `h2`. |
| Panel state matrix | Valid, empty, shape-broken, and malformed fixtures produced `populated`, `empty` with `role="status"`, `error` with `role="alert"`, and `error`. No load produced `loading`. |
| Theme repaint | Toggling the chrome switch moved `data-theme` to dark and repainted the line 1 bar pixel from `rgb(143,165,140)` to `rgb(92,114,89)`. Both values match the `--fit` tokens in `ui-ux.md` section 2. |
| Keyboard and shared selection | `?` opened the dialog, Escape closed it, `/` focused the command bar, and Space produced exactly one play and one pause. One `]` press moved heading, length row, and rail row to line 2 together and seeked the video to 7.728 seconds. |
| Fixture untouched | The repository fixture digest still reads `7dbef9e193c789d0681a67707ec1ff6013b335c6687e8f8fe14e7faa1d8ce67a`, matching round 1. |
| Svelte version hygiene | Neither owned file contains `export let`, a legacy reactive declaration, or a store. Every reactive binding uses runes. |
| Build health | `npm run check` reported zero errors and zero warnings. Not a pin. |

## Residue

Round-1 F1 has zero residue. Each flagged note now names its line. The notes live in a labelled block above the length checks rather than under the selected heading.

Round-1 F2 has zero residue. Selection no longer scrolls the picture area. The fix removed reveal behavior entirely, which produces this round's F2.

Round-1 F3 has zero residue. Preview, scrubber, timeline, and ruler all carry the true 75008 millisecond picture length.

Round-1 F4 has zero in-owns residue. The route now states that the picture plays original audio and that takes play from the length checks or Lines rail. Take playback is real. The leftover is only the named T5.2 contract. The fixture carries no per-language mixed track, and the strip's "Now hearing" wording belongs to `LanguageStrip.svelte`, which T5.2 owns.

Round-1 F5 has zero residue. The rail now derives from the active language and agrees with the length bars under both languages.

Round-1 F6 has zero residue. No processing banner renders under a forced running readiness.

Round-1 F7 has zero residue. The command bar answers every submission with a spoken status sentence.

Round-1 F8 residue is only the named T5.3 contract. The timeline still takes no part in selection or playback, and `Timeline.svelte` still lacks the inputs. I do not reopen it as an in-owns defect.

Round-1 F9 has zero residue. The project title is the route's only `h1` and reaches the document title.

Round-1 F10 has residue, restated as this round's F1. Loading remains unreachable. The remediation record's "none" claim does not match measurement.

## Contract changes

None new. Round 1 named the two leftovers this round confirms. The T5.2 mixed-track contract covers F4, and the T5.3 timeline-input contract covers F8.

## Method

I measured the running dev server on `127.0.0.1:5174` with Chrome through puppeteer-core at 1440 by 900, plus `ffprobe` and `openssl`. For the two-language, readiness, and state checks I copied the tree to a scratch directory, edited only the copied fixture, and served it on port 5199. I used synthetic clicks for the scroll measurements because puppeteer's pointer click scrolls the target into view and contaminates the reading. The scratch copy is deleted. The repository fixture is unchanged.

## Scope

I own only this review file. I edited no implementation file, no fixture, and no task board entry. I fixed nothing that I found. Every remediation belongs to the next remediation round.
