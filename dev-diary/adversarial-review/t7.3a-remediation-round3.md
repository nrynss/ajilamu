# T7.3a remediation, round 3

I fixed the one finding from `t7.3a-round3.md`. I did not change the verdict.

| Item | Sev | Where | Resolution | Pin |
|---|---|---|---|---|
| F1 | M | `internal/fit/loop.go:263-268`, `readSegmentRecord` | The loop removes a recorded take only when the run context is still live. `takeFileUsable` maps a probe failure to false, and a cancelled or expired context makes the probe fail. `readSegmentRecord` keeps the take in that case, so the next resume reuses it. The zero-length and undecodable cases still discard the take and re-render the line. | `TestPipelineCancelDuringProbeKeepsCompletedTake` runs the loop under a context it cancels once the probe has started. It requires a `context.Canceled` error and the completed take on disk, then resumes with a fresh context and requires zero synth calls. `TestPipelineDeadlineDuringProbeKeepsCompletedTake` runs the loop under a two second deadline and requires the take on disk after `context.DeadlineExceeded`. |

## Mutation checks

I reverted the guard in a scratch copy at `/tmp/t73a-remed3-scratch`, outside the
repository, and ran the pins. The restored copy passes both tests.

| Reverted fix | Test that died |
|---|---|
| Restored the unconditional `discardTakeFile` | `TestPipelineCancelDuringProbeKeepsCompletedTake` failed with `cancelled probe removed the completed take ... no such file or directory`. `TestPipelineDeadlineDuringProbeKeepsCompletedTake` failed with `expired probe removed the completed take ... no such file or directory`. |

The shipped zero-length and undecodable tests still pass. Each one runs under a live
context, so the guard does not reach them and the loop still removes and re-renders
those takes.

No test hardcodes a port or a fixed temporary directory. Both new tests use `t.TempDir()`.
