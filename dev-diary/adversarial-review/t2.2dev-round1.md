# T2.2dev Round 1 Review

**Verdict:** APPROVE
**Commit / state:** uncommitted T2.2dev delta at HEAD 109b091ab6a1003d849e097a66dfa3262d5980d3

## Severity Counts

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 0 |

## Findings

| # | Sev | Where | What | Pin | Mutation |
|---|---|-------|------|-----|----------|
| None. Every required pin holds under independent measurement. The probes table records each check. | | | | | |

## Probes

| Probe | Judgement | Pin |
|-------|-----------|-----|
| `go.mod` genai pin and oauth2 require | Intact. genai is a direct require at v1.71.0, below v2.0.0. oauth2 is not a direct require. | `go list -m google.golang.org/genai` prints `google.golang.org/genai v1.71.0`. Direct modules are only the main module and genai. `go.mod` has no oauth2 line. `go mod why -m golang.org/x/oauth2` says the main module does not need it. `go list -deps ./internal/gemini` has no oauth2 package. `go.sum` carries `google.golang.org/genai v1.71.0`. `go mod tidy -diff` is empty. |
| No leftover REST or oauth2 imports | Intact. Production Gemini code calls the SDK. Tests use httptest only to pin that SDK. | `rg -n 'golang.org/x/oauth2' internal/` is empty. `rg -n 'aiplatform.googleapis.com' internal/` is empty. `rg -n 'net/http' internal/gemini/` hits only `segment_test.go` httptest. `endpointURL`, `postGenerate`, `envelopeText`, and `http.NewRequest` are absent under `internal/gemini`. |
| Production client construction | Intact. The nil-client path passes BackendVertexAI, Project, and Location only. | Read of `newVertexClient` in `internal/gemini/segment.go`. The composite literal sets those three fields and no others. `NewSegmenter` and `NewTranslator` call it when the client is nil. SDK `client.go:348` calls `credentials.DetectDefault` when Credentials, APIKey, and HTTPClient are empty. `Client.Models.GenerateContent` is the SDK method at `models.go:5605`. `TestNewSegmenterUsesSDKClient` drives a real `genai.Client` through httptest and sees the model id on the generateContent path. |
| Charges bill from UsageMetadata | Intact. Token fields come from usage, not prompt length. Total sums both token prices. Error paths add nothing. | `geminiCharge` copies `PromptTokenCount` and `CandidatesTokenCount`. It leaves Units at zero. `Charge.Total` is `Units*UnitPrice + PromptTokens*PromptUnitPrice + CandidateTokens*CandidateUnitPrice`. Independent arithmetic: `41*150 + 17*600 = 16350` nanodollars. A scratch copy with `UsageMetadata` set to nil failed `TestSegmentSuccess` (got 0/0, want 41/17) and `TestTranslateChargeItemization` (got 0/0, want 29/11). The workspace copy was not edited. A side pin in `/home/nryn/t22dev-rev1-pins` (now deleted) printed Total 16350 with Units 0. Generate and parse errors record zero charges. |
| Config loader | Intact. Project is required. An API key or a key file cannot replace it. | Independent `LoadFromMap` cases: missing project, `GEMINI_API_KEY` alone, and `GOOGLE_APPLICATION_CREDENTIALS` alone each error and name `GOOGLE_CLOUD_PROJECT`. Project present with no key file succeeds. Model default is `gemini-3.8-flash`. Location default is `global`. `google/` prefix strips. `go test -run TestLoadMissingRequiredSecrets|TestGoogleProjectIsRequired|TestLoadDefaults|TestLoadStripsGoogleModelPrefix` passed. |
| Offline tests, race, format, vet | Intact. Poisoned env still passes. | `go test ./internal/gemini/ ./internal/cost/ ./internal/config/ -count=1` passed. `-race` passed. The same tests passed with `GEMINI_MODEL=poison`, `HTTPS_PROXY=http://127.0.0.1:1`, and `GOOGLE_APPLICATION_CREDENTIALS=/no/such/file.json`. `gofmt -l` on owned Go files printed nothing. `go vet` passed with and without `-tags live`. `go build ./...` and `go test ./...` passed. Getenv and `.env` readers in this package live in live-tagged files plus the config loader. |
| Prompt bytes, emotion, temperature | Intact. Segmentation is 658 bytes and matches Python. Normal and shorter match the Python f-strings. Fuller diagnoses the empty slot. | Independent extract of the Go const and the Python triple quote. Both measure 658 bytes and compare equal. Rendered translation prompts compare equal at 509, 479, and 529 bytes. Fuller names `7.1 seconds (7110 ms)`, says `too empty`, demands complete natural phrasing, and bans padding. Production translation sets `Temperature: genai.Ptr(float32(0.3))`. The prompt carries `Speaker emotion`. Tests fail if that temperature is missing. |
| ResponseSchema and InlineData | Intact. | `Segment` sets `ResponseMIMEType` to `application/json` and a non-nil array `ResponseSchema` with emotion required. Media goes through `genai.NewPartFromBytes`, which sets `Part.InlineData`. `TestSegmentSuccess` asserts both. The httptest body includes `responseMimeType` and `responseSchema`. |
| Live probe source | Intact. Probes force the project model, unset the API key, skip on missing ADC, and import no oauth2. | Read of `segment_live_test.go` and `translate_live_test.go`. Model is `config.DefaultGeminiModel` (`gemini-3.8-flash`). Location is `config.DefaultGoogleLocation` (`global`). `os.Unsetenv("GEMINI_API_KEY")` runs after `loadDotEnv`. Skip path uses `isADCUnusable` and names ADC. No `2.5-flash` and no `google/` override remain under `internal/gemini`. Live files do not import oauth2. Segmentation sends `testdata/clip.mp4` as `video/mp4`. I did not re-run Vertex. See Live Evidence. |
| Spec files | Intact. | `AGENTS.md` names `google.golang.org/genai` and ADC. It no longer names oauth2. `project.md` names the SDK and ADC. `infrastructure.md` records an attached service account, the metadata token, no key file, and no credential in an image layer. T2.1 **Done when** now asks for structure plus a drift table, not an exact `segments.json` match. T2.2 **Done when** bills from `UsageMetadata`. The T2.2dev handoff names the T1.2 cost amendment. `.env.example` carries bare `GEMINI_MODEL=gemini-3.8-flash`, no API key, and no `VERTEX_OPENAPI_BASE_URL`. |
| Owns | Intact. | `git diff --name-only HEAD` lists exactly the 16 owned paths. No extra path. No untracked residue from this review except this file after write. Scratch copies lived under `/tmp` and `/home/nryn/t22dev-rev1-pins` and were deleted. |
| Docs style on implementer prose | Intact. | Owned comments and the new markdown have no semicolons in prose, no em dashes, and no comment sentence over 30 words. Comments that describe billing match the token behaviour. |

## Live Evidence

I did not re-run a Vertex `generateContent` call. Spend stays outside this offline review. I did not treat the implementer transcript as a pin.

I pinned the probe source as required. Both live tests force `gemini-3.8-flash` and `global`. They unset `GEMINI_API_KEY`. They construct the production ADC client. They print prompt and candidate token counts.

I did run the skip path. `GOOGLE_APPLICATION_CREDENTIALS=/no/such/file.json` and `GEMINI_API_KEY` empty. `TestSegmentLive` and `TestTranslateLive` skipped in 0.00s with `application default credentials unusable: failed to find default credentials: open /no/such/file.json: no such file or directory`. No Vertex call ran.

No false positives this round.
