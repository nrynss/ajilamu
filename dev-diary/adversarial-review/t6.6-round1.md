# T6.6 adversarial review, round 1

Task: Editor agent on ADK. `internal/agent` builds the agent on `google.golang.org/adk/v2`.
The agent reads the ledger through a self-hosted mcp-clickhouse server and writes nothing.
`internal/cost` gains `ChargeAgent`. `internal/config` reads five optional MCP settings.
`deploy/mcp-clickhouse/` builds the read-only server image.

Reviewed against HEAD `df9afdd` plus the uncommitted working-tree diff. Go 1.27.1,
Docker 29.7.2, Docker Compose 5.5.1, Python 3.14.7, unshare from util-linux 2.42.3.

## Verdict

**REMEDIATE**

| Severity | Count |
|---|---:|
| C | 0 |
| H | 1 |
| M | 0 |
| L | 1 |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| H1 · `internal/agent/agent_live_test.go` lines 86 to 91 | The probe asserts only that the INSERT returns an error. The deploy sets `CLICKHOUSE_ALLOW_WRITE_ACCESS=false`. Upstream `run_query` then sends `readonly=1` on every query. ClickHouse rejects an INSERT in that mode, so the write flag alone produces the error. The probe fails even when `mcp_readonly` holds INSERT. It cannot confirm the grant is the boundary, which the done condition requires. | I fetched `mcp-clickhouse==0.6.0` from the PyPI JSON API and read the wheel. `build_query_settings` in `mcp_clickhouse/mcp_server.py` returns `{"readonly": "1"}` when `allow_write_access` is false. `execute_query` passes that dict to `client.query`. ClickHouse documents that `readonly=1` rejects writes with `Cannot execute query in readonly mode`. The probe never reads grants. | Revert the fix and a green probe would also pass with a write-capable database user. The security boundary stays unproven. |
| L1 · `dev-diary/PHASE-6-editing.md` lines 367 to 369 | The handoff names `web/src/routes/d/[id]/tabs/DetailsTab.svelte`, which does not exist. The file is `web/src/lib/tabs/DetailsTab.svelte`. The same sentence says no task owns it. T5.5 owns `web/src/lib/tabs/` and is done. The next agent follows a dead path and a false ownership claim. | `find web/src -name DetailsTab.svelte` returns only `web/src/lib/tabs/DetailsTab.svelte`. `dev-diary/PHASE-5-workspace.md` line 98 lists `web/src/lib/tabs/` in T5.5's owns line. T5.5 reads `done`. | Revert the correction and the next agent searches a path that does not exist. It may also treat a done task's directory as an unowned blind spot. |

## Residue against prior rounds

T6.6 has no prior round. Round 1 is the first review, so no earlier finding exists to
re-measure. The residue is empty.

## Done condition

**Clause 1 holds.** `go.mod` line 8 requires `google.golang.org/adk/v2`. Only `internal/agent`
depends on it, including its test binary.

```text
$ go list -test -f '{{.ImportPath}} {{.Deps}}' ./... | grep 'google.golang.org/adk/v2' | sed 's/ .*//' | sort -u
github.com/nrynss/ajilamu/internal/agent
github.com/nrynss/ajilamu/internal/agent.test
```

**Clause 2 holds.** The offline suite passed with the network unshared. It builds the toolset
against an in-memory fake MCP server.

```text
$ unshare -rn go test ./internal/agent/ -count=1 -v
--- PASS: TestToolsetNarrowsToReadTools (0.00s)
--- PASS: TestNewToolsetRejectsBadConfig (0.00s)
--- PASS: TestNewToolsetAcceptsHTTPAuth (0.00s)
--- PASS: TestAgentTurnEmitsChargeAgent (0.00s)
--- PASS: TestAskRejectsEmptyInput (0.00s)
--- PASS: TestNewFromConfigDisablesWithoutMCP (0.00s)
ok  	github.com/nrynss/ajilamu/internal/agent	0.011s
```

No socket opens. Go 1.27's `net.Pipe` builds a channel pair and no file descriptor. I read
`net/pipe.go`. The MCP SDK's `NewInMemoryTransports` calls `net.Pipe`.

**Clause 3 does not hold.** No mcp-clickhouse container is reachable. `docker ps -a` lists no
such container, and no `CLICKHOUSE_MCP_URL` is set. The probe compiles behind `//go:build live`
and skips without the two variables. The handoff records the missing transcript honestly. The
task cannot claim this clause as verified without a transcript. The probe itself is committed
and compiles.

**Clause 4 does not hold.** Finding H1. The probe proves the write flag blocks an INSERT. It
does not prove the `mcp_readonly` grant is the boundary.

**Clause 5 holds.** `TestAgentTurnEmitsChargeAgent` passes. The charge carries the 1200 prompt
and 340 candidate tokens the fake response reported. `RateCard` holds `AgentPerPromptToken` and
`AgentPerCandidateToken` at 150 and 600 nanodollars. `cost.ChargeAgent.String()` returns
`agent`.

**Clause 6 holds.** I started the compiled binary with no MCP settings on port 18099. It logged
`Ajilamu listening`. `GET /api/dubs` returned 200 with the fixture project. `GET /api/agent`
returned 404. `NewFromConfig` returns a nil agent when `MCPConfigured` is false, and no route
constructs the agent. The `.env.example` file carries the five MCP variables. The transport and
host list make the server listen rather than fall back to stdio. The bearer token stays empty by
policy. Upstream refuses HTTP transport without one auth mode, so a filled token starts the
server.

**Clause 7 holds.** `dev-diary/infrastructure.md` lines 69 to 71 name the `mcp_readonly` SELECT
grant as the security boundary and the flag as an accident guard. `dev-diary/PHASE-8-ship.md`
line 56 reads as building the image. `docker compose config` exits 0. The Dockerfile pins
`mcp-clickhouse==0.6.0`, runs as the `mcp_readonly` OS user, sets
`CLICKHOUSE_ALLOW_WRITE_ACCESS=false`, and binds `127.0.0.1`. Compose connects as the
`mcp_readonly` database user and passes `CLICKHOUSE_READONLY_PASSWORD` rather than the writer
password.

**Wire kind and parity.** `internal/api/wire.go` line 55 defines `ChargeAgent = "agent"`.
`web/src/lib/types.ts` line 25 adds `"agent"` to the union. `isChargeKind` in
`internal/api/wire_test.go` lines 576 to 583 accepts it. `TestChargeKindGuardCoversWireConstants`
and `TestChargeKindParity` pass. `testdata/wire/charge.json` still decodes as a translate
example in `TestDubTotalReconcilesCharges`.

**Build and audit.** `go build ./...` exits 0 with no output. `python3 tools/audit_docs.py`
exits 0 and prints `No drift. Docs and repository agree.`

## Scope and Evidence

I changed no implementation file. My only write is this review file. Every scratch tree lived
under `/tmp`. I stopped the one server I started.

### What I read

- The T6.6 block in `dev-diary/PHASE-6-editing.md` lines 142 to 236 and the handoff entry lines
  332 to 374.
- `internal/agent/agent.go`, `tools.go`, `agent_test.go`, and `agent_live_test.go` in full.
- The diff for `internal/cost/cost.go`, `internal/cost/cost_test.go`, `internal/config/config.go`,
  `internal/config/config_test.go`, `internal/api/wire.go`, `internal/api/wire_test.go`,
  `web/src/lib/types.ts`, `go.mod`, `go.sum`, `tools/audit_docs.py`, `dev-diary/infrastructure.md`,
  and `dev-diary/PHASE-8-ship.md`.
- `deploy/mcp-clickhouse/Dockerfile` and `compose.yaml`, `.env.example`, and
  `web/src/lib/tabs/DetailsTab.svelte` lines 51 to 53.
- The extracted `mcp-clickhouse==0.6.0` wheel, in particular `mcp_server.py`, `mcp_env.py`,
  `http_security.py`, and `main.py`.
- Go 1.27's `net/pipe.go`.

### Observers

The import query is `go list -test -f '{{.ImportPath}} {{.Deps}}' ./...`. The offline suite ran
under `unshare -rn`, which gives the process a network namespace with no interfaces. The server
smoke test used the compiled binary, a loopback curl, and a project-scoped process handle. The
compose check is `docker compose -f compose.yaml config`. The upstream check is the PyPI JSON API
and the wheel contents.

### Scope note

`cmd/ajilamu/main.go` and `internal/api/server.go` carry T7.4a settings-store work in this
working tree. T6.6 does not own those paths. I excluded them from this review.

## What I did not file

- **The commit read has no row assertion.** The probe logs the SELECT result and never requires a
  row. Its default `AJILAMU_LIVE_DUB_ID` is the wire fixture id, and the fixture load records the
  manifest id. The probe allows an override, and the handoff documents the missing title column.
- **`internal/ledger/takes.go` silently drops a `ChargeAgent` charge.** The switch maps three
  kinds and has no `ChargeAgent` case. No current path builds a take with one. The handoff names
  the switch and the future task that must extend it.
- **`DetailsTab.svelte` labels an agent charge as a voice render.** No wire path emits an agent
  charge. The handoff names the file and the label, apart from the path error in L1.
- **The table check is a substring.** The probe requires `commits` in the `list_tables` output
  and does not name every ledger table. I treat that as sufficient for the clause.
- **`go.mod` gains a direct require on `github.com/modelcontextprotocol/go-sdk`.** The transport
  seam needs that type. The task spec designs the seam, so I treat the require as intended.
