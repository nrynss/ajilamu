# T3.4 round 1 review

Verdict: APPROVE

I reviewed the uncommitted T3.4 export against the assembler specification.
I read AGENTS.md, the task, diary conventions, observations.md, and the project.md mux notes.
No prior T3.4 review rounds exist.
I own only this review file. The Export shape exists. I can validate this task independently.
I did not treat the implementer report as evidence.

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 0 |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| None | No findings at any severity. | Independent artifact measurements below. | Adding `-shortest` or dropping `-c:v copy` must fail the duration and packet pins. |

## Prior-round residue

No prior T3.4 rounds exist. No residue remains.

## Independent measurements

I own only this review file. The required API shapes exist. I can validate this task independently.

I copied the production assembler and media files into a temporary module outside the repository.
A small driver called `BuildBed`, `Place`, `Overlay`, `Duck`, and `Export`.
The driver supplied artifacts for external measurements. Its success messages supplied no evidence.

Every measurement selected `/opt/homebrew/bin/ffmpeg` and `/opt/homebrew/bin/ffprobe`, both version 9.0.1.
`PATH=/opt/homebrew/bin:/usr/bin:/bin`. Go reports version 1.27.1.

The implementer owns `internal/assemble/export.go` and `internal/assemble/export_test.go`.
The PHASE-3 handoff belongs to the task. No other production path changed.

### Video packets are copied

`testdata/clip.mp4` is h264 `avc1` with 4496 packets.
Both named exports match that codec, tag, frame count, and packet list.
A 16 kHz mono mix onto the same film still copies those packets.

The packet dump is `pts,dts,size,flags`. Its SHA-256 is
`db5d834e60e860e51b8fbf1232469352ac71da1c331077b95793667217b048d1`
on the source and on every testdata export.

The first three packets are `0,-2002,914,K__`, then `2002,-1001,742,___`, then `1001,0,38,___`.
The last three packets match as well. Re-encoding would change those sizes.

### Duration follows the video

| Artifact | Format duration (s) | Video packets | Audio rate | Channels | Layout |
|---|---|---|---|---|---|
| `testdata/clip.mp4` | 75.008267 | 4496 | 44100 | 2 | stereo |
| `dubbed_replaced.mp4` | 75.008267 | 4496 | 44100 | 2 | stereo |
| `dubbed_ducked.mp4` | 75.008267 | 4496 | 44100 | 2 | stereo |
| 16 kHz mono mix export | 75.008267 | 4496 | 44100 | 2 | stereo |

Each testdata export lasts 75.008267 s. That is 0.000000 s from the source.
The window is inside 10 ms. The files last about 75.0 s.

A 0.5 s mix on a 2.000000 s film still lasts 2.000000 s.
Its 60 video packets match the film. `-shortest` would have cut the file to 0.5 s.
A 2.5 s mix on the same film also lasts 2.000000 s. The extra audio is cut.

### Audio keeps film format

Source audio is AAC, 44100 Hz, stereo, 132532 bps.
Replaced audio is AAC, 44100 Hz, stereo, 131689 bps.
Ducked audio is AAC, 44100 Hz, stereo, 130884 bps.

The old clean export was 16000 Hz, mono, 38 kbps.
A 16 kHz mono mix still encodes at 44100 Hz stereo. The film format wins.

### Tail 54.5 s to 75 s is not silence

Decoded PCM uses `atrim` plus `pcm_f32le` at 44100 Hz stereo.
Each tail window holds 1808100 samples.

| Artifact | RMS | RMS (dB) | Versus source (dB) |
|---|---|---|---|
| source | 0.170700318 | -15.355 | 0.000 |
| replaced export | 0.169994105 | -15.391 | -0.036 |
| ducked export | 0.169947340 | -15.394 | -0.038 |
| bed mix | 0.170700318 | -15.355 | 0.000 |
| replaced mix | 0.170700318 | -15.355 | 0.000 |
| ducked mix | 0.170700318 | -15.355 | 0.000 |

Both exports stay inside 0.5 dB of the source. AAC accounts for the 0.036 dB gap.
`volumedetect` reports -15.4 dB mean on the source and on both exports.
`silencedetect` at -60 dB for 1 s finds no silence in this window.
The old clean export measured -91.0 dB mean here.

### Dialogue windows hold speech

| Window (s) | Source RMS | Replaced RMS | Ducked RMS |
|---|---|---|---|
| 8.5 to 12.5 | 0.208740880 | 0.230593499 | 0.140088348 |
| 39.5 to 44.0 | 0.176475653 | 0.192361781 | 0.121987168 |

Neither export is digital silence. Replaced speech sits above the source.
Ducked speech stays well above a silent floor.

### Bed survives outside speech

Seconds 0 to 5.5 measure -0.057 dB versus the source on both exports.
Seconds 35 to 38 measure -0.054 dB replaced and -0.044 dB ducked.
Those gaps still carry soundtrack. They are not a silent canvas.

### Names and failed muxes

The written files are `dubbed_replaced.mp4` and `dubbed_ducked.mp4`.
Neither name contains `clean`. Constants match those strings.

A missing mix leaves the existing output as the 15-byte marker `previous render`.
No `.assemble-*` file remains in the fail directory or the export tree.
Empty video, audio-only video, and output aliases are rejected.
A path with spaces still muxes. That export lasts 2.000000 s with 60 frames.

### Passing checks

- Fixture exports last 75.008267 s with 4496 copied video packets.
- Audio is 44100 Hz stereo AAC, not 16 kHz mono.
- Tail RMS stays within 0.04 dB of the source.
- Dialogue windows carry speech energy.
- Names are `dubbed_replaced` and `dubbed_ducked`.
- Failed muxes stay private. Temporary files do not leak.
- Owned comments stay inside the documentation style rules.

The shipped testdata test allows a 2 dB tail delta.
Independent measurement is 0.036 dB, so that looser pin did not hide a miss.
Temporary review artifacts were removed after measurement. No production code changed during review.
