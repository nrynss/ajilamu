# T4.7 adversarial review, round 1

Verdict: REMEDIATE

Counts: C 0, H 1, M 0, L 0.

## Scope and evidence

I reviewed `internal/ledger/priors.go` against T4.7, T4.2, and the frozen `take_rates`
schema contract. The query reads `take_rates`, not `takes_raw`. The view applies `FINAL`
and filters zero measured durations, so its aggregate count is a deduplicated valid-rate count.

I used a temporary local HTTP capture harness and removed it after the measurement. It sent
three controlled responses through `Client.DurationPrior`. A creator with zero samples received
the exact population rate of 2. A creator with ten samples at rate 4 received 3, with weight
0.5. At 100 samples, both the creator weight and blended rate increased while the rate remained
below 4.

The capture recorded a POST query containing only ClickHouse placeholders for owner, language,
and speaker. The owner value `o' OR 1=1 --` appeared only as `param_owner_id`. The query kept
the database parameter and filters on language and speaker. Empty input, empty response,
malformed JSON, a zero population count, and HTTP 500 each returned an error.

`gofmt -d internal/ledger/priors.go`, `go vet ./internal/ledger`, and `git diff --check --
internal/ledger/priors.go` passed. The broad package suite currently fails in the concurrently
edited T4.2 peak assertion. That failure is outside T4.7 and does not affect this verdict.

## Findings

| # | Sev | Where | What | Pin | Mutation |
|---|---|---|---|---|---|
| 1 | H | `dev-diary/PHASE-4-ledger.md`, T4.7 `owns` line | T4.7 owns no test path. The measured query binding, deduplicated sample counts, zero-history fallback, growing personal weight, and response errors have no committed regression pin. Extend the contract to own `internal/ledger/priors_test.go` and preserve the local HTTP capture as a test. | `go test ./internal/ledger -run TestDurationPriorCapture` must inspect the actual request and controlled responses. It must pin bound owner, language, and speaker parameters, report both counts, prove zero-history population fallback, prove greater personal influence with more history, and reject malformed or non-success responses. | Replace a placeholder with interpolated input, remove a filter, return an unreported sample count, reverse the weight trend, or accept malformed ClickHouse output. CI remains green without a maintained capture test. |

## Residue

There are no other findings. T4.7 cannot receive APPROVE until the persisted artifact pin
closes finding 1 and a fresh reviewer verifies it.
