# T1.4a remediation, round 1

I fixed the one finding in `t1.4a-round1.md`. I did not change that review's verdict. My writes
are `internal/api/wire_test.go` and this record.

| Finding | Fix | Pin | Residue |
|---|---|---|---|
| M1. `TestEverySharedStructHasExample` and `TestGoTSParity` take their type set from the hand-maintained `mirroredTypes` list. A new shared struct registered nowhere escaped both guards. | Added `parseWireStructs`, which reads `wire.go` and extracts every exported `type X struct` line. Added `TestEveryWireStructIsRegistered`, which asserts each parsed name appears in `mirroredTypes` and has a `wireExamples` entry. | `TestEveryWireStructIsRegistered` passed in the working tree and in a `/tmp/rem-t14a` copy. Three mutations failed it with the exact messages below. | None. The change adds a test only. It touches no production code and no fixture. |

## Mutation checks

I copied the module to `/tmp/rem-t14a`. I changed one file at a time and ran
`go test -count=1 ./internal/api`. I restored the copy after every run. The restored copy passed.

| Mutation | Exact failure |
|---|---|
| Deleted `TimelineEntry{}` from `mirroredTypes`. | `--- FAIL: TestEveryWireStructIsRegistered` with `wire_test.go:236: wire.go struct TimelineEntry is absent from mirroredTypes`. The package reported `FAIL`. |
| Added an exported `RemProbe` struct to `wire.go`, registered in no list. | `--- FAIL: TestEveryWireStructIsRegistered` with `wire_test.go:237: wire.go struct RemProbe is absent from mirroredTypes` and `wire_test.go:240: wire.go struct RemProbe has no wireExamples entry`. |
| Commented out the `timeline_entry.json` entry in `wireExamples`. | `--- FAIL: TestEverySharedStructHasExample` with `wire_test.go:174: shared struct TimelineEntry has no example payload`, and `--- FAIL: TestEveryWireStructIsRegistered` with `wire_test.go:240: wire.go struct TimelineEntry has no wireExamples entry`. |

## Validation

`go test -count=1 ./internal/api` in the working tree reported:

```
ok  	github.com/nrynss/ajilamu/internal/api	1.340s
```

`gofmt -l internal/api/wire_test.go` printed nothing. `git diff --stat` over `internal/api/wire.go`
and `web/src/lib/types.ts` is empty. `testdata/wire/` is unchanged.
