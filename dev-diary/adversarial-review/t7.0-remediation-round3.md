# T7.0 Round 3 Remediation

| Finding | Remediation | Independent pin |
|---|---|---|
| 6 | `Server.Shutdown` now drains HTTP only. The new `Server.FlushLedger` flushes on its own deadline and runs even after a failed drain. `cmd/ajilamu/main.go` splits `shutdownTimeout` into `drainTimeout` (10s) and `flushTimeout` (5s), so a slow request cannot starve the flush. A failed flush still logs the pending count, and a finished flush logs completion. `TestShutdownFlushesLedgerWhenDrainExpires` holds a request in flight, lets the drain deadline expire, and requires the flush to run. | Port 47403, production binary, one seeded queue entry, dead ClickHouse on port 47499. `curl --limit-rate 200k` pulled the 18385109 byte clip, and SIGTERM landed three seconds in. The drain expired at 10.1 seconds and logged `HTTP drain did not finish`. The flush then ran and logged `ledger flush timed out or failed` with `pending=1`. The queue file survived on disk. Exit code 1. |
| 7 | `NewServer` refuses to build a production server when no frontend root exists. Development still mounts `FrontendUnavailable`. `TestProductionServerRequiresFrontendBuild` pins both sides. | From a directory holding only a `testdata` symlink, `ENV=production` with full credentials exited 1 and printed `ENV=production requires a frontend build: set AJILAMU_FRONTEND_DIR or run npm --prefix web run build`. |
| 8 | The loader reads `AJILAMU_FRONTEND_DIR` into `Config.FrontendDir`, mirroring `AJILAMU_DATA_DIR`. The entrypoint prefers the setting and falls back to discovery beside the working directory. The T0.2 block names the new variable. Config tests pin the empty default and a custom value. | From `/tmp/t70rem3/empty`, production booted with `AJILAMU_FRONTEND_DIR=/tmp/t70rem3/live/web/build` on port 47402. `/` and `/new` returned 200. A range request on `/clip.mp4` returned 206 with `Content-Range: bytes 0-63/18385109`. SIGTERM gave exit 0. |
| 9 | `/api/ledger/ready` now pings `scheme://host:port/ping` from the config values under a three second client bound. It answers 503 when the ping fails or returns non-2xx. The route still names the missing variable first. `TestLedgerReadyProbesClickHouse` covers a live ping and a dead port. | Port 47405 with `CLICKHOUSE_HOST=127.0.0.1` and a dead port returned 503 `ClickHouse did not answer a ping.` while `/api/healthz` stayed 200. Port 47406 against a live `/ping` responder returned 200 `{"status":"ready"}`. |
| 10 | The entrypoint passes `api.ConfigHandler(nil)`. The documented 503 fires until T7.4a lands persistence. | On port 47407, a POST of `voice_key` and `translation_key` returned 503 `Settings are unavailable.` Neither value appears in the process log. |

## Mutation checks

I reintroduced each defect, measured it, then restored the fix and re-ran the
suite. Restored files match their fixed copies byte for byte.

- Finding 6: reverting `main.go` to one shared 15 second context with an early
  return reproduced the reviewer's pin. SIGTERM with the slow request in flight
  blocked 15 seconds, exited 1, and logged only
  `Ajilamu stopped error="context deadline exceeded"`. Zero `flush` or
  `pending` lines.
- Finding 7: deleting the production gate made
  `TestProductionServerRequiresFrontendBuild` fail with
  `NewServer in production without a frontend = <nil>`.
- Finding 8: making the entrypoint ignore `AJILAMU_FRONTEND_DIR` turned `/`
  back to 503 on port 47412 despite the setting.
- Finding 9: deleting the probe call made `TestLedgerReadyProbesClickHouse`
  fail with `ledger readiness against dead ClickHouse = 200, want 503`.
- Finding 10: restoring the erroring stub made the same POST return 500
  `Settings could not be saved.`

## Other measurements

`go test -count=1 ./internal/api ./internal/config ./cmd/ajilamu` passes.
`go vet` is clean on the three packages. `gofmt -l` reports nothing on the
owned files. `python3 tools/audit_docs.py` prints "No drift" and exits 0.

Residue against findings 1 through 5, re-measured on the rebuilt binary:

- Finding 1: `git check-ignore -v cmd/ajilamu/main.go` exits 1 and prints
  nothing. `git check-ignore -v ajilamu` prints `.gitignore:8:/ajilamu`.
- Finding 2: under `env -i` with no credentials on port 47407, the readiness
  route returned 503 naming `CLICKHOUSE_HOST`, health stayed 200, and SIGTERM
  gave exit 0. The log holds only the listening line.
- Finding 3: a multipart POST with a 200000 byte video and a 50000 byte music
  file returned 201 JSON. Both files landed at mode 0600 with SHA-256 digests
  matching my inputs, `6d6bd789...` and `5a05133d...`.
- Finding 4: the live upload wrote under `data/`, and `git check-ignore -v`
  on the stored video printed `.gitignore:9:/data/`. I removed the directory
  afterwards.
- Finding 5: `GET /api/nope`, `GET /api/dubs/new`, and `POST /api/healthz`
  returned 404 text. `GET /api/healthz` and `GET /api/dubs` returned 200 in
  the same run.

Every server I started received SIGTERM and exited. `lsof` reports no listener
left on ports 47401 through 47412. I never bound 8080, and I signalled built
binaries rather than `go run`.

## Scope

I edited only owned paths: `internal/api/server.go`,
`internal/api/server_test.go`, `internal/config/config.go`,
`internal/config/config_test.go`, `cmd/ajilamu/main.go`, the T0.2 block of
`dev-diary/PHASE-0-ground.md`, and this record. `.gitignore` needed no change
this round. The verdict stands, and the task status stays claimed.
