# T7.5a adversarial review, round 2

Task: Ledger workspace reads. Deliver the takes with their fit and itemized charges, the
whole-pass charges, the running total, the language list, and the project metadata that the
wire `Dub` needs.

Reviewed against the uncommitted working tree at commit `0a6f3aa`. Go toolchain go1.27.1. I
did not implement T7.5a and I fixed nothing. My only write is this review file. A clean copy
of the module lives at `/tmp/t75a-r2`. A mutation copy lives at `/tmp/t75a-r2-mut`. ClickHouse
26.8.2.7 lives at `/tmp/clickhouse-common-static-26.8.2.7`.

## Verdict

**APPROVE**

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 0 |

## What changed since round one

Round one filed one H against a charge-less take. The remediation added
`Charges: []api.Charge{}` to the `api.Take` literal in `buildLanguageTracks`. It also added
`TestWorkspaceTakesChargeLessTakeMarshalsEmptyCharges`.

A diff of the round-one scratch `workspace.go` against the working file shows one hunk. The
hunk holds the new literal and its comment. No query changed and no other field changed.

## Charge-less take measurement

I drove `WorkspaceTakes` over a stand-in. The stand-in serves one take and no charge row. The
read returned a non-nil empty `Charges` slice. The exact marshalled bytes are:

```json
{"file":"a.wav","voice":"voice-a","attempt":1,"repair":"none","fit":{"slot_ms":1820,"measured_ms":1680,"delta_ms":-140,"state":"fits"},"charges":[]}
```

The full track marshals `"lines":[...]`, `"takes":[...]`, and `"charges":[]`. Nothing in the
take is null.

## Mutation

I removed `Charges: []api.Charge{}` in `/tmp/t75a-r2-mut`. The same drive then marshalled:

```json
{"file":"a.wav","voice":"voice-a","attempt":1,"repair":"none","fit":{"slot_ms":1820,"measured_ms":1680,"delta_ms":-140,"state":"fits"},"charges":null}
```

The shipped pin failed in the mutation copy.

```
workspace_test.go:352: marshalled take = {"file":"a.wav","voice":"voice-a","attempt":1,"repair":"none","fit":{"slot_ms":1820,"measured_ms":1680,"delta_ms":-140,"state":"fits"},"charges":null}, want charges as an empty array
workspace_test.go:355: marshalled take = {... "charges":null}, want no null charges
workspace_test.go:362: decoded charges = nil from {... "charges":null}, want a non-nil empty slice
--- FAIL: TestWorkspaceTakesChargeLessTakeMarshalsEmptyCharges (0.00s)
```

The pin rejects a missing empty array, a null, and a nil decoded slice. The fix is load
bearing.

## Sweep of the same defect class

I checked every slice the six statements return. The six statements live behind five exported
reads. `WorkspaceTakes` issues two statements and returns one track list. I measured each shape
by marshalling the read result.

| Statement | Read | Slice checked | Marshalled shape | Wire contract |
|---|---|---|---|---|
| `selectWorkspaceTakes` | `WorkspaceTakes` | outer `[]api.LanguageTrack` | `[]` | `LanguageTrack[]` |
| `selectWorkspaceTakes` | `WorkspaceTakes` | `LanguageTrack.Lines` | `[]` | `Line[]` |
| `selectWorkspaceTakes` | `WorkspaceTakes` | `Line.Takes` | `[{...}]` | `Take[]` |
| `selectWorkspaceTakes` | `WorkspaceTakes` | `Take.Charges` | `[]` | `Charge[]` |
| `selectWorkspaceTakes` | `WorkspaceTakes` | `Take.Peaks` | absent | `number[]?` |
| `selectWorkspaceTakeCharges` | `WorkspaceTakes` | `Take.Charges` | `[]` | `Charge[]` |
| `selectWholePassCharges` | `WholePassCharges` | `[]api.Charge` | `[]` | `Charge[]` |
| `selectRunningTotal` | `RunningTotal` | none | no slice | none |
| `selectWorkspaceLanguages` | `Languages` | `[]string` | `[]` | `string[]` |
| `selectProjectMetadata` | `ProjectMetadata` | `DubSummary.Languages` | `[]` | `string[]` |

No slice marshals null where the wire declares an array. `Take.Peaks` carries the `omitempty`
tag, so an empty waveform is absent rather than null. The wire marks that field optional.

`ProjectMetadata` builds its language list with `slices.Clone`. A nil row would clone to nil. I
ran statement six on ClickHouse 26.8.2.7 with no rows. It returned:

```
{"commit_count":0,"created_ms":0,"updated_ms":0,"languages":[]}
```

`groupArray` answers `[]` and never null. The clone therefore stays non-nil.

## Query identity

I compared the six statement constants byte for byte against `/tmp/t75a-queries.sql`. That file
is the round-one copy of the statements. All six matched. A scratch test named
`TestZZR2QueriesUnchanged` passed. The full diff of the round-one scratch `workspace.go`
against the working file holds one hunk. No query changed.

## Pinned settings

I logged every request from my drive. All six statements carried
`output_format_json_quote_64bit_integers=0` and `max_recursive_cte_evaluation_depth=5000` and
`param_dub_id=d1`. `queryClickHouse` in `internal/ledger/client.go` sets both settings.
`TestOnlyClientPinsServerSettings` passes.

## Validation

```
go test -count=1 ./internal/ledger
ok  	github.com/nrynss/ajilamu/internal/ledger	1.008s
```

The shipped pin also passed alone.

```
--- PASS: TestWorkspaceTakesChargeLessTakeMarshalsEmptyCharges (0.00s)
    workspace_test.go:350: marshalled take = {"file":"a.wav","voice":"voice-a","attempt":1,"repair":"none","fit":{"slot_ms":1820,"measured_ms":1680,"delta_ms":-140,"state":"fits"},"charges":[]}
```

## Findings

No finding survives round two. The sweep found no slice that marshals null where the wire
declares an array.

| Where | What | Pin | Mutation |
|---|---|---|---|
| none | none | none | none |

## Residue against round one finding 1

| Where | What | Pin | Mutation |
|---|---|---|---|
| `internal/ledger/workspace.go`, the `api.Take` literal in `buildLanguageTracks` | Round one filed H. The take carried a nil `Charges` slice, so the wire carried `"charges":null`. The remediation sets `Charges: []api.Charge{}`. The take now marshals `"charges":[]`. | `TestWorkspaceTakesChargeLessTakeMarshalsEmptyCharges` drives a charge-less take. It logs `"charges":[]`, rejects `"charges":null`, and requires a non-nil decoded slice. It passes. | I removed the fix in `/tmp/t75a-r2-mut`. The take marshalled `"charges":null` and the pin failed. |

**Residue: zero.** Round one finding 1 is closed. The fix is present and load bearing.

## What I did not file

The round-one non-filed notes still stand. This round searched only the null-array defect class.
The sweep found no new instance.
