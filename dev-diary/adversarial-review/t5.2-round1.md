# T5.2 Round 1 Review

## Verdict

APPROVE

| Severity | Count |
|---|---:|
| C | 0 |
| H | 0 |
| M | 0 |
| L | 0 |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| None | No defects found. | The independent measurements below observe the component and public asset. | Not applicable. |

## Independent measurements

I own only this review file. The typed component and fixture shapes exist. I can validate this task alone.

| Check | Independent observation |
|---|---|
| Type check and production build | `npm run check` completed with zero errors and warnings. `npm run build` completed successfully. |
| Picture geometry | `ffprobe` measured the emitted `.svelte-kit/output/client/clip.mp4` at 1280 by 720 with display aspect ratio 16:9. Preview CSS also fixes the frame at `aspect-ratio: 16 / 9`. |
| Public fixture bytes | The production preview server returned the full public clip with SHA-256 `da7e0212fdf5654a81ec2d00ec1422bff636d9d42eb201c62d0bbe7ce083be9e`. The repository fixture has the same hash. |
| Public byte range | A `Range: bytes=0-63` request to the production preview server returned HTTP 206, `Content-Range: bytes 0-63/18385109`, `Accept-Ranges: bytes`, and 64 bytes. Both response and fixture prefix hash to `6ea76788125e1b620735dc5c4caabe6166a387a8f8d9b318e04fe2ec4a810055`. |
| Picture source stability | The language-derived effect creates, seeks, plays, pauses, and clears only an `HTMLAudioElement`. It mutes or unmutes the existing video, but assigns neither `video.src` nor the `src` property. |
| Controls and language choices | The component templates contain labelled Play or Pause controls, a labelled range input with spoken position text, status errors, labelled language buttons, and `aria-pressed` state. |
| Space safeguards | The window handler rejects repeated, composing, modified, and editable-target Space presses. It also rejects inputs, textareas, selects, buttons, content-editable elements, and textboxes. Native button and range behavior remains intact. |
| Svelte and creator language | A source scan found no `export let` or legacy `$:` declarations. The owned UI strings avoid banned creator vocabulary, semicolons, and em dashes. |

## Contract boundary

T5.2 now owns `web/static/clip.mp4` alongside the two player components. The public symlink resolves to the frozen fixture and the production build emits it as a public asset.

There is zero residue against the T5.2 contract change. The required `bytes=0-63` request reaches the public clip with HTTP 206, a valid range header, and fixture-matching bytes.
