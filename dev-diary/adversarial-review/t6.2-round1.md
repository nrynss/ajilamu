# T6.2 Round 1 Adversarial Review

## Verdict

**REMEDIATE**

| C | H | M | L |
|---|---|---|---|
| 0 | 1 | 0 | 0 |

One H finding opens. The fee panel reports `$0.00` for lines 3 and 4. Their last take is an atempo repair that carries no charges. A re-render always synthesizes speech, so that number understates the real cost. Every other acceptance check holds under my own measurement.

## Scope and Evidence

I reviewed the T6.2 block, the seam expansion note, the T6.2 handoff entry, `web/src/lib/edit/Speaker.svelte`, and the T6.2 hunk of `web/src/routes/d/[id]/+page.svelte`. I changed no implementation. I wrote only this file. Every probe ran against a copy at `/tmp/t62review`, never the worktree.

### The harness

I copied the worktree to `/tmp/t62review`. I ran `npm --prefix web run check` there. I ran `npm --prefix web run build` there. I built `./cmd/ajilamu` and served the bundle on port 43477. I drove `/d/fixture` in headless Chromium through `playwright-core`. The browser recorded every request, every response, every console message, and every page error.

### Health checks, not pins

`npm --prefix web run check` reported 181 files, 0 errors, and 0 warnings. Neither changed file carries `export let`, a `$:` statement, or a store import. The page raised no console message and no page error.

### Pins

Speaker set. The panel offered exactly `Mark Vande Hei` and `Suni Williams` for all eight lines, in that sorted order. A probe program called `tts.Assign` for both names and got `ml-IN-Chirp3-HD-Achird` and `ml-IN-Chirp3-HD-Achernar`. The same program rejected an unknown name. The offered set matches `knownSpeakers`.

Billable calls. The page issued zero `/api` requests. The only traffic after load was repeated `GET /clip.mp4` for the preview video. No synthesis route exists yet, and T6.2 calls none.

Cancel and confirm. Both paths issued zero requests. All ten files under `testdata/takes/` matched their HEAD blobs by `git hash-object` after the run. The server data directory stayed empty.

Fees. I read the fee for all eight lines from the live panel. I summed the fixture charges myself. The table below states both numbers.

| Line | Last take | Charges on last take (nd) | Panel fee | Last charged take | Charges on that take (nd) |
|---|---|---|---|---|---|
| 1 | `seg_1_try1.wav` | 842100 | `$0.0008421` | `seg_1_try1.wav` | 842100 |
| 2 | `seg_2_try1.wav` | 2438300 | `$0.0024383` | `seg_2_try1.wav` | 2438300 |
| 3 | `seg_3_stretched.wav` | 0 | `$0.00` | `seg_3_try1.wav` | 2318200 |
| 4 | `seg_4_stretched.wav` | 0 | `$0.00` | `seg_4_try1.wav` | 3639200 |
| 5 | `seg_5_try1.wav` | 3459900 | `$0.0034599` | `seg_5_try1.wav` | 3459900 |
| 6 | `seg_6_try1.wav` | 2949700 | `$0.0029497` | `seg_6_try1.wav` | 2949700 |
| 7 | `seg_7_try1.wav` | 4751000 | `$0.004751` | `seg_7_try1.wav` | 4751000 |
| 8 | `seg_8_try1.wav` | 2948600 | `$0.0029486` | `seg_8_try1.wav` | 2948600 |

## Findings

| # | Sev | Where | What | Pin | Mutation |
|---|-----|-------|------|-----|----------|
| 1 | H | `web/src/lib/edit/Speaker.svelte` lines 33 to 36, with the basis label at 135 to 139 and the payload at 89 to 90 | Lines 3 and 4 show `$0.00`. Their last take is an atempo repair with an empty `charges` array. The last charged take is `seg_3_try1.wav` at 2318200 nd and `seg_4_try1.wav` at 3639200 nd. A speaker change synthesizes speech, so `$0.00` understates the render. The `SpeakerChange` payload also names the zero-charge take as the estimate source, which contradicts the field comment at line 10. | Headless Chromium on port 43477 read `data-fee="0"` and `$0.00` for lines 3 and 4. The basis read `seg_3_stretched.wav` and `seg_4_stretched.wav`. `testdata/wire/take.json` records that repair as `atempo` with `charges: []`. The prior takes carry 2318200 nd and 3639200 nd. | I patched a copy to sum the last take that carries charges, rebuilt, and reloaded. Line 3 moved from `$0.00` to `$0.0023182` and line 4 from `$0.00` to `$0.0036392`. Lines 1 and 7 stayed at `$0.0008421` and `$0.004751`. |

Suggested replacement for the estimate derivation:

```ts
  const lastTake = $derived(takes.at(-1))
  const estimateTake = $derived([...takes].reverse().find((take) => take.charges.length > 0) ?? lastTake)
  const estimatedNanodollars = $derived.by(() => (
    (estimateTake?.charges ?? []).reduce((sum, charge) => sum + charge.total_nanodollars, 0)
  ))
```

The basis paragraph at line 138 and the `takeFile` field at line 90 must name the same take. Replace `lastTake.file` with `estimateTake.file` in both places.

## Residue

Cancel and confirm leave the takes alone. I measured this, not the handoff claim. Both paths issued zero requests. All ten take files hashed to their HEAD blobs. The server wrote nothing to its data directory.

No billable call runs in the background. The page issued no `/api` request at any point. The confirm path applies route-local state only. T6.5a owns the route that will make the confirm billable.

The speaker set is safe. Both offered names pass `tts.Assign`. The panel derives the set from the payload, so it cannot invent a name the backend rejects.

Svelte 5 only holds. Both changed files use runes. Neither carries `export let`, a `$:` statement, or a store import. `npm --prefix web run check` passes with zero errors and zero warnings.

## What I did not file

The estimate sums the translate charge too. A speaker-only re-render may skip translation. The T6.5a contract does not promise that skip, so I do not file it.

A payload with an unknown speaker would offer it. The fixture carries only the two known names, so I do not file it.

The page adds an `.editor-panels` grid wrapper. The T6.1 Boundary panel and the new Speaker panel share one row at desktop width and stack at narrow width. That is layout, not behavior.

## Done condition

The done condition reads: "Changing a speaker displays the associated re-render fee and leaves original takes untouched if canceled." The cancel half holds. The fee half fails on lines 3 and 4. The done condition does not hold.

## Cleanup

I stopped the server and closed the browser. The worktree holds one new file from me, this review. The copy at `/tmp/t62review` carries the built bundle and stays outside the repository.
