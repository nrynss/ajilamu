# T5.4 Round 2 Review

## Verdict

REMEDIATE

| Severity | Count |
|---|---:|
| C | 0 |
| H | 1 |
| M | 0 |
| L | 0 |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| `web/src/lib/LengthBar.svelte:164-165`, `web/src/lib/LengthBar.svelte:181-196`, `web/src/lib/LengthBar.svelte:249-254` | **H, F2.** The remediation preserves the 7,110 ms trough scale, but the canvas ends at its parent width. An 8,000 ms active take paints 85.37 px beyond the trough, then canvas clipping hides the excess. The component therefore still fails to draw the complete overrun past the slot. | `lengthbar-overflow-width`. I rendered a separate audit page with two 698 CSS-pixel canvases, 8 px insets, identical 7,110 ms slots, and `referenceSlotMs: 7110`. Each trough ended at x=690, a 682 px span. The 8,000 ms take requires a 767.37 px take width and an 85.37 px overrun, ending near x=775. The exported rendered canvas pixel row instead ended at x=697. Only 7 px of red past the trough were visible before the 698 px canvas boundary clipped the other 78 px. | After the fix, reverting its canvas-width protection must fail a regression that exports a 7,110 ms reference bar beside an 8,000 ms active take. Otherwise a long take appears almost fitted and creators cannot see its real 0.89-second excess. |

## Independent evidence

I own only this review file. The required `Take`, `Fit`, and Svelte 5 shapes exist. I can validate this component independently.

I mounted the current component in a disposable local Vite page. It supplied paired 7,110 ms slots, an 8,000 ms active take, the T1.5 segment 8 short take, an atempo take, and a rewritten take. I measured browser canvas pixels and captured rendered screenshots. These are artifact observations, not component status messages.

| Check | Independent observation |
|---|---|
| Shared trough scale | The 7,110 ms reference bar and the 7,110 ms overlong bar each held a 682 px trough within 698 CSS px canvases. Round-one F1 has no residue. |
| Overrun width | F2 remains. The 8,000 ms take needs 85.37 px after its unchanged 682 px trough. The rendered pixel row stopped at x=697, which exposes only 7 px beyond the trough. |
| Segment 8 underrun | The 4,200 ms active take filled 403 px of the 682 px trough. The remaining 279 px used the `--short` color. Its accessible sentence reads that line 8 is 2.91 seconds short. |
| Atempo and ghost | The 5,338 ms atempo take showed diagonal hatching and the `107.5% speed` label. Its earlier 5,920 ms take remained visible as a faint ghost, including the ghost overrun. |
| Rewrite | The rewritten short take used solid fills and the `rewritten` label. It showed no atempo hatching. |
| Resize repaint | Changing the audit width from 698 to 348 CSS px redrew every bar at the new width. The clipped overrun remained clipped, which corroborates F2. |
| Theme repaint | Switching the rendered page from dark to light repainted the canvas fills into the light token palette. |
| Lifecycle and runes | Source inspection confirms cleanup for both observers, the resize listener, and the queued frame. The component uses `$props`, `$derived`, and `$effect`, with no legacy `export let`, `$:`, or Svelte store reactivity. |

`npm run check` completed with zero errors and warnings. `npm run build` completed successfully. These checks establish type and build health. They are not the pins for this review.

## Residue against prior rounds

Round-one F1 is fixed. Equal 7,110 ms slots now keep equal 682 px troughs even when one active take measures 8,000 ms. F2 prevents approval because the required overrun width remains visually truncated.
