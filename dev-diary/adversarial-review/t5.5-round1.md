# T5.5 Round 1 Review

## Verdict

REMEDIATE

| Severity | Count |
|---|---:|
| C | 0 |
| H | 2 |
| M | 2 |
| L | 0 |

## Findings

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| H | `web/src/lib/tabs/HistoryTab.svelte` | The History tab is a sequential list, not the required saved DAG. It never reads `parent_commit_id`, then draws every node to its next array neighbour. It will display false ancestry as soon as a saved history forks or arrives out of order. | `jq` finds six non-root parent edges in `testdata/wire/dub.json`. `rg parent_commit_id web/src/lib web/src/routes` finds the field only in `types.ts`, not in the renderer. | Reverting a parent-aware graph preserves the list while erasing or inventing saved relationships. |
| H | `web/src/lib/tabs/HistoryTab.svelte`, `web/src/lib/tabs/DetailsTab.svelte` | The workspace specification requires the saved DAG alongside what the system learned about the creator's voice. The current tabs show only an active take's voice name. They expose no learned voice information or sample size. The fixture and `Dub` contract have no field for it. Raise and land a contract change before rendering the required evidence. | The rendered source contains no learned-voice text or sample-count field. `web/src/lib/types.ts` defines `Take.voice` but no learned-voice shape. | Reverting the new learned-voice evidence leaves a seemingly complete history panel that omits a required decision aid. |
| M | `web/src/lib/Rail.svelte:43-53` | Inactive tabs receive `tabindex=-1`, and the rail supplies no arrow-key handler. A keyboard user can reach Lines but cannot focus Details or History without a pointer. | `rg -n 'tabindex|onkeydown|role="tab"'` finds all three tab roles and the negative tabindex, but no keyboard transition. | Reverting roving-tab keyboard support returns Details and History to mouse-only controls. |
| M | `web/src/lib/Rail.svelte:69` | The desktop rail is not fixed at 352px. It is a shrinkable flex item because it has `width: 352px`, `min-width: 0`, and no nonshrinking flex basis. A constrained desktop layout can make it narrower. | The computed CSS rule is `width: 352px; min-width: 0`. Flex items retain the default `flex-shrink: 1`. | Reverting a fixed `352px` flex basis allows the details column to collapse under normal desktop layout pressure. |

## Independent evidence

I own only this review file. The `Dub`, `Take`, and `Charge` shapes exist, and I can inspect the component independently.

The fixture ledger is internally exact. `jq` measured 23,414,000 nanodollars for the declared total and all project plus take charges. It found 67,000 project nanodollars and 23,347,000 take nanodollars.

The Lines tab derives slots from `Segment.duration_ms`, displayed takes from `Take.fit.measured_ms`, and tries from `Line.takes.length`. Both displayed duration buttons call the supplied take-playback callback. Details derives itemized charges from each take and the total from `Dub.total`.

Selection updates the locally derived segment, line, and all three tab inputs synchronously. The component uses `$props`, `$state`, `$derived`, and `$effect`. The owned files contain no `export let`, `$:`, or store reactivity.

`npm run check` completed with zero diagnostics. `npm run build` completed successfully. These checks establish build health. The fixture ledger query and saved-parent inspection above are the review pins.
