# T2.1 Remediation Round 1

| # | Finding | Fix Applied |
|---|---------|-------------|
| 1 | The JWT lifetime check in `TestServiceAccountTokenSource` has an empty body. It asserts nothing, so a signed lifetime regression passes the suite. | Filled the body with `t.Errorf("jwt lifetime = %d, want 3600", claims.Exp-claims.Iat)`. Staged the reviewer's mutation `Exp: now.Add(2 * time.Hour)` and the test failed with `jwt lifetime = 7200, want 3600`. Restored the one hour expiry and the test passes. |
| 2 | `TestEndpointURL` exercises only default location and model values. Hardcoding `us-central1` or `gemini-3.8-flash` into `endpointURL` passes the whole suite. | Added a non-default assertion over project `ajilamu-prod`, location `europe-west4`, and model `gemini-2.5-flash`. Staged the `us-central1` hardcode and it failed the new assertion. Repeated with the `gemini-3.8-flash` hardcode and it failed too. Restored the config-driven code. |
| 3 | The Go prompt is 656 bytes against the 658 byte Python value. It misses the Python leading and trailing newline, so charge units drift by 2 chars from the proven basis. | Ported `segmentPrompt` byte verbatim from `tools/validate_pipeline.py` with both newlines. Dumped both strings and `cmp` accepts them as identical at 658 bytes each. The `Units` expectation derives from `len(segmentPrompt)`, so it tracks 658 with no hardcoded count. |
| 4 | The live probe assigns `config.DefaultGeminiModel` directly. `GEMINI_MODEL` never reaches it while `GOOGLE_CLOUD_LOCATION` is honored from the environment. | `GeminiModel` now reads `envDefault(t, "GEMINI_MODEL", config.DefaultGeminiModel)` beside the existing `GOOGLE_CLOUD_LOCATION` handling. The `.env` setdefault semantics and the skip logic stay untouched. The reviewer's grep on the file now returns the honoring line, and `go vet -tags live` passes. |

## Verification

`go build ./...`, `go vet ./...`, `go vet -tags live ./internal/gemini/`, and `go test ./...` all pass offline. `gofmt -l internal/gemini/` reports nothing. Each fix carries its reviewer's pin as evidence above.
