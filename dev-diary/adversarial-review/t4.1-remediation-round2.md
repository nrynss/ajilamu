# T4.1 remediation, round 2

Verdict remains REMEDIATE.

| Severity | Where | Remediation | Pin | Mutation |
| --- | --- | --- | --- | --- |
| H | `internal/ledger/client.go:21-29`, `client.go:107-145` | `Client` now owns a cancellable reconciler context, completion channel, and idempotent `Close`. `Close` cancels, joins the worker, and closes idle HTTP connections. The worker selects its retry interval against cancellation. | An isolated copied-tree harness injected a local HTTP endpoint that returned `503` for a queued row. It waited for one retry request, called `Close` twice, waited 1.2 seconds, and observed no further requests. Command: `go test -count=1 -race -run '^TestCloseStopsAndJoinsReconcilerHarness$' -v ./internal/ledger` in `/tmp/ajilamu-t41-lifecycle.SLse8E/repo`. | Reverting cancellation, join, or idempotence leaves a discarded client retrying network writes, or makes shutdown unsafe to repeat. |

Validation passed from the repository root: `go test -count=1 -race ./internal/ledger`,
`go vet ./internal/ledger`, `gofmt -d internal/ledger/client.go internal/ledger/queue.go`,
and `git diff --check -- internal/ledger/client.go internal/ledger/queue.go`.
