# T6.5 remediation, round 4

I did not change the round 4 verdict. I fixed finding M and nothing else.

I edited `web/src/lib/edit/Speaker.svelte` and this record. I did not commit.

| Finding | Fix | Pin | Residue |
|---|---|---|---|
| **M (double render).** `renderingSegmentId` held at most one in-flight segment. The `finally` cleared it with no owner check. When two lines rendered at once, the first reply cleared the second line's guard. The second line then read idle and its confirm button was live, so a third billable POST could start. The same slot also dropped the first line's flag when the second confirm ran. | `renderingSegmentIds` is a `$state<Set<number>>` of every in-flight segment id. `busy` derives from `renderingSegmentIds.has(segment.id)`. The confirm adds `segment.id` to a fresh Set. The `finally` deletes `targetSegment.id` from a fresh Set, so only the owning reply clears that line. | Headless Chromium against the built bundle, with the line 3 reply delayed 1500 ms and the line 1 reply delayed 6000 ms. After the line 3 reply landed, the reopened line 1 panel read `confirmText: "Re-rendering…"`, `confirmDisabled: true` and `cancelDisabled: true`. A click on that confirm fired no POST. The server logged two POSTs, one line 3 render and one line 1 render. The mutation read `confirmText: "Change speaker"`, `confirmDisabled: false`, `cancelDisabled: false` and fired a third POST. | None. The round 3 pins still hold. |

## What I measured

I copied `web/` to `/tmp/rem65r4/repo` and symlinked `node_modules` and `testdata`. I built
with `npm --prefix web run build`. A throwaway Node server served the built bundle and the
frozen `testdata/wire/dub.json` under the non-fixture id `t65-rem4-dub`. It delayed the line 3
reply by 1500 ms and the line 1 reply by 6000 ms. I drove the page with headless Chromium. The
harness never edited the working tree.

### 1. The fixed build, finding M

The run confirmed the line 3 speaker change at 1.4 s, switched to line 1, and confirmed line 1
at 1.7 s. The line 3 reply landed at 2.9 s. The run reopened the line 1 panel at 3.2 s, while
the line 1 render was still in flight.

```json
{
  "line1PanelDuringFlight": {
    "eyebrow": "Line 1",
    "pendingOpen": 1,
    "pendingText": "Re-rendering line 1 as Suni Williams is estimated at $0.0008421. Estimated from the take seg_1_try1.wav. Cancel Re-rendering…",
    "confirmText": "Re-rendering…",
    "confirmDisabled": true,
    "cancelDisabled": true
  },
  "postsBeforeClick": 2,
  "postsAfterClick": 2,
  "finalPosts": [
    { "segment": 3, "body": "{\"language\":\"ml\",\"speaker\":\"Mark Vande Hei\"}" },
    { "segment": 1, "body": "{\"language\":\"ml\",\"speaker\":\"Mark Vande Hei\"}" }
  ]
}
```

The line 1 confirm reads `Re-rendering…` and stays disabled after the line 3 reply. Cancel
stays disabled too. A click on the disabled confirm fired no request. The POST count stayed at
two. The third POST is zero.

The line 1 reply landed at 7.7 s. The panel then went live again, which proves the owner's
reply still clears its own line.

```json
{
  "line1PanelAfterOwnReply": {
    "confirmText": "Change speaker",
    "confirmDisabled": false,
    "cancelDisabled": false,
    "note": "Line 1 re-rendered as take seg_1_try3.wav."
  }
}
```

The run raised 0 console messages and 0 page errors.

### 2. The mutation fails the pin

I reverted the fix in `/tmp/rem65r4/repo` to the single `renderingSegmentId` with the unowned
`finally`, rebuilt, and reran the same script. The line 3 reply landed at the same point.

```json
{
  "line1PanelDuringFlight": {
    "confirmText": "Change speaker",
    "confirmDisabled": false,
    "cancelDisabled": false
  },
  "postsBeforeClick": 2,
  "postsAfterClick": 3,
  "thirdPost": { "segment": 1, "body": "{\"language\":\"ml\",\"speaker\":\"Suni Williams\"}" }
}
```

The reopened panel reads idle while its render is in flight. A click fires a third billable
POST. The pin reddens under the mutation.

### 3. The round 3 pins still hold

The same fixed bundle ran the line switch without a line 1 render. The run confirmed line 3,
switched to line 1, and chose a line 1 speaker.

```json
{
  "duringFlight": {
    "pendingOpen": 1,
    "confirmText": "Change speaker",
    "confirmDisabled": false,
    "cancelDisabled": false
  },
  "afterReply": {
    "pendingOpen": 1,
    "confirmText": "Change speaker",
    "confirmDisabled": false,
    "cancelDisabled": false
  }
}
```

The line 1 confirm stays live while line 3 re-renders. The pending panel survives the line 3
reply. Both round 3 pins hold.

### 4. The type check

```text
$ npm --prefix web run check
1788912830594 START "/home/nryn/work/ajilamu/web"
1788912830595 COMPLETED 183 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
```

Exit 0. The changed component carries no `export let`, no `$:` and no store. I did not edit
the round 4 review file. I did not touch the verdict.
