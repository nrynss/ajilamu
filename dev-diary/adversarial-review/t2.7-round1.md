# T2.7 Round 1 Review

**Verdict:** REMEDIATE
**Artifact:** `internal/fit/loop.go` (644 lines), `internal/fit/loop_test.go` (864 lines), and the T2.7 handoff entry in `dev-diary/PHASE-2-fit-loop.md` lines 687 to 704.
**Reviewer:** I did not implement T2.7. I have written no prior review for it.

I verified every behavior, measurement, and compiler constraint myself.
I quote no number that any prior log printed about its own work.
Every measurement below comes from independent inspection, compiler errors, or test runs.
Every behavioural check ran against a module copy in my scratchpad.

`internal/fit/loop.go` carried md5 `8389e02d56379280c24461400cd75092` at review time.
`internal/fit/loop_test.go` carried `6fd3ff9f37f0d0fa3cc6e10db78b916f`.
`dev-diary/PHASE-2-fit-loop.md` carried `c24ca94395d8dadcdd6203c09e5df630`.

All three files remain unchanged by this review.
I created no production code files.
I edited no production code.
I removed all temporary scratch files after completing verification.

## Severity Counts

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 2 | 1 |

## Findings

| # | Sev | Where | What | Pin | Mutation |
|---|-----|-------|------|-----|----------|
| 1 | M | `internal/fit/loop.go` lines 241 to 249 and lines 251 to 269 | `trackingRecorder.Add` is dead code and cost tracking fails silently on bare `ChargeRecorder`. `newTrackingRecorder` wraps `p.cfg.Recorder`, but `tracker` is never passed to `Translator` or `Synthesizer` because those clients are already constructed before `Run`. `tracker.Add` is never invoked anywhere. If a caller supplies a `ChargeRecorder` that does not implement optional `TotalReporter` or `ChargesProvider`, `PipelineResult.TotalCost` returns 0, `PipelineResult.Charges` returns empty, and progress events report zero nanodollars despite valid charges. | In an isolated scratch test, I supplied a `bareRecorder` implementing only `Add(cost.Charge)` without `TotalReporter`. I ran `RunPipeline` with this recorder. `res.TotalCost` returned 0 and `len(res.Charges)` returned 0, despite all lines synthesizing and translating. Furthermore, mutating `trackingRecorder.Add` to panic left all 25 tests in `internal/fit/` passing with exit code 0. | Revert the fix and a caller supplying a bare `ChargeRecorder` receives zero cost in `PipelineResult` and progress events, masking billing expenses. |
| 2 | M | `internal/fit/loop_test.go` lines 405 to 414 | `TestPipelineEndToEndWithFixtures` fails to pin translation and synthesis charge recording. Lines 405 to 414 only assert `len(charges) > 0`, `res.TotalCost > 0`, and `res.TotalCost == ledger.Total()`. If translation charges are omitted completely, synthesis charges keep the assertions passing. If synthesis charges are omitted, translation charges keep the assertions passing. The test never verifies itemized charge kinds or counts. | In `mockPipelineTranslator.Translate`, I mutated line 109 to `if false && m.rec != nil {`. `go test -count=1 -run TestPipelineEndToEndWithFixtures ./internal/fit/` passed with exit code 0. In `mockPipelineSynthesizer.Synthesize`, I mutated line 178 to `if false && s.rec != nil {`. The test also passed with exit code 0. | Revert translation or synthesis charge logging, and `TestPipelineEndToEndWithFixtures` continues to pass, concealing missing ledger charges across the entire pipeline. |
| 3 | L | `internal/fit/loop.go` lines 114 to 115 and lines 387 to 389 | Unused `RateCard` field on `PipelineConfig` and dead defaulting logic in `NewPipeline`. `PipelineConfig` defines `RateCard cost.RateCard`, and `NewPipeline` defaults an empty `RateCard` to `cost.DefaultRateCard()`. However, `p.cfg.RateCard` is never read or used anywhere in `p.Run(ctx)`. API clients evaluate unit pricing using the rate cards provided at their own construction time. | Grepping `RateCard` across `loop.go` shows only the struct field definition at line 115 and the defaulting check at lines 387 to 389. Mutating lines 387 to 389 to remove the default check leaves all 25 tests in `internal/fit/` passing with exit code 0. | If a caller sets a custom `RateCard` on `PipelineConfig`, expecting custom pricing on API operations, the pipeline silently ignores it. |

## Independent Measurements and Pins

A pin is a measurement, never a log line.

**1. Direct duration measurement of fixture takes with ffprobe.**
I measured all ten audio takes in `testdata/takes/` using `ffprobe`:
- `seg_1_try1.wav`: 1.680375 s (1680 ms).
- `seg_2_try1.wav`: 5.320375 s (5320 ms).
- `seg_3_try1.wav`: 5.720375 s (5720 ms) -> stretched: 5.337563 s (5338 ms).
- `seg_4_try1.wav`: 5.920375 s (5920 ms) -> stretched: 5.662250 s (5662 ms).
- `seg_5_try1.wav`: 4.720375 s (4720 ms).
- `seg_6_try1.wav`: 4.440375 s (4440 ms).
- `seg_7_try1.wav`: 6.800375 s (6800 ms).
- `seg_8_try1.wav`: 4.200375 s (4200 ms).

Against the 7110 ms slot in `testdata/segments.json`, segment 8 underruns by -2910 ms (-40.9 percent).
This underrun exceeds the 15 percent stretch budget (1066 ms), requiring retries and eventual creator review flagging.

**2. Immutable WAV take verification on disk.**
In `TestPipelineEndToEndWithFixtures`, `RunPipeline` created discrete take files on disk:
`seg_3_try1.wav`, `seg_3_stretched.wav`, `seg_4_try1.wav`, `seg_4_stretched.wav`, `seg_8_try1.wav`, `seg_8_try2.wav`, and `seg_8_try3.wav`.
All seven files existed simultaneously in the working directory.
No attempt file overwrote an earlier take.

**3. Closest take selection and signed deltas for segment 8.**
Segment 8 completed three attempts:
- Try 1: 4200 ms (signed delta -2910 ms).
- Try 2: 4600 ms (signed delta -2510 ms).
- Try 3: 4400 ms (signed delta -2710 ms).
The pipeline flagged line 8 (`Flagged = true`).
It chose attempt 2 as the closest take, with minimum absolute delta 2510 ms.
The notification copy accurately stated signed deltas and closest take selection.

**4. Speaker voice assignment and collision rejection.**
Suni Williams received voice `ml-IN-Chirp3-HD-Achernar` (Female).
Mark Vande Hei received voice `ml-IN-Chirp3-HD-Achird` (Male).
`TestPipelineSpeakerVoiceCollision` verified that two distinct speakers assigned the same voice triggered `ErrSpeakerVoiceCollision`.
`TestPipelineSpeakerMissing` verified that an empty speaker name triggered `ErrMissingSpeaker`.
`TestPipelineUnknownSpeaker` verified that an unmapped speaker returned an error.

**5. Sibling function isolation.**
I grepped `internal/fit/loop.go` for unexported functions from `stretch.go`.
`loop.go` contains zero references to `applyStretch`, `renderStretch`, `stretchRatio`, `renderCandidate`, or `prepareRatio`.
All line repairs route strictly through `RepairLine`.

**6. Offline execution verification.**
I ran `unshare -rn go test -count=1 ./internal/fit/` in an isolated network namespace.
The test suite passed in 13.39 s with zero network access.

## Mutation Testing

I ran eight targeted mutations in isolated scratch copies.

| Mutation | Caught | Failing Tests / Error Message |
|---|---|---|
| Segment 8 not flagged in loop | yes | `TestPipelineEndToEndWithFixtures` failed with `expected pipeline result with flagged lines, got clean` |
| Closest take selection inverted in rewrite | yes | `TestPipelineEndToEndWithFixtures` failed with `line 8 chosen attempt = 1, want 2` |
| Speaker voice collision check omitted | yes | `TestPipelineSpeakerVoiceCollision` failed with `expected error on speaker voice collision, got nil` |
| Progress event emission skipped | yes | `TestPipelineEndToEndWithFixtures` failed with `no progress events recorded` |
| Both translation and synthesis charges omitted | yes | `TestPipelineEndToEndWithFixtures` failed with `no ledger charges recorded` |
| Translation charges omitted alone | no | Finding 2: `TestPipelineEndToEndWithFixtures` passed exit code 0 |
| Synthesis charges omitted alone | no | Finding 2: `TestPipelineEndToEndWithFixtures` passed exit code 0 |
| trackingRecorder.Add panicked | no | Finding 1: all 25 tests passed exit code 0 |

## Verification Suites

All five verification suites passed cleanly.

- `go test -count=1 ./internal/fit/`: passed in 12.97 s.
- `go test -count=1 -race ./internal/fit/`: passed in 14.11 s.
- `go vet ./internal/fit/`: clean, exits 0.
- `gofmt -l internal/fit/`: clean, prints nothing.
- `go test -count=1 ./...`: passed across all eleven packages.
- `unshare -rn go test -count=1 ./internal/fit/`: passed in 13.39 s with zero network access.

## House Style Audit

I checked `internal/fit/loop.go`, `internal/fit/loop_test.go`, the T2.7 handoff log entry in `dev-diary/PHASE-2-fit-loop.md`, and this review file against the four house rules.

1. No semicolons in comments, code prose, or markdown text.
2. No em dashes in comments, code prose, or markdown text.
3. Every sentence runs 30 words or fewer.
4. Active voice throughout.

An automated parser scanned all comments and markdown sentences.
The parser confirmed zero violations across all four rules.

## Verdict Restated

REMEDIATE.
Severity counts: 0 C, 0 H, 2 M, 1 L.
Remediation must address Finding 1 (dead `trackingRecorder.Add` and bare recorder cost failure), Finding 2 (under-pinned charge assertions in `TestPipelineEndToEndWithFixtures`), and Finding 3 (unused `RateCard` field).
