# T6.2 Round 3 Adversarial Review

## Verdict

**APPROVE**

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 0 |

Zero findings. I re-measured the fee panel myself on a fresh build. All eight lines
match the record. The two remediated passages match the delivered code and the
panel. No T6.2 code changed since round 2. The residue is zero against both prior
rounds.

## Scope and Evidence

I reviewed the T6.2 block, the seam expansion note, the T6.2 handoff entry, the
round one and round two reviews, both remediation records,
`web/src/lib/edit/Speaker.svelte`, and the T6.2 hunk of
`web/src/routes/d/[id]/+page.svelte`. I changed no implementation. I wrote only
this file. Every probe ran against a copy at `/tmp/t62r3`, never the worktree.

### The harness

I copied the worktree to `/tmp/t62r3`. I ran `npm --prefix web run build` there. I
ran `npm --prefix web run check` there. I built `./cmd/ajilamu` and served the
bundle on port 43612 with a clean environment and a scratch data directory. I
drove `/d/fixture` in headless Chromium through `playwright-core`. The browser
recorded every request, every response, every console message, and every page
error. The run raised zero console messages and zero page errors.

### Health checks, not pins

`npm --prefix web run check` reported 181 files, 0 errors, and 0 warnings. Neither
changed file carries `export let`, a `$:` statement, or a store import.

### Pins

**Fee panel.** I clicked every line, chose the other speaker, and read the live
pending panel. The table reports the fee string, the `data-fee` attribute, the
basis take, and the takes on the line.

| Line | Fee string | Nanodollars | Basis take | Takes on the line |
|---|---|---|---|---|
| 1 | `$0.0008421` | 842100 | `seg_1_try1.wav` | `seg_1_try1.wav` |
| 2 | `$0.0024383` | 2438300 | `seg_2_try1.wav` | `seg_2_try1.wav` |
| 3 | `$0.0023182` | 2318200 | `seg_3_try1.wav` | `seg_3_try1.wav`, `seg_3_stretched.wav` |
| 4 | `$0.0036392` | 3639200 | `seg_4_try1.wav` | `seg_4_try1.wav`, `seg_4_stretched.wav` |
| 5 | `$0.0034599` | 3459900 | `seg_5_try1.wav` | `seg_5_try1.wav` |
| 6 | `$0.0029497` | 2949700 | `seg_6_try1.wav` | `seg_6_try1.wav` |
| 7 | `$0.004751` | 4751000 | `seg_7_try1.wav` | `seg_7_try1.wav` |
| 8 | `$0.0029486` | 2948600 | `seg_8_try1.wav` | `seg_8_try1.wav` |

This matches the round 2 remediation numbers and the round 2 review table
exactly. It also matches my own independent sum of `testdata/manifest.json`. For
each line I selected the last take whose `charges` array is non-empty, and I
summed that take's `total_nanodollars` with `jq`. Every take name and nanodollar
pair matched the panel.

**Ground truth.** `testdata/manifest.json` records line 3 as `seg_3_try1.wav` at
2318200 nanodollars and `seg_3_stretched.wav` at zero. Line 4 reads
`seg_4_try1.wav` at 3639200 and `seg_4_stretched.wav` at zero. The panel picks the
billed take on both lines.

**Speaker set.** The panel offered `Mark Vande Hei` and `Suni Williams` on all
eight lines, in that sorted order. The set matches the distinct
`Segment.speaker` values.

**Cancel.** Cancel fired zero `/api` requests on all eight lines and closed the
pending panel every time. The only traffic during a cancel was the preview
element's own `GET /clip.mp4`.

**Confirm.** Confirm fired zero `/api` requests on lines 1, 3, 4, and 7. It applied
route-local state and left the note "The take is unchanged, so a re-render is
still pending."

**Take bytes.** All ten files under `testdata/takes/` matched their HEAD blobs by
`git hash-object`. The scratch data directory held zero files.

**Zero billable calls.** The page issued zero `/api` requests across the whole
run. The server carries no synthesis route. The only route that could bill is
`POST /api/editor/commands/preview`, and the panel never calls it.

### The remediated passages, checked

Seam note, lines 299 to 301. "Newest billed take" matches `estimateTake`, the
newest take whose `charges` array is non-empty. The zero sentence matches the
`?? lastTake` fallback, whose charges sum to zero. "That number is already on the
payload" holds, because the takes and their charges ride the fixture payload and
no new endpoint exists.

Handoff entry, lines 310 to 317. The rule matches the code. The line 1, 3, 4, and
7 numbers match the measured panel. "Neither line moved" holds against the round
one table. The zero sentence matches the fallback. The quoted hint matches the
rendered hint exactly.

### No code change since round 2

`web/src/lib/edit/Speaker.svelte` and `web/src/routes/d/[id]/+page.svelte` carry
the same git blob hashes as the round 2 snapshot at `/tmp/t62r2`. I confirmed the
same by sha256. A hash sweep over `web/src`, `internal`, `cmd`, `testdata`, `sql`,
and `tools` found one drift pair only, `internal/ledger/workspace.go` and
`internal/ledger/workspace_test.go`. Those files are T7.x sibling work,
timestamped after the round 2 snapshot and outside the T6.2 seam. The round 2
remediation changed only `dev-diary/PHASE-6-editing.md`. A diff of that file
against the round 2 snapshot returns exactly the two remediated passages.

## Residue Against Round One Finding 1

Zero residue. The code sums the newest billed take, not the last take. Lines 3 and
4 read `$0.0023182` and `$0.0036392` from `seg_3_try1.wav` and `seg_4_try1.wav`.
The basis label and the `takeFile` payload name the same take as the sum. The
field comment and the hint describe billed takes.

## Residue Against Round Two Finding 1

Zero residue. The seam note and the handoff entry now describe the delivered rule.
The old phrases "last take" and "Line 3 shows `$0.00`" return nothing from
`dev-diary/PHASE-6-editing.md`. The record numbers match my own measurement.

## Findings

| # | Sev | Where | What | Pin | Mutation |
|---|---|---|---|---|---|
| | | | | | |

No finding opens this round. The four columns have no rows.

## What I did not file

The estimate sums the translate charge too. Round one raised it and did not file
it. A speaker change may skip translation. The T6.5a contract does not promise
that skip. The behaviour is deliberate, so I leave it.

The all-zero fallback. The fixture carries no line whose takes all lack charges.
The code falls back to the last take and reads zero. The hint discloses the
fallback. I judge it honest, so I file nothing.

The page ignores `estimatedNanodollars` and `takeFile`. T6.5a owns the consumer.

## Done condition

The done condition reads: "Changing a speaker displays the associated re-render
fee and leaves original takes untouched if canceled." The fee half holds on all
eight lines. The cancel half holds. The code meets the done condition. The record
meets it too.

## Cleanup

I stopped the server and closed the browser. The worktree holds one new file from
me, this review. The copy at `/tmp/t62r3` and the scratch data directory stay
outside the repository.
