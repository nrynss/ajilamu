# T4.5 adversarial review, round 1

Verdict: APPROVE

Counts: C 0, H 0, M 0, L 0

Prior-round residue: None. This is the first review round.

Findings: None

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| None | | | | |

Independent evidence:

A throwaway HTTP capture received ten requests. The first insert returned 503. Flush replayed the same row. Invalid snapshots added none.

The insert query named `INSERT INTO timeline_state_raw` and `FORMAT JSONEachRow`. It did not name the `timeline_state` view.

Decoded insert keys were commit_id, project_id, dub_id, owner_id, language, version_seq, segment_index, start_ms, end_ms, speaker, emotion, source_text, text, and take_id. created_at and ingested_at were absent.

A nudge that restated the row with an empty take_id still sent take_id as an empty string. No snapshot field was omitted.

The 503 path left one durable `.json` journal file. Pending was 1, then 0 after Flush. The replay query and JSON body equaled the failed delivery.

Inverted slot, empty slot, and negative segment_index each returned an error. Request count did not rise.

TimelineAt sent `SELECT * FROM timeline_at_commit(dub_id = {dub_id:String}, language = {language:String}, commit_id = {commit_id:String}) FORMAT JSONEachRow`. Bound params were param_dub_id=d1, param_language=ml, and param_commit_id for c1, c2, and c3. The statement contained no version_seq inequality. It did not use branch as an ancestry selector. It did not interpolate d1, ml, or the commit ids.

I reconstructed state from the fixture parent links and snapshots. Ancestry of the root was c1. Ancestry of the child was c2 and c1. Ancestry of the fork was c3 and c1. Root kept three version-1 rows. Child kept the nudged segment 1 at version 2 and inherited segments 2 and 3. Fork kept the rewritten segment 2 at version 2 and inherited segments 1 and 3.

Decoded TimelineAt stand-in rows matched that reconstruction at all three heads. ReplayAt output matched the same rows. The two program outputs did not merely match each other.

A version_seq <= 2 filter would have mixed the child nudge into the fork and the fork rewrite into the child. ReplayAt did not. Child segment 2 text stayed world. Fork segment 1 start stayed 1000.

The mock handler wrote hardcoded JSONEachRow constants. TimelineAt does not call ReplayAt.

CompareBranches cost SQL selected `sum(cost_usd)` from `charges`. It did not read charges_raw. Ancestry walked `c.commit_id = a.parent_commit_id`. It did not filter `WHERE branch =`. Branch was selected from the head row as a label. Bound params carried dub_id, language, and commit_id. Slot sums from the reconstructed rows were 3400 on the child and 3500 on the fork.

`gofmt -d` passed on the T4.5 Go files. `git diff --check` passed. `go test ./internal/ledger -count=1` passed. `go test -race ./internal/ledger -count=1` passed. `go vet ./internal/ledger` passed. Those checks do not prove the query contract.

I removed the throwaway capture after measurement.

Residue: None.
