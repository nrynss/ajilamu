# T7.7 adversarial review, round 2

Task: supported-language catalog. Round one filed one M, the leaked production client under
concurrent refreshes. The remediation added `buildMu`, a post-lock re-read, and a corrected
comment. This round re-measures the fix and the rest of the task.

Reviewed against HEAD `4622205` plus the uncommitted T7.7 working tree. Go 1.27.1. I did not
implement T7.7. I am not the round one reviewer. I fixed nothing. My only repository write is
this file.

## Verdict

**APPROVE**

| Severity | Count |
|---|---:|
| C | 0 |
| H | 0 |
| M | 0 |
| L | 0 |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| None | No finding met the criteria. | n/a | n/a |

## Round one residue

Zero residue against M1. The build is serialized and one client exists. The comment matches the
code. A failed build caches nothing. Concurrent refreshes stay independent by design.

## 1. Concurrent refresh re-measured

`go test -race -count=1 -run TestConcurrentRefreshBuildsOneClient -v ./internal/tts`

```
=== RUN   TestConcurrentRefreshBuildsOneClient
    catalog_test.go:279: client builds = 1, ListVoices calls = 8
--- PASS: TestConcurrentRefreshBuildsOneClient (0.00s)
PASS
ok  	github.com/nrynss/ajilamu/internal/tts	1.020s
```

Eight goroutines refresh at once. The client build count is 1. The ListVoices count is 8. I also
ran an independent probe with 16 goroutines through `go test -overlay` in `/tmp`. It logged
`probe: builds=1 ListVoices=16`. The repository file stayed untouched. The test passed under
`-count=20` with the race detector.

## 2. Mutation pin

I copied `internal/tts/catalog.go` to `/tmp/t77r2` and deleted lines 129 to 134, the post-lock
re-read. An overlay mapped the package file to that copy. The same test then failed.

```
=== RUN   TestConcurrentRefreshBuildsOneClient
    catalog_test.go:274: client builds = 8, want 1
    catalog_test.go:279: client builds = 8, ListVoices calls = 8
--- FAIL: TestConcurrentRefreshBuildsOneClient (0.01s)
FAIL
FAIL	github.com/nrynss/ajilamu/internal/tts	0.022s
FAIL
```

Without the re-read, eight goroutines build eight clients. Seven overwrite the cache and leak.
The test fails. The working tree was not edited.

## 3. Race suite

`go test -race -count=1 ./internal/tts ./internal/api`

```
ok  	github.com/nrynss/ajilamu/internal/tts	1.021s
ok  	github.com/nrynss/ajilamu/internal/api	2.462s
```

## 4. Failed refresh keeps the cached list

An independent probe printed the list after each failure.

```
provider failure: before=53 after=53 fromProvider=false fetchedZero=true err="fetch the supported languages from Cloud Text-to-Speech: provider is down"
build failure: before=53 after=53 fromProvider=false fetchedZero=true builds=1 err="Cloud Text-to-Speech needs Application Default Credentials: set GOOGLE_APPLICATION_CREDENTIALS or run gcloud auth application-default login: google: could not find default credentials"
build retry after failure: builds=2
```

A failed provider call and a failed client build both leave 53 committed codes. `FromProvider`
stays false and `FetchedAt` stays zero. The build caches nothing, so the next refresh retries.

## 5. Comment against the code

| Claim | Measurement |
|---|---|
| returns the injected lister or builds the production one once | Holds. `buildMu` plus the post-lock re-read runs the build once. |
| serializes construction, so one client exists and none leaks | Holds. 16 concurrent refreshes produced one build. |
| a failed build caches nothing, so the next refresh retries | Holds. The probe built once, failed, then built again. |
| does not coalesce concurrent refreshes | Holds. 16 refreshes issued 16 ListVoices calls. |
| one caller's cancellation cannot fail another's refresh | Holds. Each ListVoices call uses its caller's context. A canceled winner fails only its own build, and the next waiter rebuilds. |

## 6. No behaviour change outside the fix

| Check | Observation |
|---|---|
| Credential-free serve | `env -i HOME=<empty> AJILAMU_DATA_DIR=<tmp> PORT=18091` answered GET 200 with `Cache-Control: no-store`, 53 sorted codes, `source=committed`, and no `fetched_at`. |
| Failed refresh on that server | POST answered 502 and named Application Default Credentials and `GOOGLE_APPLICATION_CREDENTIALS`. GET after kept 53 committed codes. |
| Refresh swap | A probe blocked inside `voices.list`. Mid-flight `Current()` held 53 committed codes with `FromProvider` false. After release the cache held `ml-IN,ta-IN`, `FromProvider` true, and a fetch time. |
| Parity guard | `TestExamplesMatchJSONTags/language_catalog.json`, `TestEveryWireStructIsRegistered`, and `TestGoTSParity` passed. |
| Changed files | Only `internal/tts/catalog.go` and `internal/tts/catalog_test.go` carry a timestamp after round one. Every other T7.7 file predates the round one review. |

## 7. Residue

Zero residue against round one M1. The fix closes the leak, serializes construction, and keeps
the comment true. Round one's non-findings stay non-findings. The live credential path remains
unmeasured here, as in round one, because this review holds no provider credentials.

## Done condition

The done condition holds as measured. A clone with no credentials serves the committed list. A
failed fetch leaves the cache and names the reason, including a failed client build. The parity
guard covers the wire type. The one clause this review cannot re-measure is the live provider
call, which rests on the handoff.

## Scope

I wrote only this file. I changed no implementation file, test, fixture, task status, or commit.
All probes ran in `/tmp/t77r2` because `/tmp` had 264 MB free. The overlay approach needs only a
mutated copy and a JSON map, so the repository stayed clean.
