# T2.3 Round 1 Review

**Verdict:** APPROVE
**Commit / state:** uncommitted T2.3 delta at HEAD 51ce283e7b413dc38c6fd37c1ea978f1f260bf75

## Severity Counts

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 0 |

## Findings

| # | Sev | Where | What | Pin | Mutation |
|---|---|-------|------|-----|----------|
| None. Every required pin holds under independent measurement. The probes table records each check. | | | | | |

## Probes

| Probe | Judgement | Pin |
|-------|-----------|-----|
| `go.mod` TTS require, genai pin, oauth2 not direct | Intact. texttospeech is a direct require. genai stays at v1.71.0. oauth2 is indirect. | `go list -m cloud.google.com/go/texttospeech` prints `cloud.google.com/go/texttospeech v1.22.0`. `go list -m google.golang.org/genai` prints `google.golang.org/genai v1.71.0`. Direct modules are only the main module, texttospeech v1.22.0, and genai v1.71.0. `go list -m` reports `golang.org/x/oauth2 v0.36.0` with Indirect true. `go.mod` has no oauth2 require line outside the indirect block. `go.sum` carries both pinned modules. `go mod tidy -diff` is empty. |
| No leftover REST or oauth2 imports | Intact. Production calls the SDK `SynthesizeSpeech` method. Package sources import no HTTP client and no oauth2. | `rg -n 'oauth2\|texttospeech\.googleapis\.com\|net/http' internal/tts/` is empty. `go list` Imports for `./internal/tts` include `cloud.google.com/go/texttospeech/apiv1` and `.../texttospeechpb`. They do not include oauth2 or `net/http`. `newProductionClient` calls `texttospeech.NewClient(context.Background())` with no options, no API key, and no key file. `sdkTTSClient.SynthesizeSpeech` forwards to `*texttospeech.Client.SynthesizeSpeech` at `apiv1/text_to_speech_client.go:213`. |
| Production audio config | Intact. The request sets LINEAR16 and leaves `SampleRateHertz` unset. | Read of `internal/tts/chirp.go` AudioConfig composite. It sets `AudioEncoding: texttospeechpb.AudioEncoding_LINEAR16` and no other field. The generated field is `int32`. Zero is the proto3 default and is omitted on the wire. SDK comment at `cloud_tts.pb.go:1352-1358` says conversion happens only when a rate is specified. SDK comment at `cloud_tts.pb.go:112` and `1552` says LINEAR16 bytes include a WAV header. Python `synthesize_malayalam_tts` still sends `sampleRateHertz: 16000`. This client does not. |
| Voices for every fixture speaker | Intact. Speakers are distinct. Unknown names error. Achernar is female. Achird is male. | Independent parse of `testdata/segments.json` yields two names. Suni Williams on ids 1-6. Mark Vande Hei on ids 7-8. A `/tmp` overlay test called `Assign` for each unique name and left the repo files untouched. Suni maps to `ml-IN-Chirp3-HD-Achernar`, language `ml-IN`, gender Female. Mark maps to `ml-IN-Chirp3-HD-Achird`, language `ml-IN`, gender Male. `Assign` of `not-a-fixture-speaker` errors with that name. Google Chirp 3 HD table lists Achernar Female and Achird Male. The same page lists Malayalam (India) as `ml-IN`. |
| Fixture double copies golden audio | Intact. The double writes a byte copy of the take. Tests never touch the network. | `fixtureSynthesizer.Synthesize` reads `testdata/takes/seg_{id}_try1.wav` and writes those bytes with `O_EXCL`. Independent copy of `testdata/takes/seg_8_try1.wav` to `/tmp/t23-rev-r1-seg8.wav` compared equal. `cmp` agreed. sha256 `484c67e514ddfda52f7ab88b40b5283365d8496c81640c6d335e9fe091ae87bd`, 134456 bytes. `ffprobe` on that fixture reports pcm_s16le at 16000 Hz. That rate is the Python run, not this request. `HTTPS_PROXY=http://127.0.0.1:1 GOOGLE_APPLICATION_CREDENTIALS=/no/such/file.json go test ./internal/tts/ -count=1` passed in 0.006s. The same poison passed `-race`. |
| Charge uses rune count | Intact. Success bills Unicode runes. Error paths add nothing. | Independent count of `ചലനം`: four code points `U+0D1A U+0D32 U+0D28 U+0D02`, twelve UTF-8 bytes. `go run` on a `/tmp` pin printed `runes=4 bytes=12`. `synthesizeCharge` sets `Units: utf8.RuneCountInString(req.Text)` and `Kind: cost.ChargeSynthesize`. Default `SynthesizePerChar` is 30000. Four runes total 120000 nanodollars. Byte billing would have been 360000. `rec.Add` runs only after `writeTake` returns nil. API error, empty audio, unknown speaker, overwrite, failed write, and missing fixture all return before `Add`. |
| Offline tests, race, format, vet | Intact. | `go test ./internal/tts/ -count=1` passed. `-race` passed. `gofmt -l` on the four owned Go files printed nothing. `go vet ./internal/tts/` printed nothing. |
| Owns | Intact. Extra files under `internal/tts/` besides the four named files are absent. | `find internal/tts -type f` lists `chirp.go`, `chirp_test.go`, `voices.go`, `voices_test.go`. Unrelated dirty paths (`internal/fit/`, PHASE-2, assemble, t2.4 review) were ignored. Scratch pins lived under `/tmp` and did not enter the repo. |
| Docs style on owned comments | Intact. | Comment scan of the four files found no semicolon in prose, no em dash, and no comment sentence over 30 words. Comments that describe billing match the write-then-charge order. Comments that describe sample rate match the unset field. |

## Live Evidence

I did not call Cloud Text-to-Speech. Spend stays outside this offline review. I did not treat any implementer note as a pin.

## False positives considered

`GOOGLE_CLOUD_PROJECT` is required and then unused. Official `texttospeech.NewClient` takes ADC options only, so this is not a defect.

The SDK says the client must be Closed. One process-lifetime synthesizer matches that use. No finding.

Golden takes are 16 kHz because Python forced that rate. The Go request does not. No finding.

No false positives this round.
