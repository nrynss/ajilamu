# T3.1 round 1 remediation

I own both assembler files and this remediation record. The required shapes exist. I can validate this task independently.
This remediation addresses F1 and F2 from `t3.1-round1.md`. The review verdict remains unchanged pending independent re-review.

| Finding | Changes | Pin | Result |
|---|---|---|---|
| H, F1 | `BuildBed` materializes source timestamps with `aresample=async=1:first_pts=0` before padding and trimming. | `TestBedPreservesSourceTimestamps` measures decoded sample windows for initial silence, tone, internal gaps, and trailing silence. External ffprobe and silencedetect repeat the review fixture. | The regression fails before repair and passes afterward. External measurements place the one-second tone at seconds 1 through 2 within a three-second bed. |
| H, F2 | Supplied music and `PrepareTake` share `unityChannelFilter`. Mono inputs duplicate into stereo at unity gain. | `TestSeparateMonoMusicPreservesLevel` measures decoded RMS, sample count, channel equality, and source format. External volumedetect compares the mono input and stereo output. | The regression measures -3.010233 dB before repair and 0.000067 dB afterward. External mean and peak levels match before and after conversion. |

## Measurements

Validation selects `/opt/homebrew/bin/ffmpeg` and `/opt/homebrew/bin/ffprobe`, both version 9.0.1.
Go reports version 1.27.1. The complete assembler suite passes:

```sh
PATH=/opt/homebrew/bin:/usr/bin:/bin go test -count=1 ./internal/assemble -v
```

The existing fixture checks still measure exact decoded sample equality over seconds 0 through 75.
Both measured fixture intervals retain a 0.000000 dB level difference.
All ten fixture take conversions retain their tested duration, level, and stereo duplication.
The separate 5.1 music, padding, trimming, and failure preservation checks pass.

The new timestamp regression generates four-second video fixtures with a two-second tone beginning at one second.
Its second case removes tone packets between relative seconds 0.5 and 1.5 while retaining their timestamps.
Decoded output measures zero RMS in the expected silent windows.
Active windows measure RMS between 0.062485 and 0.062488.

For external measurements, a temporary module copies the repaired assembler and unchanged media helpers.
A small driver calls `BuildBed` on the round-one reproduction fixtures.
The measurements observe rendered artifacts independently of that driver's return value.
Artifacts remain at `/tmp/ajilamu-t31-remediation.bNnPfq` for re-review.

```text
ffprobe delayed.mkv:
  video start_time: 0.000000
  audio start_time: 1.000000
  format duration: 3.000000

silencedetect delayed-bed.wav:
  silence_start: 0
  silence_end: 1.000023
  silence_start: 2
  silence_end: 3

volumedetect mono.wav:
  mean_volume: -21.1 dB
  max_volume: -18.1 dB

volumedetect music-bed.wav:
  mean_volume: -21.1 dB
  max_volume: -18.1 dB

ffprobe music-bed.wav:
  sample_rate: 44100
  channels: 2
  duration: 2.000000
```

The delayed tone starts one sample after one second because its first sine sample equals zero.
WAV's basic stereo header omits a channel mask. ffmpeg identifies the output as stereo.
