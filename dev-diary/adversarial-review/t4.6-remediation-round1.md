# T4.6 remediation, round 1

I remediated finding 1 from `t4.6-round1.md`. I did not change its verdict.

| # | Remediation | Pin | Mutation prevented |
| --- | --- | --- | --- |
| 1 | Added `peaks_test.go`. It accepts 64 and 128 values, rejects 63 and 129, clears stale values, copies input, and decodes numeric API JSON. The API take has a nonexistent audio path, so an audio read would fail. | `go test ./internal/ledger -run '^TestApplyTakePeaks'` | Removing a size guard, retaining stale values, aliasing input, reading audio, or changing numeric API delivery fails in CI. |

Verification completed:

- `gofmt -w internal/ledger/peaks_test.go`
- `go test ./internal/ledger -run '^TestApplyTakePeaks'`
- `go vet ./internal/ledger`
