# T6.6c adversarial review, round 2

**Task:** T6.6c Record agent charges in the ledger. **Round:** 2, against HEAD `fd32354`.

## Scope and role

I am the REVIEW role. I fix nothing. I edited no code, test, phase document, or status line. My
only write is this record. I reviewed the diff `c4f1f94..fd32354` on the T6.6c owned paths. I
ignored the P5, P6 and P7 close records. I checked the non-owned command and web changes only for
their effect on the agent path.

HEAD moved to `af0fc09` during the review, when another agent closed P5. That commit changed only
`dev-diary/README.md` and added `p5-close-round3.md`. No code path differs from `fd32354`, so every
measurement below stands.

## Method

I worked in a full copy at `/tmp/t66c-r2`. I loaded a fresh ClickHouse 26.8.2.7 database from
`sql/schema.sql` on port 18931. I built the real `ajilamu` binary and the production frontend
build. I drove headless Chrome for Testing 151.0.7922.34 over raw Chrome DevTools Protocol on port
19231. I read `Runtime.exceptionThrown` and the DOM. I ran the real agent route, the real recorder
adapter, and the real ledger client against that database. Charge rows came from SQL through the
`charges` view, which applies FINAL. I treat no program report as evidence.

The real binary cannot serve the agent route offline. Its editor needs live Gemini and a live MCP
server. A throwaway harness in package `main` therefore mounted the real `api.NewServer`, the real
`newAgentChargeRecorder` adapter, and the real `ledger.Client`. Round 1 used the same shape.

## Verdict

REMEDIATE. Findings: 1 C, 1 H, 1 L.

| C | H | M | L |
|---|---|---|---|
| 1 | 1 | 0 | 1 |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| `internal/ledger/workspace.go` `selectWholePassCharges`, the agent branch | The agent branch writes the constant `'agent' AS kind` and then filters `WHERE ... AND kind = 'agent'` in the same SELECT. ClickHouse resolves `kind` to the alias rather than the column, because `prefer_column_name_to_alias` defaults to 0. The filter is always true, so the aggregate runs over every charge row of the dub. The folded agent row reports the whole dub cost, and `uniqExact(turn_id)` counts the take rows' empty `turn_id` as one extra turn. The remediation claim that the fold total "sums the itemized rows exactly" is false. The first branch escapes only because its alias `toString(kind)` repeats the column expression. | `SELECT 'agent' AS kind, count() FROM charges WHERE dub_id='d-c1' AND kind='agent'` returned 9, which is every row of the dub. The control without the alias returned 4. The same query with `SETTINGS prefer_column_name_to_alias=1` returned 4. The `d-mix` payload carried an agent row of units 3 and 865000 nanodollars, while SQL summed the agent rows to 768000. The `d-mix` Details tab rendered `Editor agent turn $0.000865` beside two segment rows, so its itemized list summed to $0.000932 against a project total of $0.000865. A dub built only by the real writer, `d-creal`, rendered `Editor agent turn $0.000798` against a real agent cost of $0.000768 and a covers sentence of two agent calls. | The fix qualifies the column in the WHERE. Reverting it restores the always true filter. The agent row then reports the whole dub cost and the itemized list exceeds the project total. |
| `web/src/lib/tabs/DetailsTab.svelte` line 90, the take charge list | The list keys each take charge by `` `${take.file}-${charge.kind}` ``. One Gemini translate call writes a prompt row and a candidate row under one take, so two charges share one key. Svelte throws `each_key_duplicate` in the production build, and the panel renders nothing. The defect predates this diff and sits outside the T6.6c owns line. It still blocks the task's own C1 exit criterion, because the Details tab must render with the agent charges present. Every dub with a translation reaches it. | I recorded one take through the real `ledger.RecordTake` with one `ChargeTranslate`. `go test ./internal/ledger -run TestT66cR2RealTakeChargeShape -v` logged `REAL_WRITER_TRANSLATE_CHARGES=2`. The real binary served `d-real` with translate charges of 500 and 200 units on one take. Raw CDP on `http://127.0.0.1:18101/d/d-real` raised `https://svelte.dev/e/each_key_duplicate`, and the panel stayed on `rail-lines`. `git show c4f1f94:web/src/lib/tabs/DetailsTab.svelte` carries the same key. | A fix that gives each take charge its own key removes the crash. Reverting it restores the duplicate key. The Details tab then renders nothing for any dub with a translation. |
| `internal/ledger/workspace_test.go` `TestWholePassChargesFoldsIdenticalAgentTurns` | The test feeds a canned JSON payload to the client and never executes `selectWholePassCharges`. `TestWorkspaceQueriesBindAndShape` checks only that the statement contains the substring `kind = 'agent'`. Both pass while the statement filters nothing, so the shipped tests cannot fail on the first finding. | `go test ./internal/ledger ./internal/api ./cmd/ajilamu` reported `ok` for all three packages while the alias shadowing was live. The test stand-in at line 448 returns the folded row without a database. | A fix that executes the statement against ClickHouse fails on the alias bug. Reverting it restores a test that passes while the SQL filters nothing. |

## Done conditions, measured

| Condition | Independent result |
|---|---|
| A fresh database from `sql/schema.sql` accepts kind `agent` | PASS. The file loaded into an empty database and the preflight returned 0. A second run returned 0 again. `system.columns` read `Enum8('segment' = 1, 'translate' = 2, 'synthesize' = 3, 'agent' = 4)`. A direct insert of an `agent` row succeeded. |
| One `POST /api/dubs/{id}/agent` writes its measured charges, read back by SQL | PASS. Two POSTs to `d-c1` wrote four rows through the real route and the real adapter. Each row carried `take_id` empty, `commit_id` empty, `segment_index` -1, attempt 0, provider `gemini-3.8-flash`, and units 1200 at price 150 or 340 at price 600. |
| The running total grows by the turn's nanodollars | PASS. `d-c1` summed 292000 nanodollars before the turn and 676000 after, a rise of 384000. The response reported 384000. |
| The `covers` sentence names the agent calls | PASS. The payload read `1 segment call, 1 translation call, 1 render call, and 2 agent calls.` SQL counted 1 segment, 1 translation, 1 render and 2 agent calls for the same dub. |
| Two identical turns record two charges rather than one | PASS. Two identical POSTs to `d-c1` produced four rows, two distinct `turn_id` values, and four distinct `event_key` values. |
| `internal/agent` imports no writer | PASS. `go list -deps ./internal/agent` names only `internal/config`, `internal/cost` and the package itself among repository packages. It names no ledger, no api and no database driver. |
| The `mcp_readonly` grant still refuses an INSERT | PASS. `SHOW GRANTS FOR mcp_readonly` read `GRANT SELECT ON t66c_r2.* TO mcp_readonly`. An INSERT of an `agent` row failed with code 497 `ACCESS_DENIED`. The same user read `SELECT count() FROM charges` and answered 11. The writer control insert succeeded. |
| A nil recorder keeps the route answering and records nothing | PASS. A server built with no `AgentCharges` answered HTTP 200 with a total of 384000. `SELECT count() FROM charges WHERE dub_id='d-nil'` returned 0. The queue directory held zero files. |
| The agent package stays read-only | PASS. The diff adds no file under `internal/agent`. `deploy/mcp-clickhouse/compose.yaml` keeps `CLICKHOUSE_ALLOW_WRITE_ACCESS: "false"` and the `mcp_readonly` user. |

**Nine conditions pass. The first finding sits beyond every one of them.** The task named storage,
the writer, the total and the boundary. It did not name the amount on the folded project row. The
folded row is wrong while every named number stays right.

## Residue against prior rounds

| Prior finding | Residue |
|---|---|
| C1 | NOT ZERO. The fold removes the agent project-row duplicate. A dub with two identical agent turns and no duplicate take charge key rendered the Details tab with no exception (`d-creal`). But the folded row reports the wrong units and the wrong total, which is finding 1. The tab also renders nothing on a dub with a translation, which is finding 2. |
| H1 | Zero. `RecordAgentTurn` journals every row before its first flush. I stopped the ClickHouse container, posted one turn, and read HTTP 500. The queue then held two files with the correct `turn_id`, prompt and candidate rows. I started the container again. The reconciler delivered both rows within one second and drained the queue. |
| H2 | Zero. A POST to `d-nocommit`, which has no commit row, answered HTTP 200. Both rows carried an empty `commit_id`. |
| M1 | Zero. The comment above `selectWholePassCharges` states that the writer attributes segmentation to the first rendered take. It separately states that agent calls own no take. |
| M2 | Zero. The running-total query counts by call identity for all four kinds. For one segment call, one translate call, one render call and two agent turns, SQL returned 1, 1, 1 and 2. The old `countIf` shape returned 2, 2, 1 and 0 on the same rows. A probe through the real writer confirmed one translate call writes two unit rows. |
| L1 | Zero. The schema header states that a genuine second take call must raise `attempt`. The `charges_raw` comment states the same rule beside the turn-id sentence. |

## Non-owned paths in the commit

`fd32354` changed 19 paths outside the T6.6c owns line. Twelve are dev-diary files, which are the
P5, P6 and P7 close records and the phase and README status lines. Three are `internal/command/parse.go`,
`internal/command/parse_test.go` and `internal/api/command_test.go`.
Four more are `web/src/routes/d/[id]/+page.svelte`, `web/src/lib/LengthBar.svelte`, `web/src/lib/Track.svelte` and `web/src/lib/progress.ts`.
The command and web changes are the P6 close round 4 overlap fix and P5 layout polish. None touches
the agent route, the ledger writer, the schema, or the Details tab. The commit therefore mixes
ownership, and the mixing did not change T6.6c behaviour.

## Evidence paths

- ClickHouse container `t66c-r2-ch` on `127.0.0.1:18931`, database `t66c_r2`, loaded from `sql/schema.sql`.
- Real binary `/tmp/t66c-r2-ajilamu` on port 18101, pointed at that database, serving `web/build`.
- Throwaway harness `cmd/ajilamu/zz_t66c_r2_harness_test.go` in package `main`, mounted the real route and adapter. Nil-recorder run used `/tmp/t66c-r2-nil.url`.
- Throwaway probe `internal/ledger/zz_t66c_r2_probe_test.go`, recorded takes through the real `RecordTake`.
- Production frontend from `npm --prefix web run build`, log at `/tmp/t66c-r2-webbuild.log`.
- Headless Chrome for Testing 151.0.7922.34 over CDP on port 19231. Probe function drove `Page.navigate`, clicked `#rail-details-tab`, and read `Runtime.exceptionThrown`.
- Seed files `/tmp/t66c-r2-seed.sql` through `/tmp/t66c-r2-seed5.sql`. Dubs `d-creal` and `d-real` took their take and charge rows from the real `RecordTake` writer.

## Cleanup statement

I deleted the `/tmp/t66c-r2` copy and both throwaway test files with it. I stopped the harness, the
nil harness, the Chrome process and the real binary. I removed the ClickHouse container. No
container, server or browser survives. `git status` in the main checkout shows only this new
untracked review file.
