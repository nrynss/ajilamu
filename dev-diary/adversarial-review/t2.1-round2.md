# T2.1 Round 2 Review

**Verdict:** APPROVE
**Commit:** Re-review of the remediated T2.1 segmentation client. Untracked files internal/gemini/segment.go, segment_test.go, and segment_live_test.go carry the four round 1 fixes.
**Round 1 residue:** Zero residue against all four round 1 findings. Each fix kills its round 1 mutation under my own measurement. See the residue table.

## Severity Counts

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 0 |

## Round 1 Residue

| # | Round 1 Finding | Residue | Pin |
|---|-----------------|---------|-----|
| 1 | Vacuous JWT lifetime check | Zero. The check at segment_test.go:425-427 asserts `claims.Exp-claims.Iat != 3600`. | Staged `Exp: now.Add(2 * time.Hour)` in scratch copy `scratch/t21rev2`. `go test -run TestServiceAccountTokenSource` failed with `jwt lifetime = 7200, want 3600` at segment_test.go:426. Restored the pristine file and the test passed. |
| 2 | Endpoint tests blind to a hardcoded default | Zero. TestEndpointURL:340-351 asserts project `ajilamu-prod`, location `europe-west4`, and model `gemini-2.5-flash` against config. | Hardcoded `"us-central1"` into the endpointURL format call. The test failed at segment_test.go:350 with the location mismatch. Hardcoded `"gemini-3.8-flash"` the same way and the test failed again at the same line. Restored the pristine file and `cmp` confirmed the restore. |
| 3 | Prompt drift against the Python reference | Zero. The Go raw literal carries the Python leading newline and the trailing `]\n`. | Extracted the Python string with the ast module and the Go literal between its backticks. Both measure 658 bytes. `cmp` reports identical. sha256 `0e8717cd` matches on both dumps in scratch/t21bytes. Units derive from `len(segmentPrompt)` at segment.go:414 and segment_test.go:137, so the cost basis tracks the const. |
| 4 | Live probe deaf to GEMINI_MODEL | Zero. segment_live_test.go:54 reads `envDefault(t, "GEMINI_MODEL", config.DefaultGeminiModel)` beside GOOGLE_CLOUD_LOCATION on line 56 with `config.DefaultGoogleLocation`. | `go vet -tags live ./internal/gemini/` passes, so the file compiles with the new wiring. Static check plus grep per the review method since the file never runs without the tag. |

## Clean Probes

| Probe | Judgement | Pin |
|-------|-----------|-----|
| Charge on success only | Intact. Segment adds the charge after postGenerate, envelopeText, and decodeSegments all succeed. | Read of segment.go:399-416. Five failure tests each assert zero charges, including TestSegmentHTTP500 and TestSegmentMalformedInnerJSON. |
| Validation rules | Intact. Start precedes end, duration equality, non-empty text, speaker, and emotion, strictly increasing ids, zero-segment rejection. | TestDecodeSegmentsValidation, TestSegmentDurationMismatch, and TestDecodeSegmentsGoldenFixture pass in the suite run. |
| No test weakened | Intact. The two touched tests only gained assertions. TestSegmentSuccess still pins one charge with `len(segmentPrompt)` units, the bearer header, the request path, the payload shape, and prompt equality. | Read of the full segment_test.go. Suite passes with `-count=1`. |
| Exported surface | Unchanged. Package gemini exports ChargeRecorder, Input, Segmenter, TokenSource, StaticTokenSource, NewSegmenter, NewStaticTokenSource, and NewServiceAccountTokenSource. | `go doc -all ./internal/gemini` lists exactly that set. |
| Offline and env isolation | Intact. Default tests touch no network and read no environment. | `HTTP_PROXY=http://127.0.0.1:9 HTTPS_PROXY=http://127.0.0.1:9 GEMINI_MODEL=poison GOOGLE_APPLICATION_CREDENTIALS=/nonexistent go test -count=1 ./internal/gemini/` passed. `Getenv` appears only in the live-tagged file. |
| Concurrency and hygiene | Clean. | `go test -race -count=1 ./internal/gemini/` passed. `gofmt -l internal/gemini/` prints nothing. `go build ./...` and `go vet ./...` pass. |
| Ownership | Clean. This review mutated only scratch copies. | `git status --porcelain` shows untracked internal/gemini/ plus the pre-existing T1.3 dirt. The mutation workspace scratch/t21rev2 restored byte-identical to the pristine snapshot. |

No false positives this round.
