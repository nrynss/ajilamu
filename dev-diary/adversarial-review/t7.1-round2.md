# T7.1 Round 2 Review

## Verdict

APPROVE

| Severity | Count |
|---|---:|
| C | 0 |
| H | 0 |
| M | 0 |
| L | 0 |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| None | Zero findings remain. | All automated and manual checks pass. | N/A |

## Independent Evidence

| Check | Observation |
|---|---|
| Live HTTP mount (C1) | I ran the compiled binary without credentials on port 18711. A multipart video request to `/api/dubs/new` returned 201 Created. The storage directory holds the created project with 0600 file permissions. |
| Fee estimate (H1) | Evaluating the formula at 75.008267 seconds yields 23,414,000 nanodollars ($0.02). This matches the reference in `testdata/wire/total.json` exactly. |
| Test suite and coverage (H2) | `go test -coverprofile` reports non-zero coverage for every function in `upload.go`. Overall statement coverage across `upload.go` reaches 74.2 percent. `upload_test.go` pins success, errors, size limits, cleanup, relative paths, and permissions. |
| Contrast calculations (M1) | Light theme error text `#7a271a` on `#F0E4D6` gives a 7.87:1 contrast ratio. Dark theme error text `#ffd3ce` on `#33261A` gives a 10.79:1 ratio. Both pass WCAG 2.1 AA normal text limits. |
| Creator vocabulary (L1) | A case-insensitive scan of `web/src/routes/new/+page.svelte` finds zero forbidden billing terms. |

## Residue Against Prior Findings

### C1 · Server mount at `POST /api/dubs/new`
T7.0 mounted `NewUploadHandler` on `POST /api/dubs/new`.
An independent POST request on port 18711 returned status 201 Created and wrote the video asset to disk.
C1 has zero residue.

### H1 · Projected fee model
The start screen calculates costs using the P2 fixture ledger baseline.
For 75.008267 seconds, the formula evaluates to 23,414,000 nanodollars.
This matches `testdata/wire/total.json` with zero error.
H1 has zero residue.

### H2 · Missing upload test coverage
`upload_test.go` pins upload creation, rejection cleanup, relative pathing, and 0600 permissions.
`go tool cover -func` confirms coverage on every function in `upload.go`.
H2 has zero residue.

### M1 · Error text contrast
Light theme contrast measures 7.87:1.
Dark theme contrast measures 10.79:1.
Both exceed the 4.5:1 WCAG 2.1 AA requirement.
M1 has zero residue.

### L1 · Forbidden billing terminology
The creation screen uses creator terminology and avoids internal billing jargon.
A scan for forbidden words returned no matches.
L1 has zero residue.
