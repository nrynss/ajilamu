# T4.1 adversarial review, round 1

Verdict: REMEDIATE

Counts: C0, H1, M0, L0.

Prior-round residue: None. This is the first review round.

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| H | `internal/ledger/client.go:54-101`, `internal/ledger/queue.go:76-108` | A recovered connection does not reconcile queued events. `New` opens the queue but never calls `Flush`, and no retry worker exists. Events reconcile only if a caller later invokes `Flush` or enqueues another row. T4.1 requires queued commits to reconcile upon reconnection. | An isolated HTTP probe forced the first response connection closed after receiving the JSON row. It then made the endpoint return `200`, constructed a second client over the same queue directory, and observed one durable `.json` entry, `Pending() == 1`, and one received request. Command: `go test -count=1 -run TestNewDoesNotReconcileRecoveredQueue -v .` in `/tmp/ajilamu-t41-review.LdxAQ0`. | Reverting a reconnect or startup reconciliation path strands creator edits after an outage when no later write happens. |

Independent checks:

- The companion dropped-response probe retained the entry after the forced disconnect. A manual `Flush` replayed it once and removed the journal file. The probe used `WithEndpoint` and a local HTTP server.
- `go test ./...` passed. The new `internal/ledger` package reports no repository test files.
- `go vet ./...`, `gofmt -d internal/ledger/client.go internal/ledger/queue.go`, and `git diff --check` produced no findings.
- The implementation batches adjacent entries with the same insert statement. It retains entries until a successful HTTP response. T1.3 natural event keys make an ambiguous response retry safe.

Scope note:

T4.1 owns no test path. That omission prevents the required network-drop evidence from landing as a durable regression test. It is a task-shape constraint, not an additional implementation finding. The remediation owner needs an explicit test-path extension or a follow-up test task.
