# T2.8b remediation, round 2

Task: Fit loop language wiring. I remediated the three findings in `t2.8b-round2.md`. Go toolchain
go1.27.1. I changed `cmd/ajilamu/main_test.go`, `dev-diary/adversarial-review/t2.8-contract-change.md`,
and the T2.8b handoff entry in `dev-diary/PHASE-2-fit-loop.md`. I did not change
`cmd/ajilamu/main.go`. I did not change the verdict. The verdict stays REMEDIATE.

## Rows

| Finding | Fix | Pin | Residue |
|---|---|---|---|
| M1 · `cmd/ajilamu/main_test.go:659-750` | Table-drove `TestPipelineRunnerLanguageWiring` over `ml` and `es-ES`. Each case asserts the resolved tag, the display name, the Cloud TTS `LanguageCode`, the Cloud TTS `Name`, and every event language. | Both cases pass. Mutation D fails the `es-ES` case. Output below. | Mutation C survives. No offline test reaches the production factory, so a factory that ignores its argument stays green. |
| L1 · `dev-diary/adversarial-review/t2.8-contract-change.md:47-49` | Replaced the false ownership and coverage paragraph. It now names the `owns` line and the two committed tests. | `dev-diary/PHASE-2-fit-loop.md:300` lists the test file. Both tests pass. | None. |
| L2 · `dev-diary/PHASE-2-fit-loop.md:759` | Replaced the false count. It now names the twenty-five literals that set Malayalam and the two that do not. | `grep -c 'PipelineConfig{' internal/fit/loop_test.go` prints 27. `grep -c 'Language: *tts.Malayalam' internal/fit/loop_test.go` prints 25. | None. |

## M1 pin output

The table-driven test drives both languages. Measured on 2026-09-09:

```
$ go test -count=1 -run 'TestPipelineRunnerLanguageWiring' -v ./cmd/ajilamu
=== RUN   TestPipelineRunnerLanguageWiring
=== RUN   TestPipelineRunnerLanguageWiring/create_screen_code
    main_test.go:720: factory language = "ml-IN"
    main_test.go:733: Cloud TTS LanguageCode = "ml-IN", Name = "ml-IN-Chirp3-HD-Achernar"
=== RUN   TestPipelineRunnerLanguageWiring/full_tag
    main_test.go:720: factory language = "es-ES"
    main_test.go:733: Cloud TTS LanguageCode = "es-ES", Name = "es-ES-Chirp3-HD-Achernar"
--- PASS: TestPipelineRunnerLanguageWiring (1.96s)
    --- PASS: TestPipelineRunnerLanguageWiring/create_screen_code (0.97s)
    --- PASS: TestPipelineRunnerLanguageWiring/full_tag (0.98s)
PASS
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	1.962s
```

The `ml` case asserts tag `ml-IN` and display name `Malayalam`. The `es-ES` case asserts tag
`es-ES` and display name `es-ES`, because the fallback names the code itself.

## M1 mutation D now fails

In `/tmp/t2.8b-rem2`, `Run` passes the constant tag at the synthesizer call:

```
$ sed -i 's|p.newSynthesizer(language.tag, charges)|p.newSynthesizer("ml-IN", charges)|' cmd/ajilamu/main.go
$ grep -n 'p.newSynthesizer' cmd/ajilamu/main.go
452:	synthesizer, err := p.newSynthesizer("ml-IN", charges)
$ go test -count=1 -run 'TestPipelineRunnerLanguageWiring' ./cmd/ajilamu
--- FAIL: TestPipelineRunnerLanguageWiring (1.94s)
    --- FAIL: TestPipelineRunnerLanguageWiring/full_tag (0.98s)
        main_test.go:720: factory language = "ml-IN"
        main_test.go:722: factory language = "ml-IN", want es-ES
        main_test.go:733: Cloud TTS LanguageCode = "ml-IN", Name = "ml-IN-Chirp3-HD-Achernar"
        main_test.go:735: Cloud TTS LanguageCode = "ml-IN", want es-ES
        main_test.go:738: Cloud TTS Name = "ml-IN-Chirp3-HD-Achernar", want es-ES-Chirp3-HD-Achernar
FAIL
FAIL	github.com/nrynss/ajilamu/cmd/ajilamu	1.947s
```

The `ml` case stays green under the mutation, because its tag is the constant. The `es-ES` case
fails on the factory, the Cloud TTS code, and the Cloud TTS name.

## M1 residue: mutation C

In `/tmp/t2.8b-rem2c`, the production closure ignores its argument:

```
$ sed -i 's|return tts.NewSynthesizer(cfg, language, rec, card, nil)|return tts.NewSynthesizer(cfg, "ml-IN", rec, card, nil)|' cmd/ajilamu/main.go
$ go test -count=1 -run 'TestPipelineRunner' ./cmd/ajilamu
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	1.973s
```

The wiring test replaces `newSynthesizer`, so it never reaches the production closure. An offline
test cannot reach `newPipelineRunner` either, because that path opens real Gemini clients. The
finding M1 pin is mutation D, and that pin now holds.

## L1 before and after

Before, `t2.8-contract-change.md:47-49`:

> The harness was removed, because `cmd/ajilamu/main_test.go` sits outside this task's `owns`.
> T7.5b and T7.7 own `cmd/ajilamu/main.go` but not its test file, so the runner wiring has no
> permanent test. The orchestrator should name an owner for it.

After, `t2.8-contract-change.md:46-50`:

> synthesizer factory each saw zero calls. No Cloud TTS request and no charge followed. The
> harness was removed after it measured the seam, because a throwaway probe does not belong in
> the tree. This task's `owns` line lists `cmd/ajilamu/main_test.go`
> (`dev-diary/PHASE-2-fit-loop.md:300`). Two committed tests pin the runner wiring:
> `TestPipelineRunnerLanguageWiring` and `TestPipelineRunnerRejectsLanguageBeforeCharge`.

The `owns` line at `dev-diary/PHASE-2-fit-loop.md:300` lists `cmd/ajilamu/main_test.go`. Both
tests pass on the clean tree.

## L2 before and after

Before, `PHASE-2-fit-loop.md:759`:

> **Tests.** Every `PipelineConfig` literal in `loop_test.go` now sets `Language: tts.Malayalam`
> and `TargetLanguageName: "Malayalam"`. Resume tests match the records they write with
> `tts.Malayalam`.

After, `PHASE-2-fit-loop.md:759`:

> **Tests.** Twenty-five `PipelineConfig` literals in `loop_test.go` set
> `Language: tts.Malayalam` and `TargetLanguageName: "Malayalam"`. The two new language tests use
> `es-ES` and a parameter instead. Resume tests match the records they write with
> `tts.Malayalam`.

The counts come from the file. `grep -c 'PipelineConfig{' internal/fit/loop_test.go` prints 27.
`grep -c 'Language: *tts.Malayalam' internal/fit/loop_test.go` prints 25. The two exceptions are
`TestPipelineLanguageWiring` at line 954 and `TestPipelineLanguageFailsBeforeCharge` at line 1004.

## Validation

```
$ go test -count=1 ./cmd/...
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	2.792s

$ gofmt -l cmd/ajilamu/main_test.go
gofmt done
```

`gofmt -l` printed nothing.

## Done condition

The three findings are closed. M1 keeps one residue, which the finding did not pin. I did not
mark the task done.
