# T2.8b remediation, round 3

Task: Fit loop language wiring. I remediated the one finding in `t2.8b-round3.md`. Go toolchain
go1.27.1. I changed `cmd/ajilamu/main.go`, `cmd/ajilamu/main_test.go`, and this record. I did not
change the verdict. The verdict stays REMEDIATE. I did not mark the task done.

## Row

| Finding | Fix | Pin | Residue |
|---|---|---|---|
| M1 · `cmd/ajilamu/main.go:429-431` | Extracted the inline synthesizer closure into `runSynthesizerFactory(cfg, card, client)` at `cmd/ajilamu/main.go:410-417`. `newPipelineRunner` now points `newSynthesizer` at `runSynthesizerFactory(cfg, card, nil)`. | `TestRunSynthesizerFactoryForwardsLanguage` calls the production factory with a fake client. It asserts the Cloud TTS `LanguageCode` and `Name` for `ml-IN` and `es-ES`. The hardcoded factory fails the `es-ES` case. | None. |

## Pin output

The test drives both languages. Measured on 2026-09-09:

```
$ go test -count=1 -run 'TestRunSynthesizerFactoryForwardsLanguage' -v ./cmd/ajilamu
=== RUN   TestRunSynthesizerFactoryForwardsLanguage
=== RUN   TestRunSynthesizerFactoryForwardsLanguage/create_screen_tag
    main_test.go:839: Cloud TTS LanguageCode = "ml-IN", Name = "ml-IN-Chirp3-HD-Achernar"
=== RUN   TestRunSynthesizerFactoryForwardsLanguage/full_tag
    main_test.go:839: Cloud TTS LanguageCode = "es-ES", Name = "es-ES-Chirp3-HD-Achernar"
--- PASS: TestRunSynthesizerFactoryForwardsLanguage (0.09s)
    --- PASS: TestRunSynthesizerFactoryForwardsLanguage/create_screen_tag (0.05s)
    --- PASS: TestRunSynthesizerFactoryForwardsLanguage/full_tag (0.05s)
PASS
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	0.098s
```

The test uses no ADC and no network. The fake client records the voice Cloud TTS receives.

## Mutation fails

In `/home/nryn/tmpgo/rem3`, the extracted factory ignores its argument and builds `ml-IN`:

```
$ sed -i 's|return tts.NewSynthesizer(cfg, language, rec, card, client)|return tts.NewSynthesizer(cfg, "ml-IN", rec, card, client)|' cmd/ajilamu/main.go
$ sed -n '414,416p' cmd/ajilamu/main.go
	return func(language string, rec tts.ChargeRecorder) (tts.Synthesizer, error) {
		return tts.NewSynthesizer(cfg, "ml-IN", rec, card, client)
	}
$ go test -count=1 -run 'TestRunSynthesizerFactoryForwardsLanguage' -v ./cmd/ajilamu
=== RUN   TestRunSynthesizerFactoryForwardsLanguage
=== RUN   TestRunSynthesizerFactoryForwardsLanguage/create_screen_tag
    main_test.go:839: Cloud TTS LanguageCode = "ml-IN", Name = "ml-IN-Chirp3-HD-Achernar"
=== RUN   TestRunSynthesizerFactoryForwardsLanguage/full_tag
    main_test.go:839: Cloud TTS LanguageCode = "ml-IN", Name = "ml-IN-Chirp3-HD-Achernar"
    main_test.go:841: Cloud TTS LanguageCode = "ml-IN", want es-ES
    main_test.go:844: Cloud TTS Name = "ml-IN-Chirp3-HD-Achernar", want es-ES-Chirp3-HD-Achernar
--- FAIL: TestRunSynthesizerFactoryForwardsLanguage (0.09s)
    --- PASS: TestRunSynthesizerFactoryForwardsLanguage/create_screen_tag (0.04s)
    --- FAIL: TestRunSynthesizerFactoryForwardsLanguage/full_tag (0.05s)
FAIL
FAIL	github.com/nrynss/ajilamu/cmd/ajilamu	0.095s
FAIL
```

The `ml-IN` case stays green, because its tag is the constant. The `es-ES` case fails on the
Cloud TTS code and the Cloud TTS name. The shared `/tmp` tmpfs was full, so the mutation copy
lived under `/home/nryn/tmpgo`.

## Validation

```
$ go build ./...
build exit=0

$ go test -count=1 ./cmd/...
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	2.965s
test exit=0
```

## Done condition

The one finding is closed with no residue. The production factory now has a committed pin. I did
not change the verdict and I did not mark the task done.
