# T1.3 Round 3 Review

**Verdict:** REMEDIATE
**Artifact:** sql/schema.sql, 626 lines, untracked at review time.
**Reviewer:** I did not implement T1.3 or remediate either prior round.

## Severity Counts

| C | H | M | L |
|---|---|---|---|
| 0 | 2 | 0 | 0 |

## Residue Against Round 2

Round 2 findings 1, 2, 3, 5 and 7 have zero residue.

1. The two snapshot writes for one commit and segment collapsed to the final speaker and corrected source text on both servers.
2. Five `timeline_at_commit` reads returned 40 rows. An independent action replay returned the same 40 rows with zero field mismatches.
3. Both databases held zero projections. A take resend left `take_rates` at one row while `takes_raw` held two deliveries.
4. Round 2 finding 4 has residue. The guard ignores materialized expressions and replacing engine parameters. Finding 2 records the measured loss.
5. Plain names remained safe read views. An insert into `charges` failed with Code 48 on both servers.
6. Round 2 finding 6 has residue across the take and charge boundary. Finding 1 records the doubled cost.
7. The removed projection left no ordering key on `created_at`. Both databases reported zero projections.

## Findings

| # | Sev | Where | What | Pin | Mutation |
|---|-----|-------|------|-----|----------|
| 1 | H | `sql/schema.sql` lines 485 to 490 and 518 to 520 | `takes_raw` excludes `take_id` from retry identity, while `charges_raw` includes it in its event key and sorting key. A reconnect that regenerates the take id dedups the take and duplicates its charges. This contradicts the fresh random id promise and the P4 no-double-count exit criterion. | I resent fixture segment 1 on both servers with a fresh take id and two fresh charge ids. `takes_raw` rose from 10 to 11 while `takes` stayed at 10. `charges_raw` and `charges` both rose from 17 to 19. The safe-view total rose from 0.023414 to 0.0242561. A later full charge resend with stable take ids raised raw rows from 19 to 36 while leaving the view at 19 and the same total. | A regression must resend one take and its charges with all delivery ids changed. Reverting the shared stable identity must raise the safe charge count and cost. |
| 2 | H | `sql/schema.sql` lines 182 to 217 | The preflight compares column types, engine names and sorting keys. It ignores materialized expressions, partition keys, constraints and replacing engine parameters. An incompatible table can pass and silently discard distinct events. | I copied `takes_raw`, changed only `event_key` to `String MATERIALIZED 'x'`, and ran the file on both servers. The guard printed 0 and the full file exited 0. Two takes from different commits then produced one raw row and one view row. I also replaced `ReplacingMergeTree(ingested_at)` with unversioned `ReplacingMergeTree` for `timeline_state_raw`. That guard also printed 0 on both servers. | A regression must change each protected expression and engine parameter independently. Reverting metadata checks must let the constant key pass and collapse two commit-distinct takes. |

## What I Confirmed Working

1. Cloud reported version 26.2.1.641. The local binary reported version 26.8.2.7.
2. One unmodified file created five tables and seven views on each server. Cloud used `SharedReplacingMergeTree`. Local used `ReplacingMergeTree`.
3. A second full run printed 0 and exited 0 on both servers. Counts stayed at 6 commits, 10 takes, 21 states, 22 actions and 17 charges.
4. The fixture load accepted 10 takes and 17 charges. Segment 8 read 7110 slot milliseconds, 4200 measured milliseconds and a signed delta of -2910.
5. Fixture charges grouped to 0.000067 for segmentation, 0.000067 for translation and 0.02328 for synthesis. Their total was 0.023414.
6. The 64-value fixture peaks array returned length 64, first value 44 and last value 92.
7. Fresh and already-built databases made the preflight print 0. A compatible half run resumed to five tables and seven views on both servers.
8. Legacy shape, missing column, wrong type, extra column, plain engine, wrong sorting key and an occupied view name each aborted with Code 395.
9. A fixed-take-id charge resend used fresh charge ids and a moved batch boundary. Raw rows increased by 17 while view rows and cost stayed fixed.
10. A take resend with a fresh take id left `takes_raw` at 11 and `takes` at 10. An action resend left `actions_raw` at 23 and `actions` at 22.
11. Two unrelated events survived in each of `takes`, `actions` and `charges`. Every view returned two rows for its pair.
12. Two identical billed calls at attempt 1 collapsed. Raising the genuine repeat to attempt 2 produced two calls.
13. The schema cannot force that attempt bump because the two calls otherwise have identical facts. The T4.1 write contract must require it.
14. Recomputed hashes found zero mismatches in all three event-key tables. Three commits reusing one `take_id` remained three rows with three event keys.
15. The main and fork ancestry walks returned exact chains with versions 1 through 5. Stored sequences were monotonic within each branch.
16. Five direct timeline reads matched an independent JSON action replay across all 40 segment states. The comparison found zero field mismatches.
17. The boundary-only commit returned start 5908, end 7928, both texts and `seg_1_try1`. No field fell back to its default.
18. The two-edit commit returned corrected source text and speaker `Mark Vande Hei`. The sibling fork retained the earlier spelling and speaker.
19. Absolute 2910 and clamped 0 deltas failed `delta_is_signed` on both servers. A 10-value peaks array failed `peaks_are_sized`.
20. Peak lengths 0, 64, 96 and 128 all inserted on both servers.
21. Empty slots, negative segment indexes, negative units and negative prices fired their named constraints on both servers.
22. Unknown repair, author and kind members failed with Code 691 on both servers.
23. `takes_raw` rejected a `cost_usd` write with Code 16. `charges_raw` rejected a direct materialized cost with Code 44.
24. A charge with 100 units at 0.00003 derived 0.003 on both servers.
25. Every `ingested_at` column reported `MATERIALIZED now64(3)`. A writer timestamp of 2099 stayed in partition 202609, with no 209912 partition.
26. I parsed every comment paragraph. I found no semicolon, em dash, en dash, sentence over 30 words or passive construction.
27. The action vocabulary remains a string because the task and product documents disagree. T4.4 must resolve that recorded contract conflict.

## Verification

I ran every measurement myself against both servers. I used only throwaway databases and local paths under `scratch/t13r3`.

I sourced the gitignored `.env` inside each Cloud invocation. The client read the user and password from the environment. I never printed or wrote credentials.

I applied `sql/schema.sql` through the ClickHouse client query-file mode. Local invocations used timeouts and closed stdin for files containing inline values.

I inspected the fixture generator against `testdata/manifest.json` and `testdata/expected/metrics.json` before loading its SQL. The load contained 10 fixture takes and 17 fixture charges.

I used separate databases for every guard mutation. I changed one guarded property at a time, then applied the unmodified artifact.

I dropped `t13_review_r3` and every `t13_review_r3_g*` Cloud database. A final query found zero matching databases.

`default.takes` held 8 rows before the review and 8 rows after cleanup. I never wrote to `default`.

I removed `scratch/t13r3` after local verification. I wrote only this verdict file.
