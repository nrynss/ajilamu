# T4.3 adversarial review, round 5

Verdict: REMEDIATE

Counts: C0, H1, M0, L0.

Prior-round residue: Round 1 remains closed. `commits_test.go` keeps the
durable queue capture for roots, chains, forks, invalid shapes, server-owned
timestamps, and replay. Round 2 remains closed. Parent lookups require a
present lower-version node, so an orphan or reverse edge cannot enter the
queue. Round 3 remains closed. The replay test holds the failed delivery
until its journal artifact exists before allowing recovery. Round 4 is closed
for sequential calls. A present `(dub_id, commit_id)` produces one lookup,
zero inserts, and an identity error. The remaining concurrent identity race
means no APPROVE is possible.

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| H | `internal/ledger/commits.go:35-44` | Identity validation and `EnqueueJSON` form an unlocked check-then-act sequence. Two concurrent `AppendCommit` calls with the same `(dub_id, commit_id)` can both read no existing row before either sends its insert. They can then enqueue different payloads for the same immutable identity. `ReplacingMergeTree` collapses the pair later, leaving either payload visible. | `TestAppendCommitRejectsDuplicateIDBeforeInsert` measures only a sequential duplicate. It waits for the existing lookup response before its one call returns. `go test -race -count=20 ./internal/ledger` passed, which detects memory races, not this allowed request interleaving. The two unguarded operations are visible in sequence at lines 39 and 45. No test holds two identity lookups at empty state and counts the two resulting inserts. | Reverting a serialization or compare-and-insert guard lets two pipeline actions give one commit id different parent, version, or message values. The history panel can read an altered DAG node even though neither caller issued an update. |

Independent evidence:

- `go test -race -count=20 ./internal/ledger` passed. This repeat race suite
  covers the existing concurrency checks, but it does not make the identity
  lookup and later enqueue atomic.
- `TestAppendCommitRejectsDuplicateIDBeforeInsert` captures the expected
  sequential protection. It sends `dub` and `root` in the lookup, returns the
  existing identity, observes zero `INSERT` requests, and receives an `already
  exists` error.
- `TestAppendCommitReplaysFailedDelivery` still captures a failed root delivery
  and its byte-identical recovered insert. The automatic reconciler invokes
  `Flush` on the durable queue, not `AppendCommit`, so an acknowledged or
  uncertain delivery retry does not falsely fail the identity lookup.
- `TestAppendCommitWritesDAGWithoutClientTimestamps` observes a root, a linear
  child, and a fork. It confirms `commits_raw`, correct parent values and
  version values, branch labels, and absent client timestamps.
- `TestAppendCommitRejectsLaterAndCyclicParentsBeforeInsert` and the invalid
  cases in the DAG capture reject absent, self, and later parents with zero
  inserts. The parent version rule still makes a completed graph acyclic.
- `go vet ./internal/ledger`, `gofmt -d internal/ledger/commits.go
  internal/ledger/commits_test.go`, and `git diff --check --
  internal/ledger/commits.go internal/ledger/commits_test.go` passed.
