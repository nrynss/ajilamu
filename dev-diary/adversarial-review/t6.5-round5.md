# T6.5 ghost take history: round 5

**Verdict: APPROVE.** Counts: **C 0, H 0, M 0, L 0.**

The round 4 remediation replaced the single in-flight id with a set of ids, and the
`finally` deletes only its own id. I re-measured the two-line overlap, three
overlapping re-renders and a failed reply. The fix holds in all three. The mutation
reddens the pin. The round three pins still hold. The open H stays with T6.5c. I fix
nothing.

## Findings

No finding met every bar. The four columns stay empty.

| Where | What | Pin | Mutation |
|---|---|---|---|
| None | No defect met the provable-impact, actionable and patch-introduced bars. | n/a | n/a |

## What I measured

I copied the working tree to `/tmp/t65r5/repo` and symlinked `node_modules` and
`testdata`. I built with `npm --prefix web run build`. A throwaway Node server served
the built bundle and the frozen `testdata/wire/dub.json` under the non-fixture id
`t65-r5-dub`. It delayed the line 3 reply by 1500 ms, the line 2 reply by 3000 ms and
the line 1 reply by 6000 ms. I drove the page with headless Chromium through
Puppeteer. The working tree was never edited for probes. The required type check ran
in the working tree.

### 1. The two-line overlap (the round 4 M)

The run confirmed the line 3 speaker change and switched to line 1 at 200 ms. It
confirmed line 1 at 400 ms. The line 3 reply landed at 2621 ms. The run reopened the
line 1 panel at 2931 ms, while the line 1 render was still in flight.

```json
{
  "line1DuringFlight": {
    "eyebrow": "Line 1",
    "header": "Mark Vande Hei",
    "pendingOpen": 1,
    "pendingText": "Re-rendering line 1 as Suni Williams is estimated at $0.0008421.",
    "confirmText": "Re-rendering…",
    "confirmDisabled": true,
    "cancelDisabled": true,
    "note": null,
    "error": null
  },
  "postsBeforeClick": 2,
  "postsAfterClick": 2
}
```

The line 1 confirm reads `Re-rendering…` and stays disabled after the line 3 reply.
Cancel stays disabled too. A click on the disabled confirm fired no request. The
third POST is zero. The line 1 reply landed at 7415 ms. The panel then went live and
named its own take, which proves the owner reply still clears its own line.

```json
{
  "line1AfterOwnReply": {
    "confirmText": "Change speaker",
    "confirmDisabled": false,
    "cancelDisabled": false,
    "note": "Line 1 re-rendered as take seg_1_try4.wav."
  },
  "finalPosts": [
    { "segment": 3, "body": "{\"language\":\"ml\",\"speaker\":\"Mark Vande Hei\"}" },
    { "segment": 1, "body": "{\"language\":\"ml\",\"speaker\":\"Mark Vande Hei\"}" }
  ]
}
```

The run raised 0 console messages and 0 page errors.

### 2. Three overlapping re-renders

The run confirmed line 3 at 1.1 s, line 1 at 1.4 s and line 2 at 1.7 s. Three POSTs
existed after the third confirm. The replies landed at 2614 ms, 4678 ms and 7397 ms.

After the line 3 reply the other two lines stayed busy, and line 3 read idle.

```json
{
  "afterLine3Reply": {
    "line1": { "confirmText": "Re-rendering…", "confirmDisabled": true, "cancelDisabled": true },
    "line2": { "confirmText": "Re-rendering…", "confirmDisabled": true, "cancelDisabled": true },
    "line3": { "confirmText": "Change speaker", "confirmDisabled": false, "cancelDisabled": false }
  },
  "attemptConfirmLine1WhileBusy": { "postsBefore": 3, "postsAfter": 3 }
}
```

After the line 2 reply line 1 stayed busy and line 2 read idle. After the line 1
reply line 1 read idle. The final POST count is three, one per line. No duplicate
fired.

```json
{
  "afterLine2Reply": {
    "line1": { "confirmText": "Re-rendering…", "confirmDisabled": true },
    "line2": { "confirmText": "Change speaker", "confirmDisabled": false }
  },
  "afterLine1Reply": { "line1": { "confirmText": "Change speaker", "confirmDisabled": false } },
  "finalPosts": 3
}
```

The run raised 0 console messages and 0 page errors.

I also double-clicked the line 3 confirm in one gesture. The browser fired one POST.
The panel closes on the first click, so the second click lands on no button.

### 3. A failed reply

I set the line 3 status to 500. First I stayed on line 3. The reply landed at 2613 ms.

```json
{
  "owningLineAfterFail": {
    "error": "The line did not re-render (500).",
    "header": "Mark Vande Hei",
    "takeFiles": "seg_3_try1.wav,seg_3_stretched.wav",
    "pendingOpen": 0
  },
  "reopenedPanel": { "confirmText": "Change speaker", "confirmDisabled": false },
  "posts": 1
}
```

The owning line shows the error and clears its own flag. The take list is unchanged,
so no take was written. The reopened confirm is live.

Then I repeated it with line 1 in flight. The line 3 failure landed at 5635 ms.

```json
{
  "line1AfterLine3Fail": {
    "confirmText": "Re-rendering…",
    "confirmDisabled": true,
    "cancelDisabled": true
  },
  "attemptConfirmLine1WhileBusy": { "postsBefore": 3, "postsAfter": 3 },
  "line3Returned": { "error": null, "note": null, "takeFiles": "seg_3_try1.wav,seg_3_stretched.wav" },
  "line1AfterOwnReply": { "takeFiles": "seg_1_try1.wav,/tmp/t65r5-storage/t65-r5-dub/work/ml-IN/seg_1_try5.wav" }
}
```

The line 3 failure did not clear the line 1 flag. A click on the busy line 1 confirm
fired no request. The count includes the earlier page load, so the delta is zero. The
line 1 reply then appended its take. The browser logged two 500 resource errors and
the page raised 0 page errors.

### 4. The mutation fails the pin

I reverted the fix in `/tmp/t65r5/mut-set` to the round 4 single id with the unowned
`finally`. I also built a second mutation, `/tmp/t65r5/mut-clear-all`, that keeps the
set and clears the whole set in the `finally`. I rebuilt both and reran the two-line
sequence.

Both mutations produced the same failure.

```json
{
  "line1DuringFlight": {
    "confirmText": "Change speaker",
    "confirmDisabled": false,
    "cancelDisabled": false
  },
  "postsBeforeClick": 2,
  "postsAfterClick": 3,
  "thirdPost": { "segment": 1, "body": "{\"language\":\"ml\",\"speaker\":\"Suni Williams\"}" }
}
```

The reopened panel reads idle while its render is in flight. A click fires a third
billable POST. Both pins redden. The fixed build fired zero extra requests in the
same sequence.

### 5. The round three pins

The fixed build ran the line switch without a line 1 render.

```json
{
  "line1DuringFlight": {
    "eyebrow": "Line 1",
    "header": "Suni Williams",
    "pendingOpen": 1,
    "pendingText": "Re-rendering line 1 as Mark Vande Hei is estimated at $0.0008421.",
    "confirmText": "Change speaker",
    "confirmDisabled": false,
    "cancelDisabled": false
  },
  "line1AfterLine3Reply": {
    "pendingOpen": 1,
    "confirmText": "Change speaker",
    "confirmDisabled": false,
    "cancelDisabled": false
  }
}
```

The line 1 confirm stays live while line 3 re-renders. The pending panel survives the
line 3 reply and keeps its estimate. The line 3 header then read `Mark Vande Hei`. The
run recorded one POST and raised 0 console messages and 0 page errors.

### 6. The type check and the console

```text
$ npm --prefix web run check
1788913153499 START "/home/nryn/work/ajilamu/web"
1788913153500 COMPLETED 183 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
```

Exit 0. The three changed web files carry no `export let`, no `$:` and no store.

## Handoff check

The T6.5 entry matches the shipped code. The strip sits in the editor section above
the `Timeline` component, at page lines 816 to 847 with `Timeline` at 850. The ghost
and active chip attributes reproduce. The seam paragraph matches the ghost loop at
`Track.svelte:79-91`. The owns line lists `Track.svelte`.

The entry predates the async guard work. It does not describe the busy set or the
owner delete. That is incomplete rather than false, so I file nothing there.

## Residue against round one

**H1 residue: open.** T6.5c owns the take audio route. The new active take stays
unplayable until that route lands.

**M1 residue: none.** The `Dub ML` lane ran top 1284 to bottom 1348, a height of 64
pixels. Three line 3 shapes sat inside it, ghost, ghost, active. The two ghosts read a
dashed border and opacity 0.72. The active shape read a solid border and opacity 1.

**M2 residue: none.** After the 201 both the Speaker panel and the strip named the new
take. Both read `Line 3 re-rendered as take seg_3_try3.wav.`

## Residue against round two

**M1 residue: none.** The line switch with a delayed reply left both line 1 panels
empty during the flight and after the line 3 reply. Neither carried the line 3
sentence.

## Residue against round three

**M1 residue: none.** The line 1 confirm reads `Change speaker` and stays live while
line 3 re-renders.

**M2 residue: none.** The line 1 pending panel survives the line 3 reply and keeps its
estimate.

**L1 residue: none.** The seam paragraph matches the ghost loop and the owns line.

**L2 residue: none.** The T6.5c block has a blank line before its `---` rule.

**L3 residue: none.** The README row reads 7 / 9. `python3 tools/audit_docs.py` printed
`No drift. Docs and repository agree.` and exited 0.

## Residue against round four

**M residue: none.** The set names every in-flight line, and the `finally` deletes
only its own id. The two-line case held the confirm disabled and kept the POST count
at two. Three overlapping renders cleared each line on its own reply. A failed reply
cleared only its own line. Both mutations redden the pin.

## Done condition

The timeline shows ghost take history in the lanes. Both fixture takes play
independently. The estimate shows before every re-render call. The line switch leaks
no note or busy flag. The round 4 double render is closed under two and three
overlapping renders and under a failed reply. The play clause holds for the fixture
and not for a real project, and T6.5c owns that half. Verdict APPROVE with zero
findings.

## Notes, not findings

1. The chip carries `aria-pressed` on a play button. Rounds one to four declined it,
   and I do not file it again.
2. A failed reply leaves the optimistic speaker in the header while the take list is
   unchanged. The optimistic update predates this patch, and rounds two, three and
   four declined it. I do not file it.
3. The run button starts a billable run and shows no estimate before the call. That
   gap predates this patch and belongs to the run task.
