# T6.6c remediation, round 1

| Finding | Fix | Pin | Residue |
|---|---|---|---|
| C1 | `WholePassCharges` folds every agent row into one project charge. Its units count turns, while its total sums the itemized rows exactly. | `TestWholePassChargesFoldsIdenticalAgentTurns` returns one row for two identical turns. The row carries both turns and their combined cost. | None. |
| H1 | The adapter no longer flushes or reads ClickHouse before journaling. `RecordAgentTurn` writes every charge row to the durable queue before its first flush. | `TestAgentChargeRecorderJournalsBeforeFailedFlush` closes the server before recording. The failed call leaves both itemized rows pending. | None. |
| H2 | Agent rows use the empty commit sentinel, including turns recorded before a dub has its first commit. | `TestAgentChargeRecorderUsesEmptyCommitSentinel` records through an empty commit history. Both delivered rows carry an empty `commit_id`. | None. |
| M1 | The whole-pass read comment again explains that the writer attributes segmentation to the first rendered take. It separately states that agent calls own no take. | Read the comment above `selectWholePassCharges` beside both statements. | None. |
| M2 | The running-total query counts segment, translate and synthesize calls by shared call identity. Prompt and candidate unit rows now count once. | `TestWorkspaceQueriesBindAndShape` requires `uniqExactIf` call identities for all three kinds. | None. |
| L1 | Both schema explanations restore the rule that a genuine repeated take call must raise `attempt`. | Read the schema header and the `charges_raw` comment. Both distinguish a second call from a delivery retry. | None. |
