# T5.5 Remediation Round 2

| Finding | Where | Fix | Pin |
|---|---|---|---|
| F1 | `web/src/lib/tabs/HistoryTab.svelte` | The commit graph now assigns each fork sibling its own branch lane. Edges first move horizontally from the parent and then vertically within the child lane, so a sibling edge cannot run through another saved node. | A synthetic root with two children produces child lanes `0` and `1`, with the second edge `M 7 13 H 33 V 149`. It does not draw a vertical stroke through the first child at x `7`. |

Validation used `npm run check` and `npm run build` in `web/`.
