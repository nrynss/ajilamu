# T7.1 Round 1 Remediation

| Finding | Remediation | Independent pin |
|---|---|---|
| C1 | T7.0 owns server wiring. T7.1's documented handler contract remains `NewUploadHandler` at `POST /api/dubs/new` with T7.0's persistent application-data directory. | T7.0 must post multipart input to the running route, assert `201` JSON, and inspect its persistent project directory. T7.1 does not modify the T7.0 seam. |
| H1 | The start screen scales the completed P2 fixture ledger. Its reference is 23,414,000 nanodollars for the 75.008267-second NASA clip. It explains that the completed ledger remains final. | `jq '.total_nanodollars' testdata/wire/total.json` returns `23414000`. Evaluating the displayed formula at 75.008267 seconds returns the same integer. |
| H2 | `upload_test.go` now pins successful video and music storage, relative response paths, file bytes, 0600 permissions, request errors, limit failures, and cleanup after rejected uploads. | `go test -coverprofile=/tmp/t71-api-coverage.out ./internal/api` executes all upload tests. `go tool cover -func` reports non-zero coverage for each `upload.go` function. |
| M1 | Error text uses `#7a271a` on the light accent surface and `#ffd3ce` on the dark accent surface. | Independent sRGB calculations measure 7.87:1 in light mode and 10.79:1 in dark mode. Both exceed WCAG 2.1 AA's 4.5:1 normal-text threshold. |
| L1 | The visible estimate description now says that it uses a completed project's measured cost. | A case-insensitive scan of `web/src/routes/new/+page.svelte` finds none of the forbidden billing phrases. |

## Scope

T7.0 owns C1 and its running-server proof. This remediation changed only T7.1-owned upload and create-screen paths, plus this record.
