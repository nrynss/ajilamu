# T7.3d adversarial review, round 1

Verdict: **APPROVE**. Findings: **0 C, 0 H, 0 M, 0 L**.

## Scope

Fresh REVIEW role, 2026-09-09. I reviewed `internal/media/media_test.go` against baseline `99a7d36`.
The reviewed file has SHA-256 `ed110e029fffd990e79618d65011bba24640f154c511dfbedb6f76551c2a685c`.

I read AGENTS.md in full, the T7.3d task, diary conventions, T7.3c round 1, and relevant passages from P7 close rounds 1 through 4.
I read the complete media test file and its production helpers. I recalled memory before code and again using the fixture names.
Memory agreed with the source. I repeated its measurements independently.

I own only this review file. The required shapes already exist. I can validate this task independently.
I changed no implementation, status, or shared document. I made no commit and spawned no agent.

The diff replaces seven legacy fixture references and adds two measured-duration comments.
It preserves every assertion, test name, cancellation test, and production function.
The added comments satisfy the repository prose rules. `git diff --check 99a7d36 -- internal/media/media_test.go` passed.

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| None | Zero C, H, M, and L findings | Independent artifact measurements and isolated full suite passed | Restoring all seven legacy references failed six tests |

## Independent artifact measurements

I extracted `git archive 99a7d36` into a fresh system temporary directory.
I overlaid only the reviewed `internal/media/media_test.go` before running the suite.
Filesystem assertions confirmed both `scratch/` and `assets/` were absent.

`git ls-files --error-unmatch` confirmed all four referenced fixtures are tracked.
`git ls-tree 99a7d36` confirmed baseline membership. Each extracted file's `git hash-object` matched its baseline blob.

| Fixture | Baseline blob |
|---|---|
| `testdata/takes/seg_3_try1.wav` | `fc4fe531123afaae37e22fe294aa69a479d427e9` |
| `testdata/takes/seg_3_stretched.wav` | `cf983ee6b236a2c5d00663b39e18747f3df4245d` |
| `testdata/takes/seg_8_try1.wav` | `8f143bf584de8a16490e193ad2319fc866406bc2` |
| `testdata/clip.mp4` | `545499c78b8a1e868baabd29c16e8b75f36780e5` |

I invoked ffprobe 9.0.1 directly on each extracted artifact:

```sh
ffprobe -v error -select_streams a:0 \
  -show_entries format=duration:stream=codec_name,sample_rate,channels,channel_layout,bit_rate \
  -of json "$fixture"
```

| Artifact | Measured duration | Duration assertion |
|---|---|---|
| `seg_3_try1.wav` | 5.720375 s | 5720 ms matches nearest-millisecond rounding |
| `seg_3_stretched.wav` | 5.337563 s | 5338 ms matches nearest-millisecond rounding |
| `seg_8_try1.wav` | 4.200375 s | Source for relative slowdown assertion |
| `clip.mp4` | 75.008267 s | Source for positive demux duration assertion |

All three WAV streams measured PCM s16le, 16000 Hz, one channel, and 256000 bits/s.
ffprobe omitted their channel layout. `AudioFormat` correctly derives mono from the channel count.
The clip measured AAC, 44100 Hz, two channels, stereo, and 132532 bits/s.
These measurements support every fixture format assertion.

I also rendered temporary artifacts directly with ffmpeg using the production arguments, then measured them with ffprobe.
The segment 8 take at `atempo=0.8000` measured 5.236938 s, longer than its 4.200375 s source.
The demuxed clip measured 75.000438 s, PCM s16le, 16000 Hz, and one channel.
These artifacts independently support the relative and positive duration assertions.

## Isolated full suite

Toolchain: `go version go1.27.1 darwin/arm64`.
I ran `go test -count=1 ./...` inside the baseline archive with only the test overlay.
The command exited 0. All 14 packages passed without cached test results.

```text
cmd/ajilamu        3.658s
internal/agent    1.551s
internal/api      8.555s
internal/assemble 17.594s
internal/command  2.251s
internal/config   2.685s
internal/cost     3.702s
internal/fit      19.364s
internal/fixtures 5.442s
internal/gemini   4.885s
internal/ledger   7.557s
internal/media    8.363s
internal/tts      5.819s
internal/types    5.307s
```

## Mutation

After the suite, I mutated only the temporary copy's media test file.
I restored five `scratch/takes/` references and two `assets/source/clip.mp4` references, leaving the duration comments intact.
I reconfirmed that neither legacy directory existed.

`go test -count=1 ./internal/media` exited 1 and failed these six tests:

- `TestDurationSeg3Try1`: missing `scratch/takes/seg_3_try1.wav`.
- `TestDurationSeg3Stretched`: missing `scratch/takes/seg_3_stretched.wav`.
- `TestAtempoSlowDownSeg8`: missing `scratch/takes/seg_8_try1.wav`.
- `TestAtempoRatioLimits`: missing `scratch/takes/seg_8_try1.wav`.
- `TestAudioFormat`: both subtests failed on their legacy paths.
- `TestDemux`: missing `assets/source/clip.mp4`.

The mutation reproduces the original clean-clone defect. I restored the temporary file afterward.

## Residue

T7.3d has no earlier review round. There is zero residue within this task.
The legacy-fixture note in each P7 close round, 1 through 4, has zero residue under this patch.
T7.3c's reviewed cancellation code and tests remain unchanged, and the isolated suite passes them.
This verdict covers T7.3d only. It does not reopen unrelated P7 findings or close the phase.
