# T6.2 Remediation Round 1

## Finding

| # | Sev | Finding | Fix | Pin | Residue |
|---|---|---|---|---|---|
| 1 | H | The estimate summed the selected line's last take. On lines 3 and 4 that take is an atempo repair with an empty `charges` array. The panel read `$0.00` and the basis named the repair. The `SpeakerChange` payload named the repair too. | `web/src/lib/edit/Speaker.svelte` derives `estimateTake` as the newest take whose `charges` array is non-empty. It falls back to the last take when no take carries charges. The estimate, the basis paragraph and the `takeFile` payload all read `estimateTake`. The field comment and the hint now describe billed takes. | Headless Chromium read the live panel on `http://127.0.0.1:43588/d/fixture`. Line 3 reads `$0.0023182` from `seg_3_try1.wav`. Line 4 reads `$0.0036392` from `seg_4_try1.wav`. Lines 1 and 7 read `$0.0008421` and `$0.004751`. A mounted component probe captured the `SpeakerChange` payload for the line 3 shape. It reported `estimatedNanodollars: 2318200` and `takeFile: "seg_3_try1.wav"`. | None for this finding. |

## Measured panel

I built the frontend, built `./cmd/ajilamu`, and served the bundle on port 43588. I drove `/d/fixture` in headless Chromium through `playwright-core`. The table reports the live fee string, the `data-fee` attribute and the basis take for every line.

| Line | Fee string | Nanodollars | Basis take | Changed by this fix |
|---|---|---|---|---|
| 1 | `$0.0008421` | 842100 | `seg_1_try1.wav` | no |
| 2 | `$0.0024383` | 2438300 | `seg_2_try1.wav` | no |
| 3 | `$0.0023182` | 2318200 | `seg_3_try1.wav` | yes |
| 4 | `$0.0036392` | 3639200 | `seg_4_try1.wav` | yes |
| 5 | `$0.0034599` | 3459900 | `seg_5_try1.wav` | no |
| 6 | `$0.0029497` | 2949700 | `seg_6_try1.wav` | no |
| 7 | `$0.004751` | 4751000 | `seg_7_try1.wav` | no |
| 8 | `$0.0029486` | 2948600 | `seg_8_try1.wav` | no |

The numbers match the reviewer's target. Lines 3 and 4 now show a non-zero fee from the charged take. Lines 1 and 7 stay at 842100 and 4751000.

## Payload pin

The page ignores the payload fields, so I mounted `Speaker.svelte` alone under a throwaway Vite server. The probe clicked the other speaker and confirmed. The captured callback for the line 3 shape named `seg_3_try1.wav` at 2318200 nanodollars. The line 1 shape named `seg_1_try1.wav` at 842100 nanodollars. Both payloads matched the panel.

## Behaviour kept

Speaker set. The panel offered `Mark Vande Hei` and `Suni Williams` for all eight lines. The set comes from the payload, so this fix does not touch it.

Cancel path. Cancel cleared the pending panel on all eight lines. I detached the preview video element first, so only editor traffic could appear. Cancel fired zero requests on every line. The plain run fired zero `/api` requests. The only traffic after load was the preview element's own repeated `GET /clip.mp4`.

Confirm path. Confirm fired zero requests on lines 1, 3, 4 and 7. It applied route-local state only.

Take bytes. All ten files under `testdata/takes/` matched their HEAD blobs by `git hash-object` after every run. The server data directory held zero files.

Type check. `npm --prefix web run check` printed `COMPLETED 181 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS`.

Runes only. The changed file carries no `export let`, no `$:` statement and no store import.

## Residue

No residue against finding 1. Lines 2, 5, 6 and 8 keep their prior fee and basis. The fallback keeps the old zero estimate for a line whose takes carry no charges at all. I did not change the verdict. I did not fix anything nobody found. I did not mark the task done.
