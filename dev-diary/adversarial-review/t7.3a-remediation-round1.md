# T7.3a remediation, round 1

I fixed the one finding from `t7.3a-round1.md`. I did not change the verdict.

| Item | Sev | Where | Resolution | Pin |
|---|---|---|---|---|
| F1 | M | `internal/fit/loop.go:212-267`, `readSegmentRecord` | `takeFileUsable` opens the recorded take and requires a non-empty regular file, so an empty or unreadable take is not completed work. `readSegmentRecord` rejects that take and removes it, because the fresh render claims the same path and the synthesizer refuses to overwrite. The `InitialTake` path and the replay path both re-render the line. | `TestPipelineRerendersEmptyRecordedTake` places a zero-length take beside a valid record, resumes the run, and requires one synth call, a non-empty take, and a measured fit. `TestPipelineRerendersEmptyReplayedTake` covers a flagged record and a stretched record, and requires a fresh render instead of the stored result. |

## Mutation checks

I reverted each part of the fix in a scratch copy at `/tmp/t73a-remed-scratch`, outside the
repository, and ran the pins. The restored copy passes both tests.

| Reverted fix | Test that died |
|---|---|
| Restored the `os.Stat`-only take check | `TestPipelineRerendersEmptyRecordedTake` failed with `repair line 1: source take could not be probed ... Invalid data found when processing input`. Both replay subtests failed with `synth requests = 0, want 1`. |
| Kept the empty-take rule and dropped `discardTakeFile` | `TestPipelineRerendersEmptyRecordedTake` failed with `tts refuses to overwrite`. The flagged replay subtest failed the same way, and the stretched subtest failed on the leftover empty take. |

No test hardcodes a port or a fixed temporary directory. Both new tests use `t.TempDir()`.
