# T5.6 Round 1 Review

## Verdict

REMEDIATE

| Severity | Count |
|---|---:|
| C | 0 |
| H | 0 |
| M | 1 |
| L | 0 |

## Findings

| ID | Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|---|
| F1 | M | `web/src/lib/shortcuts.ts:16-27` | The shortcut guard omits native links and ARIA buttons. A focused link therefore receives Space, `/`, or an arrow key as a global workspace shortcut. This takes over a focused interactive control instead of preserving its keyboard behaviour. | I compiled the handler and sent each required key through it. The handler invoked the right actions and ignored an input. The same compiled handler accepted Space from a simulated focused link and prevented its default action. | Reverting the guard expansion lets playback or selection change while a creator navigates an interactive link or custom button with the keyboard. |

## Independent evidence

I own only this review file. The state component types and event-stage contract already exist. The task can be validated without route composition.

| Check | Independent observation |
|---|---|
| Four panel states | `PanelState` exposes loading, empty, error, and populated variants. Loading uses a complete fallback sentence. Empty and error require a sentence. Populated renders the supplied snippet. |
| Active processing detail | `ProcessingBanner` maps every `EventStage` to a visible active step. It announces updates politely and includes the optional line number and language. It uses no spinner. |
| Required keys | An independently compiled handler invoked help, command focus, playback, previous, next, four arrow directions, and modal dismissal. It prevented the matching browser defaults. |
| Editable controls | The same handler left Space on a simulated input untouched. It also rejects repeat, composition, Alt, Control, and Meta modified events. |
| Svelte version hygiene | The owned Svelte components use `$props` and `$derived`. They contain no `export let`, legacy reactive declaration, or store reactivity. |
| Build health | `npm run check` reported zero errors and warnings. `npm run build` completed successfully. These checks confirm type and production build health. |
| Vocabulary and prose | The user-facing state messages are complete, plain sentences. The owned files contain none of the forbidden creator-facing terms. |

## Residue

This is round one. No earlier review residue exists.
