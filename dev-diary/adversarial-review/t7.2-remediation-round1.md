# T7.2 Round 1 Remediation

| Finding | Remediation | Independent pin |
|---|---|---|
| H1 | Sample discovery honors `AJILAMU_SAMPLE_CLIP`, then checks the fixture loader's six relative locations. Missing clips produce a structured slog error. | `TestOpenSampleVideoHonorsOverride` passes. `TestSampleHandlerLogsMissingConfiguredClip` observes the 500 sentence and captured slog event. |
| H2 | Only the fixture alias and real fixture id load completed data. New sample ids render a pending workspace with no lines, takes, or costs. | The production bundle contains the id comparison and pending sentence. Its populated workspace remains conditional on a loaded fixture dub. |
| M1 | The identity test opens `../../testdata/clip.mp4` directly instead of calling `openSampleVideo`. | Running the test with `AJILAMU_SAMPLE_CLIP` pointed at `go.mod` fails with `stored sample differs from committed clip`. |
| M2 | `server_test.go` mounts the real sample handler through `NewServer`. The entrypoint test also posts through the compiled binary. | The four focused server and sample tests pass. Removing the entrypoint mount makes its live POST receive 404 instead of 201. |
| M3 | Every sample creation failure now returns a creator-facing sentence with a retry or upload action. Missing clip details stay in server logs. | The missing-clip test observes `We could not find the sample video. Upload a video instead.` A source scan finds none of the reviewed internal labels. |
| L1 | Cleanup now closes over `staging` and checks its current value. The successful rename clears that value. | `TestSampleHandlerCreatesListedProject` finds the completed project and stored source after deferred cleanup runs. |
| L2 | The sample project and upload payload use `NASA 75-second clip` as their title. The stored path remains `source.mp4`. | The handler and mounted-server tests assert the readable title in both the response and index summary. |
| L3 | The create screen shows the measured sample fee as `$0.023414` before launch and while the request runs. | The production bundle contains the six-decimal formatter, 23,414,000 nanodollar input, and measured-fee copy. |
| L4 | The busy button keeps full opacity and names the copy step in a complete sentence. | The production CSS contains `opacity:1` for the disabled sample button. The bundle contains the active copy-step sentence. |

## Validation

- `go test -count=1 ./internal/api` passes.
- `npm --prefix web run check` reports zero errors and zero warnings.
- `npm --prefix web run build` succeeds.
