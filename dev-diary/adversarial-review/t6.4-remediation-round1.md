# T6.4 Remediation Round 1

This record remediates every finding in `t6.4-round1.md`. It does not change that review's verdict.

The task now owns the route, the parser tests, and the preview API. That matches the contract change the first implementation could not cross.

| Finding | Remediation | Independent pin | Mutation protection |
|---|---|---|---|
| C1 | The fixture workspace mounts `CommandBar` above the timeline. Preview posts to `POST /api/editor/commands/preview`. Confirm applies only the Go-derived segment. | Headless Chrome loaded `http://127.0.0.1:18764/d/fixture`. The page exposed `Editor command` and `Preview`. It did not show `This workspace cannot run editor commands yet.` Submitting `move wav 1 to 0:0005 to right.` produced `Move line 1 so its end is at 0:05.000.` with `Confirm change` and `Revise`. The same POST on the running server returned `start_ms` 3180 and `end_ms` 5000. A `shorten line 99` POST returned 422 `line 99 does not exist`. | Reverting the route import, the confirm handler, or the mux mount hides the confirmation UI or leaves `/d/fixture` unable to preview a T6.4 command. |
| H1 | `internal/command/parse_test.go` now covers the four documented commands, malformed input, unknown lines, unknown speakers, out-of-bounds moves, collisions, and empty durations. | `go test ./internal/command` compiled the package and ran `TestDocumentedCommandsParseIntoSafeMutations`, `TestParseRejectsMalformedCommands`, and `TestValidateRejectsUnsafeReferencesAndLeavesTimelineUnchanged`. The four documented cases passed. | Deleting the test file returns `[no test files]`. Changing the `0:0005` grammar or a collision check fails a named case. |
| M1 | The Parsed intent block announces itself with `role="status"` and `aria-live="polite"`. | After Preview, Chrome read `[aria-label="Parsed command intent"]`. The node had `role=status` and `aria-live=polite`. Its text included the move summary plus `Confirm change` and `Revise`. | Reverting those attributes leaves a successful parse without a live announcement. |

## Validation note

`npm run check` reported zero diagnostics. `npm run build` completed. `go test ./internal/command ./internal/api` passed, including the mux mount test for `POST /api/editor/commands/preview`.
