# T2.8b remediation, round 1

Task: Fit loop language wiring. I remediated the two findings in `t2.8b-round1.md`. Go toolchain
go1.27.1. I changed `cmd/ajilamu/main_test.go` and the T2.8b handoff entry in
`dev-diary/PHASE-2-fit-loop.md`. I did not change `cmd/ajilamu/main.go`. I did not change the
verdict. The verdict stays REMEDIATE.

## Rows

| Finding | Fix | Pin | Residue |
|---|---|---|---|
| M1 · `dev-diary/PHASE-2-fit-loop.md:763` | Rewrote the false paragraph. It now names the per-run factory, the resolved tag, and the clean build. It names the runner tests in the Tests paragraph. | `go build ./...` exits 0. `TestPipelineRunnerLanguageWiring` logs `factory language = "ml-IN"` and `Cloud TTS LanguageCode = "ml-IN", Name = "ml-IN-Chirp3-HD-Achernar"`. | None. |
| M2 · `cmd/ajilamu/main.go:429-431` | Added `TestPipelineRunnerLanguageWiring` and `TestPipelineRunnerRejectsLanguageBeforeCharge` to `cmd/ajilamu/main_test.go`. | Both tests pass. Two mutations in `/tmp/t2.8b-remediation` fail them. | None. |

## M1 before and after

Before, line 763:

> **Not done here.** The runner still builds one synthesizer for the process. `cmd/ajilamu` does
> not build, because `tts.NewSynthesizer(cfg, router, card, nil)` at `cmd/ajilamu/main.go:385`
> uses the old four-argument form. T7.5b owns the fix. The exact seam, the signature, and the line
> references live in `adversarial-review/t2.8-contract-change.md`.

After, line 763:

> **Runner wiring landed.** `cmd/ajilamu` builds clean. `pipelineRunner` holds a `newSynthesizer`
> factory (`cmd/ajilamu/main.go:404`), not one process-wide synthesizer. `newPipelineRunner` points
> that factory at `tts.NewSynthesizer(cfg, language, rec, card, nil)`
> (`cmd/ajilamu/main.go:429-431`). `Run` resolves the language first
> (`cmd/ajilamu/main.go:440`), then builds the synthesizer with the resolved tag
> (`cmd/ajilamu/main.go:452`). A `ml` run reaches Cloud TTS as `ml-IN`. `resolveRunLanguage`
> (`cmd/ajilamu/main.go:378`) maps the create-screen code onto its tag and display name. T7.5b
> still owns the request change and the source language. The exact seam, the signature, and the
> line references live in `adversarial-review/t2.8-contract-change.md`.

The Tests paragraph at line 759 gained this sentence:

> `TestPipelineRunnerLanguageWiring` and `TestPipelineRunnerRejectsLanguageBeforeCharge` in
> `cmd/ajilamu/main_test.go` pin the runner seam end to end.

## M2 pin output

```
$ go test -count=1 -run 'TestPipelineRunnerLanguageWiring|TestPipelineRunnerRejectsLanguageBeforeCharge' -v ./cmd/ajilamu
=== RUN   TestPipelineRunnerLanguageWiring
    main_test.go:706: factory language = "ml-IN"
    main_test.go:719: Cloud TTS LanguageCode = "ml-IN", Name = "ml-IN-Chirp3-HD-Achernar"
--- PASS: TestPipelineRunnerLanguageWiring (0.98s)
=== RUN   TestPipelineRunnerRejectsLanguageBeforeCharge
=== RUN   TestPipelineRunnerRejectsLanguageBeforeCharge/empty
    main_test.go:769: error = the run needs a target language
=== RUN   TestPipelineRunnerRejectsLanguageBeforeCharge/malformed
    main_test.go:769: error = the run cannot use "not a language" as a target language: malformed language code "not a language"
--- PASS: TestPipelineRunnerRejectsLanguageBeforeCharge (0.00s)
    --- PASS: TestPipelineRunnerRejectsLanguageBeforeCharge/empty (0.00s)
    --- PASS: TestPipelineRunnerRejectsLanguageBeforeCharge/malformed (0.00s)
PASS
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	0.986s
```

The wiring test drives `pipelineRunner.Run` for `ml`. It builds the synthesizer through a real
`tts.NewSynthesizer` on a fake Cloud client. It asserts the factory language, the
`TranslateRequest.TargetLanguageName`, the Cloud TTS `LanguageCode`, the Cloud TTS `Name`, and
every event language. The rejection test drives an empty code and a malformed code. It asserts an
error, zero calls at the factory, the segmenter, the translator, and the Cloud client, and zero
events. No billable client ran, so the run ledger recorded no charge.

## Mutation M1: break the resolution

In `/tmp/t2.8b-remediation`, the map changed `ml` onto `ml` instead of `ml-IN`:

```
$ sed -i 's|"ml": {tag: "ml-IN", name: "Malayalam"},|"ml": {tag: "ml", name: "Malayalam"},|' cmd/ajilamu/main.go
$ go test -count=1 -run 'TestPipelineRunnerLanguageWiring' ./cmd/ajilamu
    main_test.go:706: factory language = "ml"
    main_test.go:708: factory language = "ml", want ml-IN
    main_test.go:719: Cloud TTS LanguageCode = "ml", Name = "ml-Chirp3-HD-Achernar"
    main_test.go:721: Cloud TTS LanguageCode = "ml", want ml-IN
    main_test.go:724: Cloud TTS Name = "ml-Chirp3-HD-Achernar", want ml-IN-Chirp3-HD-Achernar
    main_test.go:731: event "Reading the source video." language = "ml", want ml-IN
    ...
FAIL
FAIL	github.com/nrynss/ajilamu/cmd/ajilamu	1.001s
```

## Mutation M2: break the language validation

In the same copy, `resolveRunLanguage` skipped the empty check and the well formed check:

```
$ sed -i 's|if code == "" {|if false {|; s|if err := tts.ValidateLanguage(code); err != nil {|if err := error(nil); err != nil {|' cmd/ajilamu/main.go
$ go test -count=1 -run 'TestPipelineRunnerRejectsLanguageBeforeCharge' -v ./cmd/ajilamu
=== RUN   TestPipelineRunnerRejectsLanguageBeforeCharge/empty
    main_test.go:769: error = open the synthesizer: tts needs a target language code
    main_test.go:774: billable calls: factory=1 segmenter=0 translator=0 tts=0, want 0
=== RUN   TestPipelineRunnerRejectsLanguageBeforeCharge/malformed
    main_test.go:769: error = open the synthesizer: malformed language code "not a language"
    main_test.go:774: billable calls: factory=1 segmenter=0 translator=0 tts=0, want 0
--- FAIL: TestPipelineRunnerRejectsLanguageBeforeCharge (0.00s)
    --- FAIL: TestPipelineRunnerRejectsLanguageBeforeCharge/empty (0.00s)
    --- FAIL: TestPipelineRunnerRejectsLanguageBeforeCharge/malformed (0.00s)
FAIL
FAIL	github.com/nrynss/ajilamu/cmd/ajilamu	0.005s
```

## Validation

```
$ go build ./...
build exit=0

$ go test -count=1 ./cmd/... ./internal/fit
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	1.813s
ok  	github.com/nrynss/ajilamu/internal/fit	16.463s
test exit=0
```

## Residue

None. M1 and M2 both close. The three residue items in `t2.8b-round1.md` stay outside this task.
T7.5b owns the api event stream, the request change, and the source language. T2.8a owns the
Cloud TTS client lifetime. T7.7 owns the fetched catalog.
