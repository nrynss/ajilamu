# T5.6 Round 3 Review

## Verdict

REMEDIATE

| Severity | Count |
|---|---:|
| C | 0 |
| H | 0 |
| M | 1 |
| L | 0 |

## Findings

| ID | Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|---|
| F3 | M | `web/src/lib/shortcuts.ts:17-18` | The shortcut guard recognizes only `contenteditable='true'` and an empty `contenteditable` attribute. It omits the standard editable `contenteditable='plaintext-only'` value. Space and arrow keys from that editor therefore reach the global handler and prevent the editor's normal input or caret movement. | I measured the selector against an element carrying `contenteditable="plaintext-only"`. Neither contenteditable selector matches that value. The handler consequently passes its editable-control check, selects the Space or arrow branch, and calls `preventDefault`. | Reverting a complete contenteditable guard makes keyboard typing or caret movement inside a plaintext editor toggle playback or move workspace selection. |

## Independent evidence

| Check | Independent observation |
|---|---|
| Prior F1 | The guard still protects native links, image-map links, disclosure controls, media controls, and ARIA buttons and links. F1 has no residue. |
| Prior F2 | The guard includes checkbox, radio, switch, tab, menu item variants, option, tree item, and focus-owning ARIA composites. F2 has no residue. |
| Required global keys | Outside a protected target, the handler maps `?`, `/`, Space, `[`, `]`, all four arrows, and Escape. It rejects repeat, composition, Alt, Control, and Meta-modified events. |
| Panel states | `PanelState` exposes loading, empty, error, and populated states. Loading, empty, and error use complete sentences. Populated state renders the supplied snippet. |
| Processing state | `ProcessingBanner` names every `EventStage`, presents the active step and optional line or language detail in a polite live region, and contains no spinner. |
| Svelte hygiene | The owned components use runes. They contain no `export let`, legacy reactive declaration, or store-driven reactivity. |
| Build health | I ran `npm run check` and `npm run build` in `web/`. Both completed with zero diagnostics. They do not exercise the omitted plaintext-only control. |

## Residue

Round one F1 and round two F2 are closed. F3 remains. The task cannot receive approval until the guard excludes every editable contenteditable state and a fresh review confirms zero residue.
