# T6.5a line re-render route: round 2

**Verdict: APPROVE.** Counts: **C 0, H 0, M 0, L 0.**

The remediation fixes both round one findings. The route reads the ledger under the
caller's code first and the tag second. The handoff numbers now reproduce from
committed artifacts.

## Findings

No findings. Each row would carry Where, What, Pin, and Mutation. No new defect met
the bar.

## What I measured

I copied the tree to `/tmp/t65a-r2/repo` and added one throwaway harness. The harness
drives the real handler and the real server. The stand-in ledger filters a track on
exact language equality. The stand-in renderer writes a real 5320 ms WAV. The working
tree was never edited.

### 1. Raw code track

The project record stores `ml`. The ledger serves a `ml` track.

```text
MEASURE case=raw_code_track status=201 asked=[ml] take="seg_3_try2.wav" rendererLanguage="ml-IN"
```

The status is 201. The ledger was asked for `ml` alone. The new take is
`seg_3_try2.wav`. The renderer received the resolved tag `ml-IN`.

### 2. Tag track

The ledger serves a `ml-IN` track.

```text
MEASURE case=tag_track status=201 asked=[ml ml-IN] take="seg_3_try2.wav" rendererLanguage="ml-IN"
```

The status is 201. The ledger was asked for `ml`, then `ml-IN`. The new take is
`seg_3_try2.wav`.

### 3. Mutation pin

I replaced the two-step read with the round one read under the tag alone. The raw code
case answers 404 before it renders. The tag case misses the first ask. Both test cases
fail.

```text
MEASURE case=raw_code_track status=404 asked=[ml-IN] take="" rendererLanguage=""
MEASURE case=tag_track status=201 asked=[ml-IN] take="seg_3_try2.wav" rendererLanguage="ml-IN"
rerender_test.go:313: re-render status = 404, want 201
rerender_test.go:323: ledger languages asked = [ml-IN], want [ml ml-IN]
--- FAIL: TestRerenderReadsTheLedgerTrackUnderTheCallerCode (0.05s)
    --- FAIL: TestRerenderReadsTheLedgerTrackUnderTheCallerCode/raw_code_track (0.00s)
    --- FAIL: TestRerenderReadsTheLedgerTrackUnderTheCallerCode/tag_track (0.05s)
```

I restored the copy after the mutation.

### 4. Handoff numbers

```text
$ sha256sum testdata/takes/seg_3_try1.wav
8928864cd133b7c718c50eec7088f6e2d7eecdfcd39201676504f5369c43ec6c  testdata/takes/seg_3_try1.wav

$ ffprobe -v error -show_entries format=duration testdata/takes/seg_3_try1.wav
[FORMAT]
duration=5.720375
[/FORMAT]
```

The hash matches the handoff. The duration matches the handoff. The exact command
prints `duration=5.720375`, so the number reproduces.

### 5. Previous take and both takes

```text
MEASURE try1Before=8928864cd133b7c718c50eec7088f6e2d7eecdfcd39201676504f5369c43ec6c try1After=8928864cd133b7c718c50eec7088f6e2d7eecdfcd39201676504f5369c43ec6c try1DurBefore=5.72s try1DurAfter=5.72s
MEASURE new take ffprobe = 5.32s
```

The first take is byte identical after the call. Both takes decode through ffprobe.
`seg_3_try1.wav` reads `pcm_s16le` at 5.72s. The new take reads 5.32s.
`TestRerenderWritesNewTakeBesidePrevious` also pins the byte identity.

### 6. Commands

```text
$ go test -count=1 ./internal/api ./cmd/...
ok  	github.com/nrynss/ajilamu/internal/api	1.459s
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	3.934s
```

The run included another task's in-flight `internal/fit` edits. The suite passed.

## Residue against round one

**C1 residue: none.** The raw `ml` track answers 201, and the ledger is asked for `ml`
alone. The tag track answers 201 after it asks `ml`, then `ml-IN`. The mutation
restores the 404, so the fix carries the behavior.

**L1 residue: none.** The committed fixture hashes to `8928864c...`. The exact probe
prints 5.720375 seconds. `TestRerenderReadsTheLedgerTrackUnderTheCallerCode` pins the
new take at 5320 ms through `media.Duration`, which shells out to ffprobe.

## Done condition

The done condition holds for both tracks. The route writes `seg_3_try2.wav` beside
`seg_3_try1.wav`. Both decode. The response names the new take. The bundled `ml`
fixture now re-renders.

## Known edge

A caller that sends `{"language":"ml-IN"}` to a project whose ledger track is `ml`
answers 404. The fallback needs the code to differ from the tag. The behavior predates
the remediation, so I file no finding. The frontend sends the stored code `ml`, so the
demo path is unaffected.
