# T7.2 Round 2 Review

## Verdict

REMEDIATE

| Severity | Count |
|---|---:|
| C | 0 |
| H | 1 |
| M | 1 |
| L | 2 |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| H1 · `web/src/lib/fixture.ts` line 57 with `cmd/ajilamu/main.go` lines 60 to 76 | The id gate accepts only the alias `fixture` and the id inside `testdata/wire/dub.json`. The server builds its index from `testdata/manifest.json`, which carries a different id. `GET /api/dubs` advertises the fixture as `d3bca364-9c8a-4107-8df9-c4faf909b008`, and the index page links that id. Loading it renders the empty pending state. No built link references `/d/fixture`, so the completed workspace is unreachable from the UI. The remediation added this gate, so the regression is new this round. | Headless Chrome on the built frontend. `/d/d3bca364-9c8a-4107-8df9-c4faf909b008` rendered `data-state="empty"` with the sentence "This sample project is waiting for dubbing. Its video is saved, but no lines, takes, or costs exist yet." The index row for that same id reads "In review" and "$0.02". `/d/fixture` and `/d/6d9c2f1a-3b4e-4a8d-9c1e-7f2b5a3d8c40` rendered populated, with 8 line buttons and video `/clip.mp4`. A bundle sweep found no `d/fixture` reference. | Revert an advertised-id acceptance and the index's completed-project row lands on a page that denies the project has lines, takes, or costs. The demo's only navigable path to the finished workspace dies. |
| M1 · `web/src/routes/+layout.svelte` lines 45 to 50 | The chrome hardcodes "Ready for review" and "$0.02 spent" on every route. A fresh sample project is pending with zero spend, yet the frame above it claims readiness and spend. Round 1 named these exact strings as finished-fixture markers. The page body is now honest, so the contradiction moved into the frame rather than disappearing. | Headless Chrome read the page text on `/d/4066299036ad6c8bf2227875d898ef2c`, an id minted by a real button click in the same session. The text contained both "Ready for review" and "$0.02" while the panel rendered `data-state="empty"`. The same strings appear on `/d/not-a-real-project`. | Revert an id-aware chrome and every sample click shows readiness and spend the project never earned. That is the exact contradiction round 1 filed. The fix needs `+layout.svelte`, which sits outside the T7.2 seam. |
| L1 · `web/src/routes/d/[id]/+page.svelte` lines 34 to 39 | Every unknown id renders the sample-pending sentence. The sentence claims a saved video for ids that own no upload. A typo or a stale link presents a nonexistent project as a waiting sample. | Headless Chrome loaded `/d/not-a-real-project`. The panel rendered `data-state="empty"` with "This sample project is waiting for dubbing. Its video is saved, but no lines, takes, or costs exist yet." `GET /api/dubs` holds no such id. | Revert an existence check and any mistyped workspace URL claims a saved video that nobody uploaded. |
| L2 · `internal/api/sample.go` lines 48 to 92 | Six failure paths after the clip opens return creator sentences but log nothing. Only the clip-open failure calls slog. A disk-full copy or a failed rename leaves the creator retrying and the operator blind. The one line that does log says only `error="file does not exist"`. It names neither the searched locations nor the `AJILAMU_SAMPLE_CLIP` override. | Source read. `slog.Error` appears once in the file, at line 42. The missing-clip probe's server log showed `ERROR The sample video could not be opened. error="file does not exist"` and nothing else. | Revert per-path logging and the next storage failure during a demo leaves an empty log. That is the same operator blindness round 1 filed under H1. |

## Independent Evidence

| Check | Observation |
|---|---|
| One-click control | Headless Chrome clicked `Try sample video (NASA 75s clip)` on the built `/new`. The browser landed on `/d/4066299036ad6c8bf2227875d898ef2c`, a fresh id minted by that click. The panel rendered `data-state="empty"` with the pending sentence, zero line buttons, and no video element. |
| Live 201 contract | `POST /api/dubs/sample` on port 8917 returned `201 Created` with `Content-Type: application/json` in 23 milliseconds. The body carried a 32-character hex id, `"language":"ml"`, `"name":"NASA 75-second clip"`, a project-relative path, and `"bytes":18385109`. |
| Deployed-layout discovery | The binary ran from `/tmp/t72r2/site`, a copy of `testdata/` outside the checkout, with no override set. The sample POST returned 201. Round 1's deployed layout failed this same request with 500. |
| Override path | The binary ran from `/tmp/t72r2/noclip`, whose `testdata/` lacks the clip, with `AJILAMU_SAMPLE_CLIP` pointing at the committed clip. The sample POST returned 201. |
| Missing clip | The same clip-less layout without the override returned 500 with the body "We could not find the sample video. Upload a video instead." The server log recorded `ERROR The sample video could not be opened. error="file does not exist"`. No uploads directory was created. |
| Byte identity | `shasum -a 256` returned `da7e0212fdf5654a81ec2d00ec1422bff636d9d42eb201c62d0bbe7ce083be9e` for `testdata/clip.mp4` and for the stored `source.mp4` from both the discovery probe and the override probe. Each measured 18,385,109 bytes. |
| Permissions | `stat` reported `-rw-------` on the stored `source.mp4` and `project.json`. The project directory measured `drwx------` and the storage root `drwxr-x---`. |
| Index honesty | The five sample rows on `/` read "Queued to start" and "$0.00". The fixture row reads "In review" and "$0.02". The sample's pending claim and the fixture's completed claim diverge correctly at the index level. |
| Workspace gating | `/d/fixture` and the wire id rendered populated workspaces with 8 line buttons, 32 segment rows, and video `/clip.mp4`. The fresh sample id rendered the empty state with zero rows and no video. |
| Advertised fixture id | The manifest id rendered the empty pending state. The index links that id. The build contains no `d/fixture` link. See finding H1. |
| Chrome text | "Ready for review" and "$0.02 spent" rendered on every route measured, including the fresh sample workspace and a garbage id. See finding M1. |
| Fee before click | The `/new` fee panel read "The NASA sample has a measured fee of $0.023414." and the button caption read "Measured fee: $0.023414" before any click. The bundle carries the six-decimal formatter and the constant `23414e3`. |
| Busy state | With the sample request held open, the button read "We are copying the NASA sample into your workspace." at computed opacity `1`, `aria-busy="true"`, and disabled. The production CSS carries the disabled rule with `opacity:1`. |
| Concurrency and cleanup | Four concurrent sample POSTs returned four distinct ids. The storage root held zero `.sample-` staging directories afterwards. |
| M1 mutation | `AJILAMU_SAMPLE_CLIP=$PWD/go.mod go test -run TestSampleHandlerCreatesListedProject ./internal/api` failed with "stored sample differs from committed clip". The test opens `../../testdata/clip.mp4` directly at `sample_test.go` line 138. |
| M2 mutations | An overlay removing the `options.Sample` mount from `server.go` failed `TestServerMountsSampleHandlerOnPersistentStorage` with `sample status = 404, want 201`. A binary built through an overlay removing the `Sample` line from `main.go` answered 404 to the live sample POST, the response `TestEntrypointExitsCleanlyWithoutLedger` asserts against. Repository source stayed untouched in both runs. |
| Focused tests | `TestSampleHandlerCreatesListedProject`, `TestOpenSampleVideoHonorsOverride`, `TestSampleHandlerLogsMissingConfiguredClip`, `TestServerMountsSampleHandlerOnPersistentStorage`, and `TestEntrypointExitsCleanlyWithoutLedger` all pass unmutated. |
| Suite state | `go test ./...` fails only `internal/media`, for missing `scratch/` fixtures. That redness predates this task. `go vet` on `./internal/api` and `./cmd/ajilamu` reported nothing. `npm run check` found zero errors and zero warnings. `npm run build` succeeded. |
| Svelte 5 hygiene | The create screen, the workspace route, and `fixture.ts` contain no `export let`, no `$:`, and no store import. |
| Vocabulary | A sweep of the sample handler found no term from the `ui-ux.md` section 8 Never Use column. All eight 500 bodies are complete sentences with an action. The 405 body remains the label `method must be POST`, which the mounted mux never reaches. Round 1 accepted that contract. |

## Residue Against Round 1

| Finding | Disposition |
|---|---|
| H1 | Cleared. Discovery from a deployed layout, the `AJILAMU_SAMPLE_CLIP` override, and the missing-clip log line each measured green on the live binary. |
| H2 | Not cleared. The new sample id now renders an honest empty body, measured through a real click-through. Two parts remain. The fixture id the server advertises renders the empty state (this round's H1). The chrome still claims readiness and spend over the pending workspace (this round's M1). |
| M1 | Cleared. The identity test names `testdata/clip.mp4` directly, and the override mutation fails it with the expected message. |
| M2 | Cleared. Both mount mutations fail their tests, one at the `NewServer` level and one through the compiled entrypoint. |
| M3 | Cleared. All eight creator-facing 500 bodies are sentences with an action. The live missing-clip response matched the test's expectation verbatim. |
| L1 | Cleared. The defer closes over `staging` and guards on its current value. A standalone probe of that shape confirmed the finished project survives the deferred cleanup. Five live samples left no staging directories. |
| L2 | Cleared. The 201 response and the index both title the project "NASA 75-second clip". |
| L3 | Cleared. The measured fee `$0.023414` appears in the fee panel and under the button before any click. |
| L4 | Cleared. The busy label names the copy step in a full sentence at computed opacity 1. |

## Scope and Residue

I created only this review file in the repository. I changed no implementation file, test, fixture, task
status, or commit. Both source mutations ran through `go test -overlay` and `go build -overlay`, which
point the compiler at copies under `/tmp` and never write to the checkout.

I regenerated `web/build` with `npm run build` so the browser probes measured the current source. That
directory is gitignored build output. Every probe server, the headless Chrome instance, and all scratch
directories lived under `/tmp/t72r2`. All servers and Chrome are stopped. No listener remains on ports
8917 through 8920 or 9333, and nothing bound 8080.

## Required Ownership Changes

M1 cannot be fixed inside the T7.2 seam. The chrome lives in `web/src/routes/+layout.svelte`, which no
task in the expanded contract owns. The contract must expand again, or a sibling task must own an
id-aware chrome.

H1 is fixable inside the seam. `fixture.ts` already imports one fixture payload and can recognize the
manifest id the same way. The deeper mismatch deserves a named decision. The repository ships two
fixture payloads for the same project, `testdata/manifest.json` and `testdata/wire/dub.json`, with two
different ids. The server advertises one and the frontend gates on the other.

L1 is fixable inside the seam. The workspace route can check the id against `GET /api/dubs` before it
claims a saved video.

The remediation must re-review all four findings.
