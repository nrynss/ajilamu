# T2.8a remediation, round 1

I remediate the three findings in `t2.8a-round1.md`. I changed three files and added this
record. I did not change the verdict. I did not mark the task done. I did not commit.

## Files changed

- `internal/gemini/translate.go`
- `internal/gemini/translate_test.go`
- `internal/tts/chirp_test.go`

## Findings

| Finding | Fix | Pin | Residue |
|---|---|---|---|
| M1 | I added `TestTranslatePromptNamesNonEnglishSource`. It sends `SourceLanguageName: "Spanish"` with a Malayalam target. | The test pins the full prompt as a literal. It also asserts `Translate this Spanish dialogue line` and `Original Spanish:`. The round 1 mutation now fails this test. | None. |
| M2 | I restored the proven opening slot. `targetOpeningSlot` returns `Malayalam script (മലയാളം)` for a Malayalam target. Every other target keeps its plain name. | `provenMalayalamPrompt` is a literal transcribed from `tools/validate_pipeline.py`. Python builds the same 509 bytes from that file. The dropped hint fails two tests. | The proven body only covers Malayalam. A later language with a script hint needs a new entry in `targetOpeningSlot`. |
| L1 | I deleted the call count assertion and the charge list assertion. I kept the non nil error assertion. I renamed the test to `TestNewSynthesizerRejectsBadLanguage`. | No mutation of `chirp.go` could fail the two deleted assertions. The round 1 matrix says so. | None. |

## 1. Malayalam prompt, byte for byte

Go builds this string for the fixture. Python builds the same string from
`tools/validate_pipeline.py`. My extractor reads lines 185 to 200 straight from that file and runs
the f-string blocks with the fixture values. `cmp` reports no difference. Both files hold 509
bytes.

```
"\nTranslate this English dialogue line into natural spoken Malayalam script (മലയാളം):\nOriginal English: \"Also, an object in motion tends to stay in motion unless acted on by an outside force.\"\nSpeaker emotion: Explanatory\nConstraint: The translated line will be spoken in Malayalam in a video slot that lasts approximately 7.1 seconds (7110 ms). Keep the translation natural, spoken, and fit the rhythm.\n\nRespond with strictly the translated Malayalam text. No markdown, no quotes, no explanation.\n"
```

Command:

```
python3 pin_prompt.py && go test -count=1 -run TestPinProbePrompts ./internal/gemini
cmp /tmp/pin_py_malayalam.txt /tmp/pin_go_malayalam.txt
```

Output:

```
python==go malayalam: OK
 509 /tmp/pin_py_malayalam.txt
 509 /tmp/pin_go_malayalam.txt
```

## 2. Spanish prompts

Spanish source, Malayalam target. This is the M1 case in
`TestTranslatePromptNamesNonEnglishSource`.

```
"\nTranslate this Spanish dialogue line into natural spoken Malayalam script (മലയാളം):\nOriginal Spanish: \"Also, an object in motion tends to stay in motion unless acted on by an outside force.\"\nSpeaker emotion: Explanatory\nConstraint: The translated line will be spoken in Malayalam in a video slot that lasts approximately 7.1 seconds (7110 ms). Keep the translation natural, spoken, and fit the rhythm.\n\nRespond with strictly the translated Malayalam text. No markdown, no quotes, no explanation.\n"
```

Spanish target, English source. This is the existing `TestTranslatePromptNamesTargetLanguage` case.

```
"\nTranslate this English dialogue line into natural spoken Spanish:\nOriginal English: \"Also, an object in motion tends to stay in motion unless acted on by an outside force.\"\nSpeaker emotion: Explanatory\nConstraint: The translated line will be spoken in Spanish in a video slot that lasts approximately 7.1 seconds (7110 ms). Keep the translation natural, spoken, and fit the rhythm.\n\nRespond with strictly the translated Spanish text. No markdown, no quotes, no explanation.\n"
```

## 3. Mutated source name path

I copied the module to `/tmp/t28a-rem`. I changed the known source return to hardcode `"English"`
in both slots. The file compiled, because the emptiness check still uses `source`.

```go
return fmt.Sprintf(translatePromptBody, "English", opening, "English", req.Text, req.Emotion, constraint, target), nil
```

`go test -count=1 ./internal/gemini` reported:

```
--- FAIL: TestTranslatePromptNamesNonEnglishSource (0.00s)
    translate_test.go:379: prompt mismatch
         got: "...Translate this English dialogue line into natural spoken Malayalam script (മലയാളം):..."
        want: "...Translate this Spanish dialogue line into natural spoken Malayalam script (മലയാളം):..."
    translate_test.go:382: prompt misses the Spanish opening slot
    translate_test.go:385: prompt misses the Spanish original slot
FAIL
FAIL	github.com/nrynss/ajilamu/internal/gemini	0.008s
```

No other test failed. This mutation is round 1 M7. It survived the old suite. It now fails.

I also dropped the script hint from `targetOpeningSlot` in the scratch copy. That mutation failed
`TestTranslatePromptPerMode` and `TestTranslatePromptNamesNonEnglishSource`. The Malayalam pin now
tracks the proven bytes.

## 4. Test run

```
$ go test -count=1 ./internal/tts ./internal/gemini
ok  	github.com/nrynss/ajilamu/internal/tts	0.007s
ok  	github.com/nrynss/ajilamu/internal/gemini	0.008s
```

20 top level tests pass in `internal/tts`. 21 pass in `internal/gemini`. The new test is the extra
one. `gofmt -l` reports no file.

## Design note

The request keeps plain names in `TargetLanguageName` and `SourceLanguageName`. `translatePrompt`
derives the opening slot from the target name. A new request field would stay empty, because
`internal/fit` builds the request and it is out of scope. The proven bytes still reach the wire.
