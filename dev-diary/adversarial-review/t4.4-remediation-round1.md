# T4.4 remediation, round 1

I did not change the round 1 verdict.

| # | Severity | What I changed | Measurement proving it closed |
| --- | --- | --- | --- |
| 1 | M | `validateAction` rejects a non-empty `before_value` or `after_value` that fails `json.Valid`. Empty strings still enqueue. The HTTP capture now stores JSON strings for `text_corrected` and `line_rewritten`. `{not json` fails before any request. The T4.4 handoff names this check. | An HTTP capture decoded `text_corrected` `before_value` as `"old line"`. `json.Valid` was true. `after_value` was `"new line"` and also valid. `line_rewritten` decoded `"first phrasing"` and `"shorter phrasing"`, both valid. `user_command` decoded empty strings and still sent one `actions_raw` JSONEachRow body. A row whose `before_value` was `{not json` returned `ledger action before_value is not JSON text` and sent zero HTTP requests. The same text in `after_value` also sent zero requests. |

`gofmt -d` was empty on the Go files. `go test ./internal/ledger -count=1` passed. `go test -race ./internal/ledger -count=1` passed. `go vet ./internal/ledger` passed. `git diff --check` passed on the touched files.
