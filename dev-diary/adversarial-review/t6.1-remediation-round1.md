# T6.1 Remediation Round 1

This record remediates both findings in `t6.1-round1.md`. It does not change that review's verdict.

| Finding | Remediation | Independent pin | Mutation protection |
|---|---|---|---|
| F1 | T6.1 now owns the smallest composition seam, `web/src/routes/d/[id]/+page.svelte`. The route renders `Boundary` for the selected fixture line and replaces that line in local fixture state after a confirmed change. | A fresh `xh GET http://127.0.0.1:5173/d/fixture` response contains one section whose class starts with `boundary-editor`. It contains `Adjust boundary for line 1` and both line 1 handle labels. `npm run check` reports zero diagnostics and `npm run build` completes. | Reverting the route import, render, or update callback removes the editor from the delivered fixture workspace or leaves confirmed changes outside its timeline state. |
| F2 | `candidateFor` now constrains the snapped boundary before it updates the slot. When the second constraint rejects a snap target, the change omits that target. | Fixture line 2 starts at 7,728ms and line 1 ends at 7,728ms. An end drag to 7,728ms first clamps to 7,828ms, snaps toward 7,728ms, then clamps back to 7,828ms. The resulting duration is 100ms. | Reverting the post-snap constraint lets the 7,728ms snap produce a zero-duration line 2 slot. |

## Validation note

The local browser hydration test remains blocked by the pre-existing `effect_update_depth_exceeded` failure in the route fixture-loading effect. It prevents live pointer and keyboard proof. This remediation does not alter that unrelated route logic.
