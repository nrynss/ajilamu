# T4.4 adversarial review, round 2

Verdict: APPROVE

Counts: C 0, H 0, M 0, L 0

Prior-round residue: Round 1 finding 1 has zero residue.

Findings: None

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| None | | | | |

Independent evidence:

A throwaway HTTP capture decoded eight JSONEachRow bodies. Every query named
`INSERT INTO actions_raw` and `FORMAT JSONEachRow`. No query named the `actions` view.

`text_corrected` `before_value` decoded as `"old line"`. `json.Valid` was true.
`after_value` decoded as `"new line"`. `json.Valid` was true.

`line_rewritten` `before_value` decoded as `"first phrasing"`. `json.Valid` was true.
`after_value` decoded as `"shorter phrasing"`. `json.Valid` was true.

`user_command` decoded empty `before_value` and `after_value`. That call still sent one
`actions_raw` JSONEachRow body.

A row whose `before_value` was `{not json` returned
`ledger action before_value is not JSON text`. Request count was 0.
The same text in `after_value` returned `ledger action after_value is not JSON text`.
Request count was 0.

The round 1 payload `old line` (unquoted) also failed before transport. Request count
stayed 0. Unquoted `first phrasing` failed the same way.

Stored `action_type` values were `segment_created`, `boundary_nudged`,
`speaker_reassigned`, `text_corrected`, `take_rendered`, `atempo_stretched`,
`line_rewritten`, and `user_command`. No body stored `stretched`.

`stretched` and the project.md aliases `stretch_audio`, `detect_segments`,
`nudge_boundary`, `reassign_speaker`, `render_take`, and `rewrite_line` each returned
errors. Request count stayed 0.

Unknown author `pipeline`, empty author, and author `Agent` each returned errors.
Request count stayed 0. Empty language, empty command-bar prompt, and `segment_index`
below -1 also sent zero requests.

Decoded bodies omitted `created_at`, `ingested_at`, and `event_key`.
`before_value` and `after_value` decoded as strings. Boundary and stretch JSON stayed
inside those strings. They did not become nested objects.

A command-bar prompt with leading spaces, quotes, angle brackets, a newline, and
Malayalam decoded equal to the input.

A 503 left one durable journal file. `RecordAction` returned `ErrPending`. Pending
count was 1. Replay sent the same `actions_raw` JSONEachRow query. Replay body bytes
equaled the first body. Replay `before_value` decoded as `"too short"` with
`json.Valid` true.

An empty `action_id` still produced a 32 character hex id on the wire.

Hygiene (not spec proof):

- `gofmt -d` was empty on `internal/ledger/actions.go` and `internal/ledger/actions_test.go`.
- `git diff --check` passed on those paths.
- `go test ./internal/ledger -count=1` passed.
- `go test -race ./internal/ledger -count=1` passed.
- `go vet ./internal/ledger` passed.

I deleted the throwaway capture after measurement.

## Residue

Zero residue against round 1 finding 1. Non-empty `before_value` and `after_value` that
fail `json.Valid` never reach transport. Empty values still enqueue. `text_corrected`
and `line_rewritten` store JSON text. No new defect appeared at any severity.
