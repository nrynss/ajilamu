# T6.2 Round 2 Adversarial Review

## Verdict

**REMEDIATE**

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 1 | 0 |

One M finding opens. The code fix holds on every line I measured. The T6.2 handoff entry still documents the pre-fix rule and reports line 3 as `$0.00`, so the record contradicts the delivered panel.

## Scope and Evidence

I reviewed the T6.2 block in `dev-diary/PHASE-6-editing.md`, the seam expansion note, the T6.2 handoff entry, the round one review and remediation records, `web/src/lib/edit/Speaker.svelte` and the T6.2 hunk of `web/src/routes/d/[id]/+page.svelte`. I changed no implementation. I wrote only this file. Every probe ran against a copy at `/tmp/t62r2`, never the worktree.

### The harness

I copied the worktree to `/tmp/t62r2`. I ran `npm --prefix web run check` and `npm --prefix web run build` there. I built `./cmd/ajilamu` and served the bundle on port 43512. I drove `/d/fixture` in headless Chromium through `playwright-core`. The browser recorded every request, every response, every console message and every page error.

### Health checks, not pins

`npm --prefix web run check` reported 181 files, 0 errors and 0 warnings. Neither changed file carries `export let`, a `$:` statement or a store import. The page raised no console message and no page error.

### Pins

**Fee panel.** I read the live panel on port 43512. The table reports the fee string, the `data-fee` attribute, the basis take and the takes on the line.

| Line | Fee string | Nanodollars | Basis take | Takes on the line |
|---|---|---|---|---|
| 1 | `$0.0008421` | 842100 | `seg_1_try1.wav` | `seg_1_try1.wav` |
| 2 | `$0.0024383` | 2438300 | `seg_2_try1.wav` | `seg_2_try1.wav` |
| 3 | `$0.0023182` | 2318200 | `seg_3_try1.wav` | `seg_3_try1.wav`, `seg_3_stretched.wav` |
| 4 | `$0.0036392` | 3639200 | `seg_4_try1.wav` | `seg_4_try1.wav`, `seg_4_stretched.wav` |
| 5 | `$0.0034599` | 3459900 | `seg_5_try1.wav` | `seg_5_try1.wav` |
| 6 | `$0.0029497` | 2949700 | `seg_6_try1.wav` | `seg_6_try1.wav` |
| 7 | `$0.004751` | 4751000 | `seg_7_try1.wav` | `seg_7_try1.wav` |
| 8 | `$0.0029486` | 2948600 | `seg_8_try1.wav` | `seg_8_try1.wav` |

Lines 3 and 4 now read a non-zero fee from their charged take. Lines 1 and 7 hold at 842100 and 4751000, which match the round one targets.

**The fallback, judged.** The fixture carries no line whose takes all lack charges. I mounted `Speaker.svelte` alone and supplied one. The panel read `$0.00` and named `seg_99_stretched.wav`. The hint read: "The estimate uses the newest billed take, or reads zero when none is billed." The screen claims a zero estimate from a named take. It never claims the re-render is free. The confirm note still says a re-render is pending. I judge the fallback honest and deliberate. Round one proposed this fallback, so I file no defect against it.

**Cancel and confirm.** Cancel fired zero `/api` requests on all eight lines. The pending panel closed on every line. Confirm fired zero requests on lines 1, 3, 4 and 7. The only traffic after load was the preview element's own `GET /clip.mp4` on line selection.

**Take bytes.** All ten files under `testdata/takes/` matched their HEAD blobs by `git hash-object` after both runs. The server data directory held zero files.

**Payload and comment.** A mounted component probe captured the `SpeakerChange` payload. Line 3 named `seg_3_try1.wav` at 2318200. Line 1 named `seg_1_try1.wav` at 842100. Each payload take matches its panel basis. The field comment at line 8 names the newest billed take. The hint repeats the rule.

**Svelte 5 only.** Both changed files use runes. Neither carries `export let`, a `$:` statement or a store import. `npm --prefix web run check` passes with zero errors and zero warnings.

## Findings

| # | Sev | Where | What | Pin | Mutation |
|---|-----|-------|------|-----|----------|
| 1 | M | `dev-diary/PHASE-6-editing.md` lines 309 to 312, with the seam note at 299 to 300 | The seam note and the T6.2 handoff entry still describe the pre-fix rule. They say the estimate sums charges on the selected line's last take. They report line 3 as `$0.00` because its last take carries no charge. The delivered code sums the newest take whose `charges` array is non-empty. Line 3 shows `$0.0023182` from `seg_3_try1.wav`. The record now contradicts the panel. | Headless Chromium on port 43512 read line 3 as `$0.0023182` with `data-fee=2318200` and basis `seg_3_try1.wav`. A mounted probe captured `takeFile: seg_3_try1.wav`. Reading the file returns "last take" and "Line 3 shows `$0.00`". | Revert the handoff rewrite and the next agent builds T6.5 against the last-take rule. That reintroduces the round one H on lines 3 and 4. |

## Residue Against Round One Finding 1

The code paths the finding named are clean. Lines 3 and 4 show non-zero fees from their charged takes. The basis label and the `takeFile` payload name the same take as the sum. The field comment matches the behaviour. One residue stays open in the record. The seam note and the handoff entry still describe the last-take rule and report line 3 as `$0.00`. That is this round's finding. The code residue is zero. The documentation residue is open.

## What I did not file

**The estimate sums the translate charge.** Round one raised it and did not file it. A speaker change may skip translation. The T6.5a contract does not promise that skip. The behaviour is deliberate, so I leave it.

**The all-zero fallback.** Round one proposed it and the hint discloses it. I judge it honest, so I file nothing.

**The page ignores `estimatedNanodollars` and `takeFile`.** T6.5a owns the consumer. The payload fields exist for that task.

**The `.editor-panels` grid wrapper.** That is layout, not behaviour.

## Done condition

The done condition reads: "Changing a speaker displays the associated re-render fee and leaves original takes untouched if canceled." The fee half now holds on all eight lines. The cancel half holds. The code meets the done condition. The record does not, which is finding 1.

## Cleanup

I stopped both servers and closed the browser. The worktree holds one new file from me, this review. The copies at `/tmp/t62r2`, `/tmp/t62r2-mount` and `/tmp/t62r2-harness` stay outside the repository.
