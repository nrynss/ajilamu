# T7.3a remediation, round 2

I fixed the one finding from `t7.3a-round2.md`. I did not change the verdict.

I chose the ffprobe probe over a cheap WAV container check. `takeFileUsable` must
accept exactly the takes `RepairLine` can measure, and `RepairLine` measures with
`media.Duration`. A header check would duplicate decoding rules and could still
accept a file that ffprobe rejects.

| Item | Sev | Where | Resolution | Pin |
|---|---|---|---|---|
| F1 | M | `internal/fit/loop.go:213-227`, `takeFileUsable` | `takeFileUsable` now takes a context. It stats the path first, so a FIFO cannot block the run, and then probes the take with `media.Duration`. A non-empty file that no decoder can read is not completed work. `readSegmentRecord` takes the context, rejects that take, and removes it, so the fresh render claims the path. Both resume paths re-render the line. | `TestPipelineRerendersUndecodableRecordedTake` places a non-empty partial-header take beside a valid record, resumes the run, and requires a clean run, one synth call, and a decodable take at the recorded path. `TestPipelineRerendersUndecodableReplayedTake` covers a flagged record and a stretched record, and requires a fresh render instead of the stored result. |

## Mutation checks

I reverted the probe in a scratch copy at `/tmp/t73a-remed2-scratch`, outside the
repository, and ran the pins. The restored copy passes both tests.

| Reverted fix | Test that died |
|---|---|
| Restored the `os.Stat`-only take check | `TestPipelineRerendersUndecodableRecordedTake` failed with `repair line 1: source take could not be probed ... Invalid data found when processing input`. Both replay subtests failed with `synth requests = 0, want 1`. |

No test hardcodes a port or a fixed temporary directory. Both new tests use `t.TempDir()`.
