# T5.6 Remediation Round 2

| Finding | Fix | Pin | Mutation |
|---|---|---|---|
| F2 | Extended the shortcut guard to exclude every keyboard-interactive ARIA widget role. The guard now includes checkbox, radio, switch, tab, menuitem variants, option, treeitem, and their focus-owning composite containers. | I inspected the selector against each ARIA widget role. Each has a matching selector before the handler can call `preventDefault`. The same keys on a non-control still reach the workspace handler. `npm run check` and `npm run build` completed successfully. | Reverting the complete ARIA role guard lets workspace playback or selection take over a focused custom control. |
