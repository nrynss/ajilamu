# T4.3 remediation, round 6

| Finding | Remediation | Pin | Mutation prevented |
| --- | --- | --- | --- |
| H pending identity release | `AppendCommit` now checks matching durable `commits_raw` queue entries before the remote lookup. The queue entry claims the immutable identity until a successful flush removes it. | `TestAppendCommitRejectsConflictingPendingIdentity` forces a 503, measures one journaled first payload, rejects a conflicting identity, then measures one recovered delivery of that first payload. | A network outage cannot journal two conflicting rows for one commit identity. |
