# T6.1 Round 2 Review

## Verdict

REMEDIATE

| Severity | Count |
|---|---:|
| C | 1 |
| H | 0 |
| M | 0 |
| L | 0 |

## Independent measurements

| Check | Observation |
|---|---|
| Delivered markup | A fresh `xh GET http://127.0.0.1:5173/d/fixture` response contained `boundary-editor` and one start and end control for line 1. The browser accessibility tree also exposed both controls as sliders. |
| Hydration and interactivity | A fresh browser load emitted Svelte `effect_update_depth_exceeded` from the route fixture-loading `$effect`. Clicking Line 2 left line 1 selected. ArrowRight on `Move start of line 1` left `aria-valuenow` at 5908. Dragging that handle also left it at 5908. No event handler could run. |
| Minimum slot invariant | Fixture line 2 begins at 7,728ms. A requested end at that value constrains to 7,828ms, snaps toward 7,728ms, then constrains to 7,828ms. The resulting slot is exactly 100ms. |
| Static checks | `npm run check` reported zero diagnostics. `npm run build` completed. The T6.1 component and route contain no Svelte 4 `export let` or `$:` syntax. |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| `web/src/routes/d/[id]/+page.svelte:201-221` | The fixture-loading `$effect` reads `dub` at line 207 after assigning a newly parsed fixture to `dub` at line 206. Svelte throws `effect_update_depth_exceeded` during hydration. SSR shows the boundary controls, but client handlers never run. A creator cannot select another line, drag a boundary, nudge with the keyboard, view live fit changes, or confirm an overlap. This is C because it breaks the T6.1 demo. The failure existed before this task, but T6.1 now owns this route seam and relies on it for the delivered controls. | Fresh browser console captured `effect_update_depth_exceeded` at this effect. A keyboard nudge, drag, and line-selection click each left their observed values unchanged. | Reverting a route hydration fix restores the recursive effect and makes all shipped boundary controls inert after the initial SSR paint. |

## Residue

Round-one F1 has no composition residue. The route imports and renders `Boundary`, and its callback replaces the confirmed segment in local state. The browser exposes the editor and both line 1 handles.

Round-one F2 has no minimum-duration residue. The post-snap constraint preserves the 100ms floor for the adjacent line 1 and line 2 boundary. Its live proof remains unavailable because the route hydration failure prevents all handlers.

The new C finding blocks approval. It also blocks the required live LengthBar-fit verification. `Boundary` derives adjusted take fit from its editable slot, but a browser cannot exercise that data flow until hydration succeeds.
