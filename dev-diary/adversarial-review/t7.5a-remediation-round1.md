# T7.5a remediation, round 1

I fixed the one finding from `t7.5a-round1.md`. I did not change the verdict.

| Finding | Fix | Pin | Residue |
|---|---|---|---|
| 1 H, `internal/ledger/workspace.go`, `buildLanguageTracks` | I initialized `Charges: []api.Charge{}` in the `api.Take` literal. A take with no itemized charge now marshals an empty array rather than null. The append of owned charges still works. I changed no query and no other field. | `TestWorkspaceTakesChargeLessTakeMarshalsEmptyCharges` drives `WorkspaceTakes` over a stand-in that serves one take and no charge row. The marshalled take is `{"file":"a.wav","voice":"voice-a","attempt":1,"repair":"none","fit":{"slot_ms":1820,"measured_ms":1680,"delta_ms":-140,"state":"fits"},"charges":[]}`. The test asserts the substring `"charges":[]`, rejects `"charges":null`, and requires a non-nil decoded slice. | None. Round 1 is the first review of T7.5a, so no prior finding exists to re-measure. |

## Mutation check

I removed the `Charges: []api.Charge{}` line from the working file and ran the pin. The test
failed with `"charges":null` and a nil decoded slice. I restored the file from a backup at `/tmp`
and the pin passed again.

## Validation

- `go test -count=1 ./internal/ledger` reported `ok github.com/nrynss/ajilamu/internal/ledger 1.012s`.
