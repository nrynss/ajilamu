# T3.1 round 3 review

Verdict: APPROVE

I reviewed the complete T3.1 change against HEAD, including both assembler files and the phase ownership update.
I read AGENTS.md, the task, diary conventions, the audio handling specification, and every prior T3.1 review and remediation record.
I own only this review file. The required shapes exist. I can validate this task independently.

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 0 |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| None | No findings at any severity. | Independent artifact measurements below. | Existing regressions pin both prior repairs. |

## Prior-round residue

**Round 1, F1: zero residue.** The source's initial offset, large internal gap, and short internal gap preserve their measured placement.
**Round 1, F2: zero residue.** Separate mono music retains unity gain during stereo conversion.
**Round 2, F1 residue: zero residue.** The 50 ms gap remains silent, and every downstream source sample retains its exact placement.
No prior finding remains open. I found no additional defect in the full task or tests.

## Independent measurements

I copied the unchanged assembler and media helpers into a temporary module outside the repository.
A small driver called BuildBed and PrepareTake. External tools measured its artifacts independently of the driver's result.
Every measurement selected `/opt/homebrew/bin/ffmpeg` and `/opt/homebrew/bin/ffprobe`, both version 9.0.1.

### Initial source offset

I regenerated the delayed source using the round-one command.
ffprobe reports video starting at zero and audio starting at 1.000000 seconds within a three-second container.
Independent silencedetect measures bed silence from zero to 1.000023 seconds and from 2.000000 to 3.000000 seconds.
The tone therefore retains its original interval, from seconds one through two.
The first sine sample equals zero, which accounts for the one-sample silence endpoint difference.
TestBedPreservesSourceTimestamps also passes both its delayed and large-gap artifact checks.

### Sub-100 ms gap and exact downstream alignment

I regenerated the 48 kHz stereo source using the round-two short-gap command.
ffprobe reports packets at 0.990000 and 1.050000 seconds, each carrying 0.010000 seconds of samples.
These packets independently establish a 50 ms gap at one second.
Silencedetect reports silence from 1.000000 to 1.050021 seconds, then from 2.050000 to 3.000000 seconds.
The one-sample endpoint difference again comes from the first sine sample.

I measured exactly 2400 frames, covering seconds 1.000 through 1.050, with astats.
Both channels report negative-infinite RMS, confirming digital silence throughout the complete gap.
I independently decoded source frames 48000 through 95999 and output frames 50400 through 98399 into float PCM.
Both sequences share SHA-1 `ec2a9e10373b46a0f90a4ee73d70b575bff95395`.
Thus the complete downstream second retains its samples exactly, including the final 50 ms of tone.

TestBedPreservesShortSourceTimestampGap also passes its packet check and full output sample comparison.
It compares every decoded output sample against source samples with exactly 4800 interleaved zeros inserted.
Removing `min_hard_comp=0` restores the prior failure and violates this regression's silent-gap and downstream-placement expectations.

### Separate mono music gain

I regenerated the round-one two-second mono music and stereo source fixtures.
Independent volumedetect reports -21.1 dB mean and -18.1 dB maximum for the mono input, supplied-music bed, and PrepareTake output.
ffprobe measures the supplied-music bed at 44100 Hz, two channels, and 2.000000 seconds.
TestSeparateMonoMusicPreservesLevel additionally measures a 0.000067 dB RMS difference and equal stereo samples.
Removing explicit mono duplication restores default rematrix attenuation and violates this measured-gain regression.

## Full-task sweep

The fixture bed measures 44100 Hz, two channels, and 75.008005 seconds.
Independent volumedetect measures -15.4 dB mean and -0.1 dB maximum for source and bed over seconds 54.5 through 75.
The existing millisecond duration contract explains the submillisecond rounding.
The fixture regression compares every decoded sample through second 75 and passes both measured-level intervals.
All ten take conversions pass their independent format, duration, RMS, and stereo-duplication checks.

I independently generated native 5.1 PCM and rendered its source bed.
ffprobe measures 48000 Hz, six channels, layout 5.1, and 2.000000 seconds.
Source and bed decoded samples share SHA-1 `d991e2155764984130b503fcd8ac2fddbd760227`.
The separate 5.1 music regression verifies exact substitution, silence padding, and source-duration trimming.

I audited source format adoption, SeparateMusic signaling, take conversion, output alias rejection, temporary rendering, and atomic publication.
The invalid-input regression preserves an existing output, rejects hard-link aliases, and leaves no temporary output files.
BuildBed and PrepareTake expose the required seams for later placement, ducking, and export tasks.

`PATH=/opt/homebrew/bin:/usr/bin:/bin go test -count=1 ./internal/assemble -v` passes.
`git diff --check` passes.
I removed this round's temporary module and artifacts after recording their measurements.
No production code, prior review record, or task status changed during this review.
