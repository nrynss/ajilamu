# T7.4 Round 1 Remediation

The worktree already contained each code remediation when this pass began. I retained the working changes and measured every finding independently.

| # | Sev | Resolution | Independent pin |
|---|---|---|---|
| 1 | H | Closed. The root route fetches `/api/dubs` and renders the returned summaries. | Headless Chrome rendered project id `pin-live-project-741` from the measured response. A source search found no literal `fixture` row. |
| 2 | M | Closed. Successful submission resets both password controls and shows a polite success region. | Headless Chrome submitted two marker values. Both controls then held empty strings, and the live region contained `Settings saved.` |
| 3 | M | Closed. `ConfigHandler` rejects every field except `voice_key` and `translation_key` with its fixed 400 response. | The unknown-field handler test received 400 and measured that the callback remained untouched. |
| 4 | L | Closed. Focused tests pin ordering, deep input copying, content types, request limits, duplicates, callback failures, and secret-free responses. | `go test -count=1 ./internal/api` passed. The package tests exercise every named behavior. |

## Validation

- `go test -count=1 ./internal/api` passed.
- `npm --prefix web run check` passed with zero errors and zero warnings.
- Browser measurements used ports 41783 and 41784.
