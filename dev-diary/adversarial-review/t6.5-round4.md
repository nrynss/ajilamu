# T6.5 ghost take history: round 4

**Verdict: REMEDIATE.** Counts: **C 0, H 0, M 1, L 0.**

The round three remediation fixes both round three M findings. I re-measured the line switch with a delayed reply and pinned it by mutation. The line 1 confirm reads `Change speaker` and both buttons stay live. The pending panel survives the line 3 reply. The lane ghosts, the Speaker note and the estimate all still hold. One new defect sits in the same state. A single in-flight id cannot name two renders, so a reply from one line clears the other line's guard. A duplicate billable render then becomes possible. The open H stays with T6.5c.

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| `web/src/lib/edit/Speaker.svelte:129-131`, with the state at `:45-46` and the set at `:112` | **M (double render).** `renderingSegmentId` names at most one in-flight segment. The `finally` clears it with no owner check. When two lines render at once, the first reply clears the second line's flag. The second line then reads idle and its confirm button is live. A third billable re-render can start. The same slot also drops the first line's flag when the second confirm runs. | I confirmed line 3, switched to line 1 at 34 ms and confirmed line 1 at 1.7 s. Line 3 replied at 2.9 s. The line 1 panel then read `confirmText: "Change speaker"`, `confirmDisabled: false` and `cancelDisabled: false` while the line 1 reply was still in flight. A click fired a third POST with body `{"language":"ml","speaker":"Suni Williams"}`. Three POSTs total. | I added the owner check `if (renderingSegmentId === targetSegment.id) renderingSegmentId = undefined` in a /tmp copy. The reopened panel then read `Re-rendering…`, `true` and `true`, and the third POST count was 0. A set of in-flight ids reddens the same pin and closes the symmetric hole. |

Suggested fix. Replace the single id with a set of in-flight ids. This closes both holes and keeps the round three pins. I verified it in `/tmp/t65r4/mut-set`.

```svelte
  let renderingSegmentIds = $state<Set<number>>(new Set())
  const busy = $derived(renderingSegmentIds.has(segment.id))
```

```svelte
    renderingSegmentIds = new Set(renderingSegmentIds).add(segment.id)
```

```svelte
      const settled = new Set(renderingSegmentIds)
      settled.delete(targetSegment.id)
      renderingSegmentIds = settled
```

## What I measured

I copied the working tree to `/tmp/t65r4/repo` and symlinked `node_modules` and `testdata`. I built with `npm --prefix web run build`. A throwaway Node server served the built bundle and the frozen `testdata/wire/dub.json` under the non-fixture id `t65-r4-dub`. It delayed the line 3 reply by 1500 ms and the line 1 reply by 6000 ms. I drove the page with headless Chromium. The working tree was never edited.

### 1. The round three M1 pin (busy across a line switch)

The run confirmed the line 3 speaker change and selected line 1 at 34 ms. The line 1 panel during the flight read:

```json
{
  "eyebrow": "Line 1",
  "confirmText": "Change speaker",
  "confirmDisabled": false,
  "cancelDisabled": false,
  "feeText": "$0.0008421",
  "pendingText": "Re-rendering line 1 as Mark Vande Hei is estimated at $0.0008421. Estimated from the take seg_1_try1.wav. Cancel Change speaker"
}
```

The mutation `const busy = $derived(renderingSegmentId !== undefined)` reddened the pin. The same read then returned `confirmText: "Re-rendering…"`, `confirmDisabled: true` and `cancelDisabled: true`.

### 2. The round three M2 pin (the stale reply)

The same run chose a line 1 speaker during the flight. The line 3 reply landed. The line 1 panel read `pendingOpen: 1` before and after the reply. It kept the same estimate sentence and both buttons stayed live.

The mutation re-added `pendingSpeaker = undefined` before the guard return. The panel then read `pendingOpen: 0` after the reply. The pin reddens.

### 3. The new M (double render)

The concurrency run confirmed line 3 at 1.4 s, confirmed line 1 at 1.7 s and reopened the line 1 panel at 4.2 s. Line 3 replied at 2.9 s. The reopened panel read `confirmText: "Change speaker"`, `confirmDisabled: false` and `cancelDisabled: false`. A click fired a third POST at 4.6 s with the body `{"language":"ml","speaker":"Suni Williams"}`. The server logged three POSTs.

The owner-check mutation and the set fix both held the reopened panel at `Re-rendering…` with both buttons disabled. Neither run fired a third POST.

### 4. The lane ghosts

Line 3 held three shapes after the re-render. The `Dub ML` lane ran top 1084 to bottom 1148, a height of 64 pixels. All three shapes sat inside it.

```json
{
  "lane": { "top": 1084, "bottom": 1148, "height": 64 },
  "laneContainsShapeCount": 3,
  "shapes": [
    { "file": "seg_3_try1.wav", "state": "ghost", "top": 1094, "bottom": 1138,
      "borderStyle": "dashed", "opacity": "0.72", "insideLane": true },
    { "file": "seg_3_stretched.wav", "state": "ghost", "top": 1097, "bottom": 1135,
      "borderStyle": "dashed", "opacity": "0.72", "insideLane": true },
    { "file": "/tmp/t65r4-storage/t65-r4-dub/work/ml-IN/seg_3_try3.wav", "state": "active",
      "top": 1100, "bottom": 1132, "borderStyle": "solid", "opacity": "1", "insideLane": true }
  ]
}
```

The older ghost carries the wider outline, so the depth order reads oldest to newest.

### 5. The Speaker note

The stay run confirmed the line 3 change and waited for the 201. Both panels named the new take.

```json
{
  "header": "Mark Vande Hei",
  "speakerNote": "Line 3 re-rendered as take seg_3_try3.wav.",
  "stripNote": "Line 3 re-rendered as take seg_3_try3.wav.",
  "pendingOpen": 0
}
```

### 6. The estimate before the call

The pending panel showed `$0.0023182` before the confirm. The server recorded zero POSTs at that point. The run recorded one POST after the click.

### 7. Playback

The two fixture takes play independently. A click on `seg_3_try1.wav` fetched `seg_3_try1.xBIiyNfY.wav` with status 200 and type `audio/wav`. A click on `seg_3_stretched.wav` fetched `seg_3_stretched.ClDJvJby.wav` with the same status and type. Each click fetched one take and raised no note.

A click on the new active take fetched no audio. The strip note read `The take /tmp/t65r4-storage/t65-r4-dub/work/ml-IN/seg_3_try3.wav has no browser audio URL.` That is the open H.

### 8. The type check and the console

```text
$ npm --prefix web run check
1788912423247 START "/home/nryn/work/ajilamu/web"
1788912423249 COMPLETED 183 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
```

Exit 0. The happy-path run raised 0 console messages and 0 page errors.

A failed reply does raise one console error. I set the line 3 status to 500. The browser logged `Failed to load resource: the server responded with a status of 500 (Internal Server Error)`. The page raised 0 page errors. That message belongs to the browser.

The concurrency run raised one page error, `https://svelte.dev/e/each_key_duplicate`. My stand-in returned the same take file for both line 1 replies, so the take list held two identical keys. A real route names a new attempt for each take. That error is a harness artifact and not a finding.

## Residue against round one

**H1 residue: open.** T6.5c owns the take audio route. The new active take stays unplayable until that route lands.

**M1 residue: none.** Three line 3 shapes sit inside the lane. They read ghost, ghost, active, and each one reports `insideLane: true`.

**M2 residue: none.** Both panels name the new take after the 201. The Speaker note and the strip note read the same sentence.

## Residue against round two

**M1 residue: none.** The line switch with a delayed reply leaves both panels on the new line empty. The mutation restores the old sentence and the pin reddens.

## Residue against round three

**M1 residue: none.** The new line reads idle while line 3 re-renders. The mutation reddens the pin.

**M2 residue: none.** The pending choice on the new line survives the stale reply. The mutation reddens the pin.

**L1 residue: none.** The seam paragraph matches the ghost loop at `Track.svelte:79-91` and the owns line at `PHASE-6-editing.md:130-131`.

**L2 residue: none.** The T6.5c block now has a blank line before its `---` rule.

**L3 residue: none.** The README row reads 7 / 9. `python3 tools/audit_docs.py` printed `No drift. Docs and repository agree.` and exited 0.

## Done condition

The timeline shows ghost take history in the lanes. Both fixture takes play independently. The estimate shows before every re-render call. The line switch no longer leaks the old note or the old busy flag. The play clause holds for the fixture and not for a real project, and T6.5c owns that half. One new M finding blocks approval.

## Notes, not findings

1. The chip carries `aria-pressed` on a play button. Rounds one to three declined it, and I do not file it again.
2. A 500 reply that lands after a line switch leaves the optimistic speaker in place and shows no error on the returned line. The optimistic update predates this patch. Rounds two and three declined it.
3. The run button starts a billable run and shows no estimate before the call. That gap predates this patch and belongs to the run task.
