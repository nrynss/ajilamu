# T5.4 Remediation Round 2

| Finding | Fix | Pin | Mutation |
|---|---|---|---|
| F2 | The component now derives time scale from its parent width and allocates its canvas through the longest take. The trough retains its parent-based scale while measured audio can paint beyond it. The resize observer watches that parent. | `lengthbar-overflow-width`. I mounted the built component in headless Chrome with a 698px parent and an 8,000ms active take. The reference canvas stayed 698px. The overlong canvas expanded to 784px. Its exported bitmap contained `--over` pixels from x=690 through x=774, a visible 85px overrun after the 682px trough. The mathematical overrun is 85.37px. `npm run check` and `npm run build` completed with zero errors and warnings. | Reverting the paint-width allocation leaves the canvas at 698px. The 8,000ms take then clips at the parent boundary and hides most of its 0.89-second excess. |
