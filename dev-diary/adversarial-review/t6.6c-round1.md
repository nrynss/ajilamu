# T6.6c adversarial review, round 1

**Scope:** T6.6c at the working tree on HEAD `c4f1f94`. The diff covers `sql/schema.sql`, the
new `internal/ledger/charges.go` and its test, and the take writer guard in
`internal/ledger/takes.go`. It also covers the running total and whole-pass reads in
`internal/ledger/workspace.go`, the recorder seam in `internal/api/agent.go` and
`internal/api/server.go`, the comment in `internal/api/wire.go`, and the
`agentChargeRecorder` adapter in `cmd/ajilamu/main.go`. I ignored the dirty parse overlap,
`web/src/lib/progress.ts`, the workspace page, and the P5, P6 and P7 close files.

**Method:** I measured a fresh ClickHouse 26.8.2.7 loaded from `sql/schema.sql`, the real
`ajilamu` binary, and the production frontend build in headless Chrome 153. I read charge rows
back with SQL through the `charges` view. I drove the Details tab over the DevTools protocol and
collected `Runtime.exceptionThrown`. I did not treat the implementer's report or any program log
as evidence.

**Verdict:** REMEDIATE. Findings: 1 C, 2 H, 2 M, 1 L.

| C | H | M | L |
|---|---|---|---|
| 1 | 2 | 2 | 1 |

I own only this review file. I fixed nothing.

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| `internal/ledger/workspace.go` `selectWholePassCharges`, `internal/api/wire.go` `Charge`, surfacing at `web/src/lib/tabs/DetailsTab.svelte` line 109 | The diff routes agent rows into `WholePassCharges`. The wire `Charge` carries kind, units and price and no turn or call identity. The Details tab keys its project charge list on `project-{kind}-{units}`. Two turns that bill the same token counts produce two rows under one key. Svelte 5.57.0 throws `each_key_duplicate` in production as well as development, so the panel holding the project total, the `covers` sentence and every charge row renders nothing. The task's own done condition requires two identical turns, so the feature breaks the screen that displays it. | Two POSTs to `/api/dubs/d3/agent` through the real route and the real adapter. `GET /api/dubs/d3` returned four charges with units 340, 340, 1200 and 1200. Headless Chrome clicked the Details tab on the production build and `Runtime.exceptionThrown` carried `Error: https://svelte.dev/e/each_key_duplicate`. The DOM held no `aria-label="Line details"` and `.project-charges li` counted 0. Dub `d11`, seeded with one turn, rendered both rows, the sentence `and 1 agent call.`, and threw nothing. `each.js` lines 355 to 362 throw on duplicate keys down both the DEV and the production branch. | Carry the turn id and the call index onto `api.Charge` and into the key, or fold one turn's calls into a single agent row inside `WholePassCharges`. Reverting the fix must make the Details tab throw again after two identical turns. |
| `cmd/ajilamu/main.go` `agentChargeRecorder.RecordAgentTurn` | The adapter flushes the ledger and reads the dub head before it journals anything. Both calls reach ClickHouse. When ClickHouse is unreachable the method returns before `journalJSON` runs, so the durable queue stays empty and the reconciler never retries. Gemini already billed the turn. The route answers 500 and discards the answer. The exact gap T6.6c exists to close reopens whenever ClickHouse blinks. | I pointed the recorder at `127.0.0.1:18924` with nothing listening and posted one turn. The route answered HTTP 500 with `The editor agent charge could not be recorded.` A filesystem walk of the queue directory found zero files and zero bytes. `internal/ledger/charges.go` journals rows only after the adapter has already returned. | Journal the charge rows before the head read decides the commit, or let the queue carry the turn. Reverting the fix must leave the queue empty after one outage POST. |
| `cmd/ajilamu/main.go` `agentChargeRecorder.RecordAgentTurn`, and `internal/ledger/charges.go` `AgentTurn.rows` | A dub with no commit loses the billed turn for good. The adapter errors with `dub has no commit` and the route answers 500. Every retry fails the same way, so no workaround exists. `charges_raw.commit_id` carries `DEFAULT ''` precisely so a row needs no commit, and `rows` still rejects an empty commit id. Only `runRecorder.persist` and `editRecorder` call `AppendCommit`, so a freshly uploaded dub has no head until a run or an edit lands. The command bar offers the ask on any parse rejection, which reaches that state before the first run. | `POST /api/dubs/d5-no-commit/agent` against the loaded schema answered HTTP 500 with `The editor agent charge could not be recorded.` `SELECT count() FROM charges WHERE dub_id = 'd5-no-commit'` returned 0. `rg AppendCommit` outside tests matches only `main.go` lines 968 and 1095, both on the run and edit paths. | Record the turn against the empty commit sentinel when a dub has no head. Reverting the fix must make the same POST answer 500 and leave no charge row. |
| `internal/ledger/workspace.go`, the comment above `selectWholePassCharges` | The new comment says segmentation and agent calls serve the whole dub rather than one take. The diff deleted the sentence that recorded the opposite for storage. `attributeWholePass` in `cmd/ajilamu/main.go` rewrites the segmentation charge onto the first rendered take, and `takes.go` writes it with that take id and that take's `segment_index`. Only the deleted sentence explained why the statement still orders by `segment_index`. The next agent will expect the `-1` sentinel on segment rows and find a real index. | `takes.go` `chargeRows` sets `TakeID: a.TakeID` and `SegmentIndex: int32(a.Segment.ID)` for `cost.ChargeSegment`. `main.go` line 992 attributes whole-pass charges to `take.Segment.ID`. SQL on a route-written agent row showed `take_id` empty and `segment_index` `-1`, so only agent rows carry the sentinel. | Restore the take attribution note for segmentation beside the new agent sentence. Reverting the fix must leave a comment that claims segment rows own no take. |
| `internal/ledger/workspace.go` `selectRunningTotal` and `RunningTotal` | The sentence mixes two counting units. `countIf(kind = 'translate')` counts unit rows, and one Gemini call bills a prompt row and a candidate row, so one call reads as two. `uniqExactIf(tuple(turn_id, call_index), kind = 'agent')` counts real calls. Segment charges take the same two-row branch as translate, so they double the same way. The displayed sentence therefore overstates three kinds and states the fourth correctly. UI rule 6 asks every displayed number to match a real measurement. The three miscounts predate this diff, and this task owns the statement and the sentence. | I inserted one translate call as the take writer emits it, a prompt row and a candidate row. `SELECT toUInt64(countIf(kind = 'translate'))` returned 2 against `count()` of 2 rows. The agent term on dub `d3` returned 2 for two turns and on `d6` returned 20 for one twenty-call turn, both exact. | Count the three older kinds by call rather than by unit row. Reverting the fix must make one translate call read as two translation calls. |
| `sql/schema.sql`, the header comment near line 36 and the `charges_raw` comment near line 757 | The diff deleted the rule that a genuine second take call repeating every identity field must raise `attempt`. That rule still governs take charges, because `turn_id` and `call_index` stay at their defaults for them. Nothing in the file now tells a take writer how to keep two identical calls apart. | `rg "raise attempt"` matches once in `git show HEAD:sql/schema.sql` and zero times in the working copy. I inserted the same synthesize row twice at one attempt into the loaded schema. `SELECT count() FROM charges` returned 1 row against 1 distinct `event_key`. | Restore the attempt rule for take charges beside the new turn id sentence. Reverting the fix must leave the file silent on how two identical take calls stay distinct. |

## Done conditions, measured

| Condition | Independent result |
|---|---|
| A fresh database from `sql/schema.sql` accepts kind `agent` | PASS. The file loaded on an empty database and the preflight returned 0. `system.columns` read `Enum8('segment' = 1, 'translate' = 2, 'synthesize' = 3, 'agent' = 4)`. Four agent rows inserted with no error, against code 691 before the diff. |
| One `POST /api/dubs/{id}/agent` writes its measured charges, read back by SQL | PASS. The real route and the real adapter wrote `turn_id` `124f5ba1d74c42d07a1cffc42c34e5ee`, `call_index` 0, kind `agent` and provider `gemini-3.8-flash`. The same rows carried commit `c-route-1`, an empty `take_id`, `segment_index` `-1`, and units 1200 and 340 at prices 0.00000015 and 0.0000006. |
| The running total grows by the turn's nanodollars | PASS. Dub `d2` read 67000 before the turn and 451000 after, a rise of 384000. The response reported 384000. Dub `d6` summed 328500 for prompt counts 100 through 119 at 150 nanodollars, which matches 2190 tokens exactly. |
| The `covers` sentence names the agent calls | PASS. `RunningTotal` returned `0 segment calls, 0 translation calls, 0 render calls, and 2 agent calls.` for two turns and `and 20 agent calls.` for one twenty-call turn. The singular and plural both read correctly. |
| Two identical turns record two charges rather than one | PASS. Two POSTs with identical charges produced 2 distinct `turn_id` values and 4 rows through `charges` FINAL, against 4 distinct `event_key` values. Two hand-built turns sharing every other field also stayed 4 rows. |
| `internal/agent` imports no writer | PASS. `go list -deps ./internal/agent` names only `internal/config`, `internal/cost` and the package itself. No ledger, no api, no database driver. |
| The `mcp_readonly` grant still refuses an INSERT | PASS. `SHOW GRANTS FOR mcp_readonly` read `GRANT SELECT ON t66c_r1.* TO mcp_readonly`. An INSERT of kind `agent` failed with code 497 `ACCESS_DENIED`. The same user still read `SELECT count() FROM charges` and answered 7. |
| A nil recorder keeps the route answering and records nothing | PASS. `ServerOptions` with no `AgentCharges` answered HTTP 200 with a total of 384000. `SELECT count() FROM charges WHERE dub_id = 'd4'` returned 0. `newAgentChargeRecorder(nil, ...)` returns a nil interface rather than a typed nil. |
| The agent package stays read-only | PASS. The diff adds no file under `internal/agent` and no import of it outside the api seam. `deploy/mcp-clickhouse/compose.yaml` keeps `CLICKHOUSE_ALLOW_WRITE_ACCESS: "false"` and the `mcp_readonly` user. |

**Nine done conditions pass. The C finding sits beyond them.** The task named storage, the
writer, the total and the boundary, and every one of those holds. It did not name the screen
that shows the total, because it assumed the existing label was enough. The screen breaks in
the exact state condition five mandates.

## Review probes

| Probe | Result |
|---|---|
| `go build ./...` and `go vet ./...` | Both exited 0. |
| `go test ./internal/... ./cmd/...` | All 14 packages `ok`. `internal/api` 7.590s, `internal/ledger` 5.728s, `cmd/ajilamu` 6.907s. |
| Take writer rejects an agent charge | PASS. The new `default` branch in `chargeRows` errors before any transport request. The added test in `takes_test.go` counts zero requests. |
| Two identical calls inside one turn | PASS. `call_index` reads 0 and 1, so the rows stay distinct under one turn id. |
| Order of one twenty-call turn on the wire | PASS. `WholePassCharges` returned prompt counts 100 through 119 in ascending order. `cmpWholePassChargeRow` ignores `turn_id` and `call_index`, so the SQL order survives only because the client sort keeps ties at this size. |
| A recorder error leaks no internal detail | PASS. The 500 body read `The editor agent charge could not be recorded.` and named neither ClickHouse nor the query. |
| Single-turn Details tab render | PASS. Dub `d11` rendered two `Editor agent turn` rows at `$0.000204` and `$0.00018`, with the covers sentence and no exception. |
| `internal/api/server_test.go` | Owned and unchanged. `TestAgentRoutePreservesChargesAndDubContext` already pins the pass-through from `ServerOptions` to the handler, so the seam is covered. |
| Svelte 5 runes in the diff | No web file changed. No `export let` and no `$:` entered the tree. |
| `internal/api/command_test.go` | Modified in the tree and absent from the `owns` line. Its content pins the P6 close round 4 overlap finding, so it belongs to another task rather than to T6.6c. |

## Contract expansions, judged

**Both accepted expansions are used as the record describes.** `internal/api/server.go` gains
`AgentCharges` on `ServerOptions` and passes it to `AgentHandler`, which is the seam the
blocker named. `internal/api/wire.go` changes only the `AgentResponse` comment, and no field
or JSON shape moved. The nil recorder keeps the route available, exactly as the record
required.

**The `internal/agent` boundary held without an expansion.** The adapter lives in
`cmd/ajilamu`, so `internal/api` still imports no ledger and `internal/agent` imports no
writer. The import graph confirms it rather than the comment.

## What I measured

- ClickHouse 26.8.2.7 in a throwaway container on `127.0.0.1:18923`, database `t66c_r1`,
  loaded from `sql/schema.sql`. I ran the local image and pulled nothing.
- The `ajilamu` binary at `/tmp/t66c-r1-ajilamu` on port 18099, pointed at that database.
- The production frontend from `npm --prefix web run build`. `web/build` is gitignored, so
  the rebuild left no tracked change.
- Headless Chrome 153.0.8010.37 over the DevTools protocol on port 19222 and 19223. The probe
  navigated, clicked the Details tab, and read `Runtime.exceptionThrown` and the DOM.
- Charge rows came from `SELECT` over the `charges` view, which applies FINAL.
- Two throwaway harness files in `cmd/ajilamu` built the real adapter with
  `newAgentChargeRecorder` and mounted the real `api.NewServer`. Package `main` holds the
  adapter unexported, so no outside package can reach it. I deleted both files, and
  `git status` returns the tree it started from.
- The container is stopped and removed. The server and both browsers are stopped. No process
  and no database survives this review.

**One note for the orchestrator, unrelated to this task.** Eight web files that the opening
tree reported as modified now match `c4f1f94`. They are `web/package.json`,
`web/src/app.d.ts`, `web/src/lib/fixture.ts`, `web/src/lib/shortcuts.ts`,
`web/src/lib/states/index.ts`, `web/src/lib/types.ts`, `web/tsconfig.json` and
`web/vite.config.ts`. I ran no command that writes those paths, and `HEAD` never moved.
Another session probably reverted them. Check before anyone assumes that work landed.
