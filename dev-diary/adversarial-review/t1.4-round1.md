# T1.4 Round 1 Review

**Verdict:** REMEDIATE
**Commit:** T1.4 wire contracts implementation (uncommitted working tree)

## Severity Counts

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 1 |

## Findings

| # | Sev | Where | What | Pin | Mutation |
|---|-----|-------|------|-----|----------|
| 1 | L | `internal/api/wire.go:201,235,263,265,287,312` and `web/src/lib/types.ts:141,175,186,188,210,235` | Twelve mirrored doc comments use semicolons. AGENTS.md bans semicolons in code comments. | An independent Python scan found six semicolon comments in each file. It found no em dash and no sentence over 30 words. | Reverting the punctuation fix restores all twelve house-style violations. |

## False Positives

I checked three plausible contract defects and found none.

**The short fit states follow the frozen domain threshold.** Golden rows 1 and 6 carry negative deltas within eight percent. `internal/types.Fit` classifies both as fits.

The wire payload matches that result. Segment 8 carries `delta_ms` -2910, state `too_short`, and a review flag. No segment 8 take serializes as fits.

**Charges are not double-countable in the example contract.** The one whole-pass charge appears only in `Dub.charges`. All sixteen attempt charges appear only on their owning takes.

The seventeen charges sum to 23414000 nanodollars. That equals the declared total. Every charge also equals units times unit price.

**The action name conflict is not a T1.4 defect.** `observations.md` names `atempo_stretched`. `PHASE-4-ledger.md` names `stretched`.

The diary README says the specification wins over a phase document. T1.4 correctly uses `atempo_stretched`. T4.4 must map its local name to this frozen wire value.

## Evidence

- I ran `go build ./...`. It exited 0.
- I ran `go test -race -count=1 ./internal/api/...`. The package passed under race detection.
- An independent Python decoder loaded all eight `testdata/wire` JSON files. It found the required six payload categories and two supporting aggregate examples.
- A separate parser compared twelve Go structs with twelve TypeScript interfaces. It found zero missing types, extra types, key differences, or optionality differences.
- The fixture parser found only snake_case keys. Every start, end, slot, measured, delta, and duration field uses the `_ms` suffix.
- The golden comparison matched all eight segments and all ten takes. Every delta equals measured minus slot.
- The same comparison classified rows 1 through 7 as fits under `FitThreshold`. It classified row 8 as too short.
- The money scan found 41 nanodollar fields. Every value decoded as an integer.
- `charge.json` preserves 2100 nanodollars, which is below one cent. `total.json` preserves 23414000 nanodollars exactly.
- `progress.json` carries the sentence `Rendering line 3 in Malayalam` and cumulative cost 3355600.
- The standalone commit carries all four DAG identity fields. It also carries action, author, and the original instruction.
- The seven commits in `dub.json` form a valid parent chain with versions 1 through 7.
- `go doc -all github.com/nrynss/ajilamu/internal/api` listed documentation for every exported package identifier.
- I read every exported TypeScript declaration and its comment. No exported declaration lacks documentation.
- The documentation scan found twelve semicolons, zero em dashes, and zero sentences over 30 words.
- I read the T1.4 block, P1 exit criteria, product specifications, consumer phase requirements, domain types, cost model, and both golden fixtures.
