# T7.5b remediation, round 1

Task: user-defined languages at upload. I remediated the two findings in
`dev-diary/adversarial-review/t7.5b-round1.md`. Go toolchain go1.27.1. I changed
`cmd/ajilamu/main.go`, `cmd/ajilamu/main_test.go`, `internal/api/run.go`, and
`internal/api/run_test.go`. I did not change the verdict. The verdict stays REMEDIATE. The task
stays not done.

## Rows

| Finding | Fix | Pin | Residue |
|---|---|---|---|
| H1 · `cmd/ajilamu/main.go:408-468` | Replaced the one-entry table with a 53-locale display-name map plus the sample code `ml`. `resolveRunLanguage` and `resolveLanguageName` both read it. | A probe printed the prompt for target `ml-IN` and source `en-US`. It reads `Translate this English dialogue line into natural spoken Malayalam script (മലയാളം):`. The `es-ES` probe reads `natural spoken Spanish` with source `English`. `TestLanguageNamesCoverCatalog` fails when a committed code falls back to itself. | A provider refresh can return a code outside the committed list. Such a code still falls back to itself as the name. The sample path stores the source code `en`, which is not a catalog code, so its source name still falls back to `en`. |
| M1 · `internal/api/run.go:586-601` | `runWorkDirFor` falls back to the flat `work/` directory when it holds a record for the run language. The language directory wins when both hold records. A flat record for another language does not count. | A probe interrupted a run in `work/`, then resumed it through the real start route. The route chose `work/` and the resume rendered `[2 3]`. The language directory rendered `[1 2 3]`. `TestRunStartResumesFlatWorkDir` and `TestRunStartPrefersLanguageWorkDir` pin the choice. | The `ml` code and the `ml-IN` tag still name two directories for a fresh run. The fallback unifies them only through the flat directory. A record with no readable `language` field does not trigger the fallback. |

## H1, the target names Malayalam

`resolveRunLanguage("ml-IN")` now returns tag `ml-IN` and name `Malayalam`.
`resolveLanguageName("en-US")` returns `English`. The probe fed those names into
`gemini.translatePrompt`, which is the function the runner reaches through `fit.PipelineConfig`.
The probe lived at `/var/tmp/t75b-probe` and reached the package through `go test -overlay`.
It wrote no file in the repository. I removed it after it measured the seam.

The measured prompt for target `ml-IN` and source `en-US`:

```text

Translate this English dialogue line into natural spoken Malayalam script (മലയാളം):
Original English: "Hello from orbit"
Speaker emotion: Warm
Constraint: The translated line will be spoken in Malayalam in a video slot that lasts approximately 1.8 seconds (1820 ms). Keep the translation natural, spoken, and fit the rhythm.

Respond with strictly the translated Malayalam text. No markdown, no quotes, no explanation.
```

The opening slot keeps `Malayalam script (മലയാളം)`. The closing slot names `Malayalam`. No
`ml-IN` code appears in the prompt.

The measured names for target `es-ES` and source `en-US`:

```text
RESOLVED-SOURCE code=en-US name="English"
RESOLVED-SOURCE code=es-ES name="Spanish"
```

The measured prompt for target `es-ES` and source `en-US`:

```text

Translate this English dialogue line into natural spoken Spanish:
Original English: "Hello from orbit"
Speaker emotion: Warm
Constraint: The translated line will be spoken in Spanish in a video slot that lasts approximately 3.0 seconds (3000 ms). Keep the translation natural, spoken, and fit the rhythm.

Respond with strictly the translated Spanish text. No markdown, no quotes, no explanation.
```

## M1, the resume reads the flat directory

The probe ran a first pass in the flat `work/` directory. The fake synthesizer failed on line 2,
so the pass left `seg_1_result.json` and `seg_1_try1.wav` in `work/`. The probe then started the
run through the real `POST /api/dubs/{id}/run` route. A runner adapter ran `fit.RunPipeline` with
the work directory the route chose. The probe printed the request and the lines the resumed pass
rendered.

```text
FIRST PASS in /tmp/TestT75bFlatResumeProbe2164306154/001/dub-flat-resume/work rendered lines [1 2]
FLAT RECORDS [/tmp/TestT75bFlatResumeProbe2164306154/001/dub-flat-resume/work/seg_1_result.json]
RESUME request language="ml-IN" workdir=/tmp/TestT75bFlatResumeProbe2164306154/001/dub-flat-resume/work
RESUMED rendered lines [2 3]
LANGUAGE DIR /tmp/TestT75bFlatResumeProbe2164306154/001/dub-flat-resume/work/ml-IN rendered lines [1 2 3]
```

The resumed run rendered lines 2 and 3. It reused line 1 from the flat directory, so it did not
re-render or re-charge line 1. The contrast line shows the pre-fix behaviour. A run in
`work/ml-IN` holds no record, so it rendered all three lines.

Both directories holding a record. The language directory wins. The measured run chose
`work/ml-IN`:

```text
run_test.go:174: both directories hold a record, work dir=/tmp/TestRunStartPrefersLanguageWorkDir412619640/001/dub-both/work/ml-IN
```

The fallback is narrower than a bare directory test. `holdsRecordFor` reads each flat record's
`language` field. It counts a record for the same language, which covers the `ml` and `ml-IN`
alias. A flat record for `ta-IN` does not move an `ml-IN` run into the flat directory. The test
case measures that:

```text
run_test.go:156: request language=ml-IN flat record=ml-IN work dir=/tmp/TestRunStartResumesFlatWorkDircatalog_code1160773615/001/dub-resume/work
run_test.go:156: request language=ml flat record=ml-IN work dir=/tmp/TestRunStartResumesFlatWorkDirsample_code3188749793/001/dub-resume/work
run_test.go:156: request language=ml-IN flat record=ta-IN work dir=/tmp/TestRunStartResumesFlatWorkDirother_language2102464413/001/dub-resume/work/ml-IN
```

## Tests

`TestPipelineRunnerLanguageWiring` gained a catalog-code case for `ml-IN`. It now asserts the
target name `Malayalam` and the source name `English` on the `TranslateRequest`. The `es-ES` case
asserts the target name `Spanish`. `TestLanguageNamesCoverCatalog` walks `tts.CommittedLanguages`
and fails when a code resolves to itself. It also pins `ml`, `ml-IN`, `en-US`, and `es-ES`.
`TestRunStartResumesFlatWorkDir` and `TestRunStartPrefersLanguageWorkDir` pin the work directory
choice.

## Command output

```text
$ go test -count=1 ./internal/api ./cmd/...
ok  	github.com/nrynss/ajilamu/internal/api	1.355s
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	4.062s
```

## Residue against round 1

H1 is fixed on the create path. M1 is fixed for a resume. The two open notes sit in the table.
Neither changes the verdict.
