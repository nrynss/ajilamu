# T4.2 remediation, round 1

| # | Where | Remediation | Pin | Mutation covered |
|---|---|---|---|---|
| 1 | `internal/ledger/takes_test.go` | Added the committed fixture replay HTTP capture for `RecordTake`. | `go test ./internal/ledger -run TestRecordTakeFixtureCapture` captures ten take rows and thirty itemized charge rows. It checks segment 8 at `-2910`, rejects an inconsistent delta before transport, and confirms replay identities. | JSON field regressions, merged Gemini charge rows, missing delta validation, and retry identity drift now fail the persisted test. |
