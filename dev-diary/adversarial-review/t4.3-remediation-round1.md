# T4.3 remediation, round 1

| # | Severity | What I changed | Measurement proving it closed |
| --- | --- | --- | --- |
| 1 | M | Added durable HTTP-capture regression tests for `AppendCommit`. They cover root, linear child, valid fork, invalid root and child versions, self-parent rejection, server-owned timestamps, the `commits_raw` insert target, and a byte-identical replay after one failed delivery. | `go test ./internal/ledger` passed. The capture measured three accepted DAG rows and zero requests for three invalid rows. A forced 503 retained one durable entry, then `Flush` replayed the exact body to `commits_raw` and reduced pending entries to zero. |
