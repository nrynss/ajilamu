# T7.5b adversarial review, round 2

Task: user-defined languages at upload. I re-read `t7.5b-round1.md`,
`t7.5b-remediation-round1.md`, and the T7.5b handoff entry in `PHASE-7-ingestion.md`. I
measured the working tree against HEAD `eb5bf07`. Go 1.27.1, Node v26.8.1. Every pin came
from a throwaway probe in a copy at `/var/tmp/t75b-r2`. `/tmp` held 192 MB free, so the copy
lives under `/var/tmp`. The remediation changed `cmd/ajilamu/main.go`,
`cmd/ajilamu/main_test.go`, `internal/api/run.go`, and `internal/api/run_test.go`.

## Verdict

**REMEDIATE**

| Severity | Count |
|---|---:|
| C | 0 |
| H | 0 |
| M | 2 |
| L | 0 |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| M1 · `internal/api/run.go` lines 597 to 604 | `runWorkDirFor` names the language directory from the raw request code. The sample code `ml` and the catalog code `ml-IN` still name two directories for one language. A resume that switches spelling reads the other directory, finds no record, and renders every line again. The creator pays twice. | A probe ran a first pass with `ml` in `work/ml`, kept line one, then started the run through the real route with `ml-IN`. The route chose `.../work/ml-IN` and the translator rendered `[1 2 3]`. The same record in the chosen directory rendered `[2 3]`. The reverse order chose `.../work/ml` and rendered `[1 2 3]`. | Normalize the directory to the resolved tag. Reverting the normalization restores the split. |
| M2 · `cmd/ajilamu/main.go` lines 413 to 422 | The map carries the sample target alias `ml` and no sample source alias `en`. `resolveLanguageName("en")` returns `en`, so the sample prompt names the source as a code. The sample is the one-click NASA path. | A probe fed the resolved names into `gemini.translatePrompt`. The sample pair read `Translate this en dialogue line into natural spoken Malayalam script (മലയാളം):` and `Original en: "Hello from orbit"`. | Add `"en": {tag: "en-US", name: "English"}` to `createScreenLanguages`. Reverting the entry restores the code in the prompt. |

## Prompt pins

`resolveRunLanguage("ml-IN")` returned tag `ml-IN` and name `Malayalam`.
`resolveLanguageName("en-US")` returned `English`. The probe fed both into
`gemini.translatePrompt`. The measured prompt for target `ml-IN` and source `en-US`:

```text

Translate this English dialogue line into natural spoken Malayalam script (മലയാളം):
Original English: "Hello from orbit"
Speaker emotion: Warm
Constraint: The translated line will be spoken in Malayalam in a video slot that lasts approximately 1.8 seconds (1800 ms). Keep the translation natural, spoken, and fit the rhythm.

Respond with strictly the translated Malayalam text. No markdown, no quotes, no explanation.
```

The opening slot keeps `Malayalam script (മലയാളം)`. The closing slot names `Malayalam`. No
`ml-IN` code appears. The measured prompt for target `es-ES` and source `en-US`:

```text

Translate this English dialogue line into natural spoken Spanish:
Original English: "Hello from orbit"
Speaker emotion: Warm
Constraint: The translated line will be spoken in Spanish in a video slot that lasts approximately 1.8 seconds (1800 ms). Keep the translation natural, spoken, and fit the rhythm.

Respond with strictly the translated Spanish text. No markdown, no quotes, no explanation.
```

The sample pair names a code. The probe measured target `ml` and source `en`:

```text

Translate this en dialogue line into natural spoken Malayalam script (മലയാളം):
Original en: "Hello from orbit"
Speaker emotion: Warm
Constraint: The translated line will be spoken in Malayalam in a video slot that lasts approximately 1.8 seconds (1800 ms). Keep the translation natural, spoken, and fit the rhythm.

Respond with strictly the translated Malayalam text. No markdown, no quotes, no explanation.
```

## Name table

The probe printed `resolveLanguageName` for all 53 committed catalog codes. Every value is a
real language name. No value equals its own code. Examples: `ar-XA` Arabic, `cmn-CN`
Mandarin Chinese, `nb-NO` Norwegian, `yue-HK` Cantonese. The four English locales share
`English`. The two Spanish locales share `Spanish`.

## Catalog coverage and the mutation pin

`TestLanguageNamesCoverCatalog` walked `tts.CommittedLanguages()` and passed. I copied the
repository to `/var/tmp/t75b-r2/repo` and deleted the `es-ES` entry from the map. The test
then failed:

```text
main_test.go:767: resolveLanguageName("es-ES") = "es-ES", want a display name
main_test.go:781: resolveLanguageName("es-ES") = "es-ES", want "Spanish"
--- FAIL: TestLanguageNamesCoverCatalog (0.00s)
```

I restored the entry after the pin.

## Resume pins

A probe ran the real `POST /api/dubs/{id}/run` route with a runner that ran `fit.RunPipeline`.
The translator recorded the line ids it rendered. The four layouts the task names:

```text
RESUME case=flat    route=ml-IN workdir=.../work        rendered=[2 3]
RESUME case=langdir route=ml-IN workdir=.../work/ml-IN  rendered=[2 3]
RESUME case=both    route=ml-IN workdir=.../work/ml-IN  rendered=[2 3]
RESUME case=other   route=ml-IN workdir=.../work/ml-IN  rendered=[1 2 3]
```

A flat record for the run language moves the run to `work/`. The language directory wins when
both hold a record. A flat record for `ta-IN` does not move an `ml-IN` run. The round-one M1
scenario is fixed. Two further layouts expose M1:

```text
RESUME case=sample-then-catalog route=ml-IN workdir=.../work/ml-IN rendered=[1 2 3]
RESUME case=catalog-then-sample route=ml    workdir=.../work/ml    rendered=[1 2 3]
```

The first pass wrote `work/ml` under the sample code. The resume sent `ml-IN` and chose
`work/ml-IN`. It found no record and rendered all three lines. The takes in `work/ml` are
orphaned. The reverse order orphans `work/ml-IN`.

## Other behaviour

The form upload record is unchanged. A real multipart post to `NewUploadHandler` answered
201 and stored:

```text
UPLOAD-RECORD source="en-US" language="ml-IN"
```

The sample record still stores `en` to `ml`. An older record with no `source_language` key
still loads. The workspace route served:

```text
response: {"id":"636f1321c02bbe027a2ab45b949fcc24","title":"stored.mp4","source_language":"en-US",...}
response: {"id":"older-record","title":"Older.mp4","source_language":"",...}
```

The workspace kept the older title. Nothing outside the two fixes changed.

## Command output

```text
$ go test -count=1 ./internal/api ./cmd/...
ok  	github.com/nrynss/ajilamu/internal/api	1.403s
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	4.025s

$ npm --prefix web run check
1788905278298 START "/home/nryn/work/ajilamu/web"
1788905278299 COMPLETED 182 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
```

## Residue against round one

**H1 has open residue.** The create path is fixed. The sample path still names the source
code `en` in the prompt. M2 records it.

**M1 has open residue.** The flat-directory fallback fixes the pre-change record. The sample
code `ml` and the catalog code `ml-IN` still name two directories. A resume across the two
codes re-renders and re-charges every line. M1 records it.

## The handoff entry

The T7.5b handoff entry still claims the prompt names `ml-IN`. It reads: "A new upload stores
`ml-IN`, so the translation prompt names `ml-IN` rather than Malayalam." The remediation fixed
that, so the entry now misdescribes the code. The remediation did not update the entry. The
next agent reads a false claim.

## Scope and evidence

I changed no implementation file. My only write is this review file. The probe copy lives at
`/var/tmp/t75b-r2`. I removed the `es-ES` entry there to pin the coverage test, then restored
it. The two commands are `go test -count=1` and `npm --prefix web run check`.
