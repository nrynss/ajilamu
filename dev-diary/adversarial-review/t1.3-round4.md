# T1.3 Round 4 Review

**Verdict:** REMEDIATE
**Artifact:** sql/schema.sql, 739 lines, untracked at review time.
**Reviewer:** I am fresh, did not implement or remediate T1.3, and differ from the round 3 reviewer.

## Severity Counts

| C | H | M | L |
|---|---|---|---|
| 0 | 4 | 0 | 0 |

## Residue Against Round 3

Round 3 finding 1 has zero residue. Fresh take and charge identifiers left the safe views at 10 takes and 17 charges.

The charge total stayed 0.023414 on both servers. The raw tables held 11 takes and 19 charges before merges.

A whole-pass resend left 17 charges and 0.023414. A 5 plus 12 resend added 17 raw rows without changing either safe-view measurement.

A genuine repeat at attempt 2 raised the view to 18 charges and 0.0234161. This behavior distinguishes a retry from another billed call.

Round 3 finding 2 retains residue outside its four named mutations. Each named mutation now aborted with Code 395 on both servers.

Those mutations changed the take event key, the timeline engine version, the partition key, and one deleted constraint. Every failed run kept all 12 existing objects.

The guard still accepts other incompatible engine parameters, materialized columns, and constraint definitions. Findings 2 through 4 record those remaining paths.

## Findings

| # | Sev | Where | What | Pin | Mutation |
|---|-----|-------|------|-----|----------|
| 1 | H | `sql/schema.sql` lines 177 to 181 and 592 to 600 | Both keys omit `commit_id` from their shared leading fields. The event expressions still treat it as part of the take identity. | Two branch takes and their charges produced four shared-prefix matches totaling 0.006 on both servers. Including `commit_id` produced two matches totaling 0.003. | Move `commit_id` into both leading key prefixes. A regression must require the two-row join and 0.003 total. |
| 2 | H | `sql/schema.sql` lines 309 to 325 | The engine check only asks whether the engine prefix ends in `ingested_at)`. It accepts other argument lists despite the exact-parameter comment. | `ReplacingMergeTree(created_at, attempt)` passed with 0 on both servers. One valid raw take then became zero rows under `FINAL` because `attempt` acted as a deletion flag. | Compare the exact engine argument list. A regression must apply this two-argument mutation and require Code 395. |
| 3 | H | `sql/schema.sql` lines 329 to 335 | The guard searches for each expected constraint as a substring. It also ignores extra constraints. | Appending `OR 1` passed with 0 and accepted ten peaks. Adding `reject_all CHECK 0` passed with 0 and rejected a valid take with Code 469. | Compare exact constraint names and expressions in both directions. Regression mutations must weaken one constraint and add another. |
| 4 | H | `sql/schema.sql` lines 287 to 294 | The guard validates only listed materialized columns. It never rejects a new materialized expression on another declared column. | Making `takes_raw.attempt` materialized passed with 0 on both servers. A normal take insert that supplied `attempt` then failed with Code 44. | Compare `default_kind` and `default_expression` for every column in both directions. This mutation must abort with Code 395. |

## What I Confirmed Working

1. Cloud reported 26.2.1.641. The local binary reported 26.8.2.7.
2. One unmodified file created five tables and seven views on each server. The preflight returned 0 on both empty databases.
3. A second full run returned 0 on both servers. A half run grew from two objects to all 12 after a successful resume.
4. The fixture SQL submitted 6 commits, 10 takes, 22 actions, 17 charges, and 22 snapshot values.
5. Immediate reads returned 6, 10, 22, 17, and 21 from the plain views. The two same-key snapshot values collapsed as designed.
6. Charges grouped to 0.000067 for segmentation, 0.000067 for translation, and 0.02328 for synthesis. The total was 0.023414.
7. Segment 8 returned slot 7110, measured 4200, and signed delta -2910. The fixture peaks returned length 64, first 44, and last 92.
8. The fresh-id retry, whole-pass retry, and re-cut full resend never changed the 17-row charge view or its 0.023414 total.
9. Raising the repeated call to attempt 2 preserved both calls. The charge view increased to 18 rows and 0.0234161.
10. Two unrelated commits, takes, snapshots, actions, and charges survived in every plain read view. `take_rates` also returned both takes.
11. Seven constraint probes failed with Code 469 on both servers. Unknown repair, author, and charge kind values failed with Code 691.
12. An insert into the plain `charges` view failed with Code 48 on both servers.
13. A commit with `created_at` in 2099 created no `209912` partition. Both servers kept the data in `202609`.
14. Five `timeline_at_commit` reads returned 40 rows. An independent action replay found zero field or version mismatches, including the fork.
15. Each required guard mutation failed with Code 395 and left 12 objects unchanged. Fresh, self-built, rerun, and resumed databases returned 0.
16. I parsed all 304 comment lines into 68 paragraphs. I found no semicolon, em dash, sentence over 30 words, or passive construction.

## Verification

I ran every measurement on ClickHouse Cloud and the local ClickHouse binary. I used a separate throwaway database or local path for each independent mutation.

I applied the unmodified file through ClickHouse query-file mode. I built each mutation from that file and changed one property for each required guard check.

I loaded fixture rows derived from `testdata/manifest.json`, `testdata/expected/metrics.json`, and `testdata/wire/take.json`. I queried views immediately after the load.

I sourced the gitignored `.env` inside each Cloud command. The client read its user and password from the environment, and no command printed them.

Local runs used `clickhouse local` under `scratch/t13r4`. Every invocation had a timeout, and files with inline values read from `/dev/null`.

I dropped every Cloud database whose name started with `t13_r4_`. A final query found zero matching databases.

`default.takes` held 8 rows before and after the review. I never wrote to `default`.
