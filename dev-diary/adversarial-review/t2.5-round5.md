# T2.5 Round 5 Review

**Verdict:** APPROVE
**Artifact:** `internal/fit/stretch.go`, 589 lines, and `internal/fit/stretch_test.go`, 1346 lines. Both untracked at review time. The T2.5 handoff entry in `dev-diary/PHASE-2-fit-loop.md` lines 381 to 645 is part of the artifact.
**Reviewer:** I did not implement T2.5. I wrote none of rounds 1, 2, 3, or 4 reviews. I remediated none of rounds 1, 2, 3, or 4.

I re-fired every pin from all four prior rounds myself. I quote no number any prior remediation record states. I quote no number the package printed about its own work.

Every duration below comes from `ffprobe`, from `ffmpeg` run outside Go, from the compiler, or from the filesystem. Every behavioural check ran against a module copy in my scratchpad.

`internal/fit/stretch.go` carried md5 `a5b37c62de49fb9c52bf2fa338f58dd9` at the start and end of this review. `internal/fit/stretch_test.go` carried `c0e362a887b5f157e2066c3a167f7592`. `dev-diary/PHASE-2-fit-loop.md` carried `b7c89c66839e144e3c43474a146ef381`.

All three files are unchanged. I created no production files. I edited no production code. I removed all scratch directories after verification.

## Severity Counts

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 0 |

## Findings

| # | Sev | Where | What | Pin | Mutation |
|---|-----|-------|------|-----|----------|

No findings. The table stays empty.

## Residue Against Round 4

Zero residue across all five findings.

| Round 4 | Status | How I verified it |
|---|---|---|
| 1 (H) Sibling file ratio bypass in production build | **closed** | Moved `stretchRatio`, `renderCandidate`, and `prepareRatio` to `stretch_test.go`. I created a sibling `internal/fit/rewrite.go` in a scratch copy calling all three. Non-test `go build ./internal/fit/` failed with `undefined` for all three symbols. The unexported boundary on `renderStretch` and `applyStretch` is documented in `stretch.go`, under "The bypass", and in the "Next agent" note. |
| 2 (M) Wrapped error caution cites stale measurement | **closed** | The caution in `stretch.go` and the handoff entry cites the extensionless output path. I measured the extensionless output at 2439 bytes across 18 lines on ffmpeg n9.0.1 with build banner on lines 2 and 3. A missing output directory fails at `claimOutput` before ffmpeg starts, returning 126 bytes on 1 line with no banner. |
| 3 (L) 35-word sentence on line 727 of `stretch_test.go` | **closed** | Split into two sentences of 19 words and 16 words. Word-boundary parsing across all comment paragraphs in `stretch_test.go` reports zero sentences exceeding 30 words. |
| 4 (L) Negative `Measured` overflow returns `RepairNone` | **closed** | `PlanStretchWithLimits` rejects `f.Measured < 0`, returning `RepairManual`. Probed `time.Duration(math.MinInt64)` and offset `math.MinInt64 + 1000`. Both return `RepairManual`. `TestPlanStretchRefusesNegativeMeasuredDuration` covers five negative duration variants. |
| 5 (L) Handoff entry omits exported types and constants | **closed** | Lines 383 to 390 in `dev-diary/PHASE-2-fit-loop.md` enumerate five functions, three types, six constants, and thirteen sentinels. Every symbol matches `go doc ./internal/fit` exactly. |

## Residue Against Round 3

Zero residue. All five findings stay closed.

| Round 3 | Status | How I verified it |
|---|---|---|
| 1 (M) Planner routes on `f.Delta` field | **closed** | Derived signed miss is `f.Measured - f.Slot`. Calling `PlanStretch` with `f.Delta: -100ms` on segment 8 still routes to `RepairRewrite`. |
| 2 (M) Ceiling prose claims listening that never occurred | **closed** | `stretch.go` and handoff mark constants as `UNVERIFIED BY EAR` and `REASONED, NOT HEARD`. No ungrounded listening claims remain. |
| 3 (M) Concurrent calls on one output path collide | **closed** | `claimOutput` uses `os.O_CREATE|os.O_EXCL|os.O_WRONLY`. 30 racing trials under `-race` yielded 30 winners and 90 `ErrOutputExists` rejections. Winning takes remained intact. |
| 4 (L) Ceiling reads as bound on take rather than call | **closed** | Documented in doc comments and handoff. The caller owns total correction across retries. |
| 5 (L) Error contract bounds wrapped text | **closed** | Corrected and verified under Round 4 Finding 2. |

## Residue Against Round 2

Zero residue. All five findings stay closed.

| Round 2 | Status | How I verified it |
|---|---|---|
| 1 (M) Ceiling admits repair no listener judged | **closed** | `MaxStretchLimit` is 0.10, matching the listening ladder reach. |
| 2 (M) Ceiling constant unpinned | **closed** | `TestMaxStretchLimitMatchesTheListeningLadder` pins `MaxStretchLimit == 0.10` against a literal. |
| 3 (M) Unclassified failure exits | **closed** | All 16 tested failure exits map to classified sentinels. |
| 4 (L) Unreachable `ErrRatioRange` listed | **closed** | Documented honestly as defence in depth. |
| 5 (L) Handoff misstates `metrics.json` row | **closed** | Handoff correctly distinguishes segment 1's label from segments 2, 5, 6, and 7. |

## Residue Against Round 1

Zero residue. All six findings stay closed.

| Round 1 | Status | How I verified it |
|---|---|---|
| 1 (H) Exported ratio bypass | **closed** | No exported function accepts an atempo ratio. `go doc ./internal/fit` lists no ratio parameter. |
| 2 (M) Dead band couples short and long budgets | **closed** | `deadBand` calls `lim.For(delta)`, keeping directional budgets independent. |
| 3 (M) Landing miss leaves orphaned file | **closed** | `applyStretch` calls `discardOutput` on landing failure. Verified output path is clean. |
| 4 (L) Failure paths lack sentinels | **closed** | 16 failure paths return designated sentinels without naming ffprobe on missing inputs. |
| 5 (L) Assertions measure code against itself | **closed** | `stretchGoldens` uses literal integers for `bandMs` and `landedMs`. |
| 6 (L) Missing handoff entry | **closed** | Handoff entry is tracked and complete in `dev-diary/PHASE-2-fit-loop.md`. |

## Independent Measurements and Pins

A pin is a measurement, never a log line.

**1. Sibling file boundary test.** I wrote a sibling file `internal/fit/rewrite.go` in a scratch module copy. It called `stretchRatio`, `renderCandidate`, and `prepareRatio`. Non-test `go build ./internal/fit/` failed with three `undefined` errors. The test suite compiled and passed.

**2. Error message sizes.** I measured both `ErrRenderFailed` paths outside Go. A missing output directory failed at `claimOutput` in 126 bytes on 1 line. An extensionless path failed inside ffmpeg in 2439 bytes across 18 lines.

**3. Negative duration checks.** I probed `PlanStretch` and `PlanStretchWithLimits` on negative durations. `-1ns`, `-100ms`, `math.MinInt64`, and offset `math.MinInt64 + 1000` all returned `RepairManual`.

**4. Sixteen failure exits.** I walked 16 distinct failure paths from an outside package. All 16 matched their expected sentinels via `errors.Is`. All 16 left the output path empty. A pre-seeded take file remained undisturbed.

```
empty source               ErrPathRequired        file clean
empty output               ErrPathRequired        file clean
output equals source       ErrSameFile            file clean
missing source             ErrSourceMissing       file clean
zero slot                  ErrNoSlot              file clean
negative slot              ErrNoSlot              file clean
symlinked output           ErrSameFile            file clean
existing output            ErrOutputExists        seed preserved
underrun refusal           ErrUnderrunTooLarge    file clean
overrun refusal            ErrOverrunTooLarge     file clean
dead band refusal          ErrDeadBand            file clean
directory as source        ErrSourceUnreadable    file clean
undecodable source         ErrSourceUnreadable    file clean
missing output directory   ErrRenderFailed        file clean
extensionless output       ErrRenderFailed        file clean
unusable limits            ErrLimits              file clean
```

**5. Eight fixture rows.** I probed every take with `ffprobe -show_entries format=duration`.

| seg | ffprobe raw s | take ms | slot ms | delta ms | plan |
|---|---|---|---|---|---|
| 1 | 1.680375 | 1680 | 1820 | -140 | rewrite |
| 2 | 5.320375 | 5320 | 5480 | -160 | atempo |
| 3 | 5.720375 | 5720 | 5320 | +400 | atempo |
| 4 | 5.920375 | 5920 | 5660 | +260 | atempo |
| 5 | 4.720375 | 4720 | 4840 | -120 | atempo |
| 6 | 4.440375 | 4440 | 4630 | -190 | atempo |
| 7 | 6.800375 | 6800 | 7040 | -240 | atempo |
| 8 | 4.200375 | 4200 | 7110 | -2910 | rewrite |

**6. Six landings rebuilt outside Go.** I ran `ffmpeg -filter:a atempo=R` and measured with `ffprobe`.

| seg | ratio | landed ms | slot ms | miss ms | pinned ms |
|---|---|---|---|---|---|
| 2 | 0.9708 | 5469 | 5480 | -11 | 5469 |
| 3 | 1.0752 | 5333 | 5320 | +13 | 5333 |
| 4 | 1.0459 | 5663 | 5660 | +3 | 5663 |
| 5 | 0.9752 | 4832 | 4840 | -8 | 4832 |
| 6 | 0.9590 | 4621 | 4630 | -9 | 4621 |
| 7 | 0.9659 | 7031 | 7040 | -9 | 7031 |

Every landing matched its pinned literal. Every miss sat within 13 ms of its slot, inside the 20 ms ceiling.

**7. Rebuilt goldens.** `seg_3_stretched.wav` measured 5338 ms, matching golden tolerance. `seg_4_stretched.wav` measured 5662 ms, matching golden tolerance.

**8. Listening ladder rebuilt.** I rendered each rung with ffmpeg and measured with ffprobe. All 22 duration and slowed percentage cells matched the handoff table.

| ratio | seg 1 ms | seg 1 slowed | seg 6 ms | seg 6 slowed |
|---|---|---|---|---|
| 0.9900 | 1689 | +0.54% | 4478 | +0.86% |
| 0.9800 | 1706 | +1.55% | 4525 | +1.91% |
| 0.9700 | 1717 | +2.20% | 4563 | +2.77% |
| 0.9600 | 1739 | +3.51% | 4615 | +3.94% |
| 0.9590 | n/a | n/a | 4621 | +4.08% |
| 0.9500 | 1753 | +4.35% | 4657 | +4.89% |
| 0.9400 | 1771 | +5.42% | 4711 | +6.10% |
| 0.9300 | 1788 | +6.43% | 4756 | +7.12% |
| 0.9231 | 1804 | +7.38% | n/a | n/a |
| 0.9200 | 1807 | +7.56% | 4813 | +8.40% |
| 0.9000 | 1845 | +9.82% | 4913 | +10.65% |

**9. Six-step chained stretch.** I chained six 10 percent stretches on segment 8 from 4200 ms to 7105 ms. Every step returned nil. The final artifact replanned `none`, proving the caller must cap cumulative attempts.

## Mutation Testing

I ran 22 mutations against `stretch.go` in a scratch copy. 21 died immediately on the test suite.

| Mutation | Caught | By |
|---|---|---|
| `MaxStretchLimit` 0.10 to 0.408 | yes | `TestStretchRejectsUnusableLimits` |
| `MaxStretchLimit` 0.10 to 0.1005 | yes | `TestMaxStretchLimitMatchesTheListeningLadder` |
| `MaxStretchLimit` 0.10 to 0.11 | yes | `TestStretchRejectsUnusableLimits` |
| `delta` returns `f.Delta` | yes | `TestPlanRoutesOnTheDerivedDeltaAndNeverOnTheField` |
| claim loses `O_EXCL` | yes | `TestConcurrentStretchesGiveOneWinnerAndOneFile` |
| `claimOutput` removed | yes | `TestConcurrentStretchesGiveOneWinnerAndOneFile` |
| `discardOutput` stops removing | yes | `TestStretchRemovesATakeThatLandedOutsideItsSlot` |
| landing check disabled | yes | `TestStretchRemovesATakeThatLandedOutsideItsSlot` |
| `usableLimit` drops ceiling | yes | `TestStretchRejectsUnusableLimits` |
| `StretchDeadBand` 40ms to 200ms | yes | `TestPlanStretchCoversEveryFixtureSegment` |
| `StretchDeadBand` 40ms to 5ms | yes | `TestPlanStretchCoversEveryFixtureSegment` |
| band boundary `<=` to `<` | yes | `TestPlanStretchHonoursDirectionalThresholds` |
| repair limit `>` to `>=` | yes | `TestPlanStretchHonoursDirectionalThresholds` |
| `For` reads Long on exact delta | yes | `TestDeadBandNeverExceedsTheRepairBand` |
| `deadBand` couples both sides | yes | `TestDeadBandNeverExceedsTheRepairBand` |
| `plan.Ratio` inverted | yes | `TestPlanStretchCoversEveryFixtureSegment` |
| `DefaultMaxStretchShort` 0.05 to 0.08 | yes | `TestPlanStretchCoversEveryFixtureSegment` |
| `DefaultMaxStretchLong` 0.08 to 0.04 | yes | `TestPlanStretchCoversEveryFixtureSegment` |
| `MinAtempoRatio` 0.5 to 0.1 | yes | `TestStretchRatioRejectsRatiosOutsideTheAtempoRange` |
| `MaxAtempoRatio` 2.0 to 8.0 | yes | `TestStretchRatioRejectsRatiosOutsideTheAtempoRange` |
| `PlanStretchWithLimits` drops negative check | yes | `TestPlanStretchRefusesNegativeMeasuredDuration` |
| landing check reads `StretchDeadBand` | **no** | equivalent mutant on exported surface |

The single survivor is the equivalent mutant analyzed in Rounds 3 and 4. On the exported surface, every reachable atempo plan has a band of 40 ms.

## Verification Commands

All required verification suites passed cleanly.

- `go test -count=1 ./internal/fit/`: passed in 9.81 s.
- `go test -count=1 -race ./internal/fit/`: passed in 11.11 s.
- `go vet ./internal/fit/`: clean, exits 0.
- `gofmt -l internal/fit/`: clean, prints nothing.
- `unshare -rn go test -count=1 ./internal/fit/`: passed in 9.89 s.
- `go test -count=1 ./...`: passed across all eleven packages.
- Cross-device temporary directory test: passed in 9.85 s.

## House Style Audit

I checked `stretch.go`, `stretch_test.go`, the T2.5 handoff entry, and this review file against the four house rules.

1. No semicolons in comments, markdown prose, or review text.
2. No em dashes or en dashes.
3. Every sentence runs 30 words or fewer.
4. Active voice throughout.

## Verdict Restated

APPROVE. Zero findings across all severities. All prior findings show zero residue.
