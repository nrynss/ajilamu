# T5.7 Round 3 Review

## Verdict

APPROVE

| Severity | Count |
|---|---:|
| C | 0 |
| H | 0 |
| M | 0 |
| L | 0 |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| None | No findings remain. | Independent Chrome puppeteer measurements and type check. | No remediation is required. |

## Independent evidence

| Check | Independent observation |
|---|---|
| Loading panel state | Requesting `/d/fixture?panel=loading` returns `data-state="loading"` with sentence "We are loading this workspace from the offline project fixture." and skips workspace mounting. |
| Populated panel state | Default load `/d/fixture` computes initial state synchronously and renders `data-state="populated"` on first paint. |
| Selected row viewport reveal | Pressing `]` seven times to select line 8 adjusts `.length-list.scrollTop` to 331px. Line 8 sits inside the length list viewport while `.picture-area` `scrollTop` stays 0. |
| Picture area scroll stability | `.picture-area` `scrollTop` remained 0 across initial paint, rail line selection, and keyboard stepping to line 8. |
| Flagged lines block | The block heading reads "Lines needing you". The flag sentence explicitly names line 8: "Line 8 is still 2.91 seconds too short after 1 try, this one needs you." |
| Picture duration and scrubber | Scrubber carries `max="75.008"`. Picture duration helper returns 75008ms. |
| Language audio disclosure | The workspace renders "The picture plays its original audio. Play discrete takes from the length checks or Lines rail." Clicking Play take fetches and plays `seg_1_try1.wav`. |
| Rail language alignment | Rail derives from active language. Selected language German matches length bar duration sentences and details tab text word for word. |
| Command bar speech | Submitting `move wav 1 to 0:0005 to right` renders a `role="status"` paragraph reading "This workspace cannot run editor commands yet." |
| Heading hierarchy | Route `h1` reads "NASA 75-second clip" and `document.title` reads "NASA 75-second clip · Ajilamu". Selected line heading uses an `h2` tag. |
| Type check | `npm run check` in `web/` reported 0 errors and 0 warnings. |

## Residue

Round-2 F1 has zero residue. The `?panel=loading` query parameter exposes the loading state cleanly while the default route path loads into populated state.

Round-2 F2 has zero residue. The `revealSelectedLengthRow` helper adjusts `.length-list.scrollTop` on selection so line 8 stays visible. `.picture-area` `scrollTop` remains 0 throughout.

Round-1 F1 has zero residue. The flagged-lines block names line 8 and sits in a separate labelled container above length checks.

Round-1 F2 has zero residue. Selection does not scroll `.picture-area`.

Round-1 F3 has zero residue. Picture duration measures 75008ms across preview, scrubber, timeline, and ruler.

Round-1 F4 has zero residue inside owned files. The playback disclosure sentence clarifies audio sources. The leftover remains the named T5.2 contract change for a per-language mixed audio track.

Round-1 F5 has zero residue. The rail filters by active language and agrees with length bar measurements.

Round-1 F6 has zero residue. No fake processing banner renders.

Round-1 F7 has zero residue. Command submission speaks a status sentence via `role="status"`.

Round-1 F8 residue remains the named T5.3 contract change. `Timeline.svelte` still lacks selection and playhead inputs, which sits outside T5.7 owned files.

Round-1 F9 has zero residue. Project title serves as the route `h1` and line selections use `h2`.

Round-1 F10 has zero residue. The loading state is now reachable as re-verified in Round-2 F1.

This fresh review found zero findings across all severities.

## Scope

I own only this review file. I edited no implementation file, no fixture, and no task board entry. I fixed nothing.
