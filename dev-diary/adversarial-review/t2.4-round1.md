# T2.4 Round 1 Review

**Verdict:** APPROVE
**Commit / state:** uncommitted `internal/fit/measure.go` and `internal/fit/measure_test.go` at HEAD 51ce283e7b413dc38c6fd37c1ea978f1f260bf75

## Severity Counts

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 0 | 0 |

## Findings

| # | Sev | Where | What | Pin | Mutation |
|---|-----|-------|------|-----|----------|
| None. Every required pin holds under independent measurement. The probes table records each check. | | | | | |

## Probes

| Probe | Judgement | Pin |
|-------|-----------|-----|
| Independent ffprobe of the 8 try1 WAVs | Intact. Every row matches `testdata/expected/metrics.json`. Segment 8 is -2910 ms. | I ran `ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1 testdata/takes/seg_N_try1.wav` for N=1 through 8. I rounded with the `media.Duration` rule `math.Round(sec * 1000)`. Signed delta is rounded take minus slot. Seg 1: 1.680375 s → 1680 ms, slot 1820, delta -140. Seg 2: 5.320375 → 5320, slot 5480, delta -160. Seg 3: 5.720375 → 5720, slot 5320, delta 400. Seg 4: 5.920375 → 5920, slot 5660, delta 260. Seg 5: 4.720375 → 4720, slot 4840, delta -120. Seg 6: 4.440375 → 4440, slot 4630, delta -190. Seg 7: 6.800375 → 6800, slot 7040, delta -240. Seg 8: 4.200375 → 4200, slot 7110, delta 4200-7110 = -2910. I did not use the implementer table as a pin. Slots also match `testdata/segments.json` `duration_ms` (seg 8 is 54491-47381 = 7110). |
| Stretched fixtures (extra) | Intact. Not required for T2.4 done-when. They still match the golden repaired durations. | `seg_3_stretched.wav` ffprobe 5.337563 s → 5338 ms. `seg_4_stretched.wav` ffprobe 5.662250 s → 5662 ms. |
| Measure source wiring | Intact. Duration goes through `internal/media`. Signed delta goes through `types.NewFit`. No local delta math. No hardcoded 4200 or -2910. | Read of `internal/fit/measure.go`. Line 19 is `measured, err := media.Duration(path)`. Line 23 is `return types.NewFit(slot, measured), nil`. `rg` on that file for `4200`, `2910`, `measured - slot`, and `slot - measured` is empty. Empty path returns a zero `Fit` and an error before the probe. |
| Tests open the WAV files | Intact. The suite probes each try1 fixture. It is not a `NewFit`-only literal test. | `takesPath` stats `../../testdata/takes/<file>`. `TestMeasureTry1Takes` calls `Measure(path, slot)` for all 8 try1 names. `TestMeasureSegment8Underrun` calls `Measure` on `seg_8_try1.wav`. A PATH wrapper around `/usr/bin/ffprobe` logged every child. Each try1 WAV appears. Seg 8 appears three times. Stretched takes appear. `TestMeasureProbesRenderedWAV` probes a temp `tone.wav`. Independent synth of that same `aevalsrc` line is 0.250000 s, which rounds to 250 ms. |
| `go test`, race, vet, format | Intact. | `go test ./internal/fit/ -count=1` passed. `-race -count=1` passed. `go vet ./internal/fit/` printed nothing. `gofmt -l internal/fit/measure.go internal/fit/measure_test.go` printed nothing. I did not treat test log `take_ms` lines as the duration pin. |
| Owns | Intact. Only the two owned files sit under `internal/fit/`. | `git ls-files --others --exclude-standard -- internal/fit/` lists `internal/fit/measure.go` and `internal/fit/measure_test.go`. `ls internal/fit/` lists those two names only. I ignored unrelated dirty paths (`internal/tts/`, `go.mod`, `PHASE-2-fit-loop.md`, `internal/assemble/bed_test.go`, `dev-diary/README.md`). |
| Docs style on comments | Intact. | Package comment is 9 words. The three `Measure` comment lines are 12, 15, and 17 words. No comment uses a semicolon or an em dash. Voice is active. The signed-delta sentence matches `types.NewFit` (`measured - slot`). |

## Live Evidence

N/A. This task measures local fixtures. I did not call Gemini or Chirp.

No false positives this round. I considered the duplicated golden literals in `measure_test.go`. That is not a defect. The tests still open each WAV through `Measure`. Independent ffprobe matches both that table and `metrics.json`.
