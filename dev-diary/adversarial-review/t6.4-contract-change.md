# T6.4 Contract Change Proposal

## Status

T6.4 can implement its parser and reusable command bar inside the listed paths. It cannot mount
that component in the workspace or retain parser regression coverage without editing unowned paths.

## Verified Assertions

| Assertion | Result | Evidence |
|---|---|---|
| I own every implementation path. | Pass | T6.4 owns only `internal/command/parse.go` and `web/src/lib/edit/CommandBar.svelte`. |
| The needed data shapes exist. | Pass | T1.4 exposes timeline segments with line IDs, bounds, and speakers. |
| I can validate parser behavior durably. | Fail | The task owns no `internal/command/parse_test.go` path. |
| I can place the command input above the timeline. | Fail | The existing form lives in `web/src/routes/d/[id]/+page.svelte`, which remains outside T6.4 ownership. |

## Required Contract Change

Expand T6.4 ownership, or add a narrow follow-up task, for these paths:

- `internal/command/parse_test.go`
- `web/src/routes/d/[id]/+page.svelte`

The route replaces its placeholder form with `CommandBar`. It supplies a server-backed parse
callback and applies only a creator-confirmed result. The test file covers each documented command,
unknown lines, unknown speakers, malformed values, out-of-bounds arithmetic, and collisions.

## Boundary

This record does not alter the route, an API handler, or any test path. The parser package exposes
`Parse`, `Validate`, `Describe`, and `Apply` so the later owner can connect the route without
duplicating timeline arithmetic in Svelte.
