# T6.5 remediation, round 1

I did not change the round 1 verdict. I fixed findings M1 and M2 and nothing else.

I edited `web/src/lib/Track.svelte`, `web/src/lib/edit/Speaker.svelte` and
`web/src/routes/d/[id]/+page.svelte`. I did not commit. The H finding stays with T6.5c.

| Finding | Fix | Pin | Residue |
|---|---|---|---|
| **M1.** The ghost strip sat above the `Timeline` component, so the timeline lanes carried no ghost shape. `Track.svelte` rendered only `takes.at(-1)`. | `Track.svelte` paints every older take as a dashed ghost shape inside the lane. Each shape carries `data-take-state="ghost"`. The active take keeps the solid fill, `data-take-state="active"` and the only audio control. | The headless Chromium run reported `insideLane: true` for every line 3 shape. The lane rect ran top 932.9 to bottom 996.9. The two ghost rects ran 942.9 to 986.9 and 945.9 to 983.9. | None. The page strip stays as the keyboard and playback control. |
| **M2.** After a re-render landed, `Speaker.svelte` still said the take was unchanged while the page strip named the new take. | `Speaker.svelte` awaits the `onchange` reply and drives its note from that reply. The page handler returns the route reply and still sets the strip note. | The headless Chromium run read `.speaker-editor .note` as `Line 3 re-rendered as take seg_3_try3.wav.` The `.take-note` read the same sentence. | None. |

## Finding M1 measurements

I built the working tree with `npm --prefix web run build`. A throwaway Node server served
`web/build` and answered the rerender POST. I drove the page with a headless Chromium tab.
The page used the fixture payload under `/d/fixture`. The measurement called
`getBoundingClientRect` and `getComputedStyle` in the page.

### 1. The ghost shape sits inside the lane

Line 3 held two takes before the re-render. The ghost shape and the active shape both sat
inside the `Dub ML` lane. The lane itself sat inside the `.timeline` element, which ran top
783.1 to bottom 1039.1.

```json
{
  "lane": { "top": 923.1, "bottom": 987.1 },
  "timeline": { "top": 783.1, "bottom": 1039.1 },
  "shapes": [
    { "file": "seg_3_try1.wav", "state": "ghost", "attempt": "1",
      "top": 936.1, "bottom": 974.1, "insideLane": true,
      "borderStyle": "dashed", "background": "rgba(0, 0, 0, 0)",
      "opacity": "0.72", "zIndex": "1" },
    { "file": "seg_3_stretched.wav", "state": "active", "attempt": "2",
      "top": 939.1, "bottom": 971.1, "insideLane": true,
      "borderStyle": "solid", "background": "rgb(180, 115, 106)",
      "opacity": "1", "zIndex": "2" }
  ]
}
```

The old pin was `stripTop` 811.6 against `timelineTop` 844 with `stripInsideTimeline` false.
The new pin is `insideLane: true` for the ghost shape. The ghost top 936.1 is below the lane
top 923.1 and its bottom 974.1 is above the lane bottom 987.1.

### 2. A line with three takes

The speaker re-render appended `seg_3_try3.wav`. Line 3 then held three lane shapes. Two
were ghosts and one was active. All three sat inside the lane.

```json
{
  "lane": { "top": 932.9, "bottom": 996.9 },
  "shapes": [
    { "file": "seg_3_try1.wav", "state": "ghost", "attempt": "1",
      "top": 942.9, "bottom": 986.9, "insideLane": true,
      "borderStyle": "dashed", "background": "rgba(0, 0, 0, 0)", "opacity": "0.72" },
    { "file": "seg_3_stretched.wav", "state": "ghost", "attempt": "2",
      "top": 945.9, "bottom": 983.9, "insideLane": true,
      "borderStyle": "dashed", "background": "rgba(0, 0, 0, 0)", "opacity": "0.72" },
    { "file": "seg_3_try3.wav", "state": "active", "attempt": "3",
      "top": 948.9, "bottom": 980.9, "insideLane": true,
      "borderStyle": "solid", "background": "rgb(143, 165, 140)", "opacity": "1" }
  ]
}
```

The strip chips then read ghost, ghost, active. Each chip was a `BUTTON`. The first chip took
focus. So keyboard access to take playback holds.

## Finding M2 measurements

### 1. The Speaker note after a re-render

The run picked the other speaker for line 3, confirmed the change and waited for the 201. The
Speaker note and the page strip note then named the new take.

```json
{
  "speakerNote": "Line 3 re-rendered as take seg_3_try3.wav.",
  "takeNote": "Line 3 re-rendered as take seg_3_try3.wav.",
  "speakerError": null
}
```

The old note read `The take is unchanged, so a re-render is still pending.` The new note does
not say that. The two panels agree.

### 2. Validation

```text
$ npm --prefix web run check
1788911136930 START "/home/nryn/work/ajilamu/web"
1788911136932 COMPLETED 183 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
```

The browser run raised 0 console messages and 0 page errors. The run listened for `console`
and `pageerror` events from the first navigation.

## Residue

I claim no residue from M1 and M2. I left the H finding untouched for T6.5c. The run button
still shows no estimate, which belongs to the run task.
