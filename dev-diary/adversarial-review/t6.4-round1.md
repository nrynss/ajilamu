# T6.4 Round 1 Review

## Verdict

REMEDIATE

| Severity | Count |
|---|---:|
| C | 1 |
| H | 1 |
| M | 1 |
| L | 0 |

## Independent Evidence

| Check | Observation |
|---|---|
| Reachable workspace | A fresh browser visit to `/d/fixture` exposes the existing `EDITOR AI COMMAND` field. It exposes no Preview, Parsed intent, Confirm change, or Revise control from `CommandBar`. Submitting `move wav 1 to 0:0005 to right.` reports `This workspace cannot run editor commands yet.` |
| Parser artifact | `go test ./internal/command` compiles the package. It reports `[no test files]`. The repository contains no `internal/command/parse_test.go`. |
| Component build | `npm run check` produced zero errors and warnings. `npm run build` completed. The new component uses Svelte 5 runes. |
| Parser semantics | The parser accepts the four documented grammars. Its `0:0005` path treats the padded seconds field as 5,000ms. `Validate` checks line references, unique speaker prefixes, bounds, positive durations, and overlap before `Apply` copies the timeline. |
| Accessible feedback | `CommandBar` gives failures an alert. Its dynamically inserted Parsed intent section has no `aria-live` attribute or status role. Focus remains on the Preview control after an asynchronous parse result arrives. |

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| `web/src/routes/d/[id]/+page.svelte`, integration seam for `web/src/lib/edit/CommandBar.svelte` | The shipped workspace does not import or render `CommandBar`. The old placeholder submits every command to a fixed unavailable message. A creator cannot obtain a parsed preview or confirm an edit. This fails the command-bar task in the actual demo. The recorded contract change correctly identifies the unowned route. Remediation needs the ownership expansion or narrow follow-up it proposes. | Browser probe on `/d/fixture` found no Preview, Parsed intent, Confirm change, or Revise control. Entering the documented move command produced `This workspace cannot run editor commands yet.` | Reverting the route integration leaves the parser and confirmation UI unreachable. No creator can use a T6.4 edit command. |
| `internal/command/parse.go`, missing `internal/command/parse_test.go` | The parser has no durable regression coverage. The task requires all documented commands to become valid mutations. It also promises clear rejection for bad references and unsafe values. The contract record identifies the missing test ownership, but the absence remains a defect until an owned test path lands. | `go test ./internal/command` reported `[no test files]`. The untracked-file inventory has `internal/command/parse.go` and no parser test. | Reverting or changing a time grammar, speaker resolution, bounds check, or collision check can silently break command validation. |
| `web/src/lib/edit/CommandBar.svelte:125-135` | Successful asynchronous validation inserts the Parsed intent preview without a live region or status role. A screen-reader user who activates Preview is not reliably told that the confirmation step appeared. This misses accessible feedback for the core confirmation path. | The rendered Svelte template assigns `role="alert"` only to errors. The Parsed intent section has neither `role="status"` nor `aria-live`. | Reverting an announcement fix leaves nonvisual creators without reliable notice of the parsed intent or confirmation controls. |

## Residue

This is T6.4 round 1. No prior T6.4 review findings exist. The contract-change record predates this review. It describes the same route and test ownership gap. It does not close either finding.

## Scope

This review examined the parser, reusable component, contract-change record, and reachable workspace. It did not modify source or the task status.
