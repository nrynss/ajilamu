# T2.6 Round 1 Review

**Verdict:** REMEDIATE
**Artifact:** `internal/fit/rewrite.go`, 482 lines, and `internal/fit/rewrite_test.go`, 995 lines. Both untracked at review time. The T2.6 handoff entry in `dev-diary/PHASE-2-fit-loop.md` lines 645 to 685 is part of the artifact.
**Reviewer:** I did not implement T2.6. I have written no prior round for it.

I verified every behavior, measurement, and compiler constraint myself.
I quote no number any prior log printed about its own work.
Every measurement below comes from independent inspection, compiler errors, or test runs.
Every behavioural check ran against a module copy in my scratchpad.

`internal/fit/rewrite.go` carried md5 `ce939b9d827e689959c16ce03696b1e3` at the start and end of this review.
`internal/fit/rewrite_test.go` carried `f950c07a4b97192f7d5db838ee9075e3`.
`dev-diary/PHASE-2-fit-loop.md` carried `478c8d75fe4879251c68a7f3d133b517`.

All three files remain unchanged.
I created no production files.
I edited no production code.
I removed all scratch directories after verification.

## Severity Counts

| C | H | M | L |
|---|---|---|---|
| 0 | 0 | 2 | 4 |

## Findings

| # | Sev | Where | What | Pin | Mutation |
|---|-----|-------|------|-----|----------|
| 1 | M | `internal/fit/rewrite.go` line 358 and `internal/fit/rewrite_test.go` lines 838 to 854 | `RepairLine` refusal of already stretched source takes is unpinned in the test suite. `TestRefuseStretchAlreadyStretched` asserts string matching in `isStretchedTake` on two static path literals. It never calls `RepairLine` with an already stretched initial take. Mutating line 358 to disable the check leaves all 17 tests passing. | I mutated line 358 to `if false && isStretchedTake(audioPath)` in a scratch module copy. `go test -count=1 ./internal/fit/` passed with zero failures. I then added a test passing `InitialTake` with filename `seg_1_try1_stretched.wav` and a 5 percent overrun. The unmutated code passed, but the mutated code failed with `--- FAIL: TestRepairLineRefusesStretchedInitialTake`. | Revert the guard and `RepairLine` accepts an already stretched take from a prior run or initial take. It stretches the take a second time, violating the non-chaining contract established in T2.5. |
| 2 | M | `internal/fit/rewrite.go` lines 122 to 124, lines 240 to 242, and lines 179 to 180 | Silent substitution of invalid `StretchLimits` conceals configuration errors. `NewRepairer` and `RepairLine` replace any invalid limits with `DefaultStretchLimits()`. Defaulting an unconfigured zero value `StretchLimits{}` is sensible, but this check also replaces explicitly invalid limits with defaults. In addition, `cleanStretchErrorMessage` maps `ErrLimits` to user copy, but `RepairLine` can never return `ErrLimits`. | I called `NewRepairer(RewriteConfig{Limits: StretchLimits{Short: 0.25, Long: 0.25}})` in a scratch test. The call returned a nil error, and `Repairer.cfg.Limits` held `{Short: 0.05, Long: 0.08}`. I also called `RepairLine` with the same configuration. It executed cleanly without returning `ErrLimits`. | Revert the fix and a caller that requests an illegal 25 percent stretch receives no error. The system silently overrides the caller explicit configuration with default limits. |
| 3 | L | `internal/fit/rewrite.go` lines 373 to 386 and `internal/fit/rewrite_test.go` lines 856 to 866 | Redundant unreachable check and unpinned test for `ErrTotalStretchExceeded`. In `RepairLine`, `math.Abs(plan.Ratio-1.0) > MaxStretchLimit+1e-9` is unreachable. `PlanStretchWithLimits` returns `RepairAtempo` only when relative miss is at or below `limit`, and `limit` cannot exceed `MaxStretchLimit`. `TestCapTotalStretchLimit` only tests a float inequality without invoking `RepairLine`. Mutating the check away leaves all tests passing. | I mutated line 373 to `if false && math.Abs(plan.Ratio-1.0) > MaxStretchLimit+1e-9` in a scratch copy. `go test -count=1 ./internal/fit/` passed with zero errors. Furthermore, for every positive slot and measured duration, `PlanStretchWithLimits` returns `RepairRewrite` whenever relative miss exceeds `limit`. | Revert the fix and dead code remains in `RepairLine`. The test continues to assert a float inequality rather than verifying package behavior. |
| 4 | L | `dev-diary/PHASE-2-fit-loop.md` line 672 | Handoff log entry misdescribes behavior. Line 672 claims: "It caps cumulative stretch ratio against `MaxStretchLimit`." No cumulative stretch ratio tracking exists in `RepairLine`. Each retry synthesizes fresh audio from Gemini translation and TTS rather than accumulating stretch across attempts. | Grepping `cumulative` in `internal/fit/rewrite.go` matches only the error text of `ErrTotalStretchExceeded`. No variable, struct field, or calculation in `rewrite.go` tracks cumulative stretch ratios across attempts. | Revert the fix and the next agent codes against an imaginary cumulative tracker. T2.7 assumes `RepairLine` tracks cumulative ratio across retries when it does not. |
| 5 | L | `dev-diary/PHASE-2-fit-loop.md` lines 662, 666, and 667 | Passive voice in handoff log entry. Line 662 uses "The direction is re-evaluated after every attempt". Line 666 uses "If three attempts fail to fit, the line is flagged." Line 667 uses "`Flagged` is set to true." | Searching for passive constructions in lines 645 to 685 of `dev-diary/PHASE-2-fit-loop.md` locates passive verbs on lines 662, 666, and 667. | Revert the fix and documentation style rule 4 remains broken in the handoff log. |
| 6 | L | `internal/fit/rewrite.go` lines 96 and 105 | `SignedDeltas()` and `SignedDeltaMs()` read `a.Fit.Delta` rather than deriving `a.Fit.Measured - a.Fit.Slot`. All other logic in `rewrite.go` derives the signed miss directly. Struct literals with omitted or conflicting `Delta` fields yield wrong values. | I constructed a `LineResult` with a `LineAttempt` holding `Fit: types.Fit{Slot: 5000ms, Measured: 5500ms, Delta: 0}`. `SignedDeltas()` returned `0s` and `SignedDeltaMs()` returned `0`. Deriving `Measured - Slot` yields `+500ms` and `500`. | Revert the fix and a caller or test using struct literals with an omitted `Delta` field gets zero deltas despite a real timing misfit. |

## Independent Measurements and Pins

A pin is a measurement, never a log line.

**1. Sibling package boundary verification.**
I verified that `internal/fit/rewrite.go` never calls unexported `applyStretch` or `renderStretch`.
All audio time stretching calls invoke `StretchWithLimits`.
In a scratch copy, I added a call to `applyStretch` in `rewrite.go`.
Non-test `go build ./internal/fit/` failed with `undefined: applyStretch`.

**2. Independent delta derivation.**
`RepairLine` derives `miss := prevFit.Measured - prevFit.Slot` for rewrite mode selection.
It derives `delta := f.Measured - f.Slot` for repair details.
It derives `minDelta := absDuration(a.Fit.Measured - a.Fit.Slot)` for closest take selection.
It never trusts `f.Delta` for routing decisions.
However, `SignedDeltas()` and `SignedDeltaMs()` read `a.Fit.Delta`.
Probing with an empty delta struct confirmed the discrepancy recorded in Finding 6.

**3. Distinct take naming prevents ErrOutputExists.**
`DefaultTakeName` generates `seg_%d_try%d.wav` for raw takes and `seg_%d_try%d_stretched.wav` for stretched takes.
I verified that across three attempts, all six generated paths are mutually distinct.
No attempt collides with another attempt.
`StretchWithLimits` claims paths exclusively and never encounters `ErrOutputExists`.

**4. Notification copy bounds and banners.**
I examined all error mapping branches in `cleanStretchErrorMessage`.
Sentinel errors like `ErrRenderFailed` and `ErrSourceUnreadable` map to clean bounded sentences.
I confirmed that raw ffmpeg and ffprobe banners never enter `NotificationCopy`.
Every sentence in `formatSuccessCopy` and `formatFlaggedCopy` stays under 30 words.
Each sentence uses active voice.

## Mutation Testing

I executed 12 targeted mutations against `internal/fit/rewrite.go` in a scratch module copy.
10 mutations died immediately on the test suite.
2 mutations survived, exposing the gaps recorded in Findings 1 and 3.

| Mutation | Caught | By |
|---|---|---|
| Mode selection inverted | yes | `TestOverrunRewriteAttempt2Fits`, `TestUnderrunRewriteAttempt2Fits`, `TestSegment8NumbersUnderrunTriggersFuller` |
| Furthest take selected instead of closest | yes | `TestClosestTakeSelection`, `TestAllThreeAttemptsFailing` |
| Line flagging omitted on failure | yes | `TestClosestTakeSelection`, `TestAllThreeAttemptsFailing` |
| Re-measurement omitted after synthesis | yes | 13 test failures across `rewrite_test.go` |
| Attempts capped at 1 | yes | `TestOverrunRewriteAttempt2Fits`, `TestAllThreeAttemptsFailing` |
| Initial take configuration ignored | yes | `TestInitialTakeSupport` |
| Static take filename used across attempts | yes | `TestDistinctTakeFilenames` |
| Translation error propagation dropped | yes | `TestErrorHandlingTranslator` |
| Synthesis error propagation dropped | yes | `TestErrorHandlingSynthesizer` |
| Render error propagation dropped | yes | `TestErrorHandlingStretch` |
| Stretched source take guard disabled | **no** | Survives. `TestRefuseStretchAlreadyStretched` only tests string helper. See Finding 1. |
| Total stretch ratio check disabled | **no** | Survives. Unreachable branch in `RepairLine`. See Finding 3. |

## Verification Suites

All required verification suites passed cleanly.

- `go test -count=1 ./internal/fit/`: passed in 10.59 s.
- `go test -count=1 -race ./internal/fit/`: passed in 11.73 s.
- `go vet ./internal/fit/`: clean, exits 0.
- `gofmt -l internal/fit/`: clean, prints nothing.
- `go test -count=1 ./...`: passed across all eleven packages.

## House Style Audit

I checked `internal/fit/rewrite.go`, `internal/fit/rewrite_test.go`, the T2.6 handoff log entry in `dev-diary/PHASE-2-fit-loop.md`, and this review file against the four house rules.

1. No semicolons in comments, markdown prose, or review text.
2. No em dashes.
3. Every sentence runs 30 words or fewer.
4. Active voice throughout.

Three sentences in the T2.6 handoff entry use passive voice.
Line 672 in the handoff entry also misdescribes cumulative stretch behavior.
Both issues are recorded in Findings 4 and 5.

## Verdict Restated

REMEDIATE.
Six findings: 0 Critical, 0 High, 2 Medium, 4 Low.
The implementation establishes sound line repair mechanisms.
Remediation must address the unpinned stretched take guard, invalid limit handling, and documentation defects.
