# T4.3 remediation, round 5

| # | Severity | What I changed | Measurement proving it closed |
| --- | --- | --- | --- |
| 1 | H | Serialized identity lookup, parent validation, durable enqueue, and initial delivery for each client and immutable `(dub_id, commit_id)`. The keyed gate deletes idle locks and does not serialize unrelated commits. Added a concurrent HTTP artifact test with conflicting payloads. | `TestAppendCommitSerializesConcurrentDuplicateIdentity` holds the first identity lookup while both callers start. It then observes exactly one `commits_raw` insert, one successful caller, and one `already exists` rejection. The accepted insert contains one supplied payload. |
