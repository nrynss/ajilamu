# T4.7 adversarial review, round 2

Verdict: APPROVE

Counts: C 0, H 0, M 0, L 0.

## Scope and evidence

I reviewed the T4.7 implementation, its round-one finding, its remediation, and the frozen
`take_rates` schema contract. The read targets `take_rates`, which applies `FINAL` to
`takes_raw` and excludes zero measured durations. Therefore both returned counts measure
deduplicated valid rates.

`TestDurationPriorCapture` uses a local HTTP server to inspect each actual ClickHouse request.
It pins POST, the configured database, bound owner, language, and speaker parameters. It also
pins the `take_rates` read, all three typed placeholders, and both count aliases. An owner value
is absent from the SQL statement, which rejects interpolation.

The controlled artifacts measure cold-start fallback at population rate 2 with 30 population
samples and zero creator samples. They measure ten creator samples at rate 4, yielding weight
0.5 and blended rate 3. They then measure 100 creator samples, whose weight and contribution
increase without exceeding the creator rate. The capture rejects malformed JSON, an empty
response, zero population samples, and an HTTP 500 response.

Independent checks passed:

- `gofmt -d internal/ledger/priors.go internal/ledger/priors_test.go`
- `git diff --check -- internal/ledger/priors.go internal/ledger/priors_test.go dev-diary/adversarial-review/t4.7-round1.md dev-diary/adversarial-review/t4.7-remediation-round1.md`
- `go test ./internal/ledger -run '^TestDurationPriorCapture$' -count=1`
- `go test -race ./internal/ledger -run '^TestDurationPriorCapture$' -count=1`
- `go vet ./internal/ledger`

## Findings

None.

## Residue

Round one finding 1 has zero residue. The committed capture now independently pins the request
shape, both sample counts, cold fallback, increasing creator influence, and error handling. No
remaining T4.7 defect was found at any severity.
