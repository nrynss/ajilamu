# T1.3 Remediation, Round 1

**Verdict:** Unchanged. I do not set verdicts. The round 1 review recorded REMEDIATE with 1 C,
3 H, 2 M and 1 L. This record reports what closed each finding.

**Inherited state.** I did not implement T1.3 and did not write the round 1 review. A prior
remediation agent died mid-flight from an API rate limit. It left `sql/schema.sql` rewritten
from 211 lines to 405 lines and wrote no record of what it changed or why. The 211 line file
the reviewer measured no longer exists on disk, and nothing in the 405 line draft was
verified by anyone before I started.

I treated every line of that draft as an unproven claim. I rebuilt the review's own repro
steps from scratch, added new probes where the review did not go far enough, and ran all of
it myself on both servers before writing a single row below. Where the draft already
contained the right fix, this record says so and shows the measurement. I made exactly one
edit to `sql/schema.sql`, described at the end, and it is not one of the seven findings.

## Servers

- ClickHouse Cloud 26.2.1.641, confirmed by `SELECT version()`. All Cloud work ran inside my
  own throwaway databases, `t13_remediation_r2` and `t13_remediation_r2_legacy`, both dropped
  when I finished. I never wrote to `default`. `default.takes` held 8 rows before I started
  and holds 8 rows now, same legacy shape throughout.
- Local ClickHouse 26.8.2.7, confirmed by `clickhouse --version`, run as `clickhouse local`
  against scratch paths under `scratch/t13rem2/`, gitignored.

I sourced the gitignored `.env` inside each command and never printed, echoed, or wrote the
password. Commands below are described rather than pasted where they would carry it.

## Findings

| # | Sev | What I changed | Measurement proving it closed |
|---|-----|-----------------|-------------------------------|
| 1 | C | Nothing. The draft's preflight `throwIf` guard already reads `system.columns` and aborts before any `CREATE TABLE` runs against an incompatible existing table. I verified it fires rather than trusting the comment above it. | I rebuilt the legacy shape (`take_id UUID`, `overrun_pct Float32`, `cost_usd Float64`, 8 rows, no `commit_id`) in a throwaway database on both servers, then ran `sql/schema.sql` unmodified over it. Local exited non-zero with `FUNCTION_THROW_IF_VALUE_IS_NON_ZERO` naming the exact mismatch. Cloud raised the same exception, `Code: 395`. On both servers the legacy table still held 8 rows afterward with no `commit_id`, `delta_ms`, or `peaks` column added. Against a fresh empty database, and against a database this file already created, the same guard passed with exit 0 on both servers. It does not false-positive on the good case. |
| 2 | H | Nothing. The draft already moved every table to `ReplacingMergeTree` with a deterministic id closing the sort key, and added `_deduped` views that apply `FINAL`. I verified the read path is correct without waiting for a background merge, and that a genuinely different event does not collapse. | I built a 17 row charges batch from `testdata/segments.json`, one segmentation call plus one translate and one synthesize per segment. Charge ids came from hashing each event's own content. I inserted the batch, then re-sent the byte-identical copy. Locally the raw table held 34 rows over 17 distinct ids, while `charges_deduped` and `SELECT ... FINAL` both held 17 rows and the correct summed cost of 0.020287, with no merge forced. On Cloud the server's own insert-level block deduplication rejected the identical resend outright. `charges_deduped` again read 18 rows, 17 plus one earlier charge, summing to 0.023287, matching the local math exactly. Separately, I inserted two rows sharing `take_id` and `kind` but carrying distinct charge ids, representing two genuinely different synthesis attempts. Both rows survived in `charges_deduped`, so the dedup key does not swallow a real second event. |
| 3 | H | Nothing. The draft's comment over `commits` already states that a `version_seq` filter or a `branch` filter alone gives the wrong ancestor set. It states that a recursive walk over `parent_commit_id` is the correct method. I verified the claim with a real fork. | I wrote a four commit DAG on both servers. `c1` leads to `c2` on `main`, then `c3` continues `main` from `c2` while `c4` forks `colloquial` from `c2`, both at `version_seq` 3. Filtering `version_seq <= 3` returned `[c1, c2, c3, c4]`, wrongly including `c3` from the other branch. Filtering `branch = 'colloquial'` returned only `[c4]`, losing the shared ancestors. A `WITH RECURSIVE` walk over `parent_commit_id` returned exactly `[c1, c2, c4]` on both servers, the correct ancestor set. |
| 4 | H | Nothing. The draft added a `timeline_state` table with a `source_text` column and rows keyed by `(dub_id, language, commit_id, segment_index)`. I verified state at a commit that renders no take agrees with a replay of the action log. | I wrote a `boundary_nudged` commit `c5` (child of the `c4` fork) that changes segment 0's `end_ms` from 7728 to 7928 and renders no take. Querying `timeline_state_deduped` for the ancestor set of `c5` with `argMax(..., version_seq)` per segment returned `start_ms=5908, end_ms=7928, take_id=''` on both servers. Independently replaying `actions_deduped` along the same ancestor set with `argMax(after_value, version_seq)` returned `{"start_ms":5908,"end_ms":7928}` on both servers, an exact match to the queried state. |
| 5 | M | Nothing. `created_at` stays `DEFAULT now64(3)` as T1.3 literally names, and every comment near it says a writer may override it and that it never decides order by itself. The draft added a separate `ingested_at DateTime64(3) MATERIALIZED now64(3)` column and partitions on that instead. I verified the comments match what the columns actually enforce. | I inserted a commit with an explicit `created_at` of `2099-12-31 23:59:59.999` on both servers. Both accepted and stored it unchanged. `system.parts` showed the partition stayed at the real current month, `202609`, on both servers, no `209912` partition appeared. Commit order is protected separately by `parent_commit_id` and `version_seq`, which the recursive walk in finding 3 exercises directly. I grepped every comment in the file for a claim that a client clock cannot decide order. I found none outside the accurate, narrower statement that `created_at` alone never decides commit order. Finding 3's walk confirms that narrower statement is true. |
| 6 | M | Nothing. `takes.repair` is already `Enum8('none'=1,'atempo'=2,'rewrite'=3,'manual'=4)`, matching `types.Repair.String()` in `internal/types/types.go` exactly. | I inserted a take with `repair = 'ludicrous_speed'` on both servers and got `UNKNOWN_ELEMENT_OF_ENUM` on both. A take with `repair = 'rewrite'` inserted and read back cleanly on both servers. |
| 7 | L | Nothing. `segment_index` is `Int32` on `takes`, `actions`, `charges`, and the new `timeline_state` table. | `system.columns` on both servers listed `segment_index` as `Int32` for all four tables and their `_deduped` views, no mixed types anywhere. |

## What I re-confirmed working

Every item the round 1 review listed under "What I Confirmed Working" still holds, re-measured
by me on both servers against the current 405 line file:

- The file names no database, ran unmodified on both servers, and a second full run over the
  same database exited 0 on both, preserving every row.
- All 10 fixture take rows loaded, built from `testdata/expected/metrics.json` and
  `testdata/segments.json` (8 first attempts plus the 2 stretched repairs for segments 3 and
  4). Segment 8 read back `delta_ms` of -2910 against `slot_ms` 7110 and `measured_ms` 4200 on
  both servers.
- `delta_is_signed` fired on an absolute value delta of 2910 and on a delta clamped to 0, on
  both servers.
- `peaks_are_sized` fired on a length 10 array and accepted lengths 0 and 64, on both servers.
- `takes` still carries no cost column. An insert naming `cost_usd` failed with
  `NO_SUCH_COLUMN_IN_TABLE` on both servers.
- `charges.cost_usd` refused a direct write with `ILLEGAL_COLUMN` on both servers and computed
  100 units at 0.00003 as 0.003 correctly on both.
- `units_are_positive` fired on units of -5 on both servers.
- `prior_by_speaker` exists and `EXPLAIN projections = 1` reports it usable on both servers.
  The planner still skips it for a 10 row table, which is correct.
- `actions.author` still rejects an unknown value. I re-ran the `author = 'system'` probe
  locally and got `UNKNOWN_ELEMENT_OF_ENUM`.
- The three value `charges.kind` enum is unchanged from the draft I inherited, still
  `Enum8('segment'=1,'translate'=2,'synthesize'=3)`. I did not widen it. The round 1 false
  positive stands.
- Every column T4.2, T4.6, and T4.7 need exists with a usable type, including the new
  `timeline_state` table's columns for T4.5.

## The one edit I made

`sql/schema.sql`, the `commits` table comment above `ORDER BY (dub_id, commit_id)`, carried a
32 word sentence, over the AGENTS.md limit of 30. I split it into two sentences. This is a
style-only change with no effect on behavior, found while I verified the house style rules the
task requires me to check. It is not one of the seven numbered findings, and I list it
separately so the round 2 reviewer can judge it on its own rather than mistake it for a
finding closure. After the edit I re-ran the full file fresh and re-run on both servers and
both still exited 0, so the edit changed no behavior.

Before: "ORDER BY (dub_id, commit_id) because the walk looks a commit up by id at every step,
and that pair is unique, so it also serves as the dedup key for a retry."

After: "ORDER BY (dub_id, commit_id) because the walk looks a commit up by id at every step.
That pair is unique, so it also serves as the dedup key for a retry."

I re-checked every comment block in the final file for semicolons, em dashes, sentences over
30 words, and passive voice, and found no further violation.

## Contract notes outside my seam

- **A stray database from the round 1 reviewer's own session, `t13_remediation_r1`, still
  sits on the Cloud service.** I saw it while listing `system.databases` before I started. It
  is not mine to drop, since my authorization here only covers `t13_remediation_r2` and its
  legacy sibling, both of which I created and dropped myself. The orchestrator should have
  someone confirm it holds nothing load-bearing and drop it, since other agents share this
  service.
- **A single-row synchronous insert against these tables is slow enough on Cloud to matter
  for T4.1.** Ten separate single-row `INSERT` statements into `takes` took over four minutes
  before I killed the hung client, most likely because each row triggers a synchronous
  rebuild of the `prior_by_speaker` projection under `deduplicate_merge_projection_mode =
  'rebuild'` on `SharedMergeTree`. The identical 10 rows as one multi-row `INSERT` completed
  in under 2 seconds. T4.1 already requires batching for throughput. This is a second,
  sharper reason: an unbatched write queue would make the live demo visibly stall on Cloud,
  not just run slower.
- **`dev-diary/PHASE-1-contracts.md` carries an unrelated pending change** (T1.4's status line
  reads `claimed:T14Impl` instead of `not-started`) that predates this remediation round and
  that I did not touch, since it falls outside both my seam and this task.
