# T6.5 remediation, round 3

I did not change the round 3 verdict. I fixed findings M1, M2 and L1 and nothing else.

I edited `web/src/lib/edit/Speaker.svelte`, the T6.5 seam paragraph in
`dev-diary/PHASE-6-editing.md`, and this record. I did not commit.

| Finding | Fix | Pin | Residue |
|---|---|---|---|
| **M1 (busy).** `busy` was component state, and the component survives a line switch. The new line inherited it. While line 3 re-rendered, the line 1 confirm read `Re-rendering…` and stayed disabled, and Cancel stayed disabled too. | A `renderingSegmentId` state holds the in-flight segment id. `busy` is now derived as `renderingSegmentId === segment.id`. Another line reads idle while the reply is in flight. | Headless Chromium against the built bundle with a 1500 ms reply. During the flight the line 1 panel read `confirmText: "Change speaker"`, `confirmDisabled: false` and `cancelDisabled: false`. The mutation read `Re-rendering…`, `true` and `true`. | None. The flag now follows the in-flight segment id instead of the component. |
| **M2 (pending).** The stale reply guard cleared `pendingSpeaker` before it returned. Across a switch it dismissed a new pending choice the creator made on the new line. | The guard returns on the segment mismatch alone. The `pendingSpeaker = undefined` write is gone. | The same run. The line 1 panel read `pendingOpen: 1` during the flight and `pendingOpen: 1` after the line 3 reply landed. The mutation read `pendingOpen: 0` after the reply. | None. The surviving panel kept its estimate and both buttons live. |
| **L1 (handoff).** The seam paragraph said `Track.svelte` renders only `takes.at(-1)`. It said the page cannot inject ghost shapes into the lane. It said moving the strip into Track needs an owns change. | The paragraph now matches the code. The lane work needs no owns change. | The paragraph reads against the ghost loop at `Track.svelte:79-91` and the `.ghost-take` style at `:214-223`. The owns line at `PHASE-6-editing.md:130-131` lists `Track.svelte`. | None. |

## The seam paragraph

Before, `dev-diary/PHASE-6-editing.md:673-676`:

> Seam. `Track.svelte` owns the timeline lanes and renders only `takes.at(-1)`. The page
> cannot inject ghost shapes into that component, so the ghost history lives in the
> page-owned strip. `LengthBar.svelte` already paints older takes as ghost shapes on its
> canvas at alpha 0.28. Moving the strip into Track needs an owns change.

After:

> Seam. `Track.svelte` owns the timeline lanes and renders the active take plus every older
> take as a dashed ghost shape inside the lane. The owns line already lists `Track.svelte`,
> so the lane work needs no owns change. The page-owned strip stays as the keyboard and
> playback control. `LengthBar.svelte` already paints older takes as ghost shapes on its
> canvas at alpha 0.28.

## What I measured

I copied `web/` to `/tmp/rem65r3/repo` and symlinked `node_modules` and `testdata`. I
built with `npm --prefix web run build`. A throwaway Node server served the built bundle,
the frozen `testdata/wire/dub.json` under the non-fixture id `t65-r3-dub`, and a rerender
reply delayed 1500 ms. I drove the page with headless Chromium. The harness never edited
the working tree.

### 1. The fixed build, M1 and M2 in one run

The run confirmed the line 3 speaker change and selected line 1 at 32 ms. It chose a line
1 speaker during the flight. The reply landed at 1500 ms. The final read happened at
2439 ms.

```json
{
  "line1DuringFlight": {
    "pendingOpen": 1,
    "confirmText": "Change speaker",
    "confirmDisabled": false,
    "cancelDisabled": false,
    "pendingText": "Re-rendering line 1 as Mark Vande Hei is estimated at $0.0008421. Estimated from the take seg_1_try1.wav. Cancel Change speaker"
  },
  "line1AfterReply": {
    "pendingOpen": 1,
    "confirmText": "Change speaker",
    "confirmDisabled": false,
    "cancelDisabled": false
  },
  "elapsedMs": 2439
}
```

The line 1 confirm reads `Change speaker` and stays live while line 3 re-renders. The
Cancel button stays live too. That is the M1 pin.

The line 1 pending panel is open during the flight and still open after the line 3 reply
landed. That is the M2 pin. The panel kept the estimate sentence.

The reply was real and it applied. The server recorded one POST with the body
`{"language":"ml","speaker":"Mark Vande Hei"}`. A later selection of line 3 read the new
header and three chips, two ghosts and one active.

```json
{
  "line3AfterReply": { "header": "Mark Vande Hei", "pendingOpen": 0 },
  "chips": ["seg_3_try1.wav:ghost", "seg_3_stretched.wav:ghost",
            "/tmp/rem65r3-storage/t65-r3-dub/work/ml-IN/seg_3_try3.wav:active"]
}
```

The run raised 0 console messages and 0 page errors. The 30 `clip.mp4` aborts are a
harness artifact, because the server ignores range requests.

### 2. The M1 mutation fails the pin

I copied the tree to `/tmp/rem65r3/mut-a` and reverted the fix to the old shape. `busy`
is a component boolean again, set on confirm and cleared in `finally`. I rebuilt and drove
the same sequence. The reply landed at the same point.

```json
{
  "line1DuringFlight": {
    "pendingOpen": 1,
    "confirmText": "Re-rendering…",
    "confirmDisabled": true,
    "cancelDisabled": true
  },
  "line1AfterReply": {
    "pendingOpen": 1,
    "confirmText": "Change speaker",
    "confirmDisabled": false,
    "cancelDisabled": false
  }
}
```

The line 1 panel inherits the line 3 busy flag again. The confirm and Cancel both disable.
The pin reddens under the mutation.

### 3. The M2 mutation fails the pin

I copied the tree to `/tmp/rem65r3/mut-b` and restored the `pendingSpeaker = undefined`
write inside the guard. I kept the M1 fix, so the panel stays readable. I rebuilt and
drove the same sequence.

```json
{
  "line1DuringFlight": {
    "pendingOpen": 1,
    "confirmText": "Change speaker"
  },
  "line1AfterReply": {
    "pendingOpen": 0,
    "confirmText": null
  }
}
```

The panel opens during the flight and vanishes when the line 3 reply lands. The pin
reddens under the mutation.

Both mutation runs recorded one POST each and applied the line 3 reply, so neither pin is
vacuous.

### 4. The type check

```text
$ npm --prefix web run check
1788912209107 START "/home/nryn/work/ajilamu/web"
1788912209108 COMPLETED 183 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
```

The changed component carries no `export let`, no `$:` and no store. I did not edit the
round 3 review file. I did not touch the verdict.
