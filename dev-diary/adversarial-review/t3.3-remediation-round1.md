# T3.3 round 1 remediation

I own both assembler duck files and this remediation record. The required shapes exist. I can validate this task independently.
This remediation addresses F1 from `t3.3-round1.md`. The review verdict remains unchanged pending independent re-review.

| Finding | Changes | Pin | Result |
|---|---|---|---|
| H, F1 | `duckFilter` pads both compressor inputs by one second. The first `amix` input is the original bed with `weights=0 1`, so `duration=first` follows the bed frame count. | `TestDuckOutputMatchesBedDuration` compares ffprobe format duration and decoded PCM sample count against the bed. Overlay on the same files is the control. | The 1.000 s case failed before repair at 0.928798 s and 327680 PCM bytes. After repair, ffprobe reports 1.000000 s and 352800 bytes for bed and mix. The 0.5 s, 2 s, and 6 s cases match as well. Restoring the bare `sidechaincompress` plus `amix` graph fails the 1 s measurement. |

## Measurements

Validation selects `/opt/homebrew/bin/ffmpeg` and `/opt/homebrew/bin/ffprobe`, both version 9.0.1.
Go reports version 1.27.1. The complete assembler suite passes:

```sh
PATH=/opt/homebrew/bin:/usr/bin:/bin go test -count=1 ./internal/assemble -v
```

`TestDuckOutputMatchesBedDuration` failed before the graph change on 1 s, 2 s, and 6 s beds.
After the change every length matches the bed. Overlay still matches the bed on each length.

Independent ffprobe on a 1.000 s pair after the repair:

```text
bed duration 1.000000
mix duration 1.000000
bed PCM bytes 352800
mix PCM bytes 352800
```

The 4 s dip check still measures a 5.310856 dB bed-band cut under speech.
Silent intervals stay at 0.000000 dB. Voice band stays within 0.000044 dB.
Fixture gaps stay at 0.000000 dB. T3.1, T3.2, and T3.5 tests pass.
