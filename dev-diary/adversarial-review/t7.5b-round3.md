# T7.5b adversarial review, round 3

Task: user-defined languages at upload. I re-read `t7.5b-round1.md`, `t7.5b-round2.md`,
`t7.5b-remediation-round1.md`, `t7.5b-remediation-round2.md`, and the T7.5b handoff entry in
`PHASE-7-ingestion.md`. I measured the working tree against HEAD `eb5bf07`. Go 1.27.1, Node
v26.8.1. Every pin came from a throwaway copy at `/var/tmp/t75b-r3/repo`, because `/tmp` held
192 MB free. The round-two remediation changed `internal/api/run.go`, `internal/api/run_test.go`,
`cmd/ajilamu/main.go`, `cmd/ajilamu/main_test.go`, the handoff entry, and its record.

## Verdict

**APPROVE**

| Severity | Count |
|---|---:|
| C | 0 |
| H | 0 |
| M | 0 |
| L | 0 |

## Findings

None. The four columns (Where, What, Pin, Mutation) have no row, because no finding survives.

## Resume pins

The probe drove the real `POST /api/dubs/{id}/run` route. A runner adapter ran
`fit.RunPipeline` behind the route. The translator recorded the lines it rendered and failed
line two on the first pass. The probe measured four layouts.

```text
R3 fresh first        route=ml     workdir=.../dub-fresh/work/ml-IN    synth=[1] err=repair line 2: probe interrupted mid run
R3 fresh resume       route=ml-IN  workdir=.../dub-fresh/work/ml-IN    synth=[2 3] err=<nil>
R3 legacy raw first   workdir=.../dub-legacy/work/ml                   synth=[1] err=repair line 2: probe interrupted mid run
R3 legacy raw resume  route=ml-IN  workdir=.../dub-legacy/work/ml      synth=[2 3] err=<nil>
R3 legacy flat first  workdir=.../dub-flat/work                        synth=[1] err=repair line 2: probe interrupted mid run
R3 legacy flat resume route=ml-IN  workdir=.../dub-flat/work           synth=[2 3] err=<nil>
R3 reverse first      route=ml-IN  workdir=.../dub-reverse/work/ml-IN  synth=[1] err=repair line 2: probe interrupted mid run
R3 reverse resume     route=ml     workdir=.../dub-reverse/work/ml-IN  synth=[2 3] err=<nil>
```

A fresh run under the sample code `ml` chose `work/ml-IN`. The catalog code `ml-IN` resumed
that directory and rendered lines two and three. It reused line one. A legacy record in the
raw-code directory `work/ml` resumed there and rendered lines two and three. A legacy record in
the flat directory `work/` resumed there and rendered lines two and three. The reverse order
chose `work/ml-IN` and resumed it under `ml`. One directory holds one language across both
spellings.

## Mutation pin

I reverted `runWorkDirFor` in the copy to name the directory from the raw request code and to
drop the raw-code reuse. The same probe then split the directories.

```text
R3 fresh resume       route=ml-IN  workdir=.../dub-fresh/work/ml-IN   synth=[1 2 3]
R3 legacy raw resume  route=ml-IN  workdir=.../dub-legacy/work/ml-IN  synth=[1 2 3]
R3 reverse resume     route=ml     workdir=.../dub-reverse/work/ml    synth=[1 2 3]
```

The resume re-rendered and re-charged line one. The mutation also failed two shipped tests.

```text
run_test.go:84: recorded work dir = ".../dub-accept/work/ml", want ".../dub-accept/work/ml-IN"
--- FAIL: TestRunStartAnswersAccepted (0.00s)
run_test.go:193: recorded work dir = ".../dub-raw-code/work/ml-IN", want ".../dub-raw-code/work/ml"
--- FAIL: TestRunStartReusesRawCodeWorkDir (0.00s)
```

I restored the copy after the pin.

## Prompt pins

The shipped map resolved the sample pair and the create pair to words.

```text
R3 NAME sample target code="ml"    tag="ml-IN" name="Malayalam" word=true
R3 NAME sample source code="en"    tag=""      name="English"   word=true
R3 NAME create target code="ml-IN" tag="ml-IN" name="Malayalam" word=true
R3 NAME create source code="en-US" tag=""      name="English"   word=true
```

The probe fed the four names into `gemini.translatePrompt`. The sample prompt and the create
prompt are identical, because both pairs resolve to the same words.

```text

Translate this English dialogue line into natural spoken Malayalam script (മലയാളം):
Original English: "Hello from orbit"
Speaker emotion: Warm
Constraint: The translated line will be spoken in Malayalam in a video slot that lasts approximately 1.8 seconds (1800 ms). Keep the translation natural, spoken, and fit the rhythm.

Respond with strictly the translated Malayalam text. No markdown, no quotes, no explanation.
```

No code appears in either prompt. I removed the `en` entry in the copy. The sample source then
resolved to `en`, and the prompt read:

```text
Translate this en dialogue line into natural spoken Malayalam script (മലയാളം):
Original en: "Hello from orbit"
```

I restored the entry after the pin.

## Handoff

The corrected T7.5b handoff entry matches the shipped state. It names every committed catalog
code and the two sample codes `ml` and `en`. It says a new upload stores `ml-IN` and the prompt
reads `Malayalam`. The measured pin carries
`TranslateRequest{TargetLanguageName:"Malayalam", SourceLanguageName:"English"}`. My probe
measured that pair on the create path and on the sample path. The 400 answer for an unsafe
language matches the `safeDubID` guard in `RunStartHandler`. The per-language work directory
matches the resume pins.

## Command output

```text
$ go test -count=1 ./internal/api ./cmd/...
ok  	github.com/nrynss/ajilamu/internal/api	1.363s
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	3.984s

$ npm --prefix web run check
1788905795784 START "/home/nryn/work/ajilamu/web"
1788905795785 COMPLETED 182 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
```

`gofmt -l` on the four Go files printed nothing.

## Residue

**Round one H1 has zero residue.** The create path names `English` and `Malayalam`. The round-two
M2 fix added the sample source `en`, so the sample path names `English` too. No code reaches
either prompt.

**Round one M1 has zero residue.** A legacy flat record in `work/` resumes there and renders
lines two and three. The flat fallback still runs.

**Round two M1 has zero residue.** The sample code `ml` and the catalog code `ml-IN` share
`work/ml-IN`. A legacy raw-code record in `work/ml` resumes there. The mutation pin fails.

**Round two M2 has zero residue.** The sample source `en` resolves to `English`. Removing the
entry restores the code, which pins the fix.

## Open note

The map does not carry a bare code the catalog extends, such as `hi`. Such a code would name the
directory `work/hi-IN` while `resolveRunLanguage` falls back to `hi`. The create screen offers no
such code, and Cloud TTS rejects the bare tag. I filed nothing, because no product path reaches
it.

## Scope and evidence

I changed no implementation file. My only write is this review file. Every scratch tree lived
under `/var/tmp/t75b-r3`, because `/tmp` was full. The probe ran in a copy at
`/var/tmp/t75b-r3/repo`. The two commands are `go test -count=1` and
`npm --prefix web run check`.
