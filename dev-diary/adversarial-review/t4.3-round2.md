# T4.3 adversarial review, round 2

Verdict: REMEDIATE

Counts: C0, H1, M0, L0.

Prior-round residue: Round 1 finding 1 is closed. `commits_test.go` now keeps the durable
transport proof in CI. It captures a root, linear child, and fork. It rejects the three invalid
shapes before transport. It verifies omitted timestamps, the `commits_raw` target, and a
byte-identical replay after a 503. No residue remains from the missing-test finding.

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| H | `internal/ledger/commits.go:30-59`, `internal/ledger/commits_test.go:21-104` | `AppendCommit` calls only stateless shape validation. It accepts `Commit{CommitID: "orphan", ParentCommitID: "missing", VersionSeq: 2}` and accepts a later reciprocal parent, so it cannot uphold its documented rule that a child names an earlier commit. The accepted graph can contain an orphan or a cycle, rather than a DAG. The new invalid-row test covers only invalid version shapes and self-parenting. | I traced the accepted artifact path: the stated orphan has all four required non-empty identifiers, a non-empty parent, version two, and a different commit id. `validateCommit` returns nil and `EnqueueJSON` sends it to `commits_raw`. No state lookup, uniqueness check, or ancestor check occurs before that send. `go test -race ./internal/ledger`, `go vet ./internal/ledger`, `gofmt -d internal/ledger/commits.go internal/ledger/commits_test.go`, and `git diff --check -- internal/ledger/commits.go internal/ledger/commits_test.go dev-diary/PHASE-4-ledger.md` passed. Those checks do not measure this untested invalid graph. | Reverting the required parent-existence and ordering protection lets one malformed pipeline action write an ancestry that `timeline_at_commit` cannot reconstruct. A cycle can make the recursive history walk fail or never reach a root. |

Independent evidence:

- `TestAppendCommitWritesDAGWithoutClientTimestamps` captures three accepted JSONEachRow
  requests. Their observed identities are root, child of root, and fork of root.
- The test decodes every captured row. It confirms `created_at` and `ingested_at` are absent.
  It also confirms the exact `commits_raw` JSONEachRow insert query.
- The invalid inputs send no request. They are only a root at version two, a child at version
  one, and a self-parent. They do not exercise an absent parent or a cycle.
- `TestAppendCommitReplaysFailedDelivery` observes a 503, one pending durable entry, two
  captured deliveries with equal bytes, the same raw-table query, and zero pending entries
  after `Flush`.
