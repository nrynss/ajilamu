# T7.4 Round 2 Remediation

I addressed every open finding from round 2 and verified each fix with independent unit tests and type checks.

| # | Sev | Resolution | Independent pin |
|---|---|---|---|
| 5 | H | Closed. Added `IndexHandlerFrom` to query a per-request provider. Retained `IndexHandler` as a snapshot wrapper. Updated the handoff contract. | `TestIndexHandlerFromQueriesProviderPerRequest` passed. Adding a project to the provider store after construction returns the new project on subsequent GET requests. |
| 6 | M | Closed. Added loading, error, empty, and populated states to `web/src/routes/+page.svelte`. | `npm --prefix web run check` passed with zero errors. Loading, error, empty, and populated views render distinct complete sentences. |
| 7 | M | Closed. Mapped readiness values to creator sentences and semantic color tokens (`--ok` for ready, `--warn` for running and review, `--dim` for pending). | `npm --prefix web run check` passed. Rendered badges display creator sentences like "Ready for review" in `--ok` green and "Dubbing in progress" in `--warn` amber. |
| 8 | M | Closed. Added `LogValue` and `String` methods to `ConfigUpdate` to redact credentials. | `TestConfigUpdateRedactsCredentialsInLogsAndStringer` passed. Structured `slog` output and `fmt.Sprintf` contain zero credential secrets. |
| 9 | L | Closed. Displayed the server's response text on non-204 responses in `web/src/routes/config/+page.svelte`. | `npm --prefix web run check` passed. Submitting an empty form surfaces the server sentence "Enter a key before saving." in the error region. |
| 10 | L | Closed. Updated `singleFormValue` so an omitted field is treated as an empty field rather than a bad request. | `TestConfigHandlerAllowsOmittedFields` passed for both omitted `voice_key` and omitted `translation_key` requests. |
| 11 | L | Closed. Detected `MaxBytesError` during form parsing and returned status `413`. | `TestConfigHandlerReturns413ForChunkedBodyOverLimit` passed. A chunked body exceeding 16 KiB returned HTTP status 413. |
| 12 | L | Closed. Added dedicated tests for missing-callback 503 responses and unparseable `created_at` timestamp fallback ordering. | `TestConfigHandlerReturns503WhenCallbackIsNil` and `TestIndexHandlerUnparseableCreatedAtSortsAsOldest` passed. Breaking either clause fails the suite. |

## Validation

- `go test -v -count=1 ./internal/api` passed all 23 tests.
- `npm --prefix web run check` passed with zero errors and zero warnings.
