# T4.4 adversarial review, round 1

Verdict: REMEDIATE

Counts: C 0, H 0, M 1, L 0

Prior-round residue: None. This is the first review round.

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| M | `internal/ledger/actions.go` `validateAction`, `internal/ledger/actions_test.go` text_corrected and line_rewritten cases | `RecordAction` enqueues any string in before_value and after_value. `sql/schema.sql` requires those columns to hold JSON text. The committed capture pins raw old line and first phrasing. Invalid JSON also reached the queue. Empty values are valid and must stay allowed. | An isolated HTTP capture decoded text_corrected before_value as old line. json.Valid was false. line_rewritten first phrasing was also false. A row whose before_value was the invalid text {not json returned nil and produced one actions_raw JSONEachRow request. The decoded field stayed a string. | Dropping a non-empty JSON text check lets a text edit store a value a JSON reader cannot parse. Explain-an-edit queries then fail on the two common string actions. |

Independent evidence:

- The capture received ten JSONEachRow bodies. Every query named `INSERT INTO actions_raw` and `FORMAT JSONEachRow`.
- No query named the `actions` view.
- Stored `action_type` values were `segment_created`, `boundary_nudged`, `speaker_reassigned`, `text_corrected`, `take_rendered`, `atempo_stretched`, `line_rewritten`, and `user_command`.
- No body stored `stretched`. PHASE-4 `stretched` and the project.md aliases returned errors with zero transport requests.
- Unknown author `pipeline`, empty author, and author `Agent` each returned errors. Request count stayed zero.
- Decoded bodies omitted `created_at`, `ingested_at`, and `event_key`.
- `before_value` and `after_value` decoded as strings. Boundary and stretch JSON stayed inside those strings. They did not become nested objects.
- `speaker_reassigned` stored `"Ada"` as JSON text. `json.Valid` was true. `text_corrected` stored `old line`. `json.Valid` was false.
- A row whose before_value was the invalid text {not json returned nil from RecordAction and produced one HTTP request.
- Command-bar prompts with leading spaces, quotes, angle brackets, newlines, and Malayalam decoded equal to the input.
- A 503 left one durable journal file. Its query named `actions_raw` JSONEachRow. The replay body bytes equaled the first body.
- `gofmt -d` passed on the T4.4 Go files.
- `git diff --check` passed.
- `go test ./internal/ledger -count=1` passed.
- `go test -race ./internal/ledger -count=1` passed.
- `go vet ./internal/ledger` passed.
- Those checks do not prove the JSON text contract.
- I removed the throwaway capture after measurement.

The vocabulary pick is coherent. The writer stores the frozen P1 name `atempo_stretched` and rejects `stretched` before enqueue.

Residue: Finding 1 is open. No other residue.
