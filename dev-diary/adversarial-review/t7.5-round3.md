# T7.5 adversarial review, round 3

Task: workspace payload route. `internal/api/workspace.go` assembles one wire `Dub` from five
ledger reads plus the history reader. `WorkspaceHandlerFrom` mounts at `GET /api/dubs/{id}`.
Round 2 filed M1 against the handoff and M2 against the missing `running` value. The round 2
remediation corrected the T7.5 and T7.5a handoff entries and wired `runs.active` into
`WorkspaceHandlerFrom`.

Reviewed against HEAD `75af966` plus the uncommitted T7.5 working tree. Go 1.27.1, Node
v26.8.1, Chrome 152.0.7977.82. I did not implement T7.5 and I did not remediate. I did not
write round 1 or round 2. I fixed nothing. My only write is this file.

## Verdict

**APPROVE**

| Severity | Count |
|---|---:|
| C | 0 |
| H | 0 |
| M | 0 |
| L | 0 |

## Findings

No rows. Round 3 files no finding. The four columns follow for the record.

| Where | What | Pin | Mutation |
|---|---|---|---|
| No finding | Round 3 accepts the round 2 remediation. | Not applicable. | Not applicable. |

## Readiness re-measurement

I drove `WorkspaceHandlerFrom` with my own stand-in reader and history. The probe ran in a
full scratch copy at `/tmp/t75r3`. It covered four ledger shapes with and without a run in
flight. Every wire value appears.

| Reader shape | Run in flight | Readiness |
|---|---|---|
| no take | no | `pending` |
| no take | yes | `running` |
| flagged line | no | `review` |
| flagged line | yes | `running` |
| two segments, one rendered line | no | `review` |
| two segments, one rendered line | yes | `running` |
| two segments, two rendered lines | no | `ready` |
| two segments, two rendered lines | yes | `running` |

The probe is `TestR3ProbeEveryReadinessState` at
`/tmp/t75r3/internal/api/zz_r3_probe_test.go`. It logged the eight rows above and passed.

Then I drove the real `NewServer` over `httptest`. My probe is
`TestR3ServerProbeReadyRunningReady`. It starts a blocking runner, reads the route before the
run, during the run, and after the run.

```text
=== RUN   TestR3ServerProbeReadyRunningReady
    zz_r3_server_probe_test.go:48: PROBE before run: "ready"
    zz_r3_server_probe_test.go:64: PROBE after run: "ready"
--- PASS: TestR3ServerProbeReadyRunningReady (0.00s)
```

The probe asserted `ready` before and `running` during. It polled to `ready` after. The shipped
pin `TestWorkspaceRouteReportsRunningThroughTheServer` reported the same three states.

```text
workspace_test.go:502: readiness = "ready"
workspace_test.go:511: readiness = "running"
workspace_test.go:517: readiness = "ready"
--- PASS: TestWorkspaceRouteReportsRunningThroughTheServer (0.00s)
```

## M2 mutation pins

I replaced `runs.active` with `nil` in `NewServer` in the scratch copy. My server probe failed.

```text
=== RUN   TestR3ServerProbeReadyRunningReady
    zz_r3_server_probe_test.go:48: PROBE before run: "ready"
    zz_r3_server_probe_test.go:58: during run = "ready", want "running"
--- FAIL: TestR3ServerProbeReadyRunningReady (0.00s)
```

The shipped server pin failed on the same wiring change.

```text
--- FAIL: TestWorkspaceRouteReportsRunningThroughTheServer (0.00s)
    workspace_test.go:502: readiness = "ready"
    workspace_test.go:511: readiness = "ready"
    workspace_test.go:512: readiness during the run = "ready", want "running"
```

I then forced the handler check `running := active != nil && active(dubID)` to `false`. The
shipped matrix pin failed on all three in-flight rows, for example
`no take with a run in flight: readiness = "pending", want "running"`.

I restored each file and re-ran. Every pin passed again.

## Round 1 M1 mutation pin

I forced the segment-coverage condition to `false`. The coverage pin failed.

```text
--- FAIL: TestWorkspaceReadinessNeedsATakeForEverySegment (0.00s)
    workspace_test.go:420: one rendered line for two segments: readiness = "ready", want "review"
```

The fix is load bearing. Two segments and one rendered line answer `review`. Two rendered
lines answer `ready`.

## Handoff sentences against the shipped code

Every corrected claim holds. I measured each one against the code and the live route.

| Claim | Evidence |
|---|---|
| `NewServer` passes `runRegistry.active` into `WorkspaceHandlerFrom` | `internal/api/server.go` line 181 passes `runs.active`. The server probe read `running` during a run. |
| No take reads `pending` | Probe row and `TestWorkspaceRouteServesEmptyDubForStoredProject`. |
| A flagged line reads `review` | Probe row and `TestWorkspaceRouteDerivesReadiness`. |
| Every take fitting with every segment covered reads `ready` | Probe row and `TestWorkspaceReadinessNeedsATakeForEverySegment`. |
| A segment with no rendered line reads `review` | Probe row and the coverage mutation. |
| `source_language` stays empty | The live route returned `"source_language":""`. The `Dub` literal omits the field. |
| The takes read selects `repair_detail` | `selectWorkspaceTakes` at `internal/ledger/workspace.go` line 28 selects `repair_detail`. |
| The reader parses the prefix into thousandths | `atempoStretchMilli` cuts the prefix and rounds `ratio * 1000`. |
| An atempo take at ratio 1.0500 carries 1050 | The live route returned `"stretch_factor_milli":1050`. The shipped pin logged the same. |
| A take with no parsed ratio omits the field | The live route omitted the field for the `none` take. `omitempty` drops zero. |

The writer emits the parsed prefix at `internal/fit/rewrite.go` line 390 as
`atempo stretch applied at ratio %.4f`. The reader and the writer agree.

## Unchanged files

`internal/api/run.go`, `internal/api/wire.go`, and `web/src/lib/types.ts` match HEAD by hash.

```text
internal/api/run.go worktree=b47cf32b480b74638af3709f35af6681a23bf87a head=b47cf32b480b74638af3709f35af6681a23bf87a UNCHANGED
internal/api/wire.go worktree=eb87b44598bb620a6daf045c42563cccdf868087 head=eb87b44598bb620a6daf045c42563cccdf868087 UNCHANGED
web/src/lib/types.ts worktree=2842f0359220d0c86ddfc7268772dd398d681fdf head=2842f0359220d0c86ddfc7268772dd398d681fdf UNCHANGED
```

The round 2 remediation wrote `internal/api/workspace.go`, `internal/api/workspace_test.go`,
`internal/api/server.go`, the two handoff entries, and its record. The T7.5 `owns` line names
every one of those paths plus `internal/ledger/workspace.go` and its test. No remediation edit
landed outside that seam.

## Route and page behaviour

I built the binary from the scratch copy and ran it against my own stand-in ClickHouse on port
18124. The stand-in answered the eight statements.

`GET /api/dubs/aabbccdd00112233` answered 200. The body carried the keys
`charges commits created_at id languages readiness segments source_language title total
updated_at`. It carried two segments, one `ml` track, the atempo ratio 1050, the whole-pass
charge with a null `segment_id`, the total, and two commits. `readiness` was `ready`.

`GET /api/dubs/new` and `GET /api/dubs/sample` answered 405 with `Allow: POST`.

Headless Chrome loaded `/d/<upload id>` on that server. The DOM carried the real title
`Real project.mp4`, two length checks, both fits, and no fixture marker.

A second binary ran with every `CLICKHOUSE_*` value empty. Its route answered 503 with the
unavailable sentence. Headless Chrome loaded `/d/fixture` on that server. The DOM carried
`NASA 75-second clip` five times and `In review` once. The real id on the same server showed
the unavailable sentence and no fixture data.

## Command output

The working tree carries a concurrent T2.8 edit to `internal/tts` and `internal/fit`. That
edit changed `tts.NewSynthesizer` and has not updated its caller. The real tree therefore fails
to build the entrypoint.

```text
$ go test -count=1 ./internal/api ./internal/ledger ./cmd/...
# github.com/nrynss/ajilamu/cmd/ajilamu [github.com/nrynss/ajilamu/cmd/ajilamu.test]
cmd/ajilamu/main.go:385:60: not enough arguments in call to tts.NewSynthesizer
	have (*config.Config, *chargeRouter, cost.RateCard, nil)
	want (*config.Config, string, tts.ChargeRecorder, cost.RateCard, tts.TTSClient)
--- FAIL: TestEntrypointExitsCleanlyWithoutLedger (0.16s)
FAIL	github.com/nrynss/ajilamu/internal/api	0.676s
ok  	github.com/nrynss/ajilamu/internal/ledger	1.010s
FAIL	github.com/nrynss/ajilamu/cmd/ajilamu [build failed]
```

Only `TestEntrypointExitsCleanlyWithoutLedger` fails in `internal/api`, because it builds the
entrypoint. The T2.8 seam owns that break, not T7.5. The scratch copy reverts the sibling
`internal/tts` and `internal/fit` edits and carries the T7.5 diff. It builds.

```text
$ go test -count=1 ./internal/api ./internal/ledger ./cmd/...
ok  	github.com/nrynss/ajilamu/internal/api	1.436s
ok  	github.com/nrynss/ajilamu/internal/ledger	1.016s
ok  	github.com/nrynss/ajilamu/cmd/ajilamu	0.982s
```

```text
$ npm --prefix web run check
1788900738590 START "/home/nryn/work/ajilamu/web"
1788900738592 COMPLETED 181 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
```

## Residue against prior rounds

**Round 1.** M1, L1, and L3 are closed. L2 stays open under a legitimate deferral.

| Finding | Residue |
|---|---|
| M1, readiness ignored segment coverage | Closed. Two segments and one rendered line answer `review`. The coverage mutation fails. |
| L1, empty timestamps for a stored project | Closed. The shipped pin read the record timestamp into both fields. |
| L2, empty `source_language` | Open, deferred to T7.5b. The live route still returns an empty field. No store holds the value. |
| L3, `stretch_factor_milli` zero | Closed. The live route returned 1050 for the atempo take and omitted the field for the `none` take. |

**Round 2.** M1 and M2 are closed.

| Finding | Residue |
|---|---|
| M1, handoff misdescribed the shipped route | Closed. Every corrected sentence matches the code and the live route. |
| M2, readiness never reported `running` | Closed. The route emits all four wire values. The `runs.active` mutation fails. |

The code residue against both rounds is zero. The one open item is the L2 deferral to T7.5b.
T7.5b sits on the board and owns the value at upload. T7.5's done-when clause does not name the
source language, so the deferral does not block this task.

## What I did not file

- **The `RunActive` comment understates ownership.** It says the run registry is one this
  package does not own. The registry lives in `internal/api/run.go` in the same package. The
  comment is imprecise and carries no behaviour.
- **The layout chrome disagrees with the payload.** `ListUploadSummaries` hardcodes
  `ReadinessPending`, so the chrome shows `Queued to start` for a real project whose route says
  `ready`. T7.5 owns neither path. Round 1 and round 2 noted the same disagreement.
- **The coverage rule counts lines, not distinct segments.** A reader that emitted two lines for
  one segment and none for the other would answer `ready`. The shipped reader emits one line per
  segment, so no current path reaches that state.

## Scope and evidence

I changed no implementation file. My only write is this review file. My scratch copy is
`/tmp/t75r3`. My pins are `zz_r3_probe_test.go` and `zz_r3_server_probe_test.go` inside that
copy. My stand-in is `/tmp/t75r3/standin.py` on port 18124. The server ran on port 18225 and
the no-credential server on port 18226. The browser is `google-chrome-stable` 152.0.7977.82. I
stopped every process I started.

### What I read

- The T7.5 and T7.5a handoff entries in `dev-diary/PHASE-7-ingestion.md`.
- `t7.5-round1.md`, `t7.5-round2.md`, and both remediation records.
- `internal/api/workspace.go` and `internal/api/workspace_test.go` in full.
- The T7.5 hunks of `internal/api/server.go` and `cmd/ajilamu/main.go`.
- `internal/ledger/workspace.go` and its test for the takes statement and the atempo parse.
- `internal/api/run.go` for the registry API and the run lifecycle.
- `internal/fit/rewrite.go` line 390 for the detail format.
- `internal/api/wire.go` and `web/src/lib/types.ts` for the four readiness values.
- `web/src/routes/d/[id]/+page.svelte` and `web/src/routes/+layout.svelte` for the consumers.
